# Tracker Bundle 400-499

This append-only audit bundle consolidates numbered experiment tracker markdown files.
Source bodies below are copied verbatim and originals are left untouched for compatibility.

## Sources

- `docs/experiments/400_variable_depth_radius_seed34_target2_sources7_txrx60_ringdown050.md`
- `docs/experiments/401_variable_depth_radius_seed34_target2_sources8_txrx60_ringdown050.md`
- `docs/experiments/402_variable_depth_radius_seed89_target2_sources7_txrx60_ringdown050.md`
- `docs/experiments/403_variable_depth_radius_seed89_target2_sources8_txrx60_ringdown050.md`
- `docs/experiments/404_variable_depth_radius_seed55_target0_sources8_txrx60_ringdown050.md`
- `docs/experiments/405_variable_depth_radius_seed55_target2_sources5_txrx60_ringdown050.md`
- `docs/experiments/406_variable_depth_radius_seed55_target1_sources5_txrx60_ringdown050.md`
- `docs/experiments/407_variable_depth_radius_seed144_target0_sources8_txrx60_ringdown050.md`
- `docs/experiments/408_seed55_ringdown050_target_specific_8_5_5_summary.md`
- `docs/experiments/409_variable_depth_radius_seed144_target2_sources5_txrx60_ringdown050.md`
- `docs/experiments/410_variable_depth_radius_seed144_target1_sources5_txrx60_ringdown050.md`
- `docs/experiments/411_seed144_ringdown050_target_specific_8_5_5_summary.md`
- `docs/experiments/412_cross_seed_ringdown050_target_specific_policy_synthesis_v2.md`
- `docs/experiments/413_variable_depth_radius_seed233_target0_sources8_txrx60_ringdown050.md`
- `docs/experiments/414_variable_depth_radius_seed233_target2_sources5_txrx60_ringdown050.md`
- `docs/experiments/415_variable_depth_radius_seed233_target1_sources5_txrx60_ringdown050.md`
- `docs/experiments/416_seed233_ringdown050_target_specific_8_5_5_summary.md`
- `docs/experiments/417_variable_depth_radius_seed377_target0_sources8_txrx60_ringdown050.md`
- `docs/experiments/418_variable_depth_radius_seed377_target2_sources5_txrx60_ringdown050.md`
- `docs/experiments/419_variable_depth_radius_seed377_target1_sources5_txrx60_ringdown050.md`
- `docs/experiments/420_seed377_ringdown050_target_specific_8_5_5_summary.md`
- `docs/experiments/421_variable_depth_radius_seed610_target0_sources8_txrx60_ringdown050.md`
- `docs/experiments/422_variable_depth_radius_seed610_target2_sources5_txrx60_ringdown050.md`
- `docs/experiments/423_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown050.md`
- `docs/experiments/424_variable_depth_radius_seed610_target2_sources11_txrx60_ringdown050.md`
- `docs/experiments/425_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown049453125.md`
- `docs/experiments/426_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown0475.md`
- `docs/experiments/427_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown0459375.md`
- `docs/experiments/428_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown040.md`
- `docs/experiments/429_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown035.md`
- `docs/experiments/430_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown025.md`
- `docs/experiments/431_variable_depth_radius_seed610_target1_sources5_txrx60_ringdown050.md`
- `docs/experiments/432_variable_depth_radius_seed610_target1_sources9_txrx60_ringdown050.md`
- `docs/experiments/433_variable_depth_radius_seed610_target1_sources8_txrx60_ringdown050.md`
- `docs/experiments/434_seed610_ringdown050_unresolved_branch_summary.md`
- `docs/experiments/435_variable_depth_radius_seed987_target0_sources8_txrx60_ringdown050.md`
- `docs/experiments/436_variable_depth_radius_seed987_target2_sources5_txrx60_ringdown050.md`
- `docs/experiments/437_variable_depth_radius_seed987_target1_sources5_txrx60_ringdown050.md`
- `docs/experiments/438_seed987_ringdown050_target_specific_8_5_5_summary.md`
- `docs/experiments/439_variable_depth_radius_seed1597_target0_sources8_txrx60_ringdown050.md`
- `docs/experiments/440_variable_depth_radius_seed1597_target2_sources5_txrx60_ringdown050.md`
- `docs/experiments/441_variable_depth_radius_seed1597_target1_sources5_txrx60_ringdown050.md`
- `docs/experiments/442_variable_depth_radius_seed1597_target1_sources9_txrx60_ringdown050.md`
- `docs/experiments/443_seed1597_ringdown050_target_specific_rescue_summary.md`
- `docs/experiments/444_variable_depth_radius_seed2584_target0_sources8_txrx60_ringdown050.md`
- `docs/experiments/445_variable_depth_radius_seed2584_target0_sources9_txrx60_ringdown050.md`
- `docs/experiments/446_variable_depth_radius_seed2584_target2_sources5_txrx60_ringdown050.md`
- `docs/experiments/447_variable_depth_radius_seed2584_target1_sources5_txrx60_ringdown050.md`
- `docs/experiments/448_seed2584_ringdown050_target_specific_rescue_summary.md`
- `docs/experiments/449_variable_depth_radius_seed4181_target0_sources8_txrx60_ringdown050.md`
- `docs/experiments/450_variable_depth_radius_seed4181_target2_sources5_txrx60_ringdown050.md`
- `docs/experiments/451_variable_depth_radius_seed4181_target1_sources5_txrx60_ringdown050.md`
- `docs/experiments/452_seed4181_ringdown050_target_specific_8_5_5_summary.md`
- `docs/experiments/453_variable_depth_radius_seed6765_target0_sources8_txrx60_ringdown050.md`
- `docs/experiments/454_variable_depth_radius_seed6765_target0_sources9_txrx60_ringdown050.md`
- `docs/experiments/455_variable_depth_radius_seed6765_target2_sources5_txrx60_ringdown050.md`
- `docs/experiments/456_variable_depth_radius_seed6765_target2_sources9_txrx60_ringdown050.md`
- `docs/experiments/457_variable_depth_radius_seed6765_target1_sources5_txrx60_ringdown050.md`
- `docs/experiments/458_seed6765_ringdown050_target_specific_rescue_summary.md`
- `docs/experiments/459_variable_depth_radius_seed10946_target0_sources8_txrx60_ringdown050.md`
- `docs/experiments/460_variable_depth_radius_seed10946_target0_sources9_txrx60_ringdown050.md`
- `docs/experiments/461_variable_depth_radius_seed10946_target2_sources5_txrx60_ringdown050.md`
- `docs/experiments/462_variable_depth_radius_seed10946_target2_sources9_txrx60_ringdown050.md`
- `docs/experiments/463_variable_depth_radius_seed10946_target1_sources5_txrx60_ringdown050.md`
- `docs/experiments/464_seed10946_ringdown050_target_specific_rescue_summary.md`
- `docs/experiments/465_variable_depth_radius_seed17711_target0_sources8_txrx60_ringdown050.md`
- `docs/experiments/466_variable_depth_radius_seed17711_target0_sources9_txrx60_ringdown050.md`
- `docs/experiments/467_variable_depth_radius_seed17711_target2_sources5_txrx60_ringdown050.md`
- `docs/experiments/468_variable_depth_radius_seed17711_target1_sources5_txrx60_ringdown050.md`
- `docs/experiments/469_seed17711_ringdown050_target_specific_rescue_summary.md`
- `docs/experiments/470_variable_depth_radius_seed28657_target0_sources8_txrx60_ringdown050.md`
- `docs/experiments/471_variable_depth_radius_seed28657_target2_sources5_txrx60_ringdown050.md`
- `docs/experiments/472_variable_depth_radius_seed28657_target1_sources5_txrx60_ringdown050.md`
- `docs/experiments/473_seed28657_ringdown050_target_specific_8_5_5_summary.md`
- `docs/experiments/474_variable_depth_radius_seed46368_target0_sources8_txrx60_ringdown050.md`
- `docs/experiments/475_variable_depth_radius_seed46368_target2_sources5_txrx60_ringdown050.md`
- `docs/experiments/476_variable_depth_radius_seed46368_target1_sources5_txrx60_ringdown050.md`
- `docs/experiments/477_seed46368_ringdown050_target_specific_8_5_5_summary.md`
- `docs/experiments/478_variable_depth_radius_seed75025_target0_sources8_txrx60_ringdown050.md`
- `docs/experiments/479_variable_depth_radius_seed75025_target2_sources5_txrx60_ringdown050.md`
- `docs/experiments/480_variable_depth_radius_seed75025_target2_sources9_txrx60_ringdown050.md`
- `docs/experiments/481_variable_depth_radius_seed75025_target1_sources5_txrx60_ringdown050.md`
- `docs/experiments/482_seed75025_ringdown050_target_specific_rescue_summary.md`
- `docs/experiments/483_variable_depth_radius_seed121393_target0_sources8_txrx60_ringdown050.md`
- `docs/experiments/484_variable_depth_radius_seed121393_target0_sources9_txrx60_ringdown050.md`
- `docs/experiments/485_variable_depth_radius_seed121393_target0_sources11_txrx60_ringdown050.md`
- `docs/experiments/486_seed121393_ringdown050_target0_unresolved_source_density_summary.md`
- `docs/experiments/487_variable_depth_radius_seed196418_target0_sources8_txrx60_ringdown050.md`
- `docs/experiments/488_variable_depth_radius_seed196418_target2_sources5_txrx60_ringdown050.md`
- `docs/experiments/489_variable_depth_radius_seed196418_target1_sources5_txrx60_ringdown050.md`
- `docs/experiments/490_variable_depth_radius_seed196418_target1_sources9_txrx60_ringdown050.md`
- `docs/experiments/491_seed196418_ringdown050_target_specific_rescue_summary.md`
- `docs/experiments/492_variable_depth_radius_seed317811_target0_sources8_txrx60_ringdown050.md`
- `docs/experiments/493_variable_depth_radius_seed317811_target2_sources5_txrx60_ringdown050.md`
- `docs/experiments/494_variable_depth_radius_seed317811_target2_sources9_txrx60_ringdown050.md`
- `docs/experiments/495_variable_depth_radius_seed317811_target1_sources5_txrx60_ringdown050.md`
- `docs/experiments/496_seed317811_ringdown050_target_specific_rescue_summary.md`
- `docs/experiments/497_variable_depth_radius_seed514229_target0_sources8_txrx60_ringdown050.md`
- `docs/experiments/498_variable_depth_radius_seed514229_target2_sources5_txrx60_ringdown050.md`
- `docs/experiments/499_variable_depth_radius_seed514229_target2_sources9_txrx60_ringdown050.md`

---

## Source: `docs/experiments/400_variable_depth_radius_seed34_target2_sources7_txrx60_ringdown050.md`

# Experiment 400: Seed34 Target-2 Sources=7 Tx/Rx=60 Ringdown050

## Purpose

Run 866 tests whether the seed34 target-2 full-ringdown050 rescue can be
reduced from 9 sources to 7 sources. This is the intermediate point between
rejected 5-source run 861 and accepted 9-source run 862.

## 866: Coordinate Optimizer Variable-Depth/Radius Seed34 Target-2 Sources=7 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/866_coordinate_optimizer_variable_depth_radius_seed34_target2_sources7_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 7 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed34:1.1,-50.0,1.1,0.10,34,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed34 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed34_target2_sources7_txrx60_ringdown050_objectives
```

## Results

Run 866 recovers the exact final geometry but is rejected by production
confidence:

```text
target: 2
sources: 7
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 4.168378e-04
offset from cutoff: -8.316e-05
confidence label: weak
fallback warning: radius_weak_confidence
elapsed: 544.68 s
```

Diagnostic objective rows all preserve the true target-2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.168378e-04 | below cutoff |
| highband | 5.281173e-04 | above cutoff |
| late | 5.940424e-04 | above cutoff |
| late_high | 6.386302e-04 | above cutoff |
| veryhigh | 5.168345e-04 | above cutoff |
| early_high | 3.382891e-04 | below cutoff |

## Interpretation

The 7-source target-2 row is exact but weak. Compared with the existing
seed34 target-2 rows:

| Run | Sources | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | --- |
| 861 | 5 | 4.575126e-04 | -4.249e-05 | rejected |
| 866 | 7 | 4.168378e-04 | -8.316e-05 | rejected |
| 862 | 9 | 5.256874e-04 | +2.569e-05 | accepted |

Seven sources does not rescue seed34 target 2. Because 7 is weaker than 5
under this source geometry, source count is not behaving monotonically enough
to infer the 8-source result. A direct 8-source test is needed.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
figure validation: coordinate_confidence_margins.png is 1549x903 RGB with nonwhite_fraction=0.216128 and full 0-255 dynamic range
visual inspection: confidence figure is readable and correctly shows one weak row below the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
source validation: all six diagnostic objectives preserve target2 truth geometry; 4/6 clear cutoff
resources: GPU utilization held around 89-90%; RAM stayed about 97-98 GiB available
elapsed: 544.68 s
```

## Next Decision

Run seed34 target2 at 8 sources and full ringdown050 before deciding whether
the policy must remain at 9 sources.

---

## Source: `docs/experiments/401_variable_depth_radius_seed34_target2_sources8_txrx60_ringdown050.md`

# Experiment 401: Seed34 Target-2 Sources=8 Tx/Rx=60 Ringdown050

## Purpose

Run 867 tests the 8-source intermediate for seed34 target 2 at full
ringdown050 after 5 and 7 sources were weak and 9 sources passed.

## 867: Coordinate Optimizer Variable-Depth/Radius Seed34 Target-2 Sources=8 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/867_coordinate_optimizer_variable_depth_radius_seed34_target2_sources8_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 8 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed34:1.1,-50.0,1.1,0.10,34,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed34 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed34_target2_sources8_txrx60_ringdown050_objectives
```

## Results

Run 867 is exact but weak:

```text
target: 2
sources: 8
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 4.722037e-04
offset from cutoff: -2.780e-05
confidence label: weak
fallback warning: radius_weak_confidence
elapsed: 669.82 s
```

Diagnostic objective rows all preserve the true target-2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.722037e-04 | below cutoff |
| highband | 6.141275e-04 | above cutoff |
| late | 7.859409e-04 | above cutoff |
| late_high | 9.184972e-04 | above cutoff |
| veryhigh | 6.005601e-04 | above cutoff |
| early_high | 4.114033e-04 | below cutoff |

## Interpretation

Seed34 target 2 still needs 9 sources at full ringdown050:

| Run | Sources | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | --- |
| 861 | 5 | 4.575126e-04 | -4.249e-05 | rejected |
| 866 | 7 | 4.168378e-04 | -8.316e-05 | rejected |
| 867 | 8 | 4.722037e-04 | -2.780e-05 | rejected |
| 862 | 9 | 5.256874e-04 | +2.569e-05 | accepted |

Eight sources is the strongest rejected intermediate, but it remains below the
production cutoff. The accepted seed34 policy remains target-specific `8/5/9`.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
figure validation: coordinate_confidence_margins.png is 1549x903 RGB with nonwhite_fraction=0.241370 and full 0-255 dynamic range
visual inspection: confidence figure is readable and correctly shows one weak row below the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
source validation: all six diagnostic objectives preserve target2 truth geometry; 4/6 clear cutoff
resources: GPU utilization held around 90-91%; RAM stayed about 97-98 GiB available
elapsed: 669.82 s
```

## Next Decision

Move the intermediate source-count test to seed89 target 2 to see whether the
9-source target-2 rescue is seed-specific or broadly required.

---

## Source: `docs/experiments/402_variable_depth_radius_seed89_target2_sources7_txrx60_ringdown050.md`

# Experiment 402: Seed89 Target-2 Sources=7 Tx/Rx=60 Ringdown050

## Purpose

Run 868 tests the 7-source intermediate for seed89 target 2 at full
ringdown050, between rejected 5-source run 843 and accepted 9-source run 844.

## 868: Coordinate Optimizer Variable-Depth/Radius Seed89 Target-2 Sources=7 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/868_coordinate_optimizer_variable_depth_radius_seed89_target2_sources7_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 7 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed89:1.1,-50.0,1.1,0.10,89,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed89 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed89_target2_sources7_txrx60_ringdown050_objectives
```

## Results

Run 868 is exact but weak:

```text
target: 2
sources: 7
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 4.616187e-04
offset from cutoff: -3.838e-05
confidence label: weak
fallback warning: radius_weak_confidence
elapsed: 534.10 s
```

Diagnostic objective rows all preserve the true target-2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.616187e-04 | below cutoff |
| highband | 5.946083e-04 | above cutoff |
| late | 6.479546e-04 | above cutoff |
| late_high | 7.401057e-04 | above cutoff |
| veryhigh | 6.148736e-04 | above cutoff |
| early_high | 3.881831e-04 | below cutoff |

## Interpretation

Seven sources are not enough for seed89 target 2 at full ringdown050:

| Run | Sources | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | --- |
| 843 | 5 | 4.414917e-04 | -5.851e-05 | rejected |
| 868 | 7 | 4.616187e-04 | -3.838e-05 | rejected |
| 844 | 9 | 5.821195e-04 | +8.212e-05 | accepted |

The 7-source result is closer to cutoff than the 5-source control, but it is
still weak. A direct 8-source check is required before deciding whether seed89
target 2 also requires 9 sources.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
figure validation: coordinate_confidence_margins.png is 1549x903 RGB with nonwhite_fraction=0.236886 and full 0-255 dynamic range
visual inspection: confidence figure is readable and correctly shows one weak row below the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
source validation: all six diagnostic objectives preserve target2 truth geometry; 4/6 clear cutoff
resources: GPU utilization held around 89-90%; RAM stayed about 97-98 GiB available
elapsed: 534.10 s
```

## Next Decision

Run seed89 target 2 at 8 sources and full ringdown050.

---

## Source: `docs/experiments/403_variable_depth_radius_seed89_target2_sources8_txrx60_ringdown050.md`

# Experiment 403: Seed89 Target-2 Sources=8 Tx/Rx=60 Ringdown050

## Purpose

Run 869 tests the 8-source intermediate for seed89 target 2 at full
ringdown050 after 5 and 7 sources were weak and 9 sources passed.

## 869: Coordinate Optimizer Variable-Depth/Radius Seed89 Target-2 Sources=8 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/869_coordinate_optimizer_variable_depth_radius_seed89_target2_sources8_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 8 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed89:1.1,-50.0,1.1,0.10,89,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed89 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed89_target2_sources8_txrx60_ringdown050_objectives
```

## Results

Run 869 is exact but weak:

```text
target: 2
sources: 8
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 4.604986e-04
offset from cutoff: -3.950e-05
confidence label: weak
fallback warning: radius_weak_confidence
elapsed: 592.48 s
```

Diagnostic objective rows all preserve the true target-2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.604986e-04 | below cutoff |
| highband | 6.091603e-04 | above cutoff |
| late | 7.496210e-04 | above cutoff |
| late_high | 8.621242e-04 | above cutoff |
| veryhigh | 5.904505e-04 | above cutoff |
| early_high | 4.134825e-04 | below cutoff |

## Interpretation

Seed89 target 2 requires 9 sources under the current full-ringdown050
production objective:

| Run | Sources | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | --- |
| 843 | 5 | 4.414917e-04 | -5.851e-05 | rejected |
| 868 | 7 | 4.616187e-04 | -3.838e-05 | rejected |
| 869 | 8 | 4.604986e-04 | -3.950e-05 | rejected |
| 844 | 9 | 5.821195e-04 | +8.212e-05 | accepted |

This matches the closed seed34 branch: 5, 7, and 8 sources are exact but
weak, while 9 sources passes.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
figure validation: coordinate_confidence_margins.png is 1549x903 RGB with nonwhite_fraction=0.235984 and full 0-255 dynamic range
visual inspection: confidence figure is readable and correctly shows one weak row below the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
source validation: all six diagnostic objectives preserve target2 truth geometry; 4/6 clear cutoff
resources: GPU utilization held around 90-91%; RAM stayed about 97-98 GiB available
elapsed: 592.48 s
```

## Next Decision

Move to fifth-seed target-0 replication at full ringdown050 to test whether
seed21's shallow-target near-miss is a recurring lower-tail case.

---

## Source: `docs/experiments/404_variable_depth_radius_seed55_target0_sources8_txrx60_ringdown050.md`

# Experiment 404: Seed55 Target-0 Sources=8 Tx/Rx=60 Ringdown050

## Purpose

Run 870 tests target 0 at full ringdown050 on a fifth noise seed. This
directly probes whether seed21's target-0 near-miss is isolated or recurring.

## 870: Coordinate Optimizer Variable-Depth/Radius Seed55 Target-0 Sources=8 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/870_coordinate_optimizer_variable_depth_radius_seed55_target0_sources8_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 8 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 0 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed55:1.1,-50.0,1.1,0.10,55,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed55 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed55_target0_sources8_txrx60_ringdown050_objectives
```

## Results

Run 870 is exact and accepted:

```text
target: 0
sources: 8
best: x=150 mm, z=80 mm, r=5.0 mm
next radius: 5.25 mm
absolute radius margin: 5.079048e-04
offset from cutoff: +7.905e-06
confidence label: moderate
fallback warning: none
elapsed: 579.40 s
```

Diagnostic objective rows all preserve the true target-0 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.079048e-04 | above cutoff |
| highband | 6.733503e-04 | above cutoff |
| late | 3.985385e-04 | below cutoff |
| late_high | 4.961680e-04 | below cutoff |
| veryhigh | 6.257398e-04 | above cutoff |
| early_high | 5.631874e-04 | above cutoff |

## Interpretation

Seed55 target 0 passes full ringdown050, but with low reserve:

| Seed | Run | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | --- |
| 13 | 840 | 5.625753e-04 | +6.258e-05 | accepted |
| 89 | 842 | 5.460353e-04 | +4.604e-05 | accepted |
| 21 | 846 | 4.975041e-04 | -2.496e-06 | rejected |
| 34 | 859 | 5.310935e-04 | +3.109e-05 | accepted |
| 55 | 870 | 5.079048e-04 | +7.905e-06 | accepted |

Seed21 remains the only target-0 full-ringdown050 failure in the tested set,
but seed55's small reserve means the shallow-target policy should not be
described as wide-margin robust.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
figure validation: coordinate_confidence_margins.png is 1549x903 RGB with nonwhite_fraction=0.258524 and full 0-255 dynamic range
visual inspection: confidence figure is readable and correctly shows one moderate row above the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target0 truth_radius_mm is 5.0 mm
source validation: all six diagnostic objectives preserve target0 truth geometry; 4/6 clear cutoff
resources: GPU utilization held around 90-91%; RAM stayed about 97-98 GiB available
elapsed: 579.40 s
```

## Next Decision

Run seed55 target 2 at 5 sources and full ringdown050 to determine whether
seed55 follows the seed13 target2 5-source pass pattern or the seed89/seed34
9-source rescue pattern.

---

## Source: `docs/experiments/405_variable_depth_radius_seed55_target2_sources5_txrx60_ringdown050.md`

# Experiment 405: Seed55 Target-2 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 871 tests whether seed55 target 2 passes at 5 sources under full
ringdown050. This distinguishes seed55 from the seed89/seed34 target-2 rescue
pattern if it passes.

## 871: Coordinate Optimizer Variable-Depth/Radius Seed55 Target-2 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/871_coordinate_optimizer_variable_depth_radius_seed55_target2_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed55:1.1,-50.0,1.1,0.10,55,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed55 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed55_target2_sources5_txrx60_ringdown050_objectives
```

## Results

Run 871 is exact and accepted:

```text
target: 2
sources: 5
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 5.677153e-04
offset from cutoff: +6.772e-05
confidence label: moderate
fallback warning: none
elapsed: 392.27 s
```

Diagnostic objective rows all preserve the true target-2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.677153e-04 | above cutoff |
| highband | 7.019763e-04 | above cutoff |
| late | 8.812680e-04 | above cutoff |
| late_high | 9.384895e-04 | above cutoff |
| veryhigh | 7.310345e-04 | above cutoff |
| early_high | 4.871218e-04 | below cutoff |

## Interpretation

Seed55 target 2 passes at 5 sources, unlike seed89 and seed34:

| Seed | Run | Sources | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | ---: | --- |
| 13 | 838 | 5 | 5.882895e-04 | +8.829e-05 | accepted |
| 89 | 843 | 5 | 4.414917e-04 | -5.851e-05 | rejected |
| 34 | 861 | 5 | 4.575126e-04 | -4.249e-05 | rejected |
| 55 | 871 | 5 | 5.677153e-04 | +6.772e-05 | accepted |

Seed55 now has target0 and target2 accepted at full ringdown050. Target1
should complete the seed55 policy set.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
figure validation: coordinate_confidence_margins.png is 1549x903 RGB with nonwhite_fraction=0.286459 and full 0-255 dynamic range
visual inspection: confidence figure is readable and correctly shows one moderate row above the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
source validation: all six diagnostic objectives preserve target2 truth geometry; 5/6 clear cutoff
resources: GPU utilization held around 87-88%; RAM stayed about 97-98 GiB available
elapsed: 392.27 s
```

## Next Decision

Run seed55 target 1 at 5 sources and full ringdown050.

---

## Source: `docs/experiments/406_variable_depth_radius_seed55_target1_sources5_txrx60_ringdown050.md`

# Experiment 406: Seed55 Target-1 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 872 completes the seed55 target-specific policy set by testing target 1
with the 5-source full-ringdown050 policy.

## 872: Coordinate Optimizer Variable-Depth/Radius Seed55 Target-1 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/872_coordinate_optimizer_variable_depth_radius_seed55_target1_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 1 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed55:1.1,-50.0,1.1,0.10,55,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed55 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed55_target1_sources5_txrx60_ringdown050_objectives
```

## Results

Run 872 is exact and accepted:

```text
target: 1
sources: 5
best: x=250 mm, z=100 mm, r=6.0 mm
next radius: 6.25 mm
absolute radius margin: 5.506251e-04
offset from cutoff: +5.063e-05
confidence label: moderate
fallback warning: none
elapsed: 364.59 s
```

Diagnostic objective rows all preserve the true target-1 geometry and all clear
cutoff:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.506251e-04 | above cutoff |
| highband | 7.372422e-04 | above cutoff |
| late | 8.415132e-04 | above cutoff |
| late_high | 9.673456e-04 | above cutoff |
| veryhigh | 6.631702e-04 | above cutoff |
| early_high | 5.474839e-04 | above cutoff |

## Interpretation

Seed55 passes all three promoted full-ringdown050 policy rows:

| Target | Run | Sources | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | ---: | --- |
| 0 | 870 | 8 | 5.079048e-04 | +7.905e-06 | accepted |
| 1 | 872 | 5 | 5.506251e-04 | +5.063e-05 | accepted |
| 2 | 871 | 5 | 5.677153e-04 | +6.772e-05 | accepted |

Seed55 therefore joins seed13 as an `8/5/5` full-ringdown050 seed, while
seed89 and seed34 remain `8/5/9` and seed21 remains target-0 limited below
ringdown050.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
figure validation: coordinate_confidence_margins.png is 1549x903 RGB with nonwhite_fraction=0.278334 and full 0-255 dynamic range
visual inspection: confidence figure is readable and correctly shows one moderate row above the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target1 truth_radius_mm is 6.0 mm
source validation: all six diagnostic objectives preserve target1 truth geometry and clear cutoff
resources: GPU utilization held around 87-88%; RAM stayed about 97-98 GiB available
elapsed: 364.59 s
```

## Next Decision

Create the seed55 full-ringdown050 `8/5/5` summary and continue target-0
lower-tail replication.

---

## Source: `docs/experiments/407_variable_depth_radius_seed144_target0_sources8_txrx60_ringdown050.md`

# Experiment 407: Seed144 Target-0 Sources=8 Tx/Rx=60 Ringdown050

## Purpose

Run 873 tests target 0 at full ringdown050 on a sixth noise seed, extending
the shallow-target lower-tail replication after seed55 passed with low reserve.

## 873: Coordinate Optimizer Variable-Depth/Radius Seed144 Target-0 Sources=8 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/873_coordinate_optimizer_variable_depth_radius_seed144_target0_sources8_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 8 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 0 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed144:1.1,-50.0,1.1,0.10,144,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed144 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed144_target0_sources8_txrx60_ringdown050_objectives
```

## Results

Run 873 is exact and accepted:

```text
target: 0
sources: 8
best: x=150 mm, z=80 mm, r=5.0 mm
next radius: 5.25 mm
absolute radius margin: 6.144391e-04
offset from cutoff: +1.144e-04
confidence label: moderate
fallback warning: none
elapsed: 554.28 s
```

Diagnostic objective rows all preserve the true target-0 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 6.144391e-04 | above cutoff |
| highband | 7.957526e-04 | above cutoff |
| late | 4.565904e-04 | below cutoff |
| late_high | 5.205671e-04 | above cutoff |
| veryhigh | 7.414143e-04 | above cutoff |
| early_high | 6.675922e-04 | above cutoff |

## Interpretation

Seed144 is not a target-0 lower-tail case. It is the strongest target0
full-ringdown050 pass observed so far:

| Seed | Run | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | --- |
| 13 | 840 | 5.625753e-04 | +6.258e-05 | accepted |
| 89 | 842 | 5.460353e-04 | +4.604e-05 | accepted |
| 21 | 846 | 4.975041e-04 | -2.496e-06 | rejected |
| 34 | 859 | 5.310935e-04 | +3.109e-05 | accepted |
| 55 | 870 | 5.079048e-04 | +7.905e-06 | accepted |
| 144 | 873 | 6.144391e-04 | +1.144e-04 | accepted |

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
figure validation: coordinate_confidence_margins.png is 1549x903 RGB with nonwhite_fraction=0.302732 and full 0-255 dynamic range
visual inspection: confidence figure is readable and correctly shows one moderate row above the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target0 truth_radius_mm is 5.0 mm
source validation: all six diagnostic objectives preserve target0 truth geometry; 5/6 clear cutoff
resources: GPU utilization held around 90-91%; RAM stayed about 97-98 GiB available
elapsed: 554.28 s
```

## Next Decision

Run seed144 target 2 at 5 sources and full ringdown050.

---

## Source: `docs/experiments/408_seed55_ringdown050_target_specific_8_5_5_summary.md`

# Experiment 408: Seed55 Ringdown050 Target-Specific 8/5/5 Summary

## Purpose

Run 874 summarizes the completed seed55 full-ringdown050 policy branch.

## 874: Seed55 Ringdown050 Target-Specific 8/5/5 Summary

Output:

```text
outputs/experiments/874_seed55_ringdown050_target_specific_8_5_5_summary
```

Generation:

```text
CPU aggregation of promoted rows from runs 870, 872, and 871, plus target2
5-source comparison rows from runs 838, 843, 861, and 871.
```

Promoted rows:

| Target | Run | Tracker | Sources | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 0 | 870 | 404 | 8 | 5.079048e-04 | +7.905e-06 | accepted |
| 1 | 872 | 406 | 5 | 5.506251e-04 | +5.063e-05 | accepted |
| 2 | 871 | 405 | 5 | 5.677153e-04 | +6.772e-05 | accepted |

## Results

Seed55 passes as a full-ringdown050 `8/5/5` seed. The limiting row is target 0
from run 870, only `7.905e-06` above cutoff.

Objective diagnostics:

```text
diagnostic rows: 18
truth-preserving diagnostic rows: 18
diagnostic rows above cutoff: 15
sub-cutoff diagnostics: target0 late, target0 late_high, target2 early_high
```

Target2 5-source cross-seed comparison:

| Seed | Run | Sources | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | ---: | --- |
| 13 | 838 | 5 | 5.882895e-04 | +8.829e-05 | accepted |
| 89 | 843 | 5 | 4.414917e-04 | -5.851e-05 | rejected |
| 34 | 861 | 5 | 4.575126e-04 | -4.249e-05 | rejected |
| 55 | 871 | 5 | 5.677153e-04 | +6.772e-05 | accepted |

## Interpretation

Seed55 joins seed13 as an `8/5/5` full-ringdown050 seed. Seed89 and seed34
remain `8/5/9`, and seed21 remains target-0 limited below full ringdown050.
The target2 split is now balanced in the tested full-ringdown050 seeds:
seed13/seed55 pass with 5 sources, while seed89/seed34 require 9.

## Validation

```text
JSON parse: seed55_ringdown050_target_specific_summary.json and run_manifest.json pass
CSV rows: base rows=3, objective rows=18, target2 cross-seed rows=4
source validation: promoted truth rows=3/3, promoted rows above cutoff=3/3
diagnostic validation: 18/18 diagnostics preserve truth, 15/18 diagnostics clear cutoff
figure validation: base margins plot is 1376x864 RGB with nonwhite_fraction=0.494790 and full 0-255 dynamic range
figure validation: objective heatmap is 1792x912 RGB with nonwhite_fraction=0.665367 and full 0-255 dynamic range
figure validation: target2 comparison plot is 1472x880 RGB with nonwhite_fraction=0.503137 and full 0-255 dynamic range
visual inspection: all three figures are readable; heatmap red outlines mark sub-cutoff cells
resources: CPU aggregation only; no GPU workload
```

## Next Decision

Use run 874 as the seed55 input to the next cross-seed synthesis, and continue
seed144 target-specific tests.

---

## Source: `docs/experiments/409_variable_depth_radius_seed144_target2_sources5_txrx60_ringdown050.md`

# Experiment 409: Seed144 Target-2 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 875 tests seed144 target 2 at full ringdown050 with 5 sources, extending
the seed144 target-specific policy branch after target 0 passed at 8 sources.

## 875: Coordinate Optimizer Variable-Depth/Radius Seed144 Target-2 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/875_coordinate_optimizer_variable_depth_radius_seed144_target2_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed144:1.1,-50.0,1.1,0.10,144,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed144 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed144_target2_sources5_txrx60_ringdown050_objectives
```

## Results

Run 875 is exact and accepted:

```text
target: 2
sources: 5
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 5.470037e-04
offset from cutoff: +4.700e-05
confidence label: moderate
fallback warning: none
elapsed: 361.60 s
```

Diagnostic objective rows all preserve the true target-2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.470037e-04 | above cutoff |
| highband | 6.555797e-04 | above cutoff |
| late | 8.058232e-04 | above cutoff |
| late_high | 8.542402e-04 | above cutoff |
| veryhigh | 6.958890e-04 | above cutoff |
| early_high | 4.773721e-04 | below cutoff |

## Interpretation

Seed144 target 2 passes the minimal 5-source full-ringdown050 policy. The
target-2 5-source cross-seed set now has accepted rows for seed13, seed55, and
seed144, with seed89 and seed34 still requiring 9-source rescue rows.

| Seed | Run | Sources | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | ---: | --- |
| 13 | 838 | 5 | 5.882895e-04 | +8.829e-05 | accepted |
| 89 | 843 | 5 | 4.414917e-04 | -5.851e-05 | rejected |
| 34 | 861 | 5 | 4.575126e-04 | -4.249e-05 | rejected |
| 55 | 871 | 5 | 5.677153e-04 | +6.772e-05 | accepted |
| 144 | 875 | 5 | 5.470037e-04 | +4.700e-05 | accepted |

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
figure validation: coordinate_confidence_margins.png is 1549x903 RGB with nonwhite_fraction=0.272970 and full 0-255 dynamic range
visual inspection: confidence figure is readable and correctly shows one moderate row above the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
source validation: all six diagnostic objectives preserve target2 truth geometry; 5/6 clear cutoff
resources: GPU production run; RAM stayed near the prior safe envelope
elapsed: 361.60 s
```

## Next Decision

Run seed144 target 1 at 5 sources and full ringdown050. If accepted, summarize
seed144 as another `8/5/5` policy seed.

---

## Source: `docs/experiments/410_variable_depth_radius_seed144_target1_sources5_txrx60_ringdown050.md`

# Experiment 410: Seed144 Target-1 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 876 completes the seed144 full-ringdown050 target-specific branch by
testing target 1 at 5 sources.

## 876: Coordinate Optimizer Variable-Depth/Radius Seed144 Target-1 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/876_coordinate_optimizer_variable_depth_radius_seed144_target1_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 1 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed144:1.1,-50.0,1.1,0.10,144,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed144 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed144_target1_sources5_txrx60_ringdown050_objectives
```

## Results

Run 876 is exact and accepted:

```text
target: 1
sources: 5
best: x=250 mm, z=100 mm, r=6.0 mm
next radius: 6.25 mm
absolute radius margin: 5.183405e-04
offset from cutoff: +1.834e-05
confidence label: moderate
fallback warning: none
elapsed: 363.63 s
```

Diagnostic objective rows all preserve the true target-1 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.183405e-04 | above cutoff |
| highband | 6.233071e-04 | above cutoff |
| late | 7.908452e-04 | above cutoff |
| late_high | 7.967119e-04 | above cutoff |
| veryhigh | 5.849819e-04 | above cutoff |
| early_high | 4.688053e-04 | below cutoff |

## Interpretation

Seed144 now has accepted full-ringdown050 target-specific rows for target 0,
target 1, and target 2:

| Target | Run | Sources | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | ---: | --- |
| 0 | 873 | 8 | 6.144391e-04 | +1.144e-04 | accepted |
| 1 | 876 | 5 | 5.183405e-04 | +1.834e-05 | accepted |
| 2 | 875 | 5 | 5.470037e-04 | +4.700e-05 | accepted |

The seed144 policy is `8/5/5`, with target 1 as the limiting row.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.259550 and full 0-255 dynamic range
visual inspection: confidence figure is readable and correctly shows one moderate row above the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target1 truth_radius_mm is 6.0 mm
source validation: all six diagnostic objectives preserve target1 truth geometry; 5/6 clear cutoff
resources: GPU utilization held around 88%; RAM stayed around 97-98 GiB available
elapsed: 363.63 s
```

## Next Decision

Generate the seed144 `8/5/5` target-specific summary and refresh the
cross-seed full-ringdown050 synthesis.

---

## Source: `docs/experiments/411_seed144_ringdown050_target_specific_8_5_5_summary.md`

# Experiment 411: Seed144 Ringdown050 Target-Specific 8/5/5 Summary

## Purpose

Run 877 summarizes the completed seed144 full-ringdown050 policy branch.

## 877: Seed144 Ringdown050 Target-Specific 8/5/5 Summary

Output:

```text
outputs/experiments/877_seed144_ringdown050_target_specific_8_5_5_summary
```

Generation:

```text
CPU aggregation of promoted rows from runs 873, 876, and 875, plus target2
5-source comparison rows from runs 838, 843, 861, 871, and 875.
```

Promoted rows:

| Target | Run | Tracker | Sources | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 0 | 873 | 407 | 8 | 6.144391e-04 | +1.144e-04 | accepted |
| 1 | 876 | 410 | 5 | 5.183405e-04 | +1.834e-05 | accepted |
| 2 | 875 | 409 | 5 | 5.470037e-04 | +4.700e-05 | accepted |

## Results

Seed144 passes as a full-ringdown050 `8/5/5` seed. The limiting row is target
1 from run 876, `1.834e-05` above cutoff.

Objective diagnostics:

```text
diagnostic rows: 18
truth-preserving diagnostic rows: 18
diagnostic rows above cutoff: 15
sub-cutoff diagnostics: target0 late, target1 early_high, target2 early_high
```

Target2 5-source cross-seed comparison:

| Seed | Run | Sources | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | ---: | --- |
| 13 | 838 | 5 | 5.882895e-04 | +8.829e-05 | accepted |
| 89 | 843 | 5 | 4.414917e-04 | -5.851e-05 | rejected |
| 34 | 861 | 5 | 4.575126e-04 | -4.249e-05 | rejected |
| 55 | 871 | 5 | 5.677153e-04 | +6.772e-05 | accepted |
| 144 | 875 | 5 | 5.470037e-04 | +4.700e-05 | accepted |

## Interpretation

Seed144 joins seed13 and seed55 as an `8/5/5` full-ringdown050 seed. Seed89
and seed34 remain `8/5/9`, and seed21 remains target-0 limited below full
ringdown050. The target2 5-source split now has three accepted seeds and two
weak seeds, reinforcing that the target2 source-count policy is seed-dependent
rather than a universal 5-source or 9-source rule.

## Validation

```text
JSON parse: seed144_ringdown050_target_specific_summary.json and run_manifest.json pass
CSV rows: base rows=3, objective rows=18, target2 cross-seed rows=5
source validation: promoted truth rows=3/3, promoted rows above cutoff=3/3
diagnostic validation: 18/18 diagnostics preserve truth, 15/18 diagnostics clear cutoff
figure validation: base margins plot is 1376x864 RGBA with nonwhite_fraction=0.425864 and full 0-255 dynamic range
figure validation: objective heatmap is 1792x912 RGBA with nonwhite_fraction=0.629932 and full 0-255 dynamic range
figure validation: target2 comparison plot is 1472x880 RGBA with nonwhite_fraction=0.417421 and full 0-255 dynamic range
visual inspection: all three figures are readable; heatmap red outlines mark sub-cutoff cells
resources: CPU aggregation only; no GPU workload
```

## Next Decision

Refresh the cross-seed policy synthesis with seed55 and seed144, and continue
full-ringdown050 lower-tail replication on the next Fibonacci seed.

---

## Source: `docs/experiments/412_cross_seed_ringdown050_target_specific_policy_synthesis_v2.md`

# Experiment 412: Cross-Seed Ringdown050 Target-Specific Policy Synthesis V2

## Purpose

Run 879 refreshes the cross-seed target-specific acquisition policy synthesis
after adding the completed seed55 and seed144 policy summaries.

## 879: Cross-Seed Ringdown050 Target-Specific Policy Synthesis V2

Output:

```text
outputs/experiments/879_cross_seed_ringdown050_target_specific_policy_synthesis_v2
```

Generation:

```text
CPU aggregation of promoted policy rows from seeds 13, 89, 34, 21, 55, and
144, plus rejected full-ringdown050 controls from seed21 target0 and the
seed89/seed34 target2 source-count refinement branch.
```

Per-seed limiting rows:

| Seed | Policy | Condition | Summary run | Limiting target | Limiting run | Limiting margin |
| ---: | --- | --- | ---: | ---: | ---: | ---: |
| 13 | 8/5/5 | full ringdown050 pass | 841 | 1 | 834 | 5.293926e-04 |
| 89 | 8/5/9 | full ringdown050 pass | 845 | 0 | 842 | 5.460353e-04 |
| 34 | 8/5/9 | full ringdown050 pass | 863 | 2 | 862 | 5.256874e-04 |
| 21 | 8/5/9 practical | target0 limited below ringdown050 | 857 | 0 | 854 | 5.000315e-04 |
| 55 | 8/5/5 | full ringdown050 pass | 874 | 0 | 870 | 5.079048e-04 |
| 144 | 8/5/5 | full ringdown050 pass | 877 | 1 | 876 | 5.183405e-04 |

## Results

The refreshed synthesis has 18 promoted policy rows:

```text
policy rows: 18
policy truth rows: 18
policy rows above cutoff: 18
rejected controls: 9
rejected truth rows: 9
rejected rows below cutoff: 9
seed summary rows: 6
```

Rejected controls:

| Seed | Target | Run | Sources | Margin | Reason |
| ---: | ---: | ---: | ---: | ---: | --- |
| 21 | 0 | 846 | 8 | 4.975041e-04 | seed21 target0 8-source ringdown050 near-miss |
| 21 | 0 | 847 | 9 | 4.718459e-04 | seed21 target0 9-source rescue weakened production row |
| 21 | 0 | 858 | 7 | 4.425949e-04 | seed21 target0 7-source rescue weakened production row |
| 89 | 2 | 843 | 5 | 4.414917e-04 | seed89 target2 5-source row weak before 9-source rescue |
| 89 | 2 | 868 | 7 | 4.616187e-04 | seed89 target2 7-source row weak before 9-source rescue |
| 89 | 2 | 869 | 8 | 4.604986e-04 | seed89 target2 8-source row weak before 9-source rescue |
| 34 | 2 | 861 | 5 | 4.575126e-04 | seed34 target2 5-source row weak before 9-source rescue |
| 34 | 2 | 866 | 7 | 4.168378e-04 | seed34 target2 7-source row weak before 9-source rescue |
| 34 | 2 | 867 | 8 | 4.722037e-04 | seed34 target2 8-source row weak before 9-source rescue |

## Interpretation

The current evidence supports a target-specific source-count policy, not a
single universal source count. Seed13, seed55, and seed144 pass at `8/5/5`.
Seed89 and seed34 pass full ringdown050 only at `8/5/9` because target 2 stays
weak at 5, 7, and 8 sources. Seed21 is a separate practical-threshold case:
target 0 and target 2 pass only at ringdown049453125, while target 1 passes at
full ringdown050.

The target-0 lower tail remains the most important open robustness question:
seed21 fails at full ringdown050, seed55 passes with a small reserve, and
seed144 passes strongly. The active seed233 target-0 run directly tests that
gap.

## Validation

```text
JSON parse: cross_seed_ringdown050_policy_synthesis.json and run_manifest.json pass
CSV rows: policy rows=18, rejected controls=9, seed summary rows=6
source validation: policy truth rows=18/18 and policy rows above cutoff=18/18
rejected validation: rejected truth rows=9/9 and rejected rows below cutoff=9/9
figure validation: full_ringdown050_base_margin_heatmap.png is 1312x1008 RGBA with nonwhite_fraction=0.657351 and full 0-255 dynamic range
figure validation: cross_seed_limiting_policy_margins.png is 1632x976 RGBA with nonwhite_fraction=0.439418 and full 0-255 dynamic range
figure validation: rejected_ringdown050_control_margins.png is 1952x1008 RGBA with nonwhite_fraction=0.417169 and full 0-255 dynamic range
visual inspection: all three figures are readable; practical-threshold and rejected-control markings are clear
resources: CPU aggregation and plotting only; no GPU workload
```

## Next Decision

Use run 879 as the current cross-seed policy synthesis and continue the active
seed233 target-0 lower-tail replication.

---

## Source: `docs/experiments/413_variable_depth_radius_seed233_target0_sources8_txrx60_ringdown050.md`

# Experiment 413: Seed233 Target-0 Sources=8 Tx/Rx=60 Ringdown050

## Purpose

Run 878 extends the full-ringdown050 target-0 lower-tail replication to the
next Fibonacci noise seed after seed144.

## 878: Coordinate Optimizer Variable-Depth/Radius Seed233 Target-0 Sources=8 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/878_coordinate_optimizer_variable_depth_radius_seed233_target0_sources8_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 8 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 0 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed233:1.1,-50.0,1.1,0.10,233,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed233 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed233_target0_sources8_txrx60_ringdown050_objectives
```

## Results

Run 878 is exact and accepted:

```text
target: 0
sources: 8
best: x=150 mm, z=80 mm, r=5.0 mm
next radius: 5.25 mm
absolute radius margin: 5.434236e-04
offset from cutoff: +4.342e-05
confidence label: moderate
fallback warning: none
elapsed: 557.96 s
```

Diagnostic objective rows all preserve the true target-0 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.434236e-04 | above cutoff |
| highband | 7.098527e-04 | above cutoff |
| late | 3.779758e-04 | below cutoff |
| late_high | 4.571601e-04 | below cutoff |
| veryhigh | 6.584969e-04 | above cutoff |
| early_high | 6.058978e-04 | above cutoff |

## Interpretation

Seed233 is another target-0 full-ringdown050 pass:

| Seed | Run | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | --- |
| 13 | 840 | 5.625753e-04 | +6.258e-05 | accepted |
| 89 | 842 | 5.460353e-04 | +4.604e-05 | accepted |
| 21 | 846 | 4.975041e-04 | -2.496e-06 | rejected |
| 34 | 859 | 5.310935e-04 | +3.109e-05 | accepted |
| 55 | 870 | 5.079048e-04 | +7.905e-06 | accepted |
| 144 | 873 | 6.144391e-04 | +1.144e-04 | accepted |
| 233 | 878 | 5.434236e-04 | +4.342e-05 | accepted |

Seed233 does not repeat the seed21 target-0 failure. The target-0 lower tail is
still important, but this row shifts the evidence toward seed21 being an
outlier rather than a common full-ringdown050 target-0 failure mode.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.270394 and full 0-255 dynamic range
visual inspection: confidence figure is readable and correctly shows one moderate row above the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target0 truth_radius_mm is 5.0 mm
source validation: all six diagnostic objectives preserve target0 truth geometry; 4/6 clear cutoff
resources: GPU utilization held around 91-92%; RAM stayed around 97-98 GiB available
elapsed: 557.96 s
```

## Next Decision

Run seed233 target 2 at 5 sources and full ringdown050.

---

## Source: `docs/experiments/414_variable_depth_radius_seed233_target2_sources5_txrx60_ringdown050.md`

# Experiment 414: Seed233 Target-2 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 880 tests seed233 target 2 at full ringdown050 with 5 sources.

## 880: Coordinate Optimizer Variable-Depth/Radius Seed233 Target-2 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/880_coordinate_optimizer_variable_depth_radius_seed233_target2_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed233:1.1,-50.0,1.1,0.10,233,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed233 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed233_target2_sources5_txrx60_ringdown050_objectives
```

## Results

Run 880 is exact and accepted:

```text
target: 2
sources: 5
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 5.878754e-04
offset from cutoff: +8.788e-05
confidence label: moderate
fallback warning: none
elapsed: 362.32 s
```

Diagnostic objective rows all preserve the true target-2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.878754e-04 | above cutoff |
| highband | 7.209866e-04 | above cutoff |
| late | 8.740772e-04 | above cutoff |
| late_high | 9.064916e-04 | above cutoff |
| veryhigh | 7.455708e-04 | above cutoff |
| early_high | 5.377057e-04 | above cutoff |

## Interpretation

Seed233 target 2 passes at 5 sources:

| Seed | Run | Sources | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | ---: | --- |
| 13 | 838 | 5 | 5.882895e-04 | +8.829e-05 | accepted |
| 89 | 843 | 5 | 4.414917e-04 | -5.851e-05 | rejected |
| 34 | 861 | 5 | 4.575126e-04 | -4.249e-05 | rejected |
| 55 | 871 | 5 | 5.677153e-04 | +6.772e-05 | accepted |
| 144 | 875 | 5 | 5.470037e-04 | +4.700e-05 | accepted |
| 233 | 880 | 5 | 5.878754e-04 | +8.788e-05 | accepted |

Seed233 aligns with the accepted 5-source target-2 seeds, not the seed89/seed34
9-source rescue branch.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.291118 and full 0-255 dynamic range
visual inspection: confidence figure is readable and correctly shows one moderate row above the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
source validation: all six diagnostic objectives preserve target2 truth geometry; 6/6 clear cutoff
resources: GPU production run; RAM stayed around 97-98 GiB available
elapsed: 362.32 s
```

## Next Decision

Run seed233 target 1 at 5 sources and full ringdown050. If accepted, summarize
seed233 as another `8/5/5` policy seed.

---

## Source: `docs/experiments/415_variable_depth_radius_seed233_target1_sources5_txrx60_ringdown050.md`

# Experiment 415: Seed233 Target-1 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 881 completes the seed233 full-ringdown050 target-specific branch by
testing target 1 at 5 sources.

## Results

Run 881 is exact and accepted:

```text
output: outputs/experiments/881_coordinate_optimizer_variable_depth_radius_seed233_target1_sources5_txrx60_ringdown050_objectives
target: 1
sources: 5
best: x=250 mm, z=100 mm, r=6.0 mm
next radius: 6.25 mm
absolute radius margin: 5.608831e-04
offset from cutoff: +6.088e-05
confidence label: moderate
fallback warning: none
elapsed: 362.25 s
```

Diagnostic objective rows all preserve the true target-1 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.608831e-04 | above cutoff |
| highband | 7.586555e-04 | above cutoff |
| late | 8.291244e-04 | above cutoff |
| late_high | 9.892309e-04 | above cutoff |
| veryhigh | 7.180034e-04 | above cutoff |
| early_high | 5.581712e-04 | above cutoff |

## Interpretation

Seed233 completes as an `8/5/5` full-ringdown050 seed:

| Target | Run | Sources | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | ---: | --- |
| 0 | 878 | 8 | 5.434236e-04 | +4.342e-05 | accepted |
| 1 | 881 | 5 | 5.608831e-04 | +6.088e-05 | accepted |
| 2 | 880 | 5 | 5.878754e-04 | +8.788e-05 | accepted |

Target 0 is the limiting seed233 row.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.278486 and full 0-255 dynamic range
visual inspection: confidence figure is readable and correctly shows one moderate row above the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
source validation: all six diagnostic objectives preserve target1 truth geometry; 6/6 clear cutoff
```

## Next Decision

Generate the seed233 `8/5/5` policy summary.

---

## Source: `docs/experiments/416_seed233_ringdown050_target_specific_8_5_5_summary.md`

# Experiment 416: Seed233 Ringdown050 Target-Specific 8/5/5 Summary

## Purpose

Run 882 summarizes the completed seed233 full-ringdown050 policy branch.

## 882: Seed233 Ringdown050 Target-Specific 8/5/5 Summary

Output:

```text
outputs/experiments/882_seed233_ringdown050_target_specific_8_5_5_summary
```

Promoted rows:

| Target | Run | Tracker | Sources | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 0 | 878 | 413 | 8 | 5.434236e-04 | +4.342e-05 | accepted |
| 1 | 881 | 415 | 5 | 5.608831e-04 | +6.088e-05 | accepted |
| 2 | 880 | 414 | 5 | 5.878754e-04 | +8.788e-05 | accepted |

## Results

Seed233 passes as a full-ringdown050 `8/5/5` seed. The limiting row is target
0 from run 878.

Objective diagnostics:

```text
diagnostic rows: 18
truth-preserving diagnostic rows: 18
diagnostic rows above cutoff: 16
sub-cutoff diagnostics: target0 late, target0 late_high
```

Target2 5-source cross-seed comparison:

| Seed | Run | Sources | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | ---: | --- |
| 13 | 838 | 5 | 5.882895e-04 | +8.829e-05 | accepted |
| 89 | 843 | 5 | 4.414917e-04 | -5.851e-05 | rejected |
| 34 | 861 | 5 | 4.575126e-04 | -4.249e-05 | rejected |
| 55 | 871 | 5 | 5.677153e-04 | +6.772e-05 | accepted |
| 144 | 875 | 5 | 5.470037e-04 | +4.700e-05 | accepted |
| 233 | 880 | 5 | 5.878754e-04 | +8.788e-05 | accepted |

## Interpretation

Seed233 joins seed13, seed55, and seed144 as an `8/5/5`
full-ringdown050 seed. Its limiting target-0 reserve is materially larger than
seed55 and much stronger than the seed21 full-ringdown050 near-miss. For target
2, the 5-source split is now four accepted seeds versus two weak seeds.

## Validation

```text
JSON parse: seed233_ringdown050_target_specific_summary.json and run_manifest.json pass
CSV rows: base rows=3, objective rows=18, target2 cross-seed rows=6
source validation: promoted truth rows=3/3, promoted rows above cutoff=3/3
diagnostic validation: 18/18 diagnostics preserve truth, 16/18 diagnostics clear cutoff
figure validation: base margins plot is 1376x864 RGBA with nonwhite_fraction=0.447423 and full 0-255 dynamic range
figure validation: objective heatmap is 1792x912 RGBA with nonwhite_fraction=0.628758 and full 0-255 dynamic range
figure validation: target2 comparison plot is 1568x880 RGBA with nonwhite_fraction=0.426705 and full 0-255 dynamic range
visual inspection: all three figures are readable; heatmap red outlines mark sub-cutoff cells
resources: CPU aggregation only; no GPU workload
```

## Next Decision

Use run 882 as the seed233 policy input to the next cross-seed synthesis and
continue target-0 lower-tail replication.

---

## Source: `docs/experiments/417_variable_depth_radius_seed377_target0_sources8_txrx60_ringdown050.md`

# Experiment 417: Seed377 Target-0 Sources=8 Tx/Rx=60 Ringdown050

## Purpose

Run 883 extends the full-ringdown050 target-0 lower-tail replication to the
next Fibonacci noise seed after seed233.

## Results

Run 883 is exact and accepted:

```text
output: outputs/experiments/883_coordinate_optimizer_variable_depth_radius_seed377_target0_sources8_txrx60_ringdown050_objectives
target: 0
sources: 8
best: x=150 mm, z=80 mm, r=5.0 mm
next radius: 5.25 mm
absolute radius margin: 5.527795e-04
offset from cutoff: +5.278e-05
confidence label: moderate
fallback warning: none
elapsed: 558.91 s
```

Diagnostic objective rows all preserve the true target-0 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.527795e-04 | above cutoff |
| highband | 7.266302e-04 | above cutoff |
| late | 3.914056e-04 | below cutoff |
| late_high | 4.474819e-04 | below cutoff |
| veryhigh | 6.871787e-04 | above cutoff |
| early_high | 6.204456e-04 | above cutoff |

## Interpretation

Seed377 is another target-0 full-ringdown050 pass, sitting above seeds 89,
233, 34, and 55 in reserve. Seed21 remains the only observed target-0
full-ringdown050 failure in this Fibonacci seed sequence.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.274871 and full 0-255 dynamic range
visual inspection: confidence figure is readable and correctly shows one moderate row above the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
source validation: all six diagnostic objectives preserve target0 truth geometry; 4/6 clear cutoff
```

## Next Decision

Run seed377 target 2 at 5 sources and full ringdown050.

---

## Source: `docs/experiments/418_variable_depth_radius_seed377_target2_sources5_txrx60_ringdown050.md`

# Experiment 418: Seed377 Target-2 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 884 tests seed377 target 2 at full ringdown050 with 5 sources.

## Results

Run 884 is exact and accepted, but low-reserve:

```text
output: outputs/experiments/884_coordinate_optimizer_variable_depth_radius_seed377_target2_sources5_txrx60_ringdown050_objectives
target: 2
sources: 5
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 5.096084e-04
offset from cutoff: +9.608e-06
confidence label: moderate
fallback warning: none
elapsed: 363.99 s
```

Diagnostic objective rows all preserve the true target-2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.096084e-04 | above cutoff |
| highband | 6.306553e-04 | above cutoff |
| late | 8.124983e-04 | above cutoff |
| late_high | 8.090769e-04 | above cutoff |
| veryhigh | 6.267420e-04 | above cutoff |
| early_high | 4.615672e-04 | below cutoff |

## Interpretation

Seed377 target 2 passes at 5 sources, but it is the weakest accepted target-2
5-source row so far. Keep it as accepted, but preserve the low-reserve warning
in the seed377 summary.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.255027 and full 0-255 dynamic range
visual inspection: confidence figure is readable and correctly shows one moderate row just above the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
source validation: all six diagnostic objectives preserve target2 truth geometry; 5/6 clear cutoff
```

## Next Decision

Run seed377 target 1 at 5 sources and full ringdown050.

---

## Source: `docs/experiments/419_variable_depth_radius_seed377_target1_sources5_txrx60_ringdown050.md`

# Experiment 419: Seed377 Target-1 Sources=5 Tx/Rx=60 Ringdown050

Run 885 completes the seed377 target-specific set. It is exact/moderate with a
5.619730e-04 base margin, +6.197e-05 above cutoff. All six diagnostic
objectives preserve target-1 truth and all six clear cutoff.

Seed377 therefore has accepted rows for target 0 run 883, target 1 run 885,
and target 2 run 884. The policy is `8/5/5`, but target 2 is the limiting
low-reserve row.

Validation: JSON and CSV artifacts parse; CSV rows are confidence=1,
diagnostics=6, top candidates=72, state history=2, candidates=12; the figure is
1549x903 RGBA with nonwhite_fraction=0.279356 and full dynamic range.

---

## Source: `docs/experiments/420_seed377_ringdown050_target_specific_8_5_5_summary.md`

# Experiment 420: Seed377 Ringdown050 Target-Specific 8/5/5 Summary

## Purpose

Run 886 summarizes the completed seed377 full-ringdown050 policy branch.

## Results

Output:

```text
outputs/experiments/886_seed377_ringdown050_target_specific_8_5_5_summary
```

Promoted rows:

| Target | Run | Tracker | Sources | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 0 | 883 | 417 | 8 | 5.527795e-04 | +5.278e-05 | accepted |
| 1 | 885 | 419 | 5 | 5.619730e-04 | +6.197e-05 | accepted |
| 2 | 884 | 418 | 5 | 5.096084e-04 | +9.608e-06 | accepted |

Seed377 passes as a full-ringdown050 `8/5/5` seed, but target 2 is the
limiting low-reserve row.

Objective diagnostics:

```text
diagnostic rows: 18
truth-preserving diagnostic rows: 18
diagnostic rows above cutoff: 15
sub-cutoff diagnostics: target0 late, target0 late_high, target2 early_high
```

## Interpretation

Seed377 joins seed13, seed55, seed144, and seed233 as an `8/5/5` seed. Its
target-2 row is only `+9.608e-06` above cutoff, making it the weakest accepted
target-2 5-source row so far.

## Validation

```text
JSON parse: seed377_ringdown050_target_specific_summary.json and run_manifest.json pass
CSV rows: base rows=3, objective rows=18, target2 cross-seed rows=7
source validation: promoted truth rows=3/3, promoted rows above cutoff=3/3
diagnostic validation: 18/18 diagnostics preserve truth, 15/18 diagnostics clear cutoff
figure validation: base margins plot is 1376x864 RGBA with nonwhite_fraction=0.436385 and full 0-255 dynamic range
figure validation: objective heatmap is 1792x912 RGBA with nonwhite_fraction=0.629922 and full 0-255 dynamic range
figure validation: target2 comparison plot is 1664x880 RGBA with nonwhite_fraction=0.426109 and full 0-255 dynamic range
visual inspection: all three figures are readable; target2 low-reserve row is clear
resources: CPU aggregation only; no GPU workload
```

## Next Decision

Use run 886 as the seed377 policy input to the next cross-seed synthesis and
continue target-0 lower-tail replication with seed610.

---

## Source: `docs/experiments/421_variable_depth_radius_seed610_target0_sources8_txrx60_ringdown050.md`

# Experiment 421: Seed610 Target-0 Sources=8 Tx/Rx=60 Ringdown050

Run 887 tests seed610 target 0 at full ringdown050 with 8 sources. The row is
exact/moderate and technically accepted, but only by `5.897e-07` above the
5.0e-04 cutoff.

Diagnostic objective rows all preserve truth. Base, highband, late_high,
veryhigh, and early_high clear cutoff; late is below cutoff.

Interpretation: seed610 is now the closest accepted target-0 row. Together
with seed21's failure and seed55's low reserve, this keeps the target-0 lower
tail active even though most Fibonacci seeds pass full ringdown050.

Validation: JSON and CSV artifacts parse; CSV rows are confidence=1,
diagnostics=6, top candidates=72, state history=2, candidates=12; the figure is
1549x903 RGBA with nonwhite_fraction=0.251477 and full dynamic range.

---

## Source: `docs/experiments/422_variable_depth_radius_seed610_target2_sources5_txrx60_ringdown050.md`

# Experiment 422: Seed610 Target-2 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 888 tests seed610 target 2 at full ringdown050 with 5 sources after the
same seed produced a razor-edge accepted target-0 row in run 887. The question
is whether seed610 follows the seed13/55/144/233/377 5-source target-2 pass
branch or the seed89/34 9-source rescue branch.

## 888: Coordinate Optimizer Variable-Depth/Radius Seed610 Target-2 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/888_coordinate_optimizer_variable_depth_radius_seed610_target2_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed610:1.1,-50.0,1.1,0.10,610,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed610 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed610_target2_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed610
target: 2
sources: 5
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 4.635149e-04
offset from cutoff: -3.648514e-05
relative radius margin: 2.743712e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.016893717117471795
next radius misfit: 0.017357231978786043
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: 360.96 s
```

Diagnostic objective rows all preserve the true target-2 geometry, but only
four of six clear the production cutoff:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.635149e-04 | below cutoff |
| highband | 5.621963e-04 | above cutoff |
| late | 7.181446e-04 | above cutoff |
| late_high | 7.297233e-04 | above cutoff |
| veryhigh | 6.462786e-04 | above cutoff |
| early_high | 3.891933e-04 | below cutoff |

## Interpretation

Run 888 is exact but weak. It should not be promoted as a production 5-source
target-2 row because the base margin misses the 5.0e-04 cutoff by
`3.649e-05`, and early_high is also below cutoff. The result is still useful:
the top candidate is the true geometry for every objective variant, so the
failure mode is insufficient radius separation, not a wrong-coordinate
selection.

This places seed610 target 2 in the same broad branch as seed89 and seed34,
where 5-source target-2 rows were exact but weak and 9-source rescues were
needed. Because seed34 and seed89 both showed that 7- and 8-source
intermediates remain weak while 9 sources pass, the next substantive run is a
direct 9-source seed610 target-2 rescue rather than repeating both
intermediates first.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target-2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.234292 and full dynamic range
visual inspection: confidence figure is readable and correctly shows one weak row below the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: GPU utilization held around 88%; RAM stayed about 97 GiB available
elapsed: 360.96 s
```

## Next Decision

Run seed610 target 2 with 9 sources at full ringdown050. If it clears the
cutoff, seed610 likely follows an `8/5/9` target-specific policy unless
target 1 creates a new limiting row.

---

## Source: `docs/experiments/423_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown050.md`

# Experiment 423: Seed610 Target-2 Sources=9 Tx/Rx=60 Ringdown050

## Purpose

Run 889 tests whether the usual 9-source target-2 rescue restores production
confidence for seed610 after run 888 showed an exact but weak 5-source target-2
row at full ringdown050.

## 889: Coordinate Optimizer Variable-Depth/Radius Seed610 Target-2 Sources=9 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/889_coordinate_optimizer_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 9 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed610:1.1,-50.0,1.1,0.10,610,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed610 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed610
target: 2
sources: 9
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 4.802438e-04
offset from cutoff: -1.975621e-05
relative radius margin: 3.069278e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.01564680341262918
next radius misfit: 0.01612704720438468
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: 714.54 s
```

Diagnostic objective rows all preserve the true target-2 geometry, but base
and early_high remain below cutoff:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.802438e-04 | below cutoff |
| highband | 6.158541e-04 | above cutoff |
| late | 6.839814e-04 | above cutoff |
| late_high | 7.804717e-04 | above cutoff |
| veryhigh | 6.585882e-04 | above cutoff |
| early_high | 4.584077e-04 | below cutoff |

## Interpretation

Run 889 does not rescue seed610 target 2. It improves the base margin over
run 888 by only `1.673e-05`, leaving the row `1.976e-05` below the production
cutoff. This is different from seed34 and seed89, where 9 sources cleared the
target-2 branch after exact-but-weak 5-, 7-, and 8-source rows.

The failure mode is still radius separation rather than geometry selection:
the exact target-2 geometry is rank 1 for all six objective variants. That
makes a denser acquisition test justified before reducing ringdown severity or
changing the objective. The next run should test seed610 target 2 with
11 sources at the same Tx/Rx=60 and ringdown050 settings.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target-2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.241504 and full dynamic range
visual inspection: confidence figure is readable and correctly shows one weak row below the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: GPU utilization held around 91%; RAM stayed about 97 GiB available
elapsed: 714.54 s
```

## Next Decision

Run seed610 target 2 with 11 sources at full ringdown050. If 11 sources pass,
seed610 should be treated as a higher-density target-2 outlier rather than an
`8/5/9` seed.

---

## Source: `docs/experiments/424_variable_depth_radius_seed610_target2_sources11_txrx60_ringdown050.md`

# Experiment 424: Seed610 Target-2 Sources=11 Tx/Rx=60 Ringdown050

## Purpose

Run 890 tests whether a denser 11-source target-2 acquisition can rescue
seed610 after both 5-source run 888 and 9-source run 889 were exact but weak
at full ringdown050.

## 890: Coordinate Optimizer Variable-Depth/Radius Seed610 Target-2 Sources=11 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/890_coordinate_optimizer_variable_depth_radius_seed610_target2_sources11_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 11 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed610:1.1,-50.0,1.1,0.10,610,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed610 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed610_target2_sources11_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed610
target: 2
sources: 11
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 3.969889e-04
offset from cutoff: -1.030111e-04
relative radius margin: 2.724090e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.014574679979295697
next radius misfit: 0.014971668848926839
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: 929.29 s
```

Diagnostic objective rows all preserve the true target-2 geometry, but only
two of six clear cutoff:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 3.969889e-04 | below cutoff |
| highband | 4.889197e-04 | below cutoff |
| late | 5.627489e-04 | above cutoff |
| late_high | 6.146249e-04 | above cutoff |
| veryhigh | 4.881123e-04 | below cutoff |
| early_high | 3.196800e-04 | below cutoff |

## Interpretation

Run 890 is a negative source-density result. It confirms that seed610 target 2
is not solved by increasing from the usual 9-source rescue to 11 sources; the
base margin actually drops by `8.325e-05` relative to run 889. The exact
geometry remains rank 1 for every objective variant, so this is still a
confidence-separation problem rather than a coordinate-recovery failure.

Seed610 target-2 full-ringdown050 source-count branch:

| Run | Sources | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | --- |
| 888 | 5 | 4.635149e-04 | -3.649e-05 | rejected |
| 889 | 9 | 4.802438e-04 | -1.976e-05 | rejected |
| 890 | 11 | 3.969889e-04 | -1.030e-04 | rejected |

The best full-ringdown050 row is therefore still the 9-source run 889, but it
is not production-confident. The next step should follow the seed21
practical-threshold pattern: keep the best source count at 9 and reduce the
ringdown coefficient to the established `0.49453125` threshold point.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target-2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.204543 and full dynamic range
visual inspection: confidence figure is readable and correctly shows one weak row below the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: GPU utilization held around 91-93%; RAM stayed about 97 GiB available
elapsed: 929.29 s
```

## Next Decision

Run seed610 target 2 with 9 sources at `ringdown049453125`, matching the
existing practical threshold used to close the seed21 branch. If that passes,
bisect upward toward full ringdown050; if it fails, lower the ringdown stress
before re-testing source/aperture variants.

---

## Source: `docs/experiments/425_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown049453125.md`

# Experiment 425: Seed610 Target-2 Sources=9 Tx/Rx=60 Ringdown049453125

## Purpose

Run 891 tests whether seed610 target 2 becomes production-confident at the
existing practical threshold point `ringdown049453125` after 5-, 9-, and
11-source full-ringdown050 rows all remained exact but weak.

## 891: Coordinate Optimizer Variable-Depth/Radius Seed610 Target-2 Sources=9 Tx/Rx=60 Ringdown049453125

Output:

```text
outputs/experiments/891_coordinate_optimizer_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown049453125_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 9 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown049453125_noise10_seed610:1.1,-50.0,1.1,0.10,610,0.49453125,180.0,0.8 \
  --update-case-label source_mismatch_ringdown049453125_noise10_seed610 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown049453125_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown049453125_noise10_seed610
target: 2
sources: 9
ringdown coefficient: 0.49453125
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 4.818309e-04
offset from cutoff: -1.816907e-05
relative radius margin: 3.061405e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.01574460722134132
next radius misfit: 0.016226438150403213
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: 732.16 s
```

Diagnostic objective rows all preserve the true target-2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.818309e-04 | below cutoff |
| highband | 6.149381e-04 | above cutoff |
| late | 6.841174e-04 | above cutoff |
| late_high | 7.800690e-04 | above cutoff |
| veryhigh | 6.576093e-04 | above cutoff |
| early_high | 4.561506e-04 | below cutoff |

## Interpretation

Run 891 is exact but still weak. Compared with full-ringdown050 run 889, the
base margin improves by only `1.587e-06`:

| Run | Sources | Ringdown | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | ---: | --- |
| 889 | 9 | 0.50000000 | 4.802438e-04 | -1.976e-05 | rejected |
| 891 | 9 | 0.49453125 | 4.818309e-04 | -1.817e-05 | rejected |

This is not enough movement to justify fine bisection near `0.49453125`.
Seed610 target 2 needs a coarser stress bracket. The next run should keep the
best source count at 9 and test `ringdown0475`.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target-2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.224643 and full dynamic range
visual inspection: confidence figure is readable and correctly shows one weak row below the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: GPU utilization held around 91%; RAM stayed about 97 GiB available
elapsed: 732.16 s
```

## Next Decision

Run seed610 target 2 with 9 sources at `ringdown0475`. If that passes, bracket
the practical threshold between 0.475 and 0.49453125; if it fails, the seed610
target-2 stress threshold is materially lower than the seed21 practical point.

---

## Source: `docs/experiments/426_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown0475.md`

# Experiment 426: Seed610 Target-2 Sources=9 Tx/Rx=60 Ringdown0475

## Purpose

Run 892 tests a coarser ringdown bracket for seed610 target 2 after run 891
showed that the `ringdown049453125` point was still exact but weak.

## 892: Coordinate Optimizer Variable-Depth/Radius Seed610 Target-2 Sources=9 Tx/Rx=60 Ringdown0475

Output:

```text
outputs/experiments/892_coordinate_optimizer_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown0475_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 9 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown0475_noise10_seed610:1.1,-50.0,1.1,0.10,610,0.475,180.0,0.8 \
  --update-case-label source_mismatch_ringdown0475_noise10_seed610 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown0475_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown0475_noise10_seed610
target: 2
sources: 9
ringdown coefficient: 0.475
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 4.869258e-04
offset from cutoff: -1.307417e-05
relative radius margin: 3.025412e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.016090625891697546
next radius misfit: 0.016577551722957527
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: 723.36 s
```

Diagnostic objective rows all preserve the true target-2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.869258e-04 | below cutoff |
| highband | 6.106184e-04 | above cutoff |
| late | 6.839660e-04 | above cutoff |
| late_high | 7.776120e-04 | above cutoff |
| veryhigh | 6.538944e-04 | above cutoff |
| early_high | 4.474556e-04 | below cutoff |

## Interpretation

Run 892 is exact but still weak. The margin improves over run 891 by only
`5.096e-06`, and remains `1.307e-05` below cutoff:

| Run | Sources | Ringdown | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | ---: | --- |
| 889 | 9 | 0.50000000 | 4.802438e-04 | -1.976e-05 | rejected |
| 891 | 9 | 0.49453125 | 4.818309e-04 | -1.817e-05 | rejected |
| 892 | 9 | 0.47500000 | 4.869258e-04 | -1.307e-05 | rejected |

This keeps the seed610 target-2 failure mode in the confidence-separation
category: the true geometry is rank 1 for all objective variants, but the base
gap remains below the production threshold. The next lower bracket should be
`ringdown0459375`, a previously studied stress point in the ringdown-threshold
series.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target-2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.241527 and full dynamic range
visual inspection: confidence figure is readable and correctly shows one weak row below the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: GPU utilization held around 90-91%; RAM stayed about 97 GiB available
elapsed: 723.36 s
```

## Next Decision

Run seed610 target 2 with 9 sources at `ringdown0459375`. If it passes, bracket
the seed610 target-2 practical threshold between 0.459375 and 0.475; if it
fails, lower stress or an objective/receiver change will be needed before
seed610 target 2 can be promoted.

---

## Source: `docs/experiments/427_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown0459375.md`

# Experiment 427: Seed610 Target-2 Sources=9 Tx/Rx=60 Ringdown0459375

## Purpose

Run 893 tests seed610 target 2 at `ringdown0459375`, the next lower threshold
bracket after the `ringdown0475` row from run 892 remained exact but weak.

## 893: Coordinate Optimizer Variable-Depth/Radius Seed610 Target-2 Sources=9 Tx/Rx=60 Ringdown0459375

Output:

```text
outputs/experiments/893_coordinate_optimizer_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown0459375_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 9 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown0459375_noise10_seed610:1.1,-50.0,1.1,0.10,610,0.459375,180.0,0.8 \
  --update-case-label source_mismatch_ringdown0459375_noise10_seed610 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown0459375_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown0459375_noise10_seed610
target: 2
sources: 9
ringdown coefficient: 0.459375
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 4.903121e-04
offset from cutoff: -9.687927e-06
relative radius margin: 2.996686e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.016362048547225144
next radius misfit: 0.016852360619814308
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: 714.83 s
```

Diagnostic objective rows all preserve the true target-2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.903121e-04 | below cutoff |
| highband | 6.060156e-04 | above cutoff |
| late | 6.831177e-04 | above cutoff |
| late_high | 7.745000e-04 | above cutoff |
| veryhigh | 6.506803e-04 | above cutoff |
| early_high | 4.398528e-04 | below cutoff |

## Interpretation

Run 893 is exact but remains weak. The seed610 target-2 threshold trend is
monotone after the 0.49453125 point, but the lower bracket is still below
cutoff:

| Run | Sources | Ringdown | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | ---: | --- |
| 889 | 9 | 0.50000000 | 4.802438e-04 | -1.976e-05 | rejected |
| 891 | 9 | 0.49453125 | 4.818309e-04 | -1.817e-05 | rejected |
| 892 | 9 | 0.47500000 | 4.869258e-04 | -1.307e-05 | rejected |
| 893 | 9 | 0.45937500 | 4.903121e-04 | -9.688e-06 | rejected |

Because the current slope still leaves the row below cutoff, the next useful
run is a coarser `ringdown040` lower-bound test rather than a small bisection.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target-2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.234459 and full dynamic range
visual inspection: confidence figure is readable and correctly shows one weak row below the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: GPU utilization held around 91%; RAM stayed about 97 GiB available
elapsed: 714.83 s
```

## Next Decision

Run seed610 target 2 with 9 sources at `ringdown040`. If it passes, bracket
the practical threshold between 0.400 and 0.459375; if it fails, seed610 target
2 needs a substantially lower stress condition or an objective/receiver change.

---

## Source: `docs/experiments/428_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown040.md`

# Experiment 428: Seed610 Target-2 Sources=9 Tx/Rx=60 Ringdown040

## Purpose

Run 894 tests a coarse lower-bound bracket for seed610 target 2 after
`ringdown0459375` remained exact but weak.

## 894: Coordinate Optimizer Variable-Depth/Radius Seed610 Target-2 Sources=9 Tx/Rx=60 Ringdown040

Output:

```text
outputs/experiments/894_coordinate_optimizer_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown040_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 9 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown040_noise10_seed610:1.1,-50.0,1.1,0.10,610,0.4,180.0,0.8 \
  --update-case-label source_mismatch_ringdown040_noise10_seed610 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown040_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown040_noise10_seed610
target: 2
sources: 9
ringdown coefficient: 0.400
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 4.969192e-04
offset from cutoff: -3.080791e-06
relative radius margin: 2.869842e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.017317531265025915
next radius misfit: 0.01781445047392895
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: 725.96 s
```

Diagnostic objective rows all preserve the true target-2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.969192e-04 | below cutoff |
| highband | 5.801941e-04 | above cutoff |
| late | 6.739557e-04 | above cutoff |
| late_high | 7.536980e-04 | above cutoff |
| veryhigh | 6.365951e-04 | above cutoff |
| early_high | 4.070302e-04 | below cutoff |

## Interpretation

Run 894 is the closest seed610 target-2 threshold row so far, but it still
does not pass. The target-2 threshold branch now looks like this:

| Run | Sources | Ringdown | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | ---: | --- |
| 889 | 9 | 0.50000000 | 4.802438e-04 | -1.976e-05 | rejected |
| 891 | 9 | 0.49453125 | 4.818309e-04 | -1.817e-05 | rejected |
| 892 | 9 | 0.47500000 | 4.869258e-04 | -1.307e-05 | rejected |
| 893 | 9 | 0.45937500 | 4.903121e-04 | -9.688e-06 | rejected |
| 894 | 9 | 0.40000000 | 4.969192e-04 | -3.081e-06 | rejected |

Because `ringdown040` is only `3.081e-06` below cutoff, an accepted lower
bracket is likely nearby. Use `ringdown035`, an existing lower-stress condition,
to establish a clear accepted row before bisection.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target-2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.249616 and full dynamic range
visual inspection: confidence figure is readable and correctly shows one weak row below the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: GPU utilization held around 91%; RAM stayed about 97 GiB available
elapsed: 725.96 s
```

## Next Decision

Run seed610 target 2 with 9 sources at `ringdown035`. If it passes, bracket the
practical threshold between 0.350 and 0.400 and then bisect upward.

---

## Source: `docs/experiments/429_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown035.md`

# Experiment 429: Seed610 Target-2 Sources=9 Tx/Rx=60 Ringdown035

## Purpose

Run 895 tests the established lower-stress `ringdown035` point after the
`ringdown040` seed610 target-2 row remained a near miss.

## 895: Coordinate Optimizer Variable-Depth/Radius Seed610 Target-2 Sources=9 Tx/Rx=60 Ringdown035

Output:

```text
outputs/experiments/895_coordinate_optimizer_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown035_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 9 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown035_noise10_seed610:1.1,-50.0,1.1,0.10,610,0.35,180.0,0.8 \
  --update-case-label source_mismatch_ringdown035_noise10_seed610 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown035_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown035_noise10_seed610
target: 2
sources: 9
ringdown coefficient: 0.350
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 4.942263e-04
offset from cutoff: -5.773738e-06
relative radius margin: 2.746130e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.017980962056706257
next radius misfit: 0.01847518831871689
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: 715.81 s
```

Diagnostic objective rows all preserve the true target-2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.942263e-04 | below cutoff |
| highband | 5.503978e-04 | above cutoff |
| late | 6.592634e-04 | above cutoff |
| late_high | 7.263726e-04 | above cutoff |
| veryhigh | 6.226366e-04 | above cutoff |
| early_high | 3.766502e-04 | below cutoff |

## Interpretation

Run 895 is exact but weak, and it is weaker than the `ringdown040` near miss.
The lower-stress branch is therefore non-monotone rather than a clean bisection
curve:

| Run | Sources | Ringdown | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | ---: | --- |
| 889 | 9 | 0.50000000 | 4.802438e-04 | -1.976e-05 | rejected |
| 891 | 9 | 0.49453125 | 4.818309e-04 | -1.817e-05 | rejected |
| 892 | 9 | 0.47500000 | 4.869258e-04 | -1.307e-05 | rejected |
| 893 | 9 | 0.45937500 | 4.903121e-04 | -9.688e-06 | rejected |
| 894 | 9 | 0.40000000 | 4.969192e-04 | -3.081e-06 | rejected |
| 895 | 9 | 0.35000000 | 4.942263e-04 | -5.774e-06 | rejected |

Because all rows are truth-preserving but weak, the next useful lower-stress
check is `ringdown025`, which is an established condition in earlier
source-density work. If `ringdown025` still fails, the seed610 target-2 issue
is not explained by ringdown severity alone.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target-2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.247802 and full dynamic range
visual inspection: confidence figure is readable and correctly shows one weak row below the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: GPU utilization held around 90-91%; RAM stayed about 97 GiB available
elapsed: 715.81 s
```

## Next Decision

Run seed610 target 2 with 9 sources at `ringdown025`. If it passes, use it as
the lower accepted bracket; if it fails, investigate objective/receiver
settings instead of continuing ringdown-only bisection.

---

## Source: `docs/experiments/430_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown025.md`

# Experiment 430: Seed610 Target-2 Sources=9 Tx/Rx=60 Ringdown025

## Purpose

Run 896 tests the established low-stress `ringdown025` condition for seed610
target 2 after ringdown035 and ringdown040 both remained exact but weak.

## 896: Coordinate Optimizer Variable-Depth/Radius Seed610 Target-2 Sources=9 Tx/Rx=60 Ringdown025

Output:

```text
outputs/experiments/896_coordinate_optimizer_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown025_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 9 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown025_noise10_seed610:1.1,-50.0,1.1,0.10,610,0.25,180.0,0.8 \
  --update-case-label source_mismatch_ringdown025_noise10_seed610 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed610_target2_sources9_txrx60_ringdown025_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown025_noise10_seed610
target: 2
sources: 9
ringdown coefficient: 0.250
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 4.677785e-04
offset from cutoff: -3.222150e-05
relative radius margin: 2.491576e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.0187773960223498
next radius misfit: 0.019245174523301517
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: 719.75 s
```

Diagnostic objective rows all preserve the true target-2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.677785e-04 | below cutoff |
| highband | 4.801430e-04 | below cutoff |
| late | 6.145703e-04 | above cutoff |
| late_high | 6.537967e-04 | above cutoff |
| veryhigh | 5.901716e-04 | above cutoff |
| early_high | 3.161886e-04 | below cutoff |

## Interpretation

Run 896 rules out a simple ringdown-only rescue for seed610 target 2. The
target remains truth-preserving, but margin separation is weak across high
ringdown, intermediate ringdown, and low ringdown:

| Run | Sources | Ringdown | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | ---: | --- |
| 889 | 9 | 0.50000000 | 4.802438e-04 | -1.976e-05 | rejected |
| 891 | 9 | 0.49453125 | 4.818309e-04 | -1.817e-05 | rejected |
| 892 | 9 | 0.47500000 | 4.869258e-04 | -1.307e-05 | rejected |
| 893 | 9 | 0.45937500 | 4.903121e-04 | -9.688e-06 | rejected |
| 894 | 9 | 0.40000000 | 4.969192e-04 | -3.081e-06 | rejected |
| 895 | 9 | 0.35000000 | 4.942263e-04 | -5.774e-06 | rejected |
| 896 | 9 | 0.25000000 | 4.677785e-04 | -3.222e-05 | rejected |

Because all rows select the true geometry, the remaining issue is objective or
aperture margin separation. Stop the ringdown-only branch. Complete the
remaining seed610 target1 production row before choosing a target2
aperture/objective follow-up.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target-2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.236086 and full dynamic range
visual inspection: confidence figure is readable and correctly shows one weak row below the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: GPU utilization held around 90-91%; RAM stayed about 97 GiB available
elapsed: 719.75 s
```

## Next Decision

Run seed610 target 1 with 5 sources at full ringdown050. After target1 is
closed, summarize seed610 as target0 accepted, target1 pending/accepted, and
target2 unresolved under ringdown-only and simple source-density controls.

---

## Source: `docs/experiments/431_variable_depth_radius_seed610_target1_sources5_txrx60_ringdown050.md`

# Experiment 431: Seed610 Target-1 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 897 tests seed610 target 1 at full ringdown050 with the usual 5-source
target1 acquisition. This completes the non-target2 seed610 production row
after target2 failed the ringdown-only/source-density branch.

## 897: Coordinate Optimizer Variable-Depth/Radius Seed610 Target-1 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/897_coordinate_optimizer_variable_depth_radius_seed610_target1_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 1 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed610:1.1,-50.0,1.1,0.10,610,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed610 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed610_target1_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed610
target: 1
sources: 5
best: x=250 mm, z=100 mm, r=6.0 mm
next radius: 6.25 mm
absolute radius margin: 4.677410e-04
offset from cutoff: -3.225896e-05
relative radius margin: 2.769158e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.016893717117471795
next radius misfit: 0.017361458158032743
listed competing geometry: x=250 mm, z=101 mm, r=6.25 mm
elapsed: 399.24 s
```

Diagnostic objective rows all preserve the true target-1 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.677410e-04 | below cutoff |
| highband | 6.050814e-04 | above cutoff |
| late | 6.993492e-04 | above cutoff |
| late_high | 7.672181e-04 | above cutoff |
| veryhigh | 5.661710e-04 | above cutoff |
| early_high | 4.569247e-04 | below cutoff |

## Interpretation

Run 897 is exact but weak. This is a new seed610 result: target1 usually passes
with 5 sources in the recent full-ringdown050 seeds, but seed610 does not.
Together with the unresolved target2 branch, seed610 is now a broad low-margin
seed rather than only a target2 outlier.

Because the failure mode is radius separation and the truth geometry is rank 1
for every objective, the next target1 step should be a 9-source acquisition
rescue before changing ringdown or objective settings.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target-1 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.236082 and full dynamic range
visual inspection: confidence figure is readable and correctly shows one weak row below the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target1 truth_radius_mm is 6.0 mm
resources: GPU utilization held around 87-88%; RAM stayed about 97 GiB available
elapsed: 399.24 s
```

## Next Decision

Run seed610 target 1 with 9 sources at full ringdown050. If it passes, seed610
target1 can use a rescue row while target2 remains unresolved; if it fails,
seed610 requires a broader low-margin-seed analysis.

---

## Source: `docs/experiments/432_variable_depth_radius_seed610_target1_sources9_txrx60_ringdown050.md`

# Experiment 432: Seed610 Target-1 Sources=9 Tx/Rx=60 Ringdown050

## Purpose

Run 898 tests whether a 9-source acquisition rescues the weak seed610 target1
5-source control from run 897.

## 898: Coordinate Optimizer Variable-Depth/Radius Seed610 Target-1 Sources=9 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/898_coordinate_optimizer_variable_depth_radius_seed610_target1_sources9_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 9 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 1 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed610:1.1,-50.0,1.1,0.10,610,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed610 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed610_target1_sources9_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed610
target: 1
sources: 9
best: x=250 mm, z=100 mm, r=6.0 mm
next radius: 6.25 mm
absolute radius margin: 4.197879e-04
offset from cutoff: -8.021206e-05
relative radius margin: 2.683717e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.01564680341262918
next radius misfit: 0.016066591350369277
listed competing geometry: x=250 mm, z=101 mm, r=6.25 mm
elapsed: 715.54 s
```

Diagnostic objective rows all preserve the true target-1 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.197879e-04 | below cutoff |
| highband | 5.778697e-04 | above cutoff |
| late | 6.383743e-04 | above cutoff |
| late_high | 7.701699e-04 | above cutoff |
| veryhigh | 5.341169e-04 | above cutoff |
| early_high | 4.350460e-04 | below cutoff |

## Interpretation

Run 898 is a negative 9-source rescue. The exact target1 geometry remains
rank 1 for every diagnostic objective, but the base margin drops below both
the cutoff and the 5-source control:

| Run | Sources | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | --- |
| 897 | 5 | 4.677410e-04 | -3.226e-05 | rejected |
| 898 | 9 | 4.197879e-04 | -8.021e-05 | rejected |

The source-count response is non-monotone and source9 is not a rescue. The
next target1 rescue should test the 8-source aperture directly before changing
ringdown or objective settings.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target-1 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.213544 and full dynamic range
visual inspection: confidence figure is readable and correctly shows one weak row below the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target1 truth_radius_mm is 6.0 mm
resources: GPU utilization held around 91%; RAM stayed about 97 GiB available
elapsed: 715.54 s
```

## Next Decision

Run seed610 target 1 with 8 sources at full ringdown050. If 8 sources fail,
seed610 target1 should be treated as another unresolved low-margin row.

---

## Source: `docs/experiments/433_variable_depth_radius_seed610_target1_sources8_txrx60_ringdown050.md`

# Experiment 433: Seed610 Target-1 Sources=8 Tx/Rx=60 Ringdown050

## Purpose

Run 899 tests the 8-source target1 aperture after seed610 target1 failed at
both 5 sources in run 897 and 9 sources in run 898.

## 899: Coordinate Optimizer Variable-Depth/Radius Seed610 Target-1 Sources=8 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/899_coordinate_optimizer_variable_depth_radius_seed610_target1_sources8_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 8 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 1 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed610:1.1,-50.0,1.1,0.10,610,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed610 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed610_target1_sources8_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed610
target: 1
sources: 8
best: x=250 mm, z=100 mm, r=6.0 mm
next radius: 6.25 mm
absolute radius margin: 4.205166e-04
offset from cutoff: -7.948342e-05
relative radius margin: 2.680636e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.015687167280586604
next radius misfit: 0.016107683862244072
listed competing geometry: x=250 mm, z=101 mm, r=6.25 mm
elapsed: 623.22 s
```

Diagnostic objective rows all preserve the true target-1 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.205166e-04 | below cutoff |
| highband | 5.602220e-04 | above cutoff |
| late | 5.778854e-04 | above cutoff |
| late_high | 6.853107e-04 | above cutoff |
| veryhigh | 4.949910e-04 | below cutoff |
| early_high | 4.237118e-04 | below cutoff |

## Interpretation

Run 899 is a negative 8-source target1 rescue. Seed610 target1 simple aperture
branch:

| Run | Sources | Margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | --- |
| 897 | 5 | 4.677410e-04 | -3.226e-05 | rejected |
| 899 | 8 | 4.205166e-04 | -7.948e-05 | rejected |
| 898 | 9 | 4.197879e-04 | -8.021e-05 | rejected |

The best simple aperture remains the 5-source row, but it is still below
cutoff. Seed610 now has target0 accepted only by a razor margin, target1
unresolved under 5/8/9 sources, and target2 unresolved under source-density and
ringdown-only controls. Create a seed610 unresolved-branch summary before
choosing a specialized follow-up.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target-1 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.214446 and full dynamic range
visual inspection: confidence figure is readable and correctly shows one weak row below the 0.0005 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target1 truth_radius_mm is 6.0 mm
resources: GPU utilization held around 90-91%; RAM stayed about 97 GiB available
elapsed: 623.22 s
```

## Next Decision

Create a seed610 unresolved-branch summary, then move GPU replication to the
next Fibonacci seed target0 while preserving seed610 for a specialized
aperture/objective follow-up.

---

## Source: `docs/experiments/434_seed610_ringdown050_unresolved_branch_summary.md`

# Experiment 434: Seed610 Ringdown050 Unresolved Branch Summary

## Purpose

Run 901 consolidates the seed610 branch after target0 barely passed and
target1/target2 remained exact but weak through source-density and ringdown-only
controls.

## 901: Seed610 Ringdown050 Unresolved Branch Summary

Output:

```text
outputs/experiments/901_seed610_ringdown050_unresolved_branch_summary
```

Generation:

```text
CPU-side structured aggregation of completed coordinate-optimizer outputs
887-899. No new FDTD scene was launched for this summary.
```

Source runs:

```text
887 target0 sources=8 ringdown050
888 target2 sources=5 ringdown050
889 target2 sources=9 ringdown050
890 target2 sources=11 ringdown050
891 target2 sources=9 ringdown049453125
892 target2 sources=9 ringdown0475
893 target2 sources=9 ringdown0459375
894 target2 sources=9 ringdown040
895 target2 sources=9 ringdown035
896 target2 sources=9 ringdown025
897 target1 sources=5 ringdown050
898 target1 sources=9 ringdown050
899 target1 sources=8 ringdown050
```

## Results

Summary counts:

```text
row count: 13
diagnostic objective rows: 78
accepted runs: 887
rejected truth-preserving runs: 888-899
cutoff: 5.0e-4
```

Best row by target:

| Target | Best run | Sources | Ringdown | Base margin | Offset from cutoff | Status |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| target0 | 887 | 8 | 0.50 | 5.005897e-04 | +5.897e-07 | accepted |
| target1 | 897 | 5 | 0.50 | 4.677410e-04 | -3.226e-05 | rejected |
| target2 | 894 | 9 | 0.40 | 4.969192e-04 | -3.081e-06 | rejected |

Target1 simple aperture branch:

| Run | Sources | Base margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | --- |
| 897 | 5 | 4.677410e-04 | -3.226e-05 | rejected |
| 899 | 8 | 4.205166e-04 | -7.948e-05 | rejected |
| 898 | 9 | 4.197879e-04 | -8.021e-05 | rejected |

Target2 ringdown branch:

| Run | Ringdown | Base margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | --- |
| 889 | 0.50 | 4.802438e-04 | -1.976e-05 | rejected |
| 891 | 0.49453125 | 4.818309e-04 | -1.817e-05 | rejected |
| 892 | 0.475 | 4.869258e-04 | -1.307e-05 | rejected |
| 893 | 0.459375 | 4.903121e-04 | -9.688e-06 | rejected |
| 894 | 0.40 | 4.969192e-04 | -3.081e-06 | rejected |
| 895 | 0.35 | 4.942263e-04 | -5.774e-06 | rejected |
| 896 | 0.25 | 4.677785e-04 | -3.222e-05 | rejected |

## Interpretation

Seed610 is a broad low-margin seed, not a single bad target row. Target0 clears
the cutoff by only 5.897e-07. Target1 is best at the 5-source control and gets
worse at 8 and 9 sources. Target2 is closest at ringdown040 but still misses
the cutoff and becomes non-monotone at lower ringdown.

The important positive evidence is that every rejected row is truth-preserving:
the optimizer keeps the correct x/z/r geometry as rank 1. The unresolved issue
is radius-margin reserve. That means another simple source-count or ringdown
test is unlikely to add useful information; a seed610 revisit should use a
specialized aperture/objective design rather than extending the exhausted
branches.

## Validation

```text
JSON parse: run_manifest.json and seed610_branch_summary.json pass
CSV rows: seed610_branch_summary.csv has 13 data rows
diagnostics CSV rows: seed610_objective_diagnostics.csv has 78 data rows
base margins figure: 2400x960 RGBA, nonwhite_fraction=0.259229
objective heatmap: 2400x768 RGBA, nonwhite_fraction=0.623955
branch trends figure: 2080x768 RGBA, nonwhite_fraction=0.061759
visual inspection: all three figures are readable and support the conclusion
figure notes: figures/FIGURE_NOTES.md present and run-specific
resources: no new FDTD scene; GPU stayed occupied by the next production run
```

## Next Decision

Move GPU replication to seed987 target0 while keeping seed610 reserved for a
specialized objective/aperture follow-up if later cross-seed evidence justifies
it.

---

## Source: `docs/experiments/435_variable_depth_radius_seed987_target0_sources8_txrx60_ringdown050.md`

# Experiment 435: Seed987 Target0 Sources=8 Tx/Rx=60 Ringdown050

## Purpose

Run 900 starts the next Fibonacci seed after the seed610 unresolved summary.
The first check uses the established production target0 setting: 8 sources,
Tx/Rx offset 60 mm, full ringdown050, and target-specific radius truth.

## 900: Coordinate Optimizer Variable-Depth/Radius Seed987 Target0 Sources=8 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/900_coordinate_optimizer_variable_depth_radius_seed987_target0_sources8_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 8 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 0 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed987:1.1,-50.0,1.1,0.10,987,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed987 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed987_target0_sources8_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed987
target: 0
sources: 8
best: x=150 mm, z=80 mm, r=5.0 mm
next radius: 5.25 mm
absolute radius margin: 5.111568e-04
offset from cutoff: +1.115678e-05
relative radius margin: 3.217056e-02
confidence label: moderate
fallback warning: none
best misfit: 0.015888961393029068
next radius misfit: 0.01640011816949682
listed competing geometry: x=150 mm, z=81 mm, r=6.0 mm
elapsed: about 628.4 s
```

Diagnostic objective rows all preserve the true target0 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.111568e-04 | above cutoff |
| highband | 6.569749e-04 | above cutoff |
| late | 3.641318e-04 | below cutoff |
| late_high | 4.362428e-04 | below cutoff |
| veryhigh | 6.557263e-04 | above cutoff |
| early_high | 5.681431e-04 | above cutoff |

## Interpretation

Run 900 is an accepted seed987 target0 row. The margin clears the production
cutoff by 1.116e-05, which is more comfortable than the seed610 target0 pass
but still not high-reserve across objective variants. The late and late_high
diagnostics fall below cutoff even though they keep the true geometry rank 1.

This supports continuing the normal target-specific policy rather than treating
seed987 as solved from target0 alone. The next useful row is target2 with the
5-source full-ringdown control; if that fails, the established rescue is a
9-source target2 acquisition.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target0 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.255780 and full dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target0 truth_radius_mm is 5.0 mm
resources: GPU utilization held around 90-91%; RAM stayed about 97 GiB available
```

## Next Decision

Continue the seed987 branch with target2, sources=5, Tx/Rx=60, and
ringdown050.

---

## Source: `docs/experiments/436_variable_depth_radius_seed987_target2_sources5_txrx60_ringdown050.md`

# Experiment 436: Seed987 Target2 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 902 tests the seed987 target2 5-source full-ringdown control after seed987
target0 passed in run 900.

## 902: Coordinate Optimizer Variable-Depth/Radius Seed987 Target2 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/902_coordinate_optimizer_variable_depth_radius_seed987_target2_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed987:1.1,-50.0,1.1,0.10,987,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed987 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed987_target2_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed987
target: 2
sources: 5
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 5.518990e-04
offset from cutoff: +5.189904e-05
relative radius margin: 3.228303e-02
confidence label: moderate
fallback warning: none
best misfit: 0.01709563876882805
next radius misfit: 0.01764753781199895
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: about 401.0 s
```

Diagnostic objective rows all preserve the true target2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.518990e-04 | above cutoff |
| highband | 6.351389e-04 | above cutoff |
| late | 8.173949e-04 | above cutoff |
| late_high | 8.347177e-04 | above cutoff |
| veryhigh | 6.558117e-04 | above cutoff |
| early_high | 4.144989e-04 | below cutoff |

## Interpretation

Run 902 is an accepted seed987 target2 row. It passes at the 5-source control,
so the seed987 target2 branch does not need the usual 9-source rescue. The
only weak diagnostic is early_high, while base, highband, late, late_high, and
veryhigh all clear cutoff and preserve the true geometry.

This is a strong contrast with seed610 target2, where the best row remained
below cutoff even after source-density and ringdown-only controls. Continue to
seed987 target1 at the 5-source full-ringdown control to complete the
target-specific seed row.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.274702 and full dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: GPU utilization held around 87-88%; RAM stayed about 97 GiB available
```

## Next Decision

Continue the seed987 branch with target1, sources=5, Tx/Rx=60, and
ringdown050.

---

## Source: `docs/experiments/437_variable_depth_radius_seed987_target1_sources5_txrx60_ringdown050.md`

# Experiment 437: Seed987 Target1 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 903 completes the seed987 target-specific 8/5/5 branch after target0 and
target2 passed.

## 903: Coordinate Optimizer Variable-Depth/Radius Seed987 Target1 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/903_coordinate_optimizer_variable_depth_radius_seed987_target1_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 1 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed987:1.1,-50.0,1.1,0.10,987,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed987 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed987_target1_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed987
target: 1
sources: 5
best: x=250 mm, z=100 mm, r=6.0 mm
next radius: 6.25 mm
absolute radius margin: 5.664717e-04
offset from cutoff: +6.647174e-05
relative radius margin: 3.313545e-02
confidence label: moderate
fallback warning: none
best misfit: 0.01709563876882805
next radius misfit: 0.01766211050505033
listed competing geometry: x=250 mm, z=101 mm, r=6.75 mm
elapsed: about 404.3 s
```

Diagnostic objective rows all preserve the true target1 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.664717e-04 | above cutoff |
| highband | 7.343407e-04 | above cutoff |
| late | 8.455347e-04 | above cutoff |
| late_high | 9.683349e-04 | above cutoff |
| veryhigh | 6.941379e-04 | above cutoff |
| early_high | 5.488049e-04 | above cutoff |

## Interpretation

Run 903 is an accepted seed987 target1 row. Unlike target0 and target2, all six
diagnostic objective variants clear cutoff. This completes seed987 with three
accepted target-specific rows:

| Run | Target | Sources | Base margin | Offset from cutoff |
| ---: | --- | ---: | ---: | ---: |
| 900 | target0 | 8 | 5.111568e-04 | +1.116e-05 |
| 902 | target2 | 5 | 5.518990e-04 | +5.190e-05 |
| 903 | target1 | 5 | 5.664717e-04 | +6.647e-05 |

Create a seed987 summary and continue replication with the next Fibonacci seed
target0.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target1 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.281001 and full dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target1 truth_radius_mm is 6.0 mm
resources: GPU utilization held around 87-88%; RAM stayed about 97 GiB available
```

## Next Decision

Create the seed987 target-specific 8/5/5 summary and move the GPU to seed1597
target0.

---

## Source: `docs/experiments/438_seed987_ringdown050_target_specific_8_5_5_summary.md`

# Experiment 438: Seed987 Ringdown050 Target-Specific 8/5/5 Summary

## Purpose

Run 904 consolidates the accepted seed987 target-specific branch after target0,
target2, and target1 all passed under full ringdown050.

## 904: Seed987 Ringdown050 Target-Specific 8/5/5 Summary

Output:

```text
outputs/experiments/904_seed987_ringdown050_target_specific_8_5_5_summary
```

Generation:

```text
CPU-side structured aggregation of completed coordinate-optimizer outputs
900, 902, and 903. No new FDTD scene was launched for this summary.
```

Source runs:

```text
900 target0 sources=8 ringdown050
902 target2 sources=5 ringdown050
903 target1 sources=5 ringdown050
```

## Results

Summary counts:

```text
row count: 3
diagnostic objective rows: 18
accepted runs: 900, 902, 903
rejected runs: none
cutoff: 5.0e-4
```

Target-specific branch:

| Target | Run | Sources | Base margin | Offset from cutoff | Status |
| --- | ---: | ---: | ---: | ---: | --- |
| target0 | 900 | 8 | 5.111568e-04 | +1.116e-05 | accepted |
| target2 | 902 | 5 | 5.518990e-04 | +5.190e-05 | accepted |
| target1 | 903 | 5 | 5.664717e-04 | +6.647e-05 | accepted |

Objective reserve:

| Run | Target | Below-cutoff variants |
| ---: | --- | --- |
| 900 | target0 | late, late_high |
| 902 | target2 | early_high |
| 903 | target1 | none |

## Interpretation

Seed987 is a clean accepted seed under the current target-specific policy. The
target0 margin is the lowest reserve, but it still clears cutoff and preserves
the true geometry across all objective variants. Target2 and target1 both pass
with more base reserve than target0.

This makes seed987 a useful positive contrast to seed610. Seed610 stayed
truth-preserving but weak on target1 and target2; seed987 stays
truth-preserving and clears the production cutoff on all three targets without
rescue runs.

## Validation

```text
JSON parse: run_manifest.json and seed987_branch_summary.json pass
CSV rows: seed987_branch_summary.csv has 3 data rows
diagnostics CSV rows: seed987_objective_diagnostics.csv has 18 data rows
base margins figure: 1600x960 RGBA, nonwhite_fraction=0.466273
objective heatmap: 1600x919 RGBA, nonwhite_fraction=0.615909
offset figure: 1600x960 RGBA, nonwhite_fraction=0.391625
visual inspection: all three figures are readable and support the conclusion
figure notes: figures/FIGURE_NOTES.md present and run-specific
resources: no new FDTD scene; GPU was moved to seed1597 target0
```

## Next Decision

Continue full-ringdown replication with seed1597 target0, sources=8, Tx/Rx=60.

---

## Source: `docs/experiments/439_variable_depth_radius_seed1597_target0_sources8_txrx60_ringdown050.md`

# Experiment 439: Seed1597 Target0 Sources=8 Tx/Rx=60 Ringdown050

## Purpose

Run 905 starts the next Fibonacci seed after seed987 passed the full target
specific 8/5/5 policy. The first seed1597 row tests target0 with 8 sources and
full ringdown050.

## 905: Coordinate Optimizer Variable-Depth/Radius Seed1597 Target0 Sources=8 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/905_coordinate_optimizer_variable_depth_radius_seed1597_target0_sources8_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 8 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 0 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed1597:1.1,-50.0,1.1,0.10,1597,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed1597 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed1597_target0_sources8_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed1597
target: 0
sources: 8
best: x=150 mm, z=80 mm, r=5.0 mm
next radius: 5.25 mm
absolute radius margin: 5.003598e-04
offset from cutoff: +3.597983e-07
relative radius margin: 3.138514e-02
confidence label: moderate
fallback warning: none
best misfit: 0.01594257211339581
next radius misfit: 0.016442931911681526
listed competing geometry: x=150 mm, z=81 mm, r=6.0 mm
elapsed: about 623.2 s
```

Diagnostic objective rows all preserve the true target0 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.003598e-04 | above cutoff |
| highband | 6.455801e-04 | above cutoff |
| late | 3.883610e-04 | below cutoff |
| late_high | 4.778189e-04 | below cutoff |
| veryhigh | 5.853901e-04 | above cutoff |
| early_high | 5.382777e-04 | above cutoff |

## Interpretation

Run 905 is accepted but fragile. The base margin clears cutoff by only
3.598e-07, making it the lowest-reserve accepted target0 row in the current
Fibonacci branch. The late and late_high variants are below cutoff, while the
other variants and base row keep the true geometry rank 1.

This does not justify stopping the seed1597 branch; it makes the target2 and
target1 rows more important. Continue with target2 at 5 sources and full
ringdown050. If seed1597 shows the same broad low-margin pattern as seed610,
record it explicitly before attempting specialized controls.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target0 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.247682 and full dynamic range
visual inspection: confidence figure is readable and shows one moderate row just above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target0 truth_radius_mm is 5.0 mm
resources: GPU utilization held around 90-91%; RAM stayed about 97 GiB available
```

## Next Decision

Continue the seed1597 branch with target2, sources=5, Tx/Rx=60, and
ringdown050.

---

## Source: `docs/experiments/440_variable_depth_radius_seed1597_target2_sources5_txrx60_ringdown050.md`

# Experiment 440: Seed1597 Target2 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 906 tests seed1597 target2 at the 5-source full-ringdown control after
target0 passed by only a razor margin.

## 906: Coordinate Optimizer Variable-Depth/Radius Seed1597 Target2 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/906_coordinate_optimizer_variable_depth_radius_seed1597_target2_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed1597:1.1,-50.0,1.1,0.10,1597,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed1597 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed1597_target2_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed1597
target: 2
sources: 5
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 5.227926e-04
offset from cutoff: +2.279257e-05
relative radius margin: 3.068533e-02
confidence label: moderate
fallback warning: none
best misfit: 0.017037216038311938
next radius misfit: 0.017560008603844025
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: about 398.6 s
```

Diagnostic objective rows all preserve the true target2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.227926e-04 | above cutoff |
| highband | 6.337371e-04 | above cutoff |
| late | 7.664166e-04 | above cutoff |
| late_high | 8.007216e-04 | above cutoff |
| veryhigh | 6.346520e-04 | above cutoff |
| early_high | 4.566385e-04 | below cutoff |

## Interpretation

Run 906 is an accepted seed1597 target2 row. It clears the production cutoff by
2.279e-05 at 5 sources, so the target2 branch does not need a 9-source rescue.
The early_high diagnostic remains weak, but base, highband, late, late_high,
and veryhigh all clear cutoff and preserve the true geometry.

This partially offsets the seed1597 target0 concern: the seed is not obviously
as broad-low-margin as seed610. Complete target1 before creating a seed-level
summary.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.257589 and full dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: GPU utilization held around 87-88%; RAM stayed about 97 GiB available
```

## Next Decision

Continue the seed1597 branch with target1, sources=5, Tx/Rx=60, and
ringdown050.

---

## Source: `docs/experiments/441_variable_depth_radius_seed1597_target1_sources5_txrx60_ringdown050.md`

# Experiment 441: Seed1597 Target1 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 907 tests the seed1597 target1 5-source control after target0 and target2
both recovered the true geometry.

## 907: Coordinate Optimizer Variable-Depth/Radius Seed1597 Target1 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/907_coordinate_optimizer_variable_depth_radius_seed1597_target1_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 1 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed1597:1.1,-50.0,1.1,0.10,1597,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed1597 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed1597_target1_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed1597
target: 1
sources: 5
best: x=250 mm, z=100 mm, r=6.0 mm
next radius: 6.25 mm
absolute radius margin: 4.919485e-04
offset from cutoff: -8.051534e-06
relative radius margin: 2.887493e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.017037216038311938
next radius misfit: 0.017529164503862667
listed competing geometry: x=250 mm, z=101 mm, r=6.75 mm
elapsed: about 400.2 s
```

Diagnostic objective rows all preserve the true target1 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.919485e-04 | below cutoff |
| highband | 6.361327e-04 | above cutoff |
| late | 8.010195e-04 | above cutoff |
| late_high | 9.121122e-04 | above cutoff |
| veryhigh | 6.158115e-04 | above cutoff |
| early_high | 4.833063e-04 | below cutoff |

## Interpretation

Run 907 is a truth-preserving target1 miss. The true geometry is rank 1, but
the base margin is 8.052e-06 below cutoff and early_high is also below cutoff.
This makes seed1597 different from seed987, where target1 passed all objective
variants at 5 sources.

The next justified action is the established 9-source target1 rescue. Do not
switch to ringdown or objective-specialized controls until the source-density
rescue is tested.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target1 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.244054 and full dynamic range
visual inspection: confidence figure is readable and shows one weak row below the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target1 truth_radius_mm is 6.0 mm
resources: GPU utilization held around 87-88%; RAM stayed about 97 GiB available
```

## Next Decision

Run seed1597 target1 with 9 sources, Tx/Rx=60, and ringdown050.

---

## Source: `docs/experiments/442_variable_depth_radius_seed1597_target1_sources9_txrx60_ringdown050.md`

# Experiment 442: Seed1597 Target1 Sources=9 Tx/Rx=60 Ringdown050

## Purpose

Run 908 tests the seed1597 target1 9-source rescue after the 5-source control
in run 907 was exact but weak.

## 908: Coordinate Optimizer Variable-Depth/Radius Seed1597 Target1 Sources=9 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/908_coordinate_optimizer_variable_depth_radius_seed1597_target1_sources9_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 9 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 1 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed1597:1.1,-50.0,1.1,0.10,1597,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed1597 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed1597_target1_sources9_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed1597
target: 1
sources: 9
best: x=250 mm, z=100 mm, r=6.0 mm
next radius: 6.25 mm
absolute radius margin: 5.127098e-04
offset from cutoff: +1.270976e-05
relative radius margin: 3.224237e-02
confidence label: moderate
fallback warning: none
best misfit: 0.015901737879802525
next radius misfit: 0.016414447639132475
listed competing geometry: x=250 mm, z=101 mm, r=6.75 mm
elapsed: about 709.0 s
```

Diagnostic objective rows all preserve the true target1 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.127098e-04 | above cutoff |
| highband | 7.192843e-04 | above cutoff |
| late | 7.541665e-04 | above cutoff |
| late_high | 9.578916e-04 | above cutoff |
| veryhigh | 6.946527e-04 | above cutoff |
| early_high | 5.451536e-04 | above cutoff |

## Interpretation

Run 908 rescues seed1597 target1. The source-density increase from 5 to 9
sources moves the base margin from 4.919e-04 to 5.127e-04 and all diagnostic
variants clear cutoff. The branch is now accepted, but with less reserve than
seed987 target1.

| Run | Sources | Base margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | --- |
| 907 | 5 | 4.919485e-04 | -8.052e-06 | rejected |
| 908 | 9 | 5.127098e-04 | +1.271e-05 | accepted |

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target1 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.253069 and full dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target1 truth_radius_mm is 6.0 mm
resources: GPU utilization held around 91-92%; RAM stayed about 97 GiB available
```

## Next Decision

Create a seed1597 target-specific rescue summary and move the GPU to seed2584
target0.

---

## Source: `docs/experiments/443_seed1597_ringdown050_target_specific_rescue_summary.md`

# Experiment 443: Seed1597 Ringdown050 Target-Specific Rescue Summary

## Purpose

Run 909 consolidates the seed1597 branch after target0 passed by a razor
margin, target2 passed at 5 sources, target1 failed at 5 sources, and target1
was rescued at 9 sources.

## 909: Seed1597 Ringdown050 Target-Specific Rescue Summary

Output:

```text
outputs/experiments/909_seed1597_ringdown050_target_specific_rescue_summary
```

Generation:

```text
CPU-side structured aggregation of completed coordinate-optimizer outputs
905-908. No new FDTD scene was launched for this summary.
```

Source runs:

```text
905 target0 sources=8 ringdown050
906 target2 sources=5 ringdown050
907 target1 sources=5 ringdown050
908 target1 sources=9 ringdown050
```

## Results

Summary counts:

```text
row count: 4
diagnostic objective rows: 24
accepted runs: 905, 906, 908
rejected truth-preserving runs: 907
cutoff: 5.0e-4
```

Best row by target:

| Target | Best run | Sources | Base margin | Offset from cutoff | Status |
| --- | ---: | ---: | ---: | ---: | --- |
| target0 | 905 | 8 | 5.003598e-04 | +3.598e-07 | accepted |
| target2 | 906 | 5 | 5.227926e-04 | +2.279e-05 | accepted |
| target1 | 908 | 9 | 5.127098e-04 | +1.271e-05 | accepted |

Target1 rescue:

| Run | Sources | Base margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | --- |
| 907 | 5 | 4.919485e-04 | -8.052e-06 | rejected |
| 908 | 9 | 5.127098e-04 | +1.271e-05 | accepted |

## Interpretation

Seed1597 is an accepted but low-reserve seed. Every row preserves the true
geometry, but target0 passes by only 3.598e-07 and target1 needs the 9-source
rescue. It is therefore closer to seed610 than seed987 in margin reserve, but
unlike seed610 it has a source-density rescue that brings every target above
cutoff.

Use seed1597 as evidence that the 8/5/5 policy is not sufficient for every
seed, but that the target1 9-source rescue can be enough when the 5-source miss
is shallow.

## Validation

```text
JSON parse: run_manifest.json and seed1597_branch_summary.json pass
CSV rows: seed1597_branch_summary.csv has 4 data rows
diagnostics CSV rows: seed1597_objective_diagnostics.csv has 24 data rows
base margins figure: 1839x960 RGBA, nonwhite_fraction=0.440328
objective heatmap: 1839x960 RGBA, nonwhite_fraction=0.630825
target1 rescue trend: 1520x960 RGBA, nonwhite_fraction=0.036398
visual inspection: all three figures are readable and support the conclusion
figure notes: figures/FIGURE_NOTES.md present and run-specific
resources: no new FDTD scene; GPU was moved to seed2584 target0
```

## Next Decision

Continue full-ringdown replication with seed2584 target0, sources=8, Tx/Rx=60.

---

## Source: `docs/experiments/444_variable_depth_radius_seed2584_target0_sources8_txrx60_ringdown050.md`

# Experiment 444: Seed2584 Target0 Sources=8 Tx/Rx=60 Ringdown050

## Purpose

Run 910 starts the seed2584 branch after seed1597 completed with a target1
9-source rescue. The first seed2584 row tests target0 at the 8-source
full-ringdown production setting.

## 910: Coordinate Optimizer Variable-Depth/Radius Seed2584 Target0 Sources=8 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/910_coordinate_optimizer_variable_depth_radius_seed2584_target0_sources8_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 8 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 0 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed2584:1.1,-50.0,1.1,0.10,2584,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed2584 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed2584_target0_sources8_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed2584
target: 0
sources: 8
best: x=150 mm, z=80 mm, r=5.0 mm
next radius: 5.25 mm
absolute radius margin: 4.822702e-04
offset from cutoff: -1.772975e-05
relative radius margin: 3.106548e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.015524314676615605
next radius misfit: 0.016006584926598285
listed competing geometry: x=150 mm, z=81 mm, r=6.0 mm
elapsed: about 620.0 s
```

Diagnostic objective rows all preserve the true target0 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.822702e-04 | below cutoff |
| highband | 6.442949e-04 | above cutoff |
| late | 3.610283e-04 | below cutoff |
| late_high | 4.254642e-04 | below cutoff |
| veryhigh | 5.915389e-04 | above cutoff |
| early_high | 5.201915e-04 | above cutoff |

## Interpretation

Run 910 is a truth-preserving target0 miss. The true geometry is rank 1, but
the base margin is 1.773e-05 below cutoff. This is weaker than seed1597
target0, which barely passed, and it carries the same late/late_high weakness
seen in other low-reserve target0 rows.

The next justified action is a 9-source target0 rescue. Do not move to
ringdown thresholding until source-density response is measured.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target0 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.239597 and full dynamic range
visual inspection: confidence figure is readable and shows one weak row below the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target0 truth_radius_mm is 5.0 mm
resources: GPU utilization held around 90%; RAM stayed about 97 GiB available
```

## Next Decision

Run seed2584 target0 with 9 sources, Tx/Rx=60, and ringdown050.

---

## Source: `docs/experiments/445_variable_depth_radius_seed2584_target0_sources9_txrx60_ringdown050.md`

# Experiment 445: Seed2584 Target0 Sources=9 Tx/Rx=60 Ringdown050

## Purpose

Run 911 tests the 9-source target0 rescue after seed2584 target0 was exact but
weak at 8 sources in run 910.

## 911: Coordinate Optimizer Variable-Depth/Radius Seed2584 Target0 Sources=9 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/911_coordinate_optimizer_variable_depth_radius_seed2584_target0_sources9_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 9 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 0 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed2584:1.1,-50.0,1.1,0.10,2584,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed2584 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed2584_target0_sources9_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed2584
target: 0
sources: 9
best: x=150 mm, z=80 mm, r=5.0 mm
next radius: 5.25 mm
absolute radius margin: 5.468829e-04
offset from cutoff: +4.688295e-05
relative radius margin: 3.543669e-02
confidence label: moderate
fallback warning: none
best misfit: 0.01543267701366276
next radius misfit: 0.015979559959111517
listed competing geometry: x=150 mm, z=81 mm, r=6.0 mm
elapsed: about 712.2 s
```

Diagnostic objective rows all preserve the true target0 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.468829e-04 | above cutoff |
| highband | 7.638746e-04 | above cutoff |
| late | 4.360870e-04 | below cutoff |
| late_high | 5.147448e-04 | above cutoff |
| veryhigh | 7.927162e-04 | above cutoff |
| early_high | 6.542915e-04 | above cutoff |

## Interpretation

Run 911 rescues seed2584 target0 at 9 sources. The base margin improves from
4.823e-04 in run 910 to 5.469e-04 in run 911. The late diagnostic remains below
cutoff, so this is accepted but not a uniformly high-reserve row.

The target0 branch is now accepted. Continue to the standard seed2584 target2
5-source full-ringdown control.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target0 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.268445 and full dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target0 truth_radius_mm is 5.0 mm
resources: GPU utilization held around 91-92%; RAM stayed about 97 GiB available
```

## Next Decision

Continue the seed2584 branch with target2, sources=5, Tx/Rx=60, and
ringdown050.

---

## Source: `docs/experiments/446_variable_depth_radius_seed2584_target2_sources5_txrx60_ringdown050.md`

# Experiment 446: Seed2584 Target2 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 912 tests seed2584 target2 at the 5-source full-ringdown control after
target0 was rescued at 9 sources.

## 912: Coordinate Optimizer Variable-Depth/Radius Seed2584 Target2 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/912_coordinate_optimizer_variable_depth_radius_seed2584_target2_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed2584:1.1,-50.0,1.1,0.10,2584,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed2584 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed2584_target2_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed2584
target: 2
sources: 5
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 5.528720e-04
offset from cutoff: +5.287201e-05
relative radius margin: 3.293314e-02
confidence label: moderate
fallback warning: none
best misfit: 0.016787710901228395
next radius misfit: 0.017340582911007693
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: about 397.8 s
```

Diagnostic objective rows all preserve the true target2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.528720e-04 | above cutoff |
| highband | 6.614402e-04 | above cutoff |
| late | 8.311015e-04 | above cutoff |
| late_high | 8.131028e-04 | above cutoff |
| veryhigh | 6.867633e-04 | above cutoff |
| early_high | 4.725436e-04 | below cutoff |

## Interpretation

Run 912 is an accepted seed2584 target2 row. It passes at the 5-source control,
so target2 does not need a 9-source rescue. This keeps the seed2584 branch
focused on target1 after target0 already needed source-density rescue.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.271141 and full dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: GPU utilization held around 87%; RAM stayed about 97 GiB available
```

## Next Decision

Continue the seed2584 branch with target1, sources=5, Tx/Rx=60, and
ringdown050.

---

## Source: `docs/experiments/447_variable_depth_radius_seed2584_target1_sources5_txrx60_ringdown050.md`

# Experiment 447: Seed2584 Target1 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 913 tests seed2584 target1 at the 5-source full-ringdown control after
target0 was rescued at 9 sources and target2 passed at 5 sources.

## 913: Coordinate Optimizer Variable-Depth/Radius Seed2584 Target1 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/913_coordinate_optimizer_variable_depth_radius_seed2584_target1_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 1 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed2584:1.1,-50.0,1.1,0.10,2584,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed2584 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed2584_target1_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed2584
target: 1
sources: 5
best: x=250 mm, z=100 mm, r=6.0 mm
next radius: 6.25 mm
absolute radius margin: 5.254512e-04
offset from cutoff: +2.545123e-05
relative radius margin: 3.129975e-02
confidence label: moderate
fallback warning: none
best misfit: 0.016787710901228395
next radius misfit: 0.017313162127445644
listed competing geometry: x=250 mm, z=101 mm, r=6.75 mm
elapsed: about 400.2 s
```

Diagnostic objective rows all preserve the true target1 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.254512e-04 | above cutoff |
| highband | 6.890583e-04 | above cutoff |
| late | 7.845651e-04 | above cutoff |
| late_high | 9.369825e-04 | above cutoff |
| veryhigh | 6.315315e-04 | above cutoff |
| early_high | 5.082966e-04 | above cutoff |

## Interpretation

Run 913 is an accepted seed2584 target1 row. It passes at the 5-source control
and all objective variants clear cutoff, so target1 does not need a 9-source
rescue. The seed2584 branch is therefore complete: only target0 required the
source-density rescue.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target1 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.258717 and full dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target1 truth_radius_mm is 6.0 mm
resources: GPU utilization held around 87%; RAM stayed about 97 GiB available
```

## Next Decision

Create the seed2584 target-specific rescue summary, then continue
full-ringdown replication with seed4181 target0.

---

## Source: `docs/experiments/448_seed2584_ringdown050_target_specific_rescue_summary.md`

# Experiment 448: Seed2584 Ringdown050 Target-Specific Rescue Summary

## Purpose

Run 914 consolidates the seed2584 branch after target0 failed at the 8-source
control, target0 passed a 9-source rescue, and target2 and target1 passed at
the 5-source controls.

## 914: Seed2584 Ringdown050 Target-Specific Rescue Summary

Output:

```text
outputs/experiments/914_seed2584_ringdown050_target_specific_rescue_summary
```

Generation:

```text
CPU-side structured aggregation of completed coordinate-optimizer outputs
910-913. No new FDTD scene was launched for this summary.
```

Source runs:

```text
910 target0 sources=8 ringdown050
911 target0 sources=9 ringdown050
912 target2 sources=5 ringdown050
913 target1 sources=5 ringdown050
```

## Results

Summary counts:

```text
row count: 4
diagnostic objective rows: 24
accepted runs: 911, 912, 913
rejected truth-preserving runs: 910
cutoff: 5.0e-4
```

Best row by target:

| Target | Best run | Sources | Base margin | Offset from cutoff | Status |
| --- | ---: | ---: | ---: | ---: | --- |
| target0 | 911 | 9 | 5.468829e-04 | +4.688295e-05 | accepted |
| target2 | 912 | 5 | 5.528720e-04 | +5.287201e-05 | accepted |
| target1 | 913 | 5 | 5.254512e-04 | +2.545123e-05 | accepted |

Target0 rescue:

| Run | Sources | Base margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | --- |
| 910 | 8 | 4.822702e-04 | -1.772975e-05 | rejected |
| 911 | 9 | 5.468829e-04 | +4.688295e-05 | accepted |

## Interpretation

Seed2584 is accepted after one source-density rescue. Every row preserves the
true geometry, but the 8-source target0 row falls below cutoff. The 9-source
target0 rescue clears cutoff with a healthier reserve than the seed1597 target1
rescue, while target2 and target1 need no rescue at the 5-source control.

Use seed2584 as evidence that the target-specific policy should keep the
rescue branch targeted: increase source density only for the failing target
before changing ringdown or objective windows.

## Validation

```text
JSON parse: run_manifest.json and seed2584_branch_summary.json pass
CSV rows: seed2584_branch_summary.csv has 4 data rows
diagnostics CSV rows: seed2584_objective_diagnostics.csv has 24 data rows
base margins figure: 1839x960 RGBA, nonwhite_fraction=0.433435
objective heatmap: 1839x960 RGBA, nonwhite_fraction=0.630360
target0 rescue trend: 1520x960 RGBA, nonwhite_fraction=0.045302
visual inspection: all three figures are readable; target0 rescue trend labels are clear after regeneration
figure notes: figures/FIGURE_NOTES.md present and run-specific
resources: no new FDTD scene; GPU was moved to seed4181 target0
```

## Next Decision

Continue full-ringdown replication with seed4181 target0, sources=8, Tx/Rx=60.

---

## Source: `docs/experiments/449_variable_depth_radius_seed4181_target0_sources8_txrx60_ringdown050.md`

# Experiment 449: Seed4181 Target0 Sources=8 Tx/Rx=60 Ringdown050

## Purpose

Run 915 starts the seed4181 branch with the standard target0 8-source
full-ringdown control.

## 915: Coordinate Optimizer Variable-Depth/Radius Seed4181 Target0 Sources=8 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/915_coordinate_optimizer_variable_depth_radius_seed4181_target0_sources8_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 8 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 0 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed4181:1.1,-50.0,1.1,0.10,4181,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed4181 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed4181_target0_sources8_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed4181
target: 0
sources: 8
best: x=150 mm, z=80 mm, r=5.0 mm
next radius: 5.25 mm
absolute radius margin: 5.844588e-04
offset from cutoff: +8.445876e-05
relative radius margin: 3.769209e-02
confidence label: moderate
fallback warning: none
best misfit: 0.015506139446628035
next radius misfit: 0.0160905982071135
listed competing geometry: x=150 mm, z=81 mm, r=6.0 mm
elapsed: about 630.6 s
```

Diagnostic objective rows all preserve the true target0 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.844588e-04 | above cutoff |
| highband | 7.667026e-04 | above cutoff |
| late | 5.216869e-04 | above cutoff |
| late_high | 6.104873e-04 | above cutoff |
| veryhigh | 6.848315e-04 | above cutoff |
| early_high | 6.198516e-04 | above cutoff |

## Interpretation

Run 915 is an accepted seed4181 target0 row. It has more reserve than the
low-margin target0 rows from seed1597 and seed2584, and every diagnostic
objective variant clears cutoff. Do not run a target0 rescue.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target0 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.285754 and full dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target0 truth_radius_mm is 5.0 mm
resources: GPU utilization held around 90%; RAM stayed about 97 GiB available
```

## Next Decision

Continue the seed4181 branch with target2, sources=5, Tx/Rx=60, and
ringdown050.

---

## Source: `docs/experiments/450_variable_depth_radius_seed4181_target2_sources5_txrx60_ringdown050.md`

# Experiment 450: Seed4181 Target2 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 916 tests seed4181 target2 at the 5-source full-ringdown control after
target0 passed the standard 8-source production row.

## 916: Coordinate Optimizer Variable-Depth/Radius Seed4181 Target2 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/916_coordinate_optimizer_variable_depth_radius_seed4181_target2_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed4181:1.1,-50.0,1.1,0.10,4181,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed4181 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed4181_target2_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed4181
target: 2
sources: 5
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 5.898714e-04
offset from cutoff: +8.987140e-05
relative radius margin: 3.564915e-02
confidence label: moderate
fallback warning: none
best misfit: 0.016546574675951033
next radius misfit: 0.017136446073141534
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: about 401.1 s
```

Diagnostic objective rows all preserve the true target2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.898714e-04 | above cutoff |
| highband | 7.232563e-04 | above cutoff |
| late | 8.716237e-04 | above cutoff |
| late_high | 9.417643e-04 | above cutoff |
| veryhigh | 7.167226e-04 | above cutoff |
| early_high | 5.175439e-04 | above cutoff |

## Interpretation

Run 916 is an accepted seed4181 target2 row. It passes at the 5-source control
and all objective variants clear cutoff, so target2 does not need a rescue.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.288447 and full dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: GPU utilization held around 87%; RAM stayed about 97 GiB available
```

## Next Decision

Continue the seed4181 branch with target1, sources=5, Tx/Rx=60, and
ringdown050.

---

## Source: `docs/experiments/451_variable_depth_radius_seed4181_target1_sources5_txrx60_ringdown050.md`

# Experiment 451: Seed4181 Target1 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 917 tests seed4181 target1 at the 5-source full-ringdown control after
target0 and target2 both passed.

## 917: Coordinate Optimizer Variable-Depth/Radius Seed4181 Target1 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/917_coordinate_optimizer_variable_depth_radius_seed4181_target1_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 1 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed4181:1.1,-50.0,1.1,0.10,4181,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed4181 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed4181_target1_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed4181
target: 1
sources: 5
best: x=250 mm, z=100 mm, r=6.0 mm
next radius: 6.25 mm
absolute radius margin: 6.807359e-04
offset from cutoff: +1.807359e-04
relative radius margin: 4.114059e-02
confidence label: moderate
fallback warning: none
best misfit: 0.016546574675951033
next radius misfit: 0.017227310582213533
listed competing geometry: x=250 mm, z=101 mm, r=6.75 mm
elapsed: about 400.1 s
```

Diagnostic objective rows all preserve the true target1 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 6.807359e-04 | above cutoff |
| highband | 8.798761e-04 | above cutoff |
| late | 1.036880e-03 | above cutoff |
| late_high | 1.151941e-03 | above cutoff |
| veryhigh | 8.029410e-04 | above cutoff |
| early_high | 6.575659e-04 | above cutoff |

## Interpretation

Run 917 is an accepted seed4181 target1 row. It has the strongest base margin
of the seed4181 branch, and all diagnostics clear cutoff. Seed4181 needs no
rescue run.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target1 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.328103 and full dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target1 truth_radius_mm is 6.0 mm
resources: GPU utilization held around 87%; RAM stayed about 97 GiB available
```

## Next Decision

Create the seed4181 target-specific 8/5/5 summary, then continue
full-ringdown replication with seed6765 target0.

---

## Source: `docs/experiments/452_seed4181_ringdown050_target_specific_8_5_5_summary.md`

# Experiment 452: Seed4181 Ringdown050 Target-Specific 8/5/5 Summary

## Purpose

Run 918 consolidates the seed4181 branch after target0, target2, and target1
all passed without rescue.

## 918: Seed4181 Ringdown050 Target-Specific 8/5/5 Summary

Output:

```text
outputs/experiments/918_seed4181_ringdown050_target_specific_8_5_5_summary
```

Generation:

```text
CPU-side structured aggregation of completed coordinate-optimizer outputs
915-917. No new FDTD scene was launched for this summary.
```

Source runs:

```text
915 target0 sources=8 ringdown050
916 target2 sources=5 ringdown050
917 target1 sources=5 ringdown050
```

## Results

Summary counts:

```text
row count: 3
diagnostic objective rows: 18
accepted runs: 915, 916, 917
rejected runs: none
cutoff: 5.0e-4
```

Best row by target:

| Target | Run | Sources | Base margin | Offset from cutoff | Status |
| --- | ---: | ---: | ---: | ---: | --- |
| target0 | 915 | 8 | 5.844588e-04 | +8.446e-05 | accepted |
| target2 | 916 | 5 | 5.898714e-04 | +8.987e-05 | accepted |
| target1 | 917 | 5 | 6.807359e-04 | +1.807e-04 | accepted |

## Interpretation

Seed4181 cleanly passes the target-specific 8/5/5 full-ringdown policy. All
three targets are exact, all base margins are above cutoff, and all 18
objective-diagnostic rows also clear cutoff. This is stronger than the
seed1597 and seed2584 branches, which required targeted rescue.

Use seed4181 as evidence that the rescue policy should remain conditional:
launch rescue only for a target-specific miss rather than preemptively
increasing source density for every target.

## Validation

```text
JSON parse: run_manifest.json and seed4181_branch_summary.json pass
CSV rows: seed4181_branch_summary.csv has 3 data rows
diagnostics CSV rows: seed4181_objective_diagnostics.csv has 18 data rows
base margins figure: 1600x960 RGBA, nonwhite_fraction=0.472575
objective heatmap: 1600x920 RGBA, nonwhite_fraction=0.239709
offset figure: 1600x960 RGBA, nonwhite_fraction=0.164063
visual inspection: all three figures are readable; heatmap text contrast was regenerated before documentation
figure notes: figures/FIGURE_NOTES.md present and run-specific
resources: no new FDTD scene; GPU was moved to seed6765 target0
```

## Next Decision

Continue full-ringdown replication with seed6765 target0, sources=8, Tx/Rx=60.

---

## Source: `docs/experiments/453_variable_depth_radius_seed6765_target0_sources8_txrx60_ringdown050.md`

# Experiment 453: Seed6765 Target0 Sources=8 Tx/Rx=60 Ringdown050

## Purpose

Run 919 starts the seed6765 branch with the standard target0 8-source
full-ringdown control.

## 919: Coordinate Optimizer Variable-Depth/Radius Seed6765 Target0 Sources=8 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/919_coordinate_optimizer_variable_depth_radius_seed6765_target0_sources8_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 8 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 0 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed6765:1.1,-50.0,1.1,0.10,6765,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed6765 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed6765_target0_sources8_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed6765
target: 0
sources: 8
best: x=150 mm, z=80 mm, r=5.0 mm
next radius: 5.25 mm
absolute radius margin: 4.905837e-04
offset from cutoff: -9.416301e-06
relative radius margin: 3.107835e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.01578538552509494
next radius misfit: 0.016275969223640836
listed competing geometry: x=150 mm, z=81 mm, r=6.0 mm
elapsed: about 627.0 s
```

Diagnostic objective rows all preserve the true target0 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.905837e-04 | below cutoff |
| highband | 6.738212e-04 | above cutoff |
| late | 4.395305e-04 | below cutoff |
| late_high | 5.235599e-04 | above cutoff |
| veryhigh | 6.147442e-04 | above cutoff |
| early_high | 5.679365e-04 | above cutoff |

## Interpretation

Run 919 is exact but rejected under the production cutoff. The base margin is
9.416e-06 below cutoff, the confidence label is weak, and the late objective
variant is also below cutoff. Since the true geometry is still rank 1, the
proper next action is the targeted 9-source target0 rescue.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target0 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.243411 and full dynamic range
visual inspection: confidence figure is readable and shows one weak row below the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target0 truth_radius_mm is 5.0 mm
resources: GPU utilization held around 90%; RAM stayed about 97 GiB available
```

## Next Decision

Run seed6765 target0 with 9 sources, Tx/Rx=60, and ringdown050.

---

## Source: `docs/experiments/454_variable_depth_radius_seed6765_target0_sources9_txrx60_ringdown050.md`

# Experiment 454: Seed6765 Target0 Sources=9 Tx/Rx=60 Ringdown050

## Purpose

Run 920 tests the targeted 9-source rescue after seed6765 target0 was exact but
weak at the standard 8-source control.

## 920: Coordinate Optimizer Variable-Depth/Radius Seed6765 Target0 Sources=9 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/920_coordinate_optimizer_variable_depth_radius_seed6765_target0_sources9_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 9 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 0 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed6765:1.1,-50.0,1.1,0.10,6765,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed6765 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed6765_target0_sources9_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed6765
target: 0
sources: 9
best: x=150 mm, z=80 mm, r=5.0 mm
next radius: 5.25 mm
absolute radius margin: 5.485377e-04
offset from cutoff: +4.853773e-05
relative radius margin: 3.480489e-02
confidence label: moderate
fallback warning: none
best misfit: 0.015760362093193533
next radius misfit: 0.016308899820393328
listed competing geometry: x=150 mm, z=81 mm, r=6.0 mm
elapsed: about 714.1 s
```

Diagnostic objective rows all preserve the true target0 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.485377e-04 | above cutoff |
| highband | 7.639709e-04 | above cutoff |
| late | 4.048140e-04 | below cutoff |
| late_high | 4.924308e-04 | below cutoff |
| veryhigh | 8.207545e-04 | above cutoff |
| early_high | 6.538211e-04 | above cutoff |

## Interpretation

Run 920 rescues seed6765 target0: the base margin is accepted and the
confidence label is moderate. It remains a lower-reserve accepted rescue
because late and late_high objective variants are still below cutoff.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target0 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.269555 and full dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target0 truth_radius_mm is 5.0 mm
resources: GPU utilization held around 91%; RAM stayed about 97 GiB available
```

## Next Decision

Continue the seed6765 branch with target2, sources=5, Tx/Rx=60, and
ringdown050.

---

## Source: `docs/experiments/455_variable_depth_radius_seed6765_target2_sources5_txrx60_ringdown050.md`

# Experiment 455: Seed6765 Target2 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 921 tests seed6765 target2 at the standard 5-source full-ringdown control
after target0 was rescued at 9 sources.

## 921: Coordinate Optimizer Variable-Depth/Radius Seed6765 Target2 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/921_coordinate_optimizer_variable_depth_radius_seed6765_target2_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed6765:1.1,-50.0,1.1,0.10,6765,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed6765 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed6765_target2_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed6765
target: 2
sources: 5
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 4.840468e-04
offset from cutoff: -1.595316e-05
relative radius margin: 2.911477e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.01662547515436525
next radius misfit: 0.017109521997320156
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: about 400.1 s
```

Diagnostic objective rows all preserve the true target2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.840468e-04 | below cutoff |
| highband | 6.051937e-04 | above cutoff |
| late | 7.326723e-04 | above cutoff |
| late_high | 7.470182e-04 | above cutoff |
| veryhigh | 6.476571e-04 | above cutoff |
| early_high | 4.423167e-04 | below cutoff |

## Interpretation

Run 921 is exact but rejected under the production cutoff. The base margin is
1.595e-05 below cutoff and early_high is also weak. Since the true geometry is
still rank 1, the next action is a targeted 9-source target2 rescue.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.240695 and full dynamic range
visual inspection: confidence figure is readable and shows one weak row below the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: GPU utilization held around 88%; RAM stayed about 97 GiB available
```

## Next Decision

Run seed6765 target2 with 9 sources, Tx/Rx=60, and ringdown050.

---

## Source: `docs/experiments/456_variable_depth_radius_seed6765_target2_sources9_txrx60_ringdown050.md`

# Experiment 456: Seed6765 Target2 Sources=9 Tx/Rx=60 Ringdown050

## Purpose

Run 922 tests the targeted 9-source rescue after seed6765 target2 was exact but
weak at the 5-source control.

## 922: Coordinate Optimizer Variable-Depth/Radius Seed6765 Target2 Sources=9 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/922_coordinate_optimizer_variable_depth_radius_seed6765_target2_sources9_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 9 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed6765:1.1,-50.0,1.1,0.10,6765,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed6765 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed6765_target2_sources9_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed6765
target: 2
sources: 9
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 5.066980e-04
offset from cutoff: +6.698036e-06
relative radius margin: 3.215015e-02
confidence label: moderate
fallback warning: none
best misfit: 0.015760362093193533
next radius misfit: 0.016267060129421054
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: about 711.3 s
```

Diagnostic objective rows all preserve the true target2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.066980e-04 | above cutoff |
| highband | 6.975556e-04 | above cutoff |
| late | 7.215200e-04 | above cutoff |
| late_high | 9.146141e-04 | above cutoff |
| veryhigh | 7.243574e-04 | above cutoff |
| early_high | 5.030712e-04 | above cutoff |

## Interpretation

Run 922 rescues seed6765 target2 but leaves little reserve. The base margin is
only 6.698e-06 above cutoff, and early_high is only 3.071e-06 above cutoff.
Continue to target1, but keep seed6765 classified as a low-reserve branch.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.250612 and full dynamic range
visual inspection: confidence figure is readable and shows one moderate row just above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: GPU utilization held around 91%; RAM stayed about 97 GiB available
```

## Next Decision

Continue the seed6765 branch with target1, sources=5, Tx/Rx=60, and
ringdown050.

---

## Source: `docs/experiments/457_variable_depth_radius_seed6765_target1_sources5_txrx60_ringdown050.md`

# Experiment 457: Seed6765 Target1 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 923 tests seed6765 target1 at the 5-source full-ringdown control after
target0 and target2 both required 9-source rescues.

## 923: Coordinate Optimizer Variable-Depth/Radius Seed6765 Target1 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/923_coordinate_optimizer_variable_depth_radius_seed6765_target1_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 1 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed6765:1.1,-50.0,1.1,0.10,6765,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed6765 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed6765_target1_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed6765
target: 1
sources: 5
best: x=250 mm, z=100 mm, r=6.0 mm
next radius: 6.25 mm
absolute radius margin: 6.067870e-04
offset from cutoff: +1.067870e-04
relative radius margin: 3.649742e-02
confidence label: moderate
fallback warning: none
best misfit: 0.01662547515436525
next radius misfit: 0.017232262165857517
listed competing geometry: x=250 mm, z=101 mm, r=6.75 mm
elapsed: about 401.6 s
```

Diagnostic objective rows all preserve the true target1 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 6.067870e-04 | above cutoff |
| highband | 7.875123e-04 | above cutoff |
| late | 9.233462e-04 | above cutoff |
| late_high | 1.037612e-03 | above cutoff |
| veryhigh | 7.210743e-04 | above cutoff |
| early_high | 5.929079e-04 | above cutoff |

## Interpretation

Run 923 is an accepted seed6765 target1 row. It passes cleanly at 5 sources and
does not need a rescue. Seed6765 is now ready for branch-level summary.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target1 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.293874 and full dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target1 truth_radius_mm is 6.0 mm
resources: GPU utilization held around 88%; RAM stayed about 97 GiB available
```

## Next Decision

Create the seed6765 target-specific rescue summary, then continue
full-ringdown replication with seed10946 target0.

---

## Source: `docs/experiments/458_seed6765_ringdown050_target_specific_rescue_summary.md`

# Experiment 458: Seed6765 Ringdown050 Target-Specific Rescue Summary

## Purpose

Run 924 consolidates the seed6765 branch after target0 and target2 required
9-source rescue while target1 passed at the 5-source control.

## 924: Seed6765 Ringdown050 Target-Specific Rescue Summary

Output:

```text
outputs/experiments/924_seed6765_ringdown050_target_specific_rescue_summary
```

Generation:

```text
CPU-side structured aggregation of completed coordinate-optimizer outputs
919-923. No new FDTD scene was launched for this summary.
```

## Results

Summary counts:

```text
row count: 5
diagnostic objective rows: 30
accepted runs: 920, 922, 923
rejected truth-preserving runs: 919, 921
cutoff: 5.0e-4
```

Best row by target:

| Target | Best run | Sources | Base margin | Offset from cutoff | Status |
| --- | ---: | ---: | ---: | ---: | --- |
| target0 | 920 | 9 | 5.485377e-04 | +4.854e-05 | accepted |
| target2 | 922 | 9 | 5.066980e-04 | +6.698e-06 | accepted |
| target1 | 923 | 5 | 6.067870e-04 | +1.068e-04 | accepted |

## Interpretation

Seed6765 is accepted but low-reserve. All rows preserve the true geometry, but
both target0 and target2 fail their first controls and need 9-source rescue.
The target2 rescue barely clears cutoff, so seed6765 should be used as evidence
that the targeted rescue policy works but does not always create large reserve.

## Validation

```text
JSON parse: run_manifest.json and seed6765_branch_summary.json pass
CSV rows: seed6765_branch_summary.csv has 5 data rows
diagnostics CSV rows: seed6765_objective_diagnostics.csv has 30 data rows
base margins figure: 1840x960 RGBA, nonwhite_fraction=0.448404
objective heatmap: 1840x960 RGBA, nonwhite_fraction=0.347938
target0 rescue trend: 1520x960 RGBA, nonwhite_fraction=0.034151
target2 rescue trend: 1520x960 RGBA, nonwhite_fraction=0.033207
visual inspection: all four figures are readable and support the summary
figure notes: figures/FIGURE_NOTES.md present and run-specific
resources: no new FDTD scene; GPU was moved to seed10946 target0
```

## Next Decision

Continue full-ringdown replication with seed10946 target0, sources=8, Tx/Rx=60.

---

## Source: `docs/experiments/459_variable_depth_radius_seed10946_target0_sources8_txrx60_ringdown050.md`

# Experiment 459: Seed10946 Target0 Sources=8 Tx/Rx=60 Ringdown050

## Purpose

Run 925 starts the seed10946 branch with the standard target0 8-source
full-ringdown control.

## 925: Coordinate Optimizer Variable-Depth/Radius Seed10946 Target0 Sources=8 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/925_coordinate_optimizer_variable_depth_radius_seed10946_target0_sources8_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 8 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 0 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed10946:1.1,-50.0,1.1,0.10,10946,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed10946 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed10946_target0_sources8_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed10946
target: 0
sources: 8
best: x=150 mm, z=80 mm, r=5.0 mm
next radius: 5.25 mm
absolute radius margin: 4.749206e-04
offset from cutoff: -2.507936e-05
relative radius margin: 3.034801e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.015649152336998436
next radius misfit: 0.01612407298003942
listed competing geometry: x=150 mm, z=81 mm, r=6.0 mm
elapsed: about 633.1 s
```

Diagnostic objective rows all preserve the true target0 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.749206e-04 | below cutoff |
| highband | 6.154735e-04 | above cutoff |
| late | 3.621087e-04 | below cutoff |
| late_high | 3.735629e-04 | below cutoff |
| veryhigh | 5.744273e-04 | above cutoff |
| early_high | 5.201573e-04 | above cutoff |

## Interpretation

Run 925 is exact but rejected under the production cutoff. It is a stronger
target0 weak-control miss than seed6765: base, late, and late_high are all
below cutoff. The targeted 9-source rescue is required before moving to other
seed10946 targets.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target0 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.233545 and full dynamic range
visual inspection: confidence figure is readable and shows one weak row below the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target0 truth_radius_mm is 5.0 mm
resources: GPU utilization held around 90%; RAM stayed about 97 GiB available
```

## Next Decision

Run seed10946 target0 with 9 sources, Tx/Rx=60, and ringdown050.

---

## Source: `docs/experiments/460_variable_depth_radius_seed10946_target0_sources9_txrx60_ringdown050.md`

# Experiment 460: Seed10946 Target0 Sources=9 Tx/Rx=60 Ringdown050

## Purpose

Run 926 tests the targeted 9-source rescue after seed10946 target0 was exact
but weak at the 8-source control.

## 926: Coordinate Optimizer Variable-Depth/Radius Seed10946 Target0 Sources=9 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/926_coordinate_optimizer_variable_depth_radius_seed10946_target0_sources9_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 9 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 0 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed10946:1.1,-50.0,1.1,0.10,10946,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed10946 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed10946_target0_sources9_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed10946
target: 0
sources: 9
best: x=150 mm, z=80 mm, r=5.0 mm
next radius: 5.25 mm
absolute radius margin: 5.206657e-04
offset from cutoff: +2.066572e-05
relative radius margin: 3.330089e-02
confidence label: moderate
fallback warning: none
best misfit: 0.015635188910180727
next radius misfit: 0.01615585462836189
listed competing geometry: x=150 mm, z=81 mm, r=6.0 mm
elapsed: about 717.4 s
```

Diagnostic objective rows all preserve the true target0 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.206657e-04 | above cutoff |
| highband | 7.250439e-04 | above cutoff |
| late | 3.846288e-04 | below cutoff |
| late_high | 4.749423e-04 | below cutoff |
| veryhigh | 7.868808e-04 | above cutoff |
| early_high | 6.160364e-04 | above cutoff |

## Interpretation

Run 926 rescues seed10946 target0, but it is not a uniformly high-reserve row.
The base margin is accepted, while late and late_high remain below cutoff.
Continue to target2 under the target-specific policy.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target0 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.253379 and full dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target0 truth_radius_mm is 5.0 mm
resources: GPU utilization held around 91%; RAM stayed about 97 GiB available
```

## Next Decision

Continue the seed10946 branch with target2, sources=5, Tx/Rx=60, and
ringdown050.

---

## Source: `docs/experiments/461_variable_depth_radius_seed10946_target2_sources5_txrx60_ringdown050.md`

# Experiment 461: Seed10946 Target2 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 927 tests seed10946 target2 at the standard 5-source full-ringdown control
after target0 was rescued at 9 sources.

## 927: Coordinate Optimizer Variable-Depth/Radius Seed10946 Target2 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/927_coordinate_optimizer_variable_depth_radius_seed10946_target2_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed10946:1.1,-50.0,1.1,0.10,10946,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed10946 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed10946_target2_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed10946
target: 2
sources: 5
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 4.932705e-04
offset from cutoff: -6.729525e-06
relative radius margin: 2.919276e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.0168970148036774
next radius misfit: 0.01739028527881966
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: about 399.1 s
```

Diagnostic objective rows all preserve the true target2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.932705e-04 | below cutoff |
| highband | 5.927207e-04 | above cutoff |
| late | 7.359370e-04 | above cutoff |
| late_high | 7.890547e-04 | above cutoff |
| veryhigh | 5.943745e-04 | above cutoff |
| early_high | 4.055549e-04 | below cutoff |

## Interpretation

Run 927 is exact but rejected under the production cutoff. Base is only
6.730e-06 below cutoff, but early_high is much weaker. The targeted target2
9-source rescue is the next action.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.241648 and full dynamic range
visual inspection: confidence figure is readable and shows one weak row below the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: GPU utilization held around 87%; RAM stayed about 97 GiB available
```

## Next Decision

Run seed10946 target2 with 9 sources, Tx/Rx=60, and ringdown050.

---

## Source: `docs/experiments/462_variable_depth_radius_seed10946_target2_sources9_txrx60_ringdown050.md`

# Experiment 462: Seed10946 Target2 Sources=9 Tx/Rx=60 Ringdown050

## Purpose

Run 928 tests the targeted 9-source rescue after seed10946 target2 was exact
but weak at the 5-source control.

## 928: Coordinate Optimizer Variable-Depth/Radius Seed10946 Target2 Sources=9 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/928_coordinate_optimizer_variable_depth_radius_seed10946_target2_sources9_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 9 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed10946:1.1,-50.0,1.1,0.10,10946,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed10946 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed10946_target2_sources9_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed10946
target: 2
sources: 9
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 5.592681e-04
offset from cutoff: +5.926810e-05
relative radius margin: 3.576983e-02
confidence label: moderate
fallback warning: none
best misfit: 0.015635188910180727
next radius misfit: 0.016194457008067296
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: about 717.9 s
```

Diagnostic objective rows all preserve the true target2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.592681e-04 | above cutoff |
| highband | 6.989243e-04 | above cutoff |
| late | 8.540429e-04 | above cutoff |
| late_high | 8.939400e-04 | above cutoff |
| veryhigh | 7.360320e-04 | above cutoff |
| early_high | 4.888483e-04 | below cutoff |

## Interpretation

Run 928 rescues seed10946 target2 with moderate base reserve. It is accepted,
though early_high remains slightly below cutoff. Continue to target1 at the
standard 5-source control.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.271399 and full dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: GPU utilization held around 91%; RAM stayed about 96 GiB available
```

## Next Decision

Continue the seed10946 branch with target1, sources=5, Tx/Rx=60, and
ringdown050.

---

## Source: `docs/experiments/463_variable_depth_radius_seed10946_target1_sources5_txrx60_ringdown050.md`

# Experiment 463: Seed10946 Target1 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 929 tests the standard 5-source target1 control after seed10946 target0 and
target2 required targeted 9-source rescues.

## 929: Coordinate Optimizer Variable-Depth/Radius Seed10946 Target1 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/929_coordinate_optimizer_variable_depth_radius_seed10946_target1_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 1 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed10946:1.1,-50.0,1.1,0.10,10946,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed10946 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed10946_target1_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed10946
target: 1
sources: 5
best: x=250 mm, z=100 mm, r=6.0 mm
next radius: 6.25 mm
absolute radius margin: 5.980473e-04
offset from cutoff: +9.804734e-05
relative radius margin: 3.539367e-02
confidence label: moderate
fallback warning: none
best misfit: 0.0168970148036774
next radius misfit: 0.017495062141494665
listed competing geometry: x=250 mm, z=101 mm, r=6.75 mm
elapsed: about 401.0 s
```

Diagnostic objective rows all preserve the true target1 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.980473e-04 | above cutoff |
| highband | 7.320357e-04 | above cutoff |
| late | 9.042222e-04 | above cutoff |
| late_high | 9.508260e-04 | above cutoff |
| veryhigh | 6.926339e-04 | above cutoff |
| early_high | 5.481827e-04 | above cutoff |

## Interpretation

Run 929 is accepted as the seed10946 target1 5-source control. Unlike target0
and target2, target1 does not require a 9-source rescue. The all-objective
rank-1 exact result makes this the healthiest seed10946 target row.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target1 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGB with nonwhite_fraction=0.287616 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target1 truth_radius_mm is 6.0 mm
resources: GPU utilization held around 87%; RAM stayed about 96 GiB available
```

## Next Decision

Create a seed10946 rescue summary across target0, target2, and target1 before
continuing the Fibonacci seed replication with seed17711 target0.

---

## Source: `docs/experiments/464_seed10946_ringdown050_target_specific_rescue_summary.md`

# Experiment 464: Seed10946 Ringdown050 Target-Specific Rescue Summary

## Purpose

Run 930 consolidates the seed10946 branch after target0 and target2 required
9-source rescue while target1 passed at the 5-source control.

## 930: Seed10946 Ringdown050 Target-Specific Rescue Summary

Output:

```text
outputs/experiments/930_seed10946_ringdown050_target_specific_rescue_summary
```

Generation:

```text
CPU-side structured aggregation of completed coordinate-optimizer outputs
925-929. No new FDTD scene was launched for this summary.
```

## Results

Summary counts:

```text
row count: 5
diagnostic objective rows: 30
accepted runs: 926, 928, 929
rejected truth-preserving runs: 925, 927
cutoff: 5.0e-4
```

Best row by target:

| Target | Best run | Sources | Base margin | Offset from cutoff | Status |
| --- | ---: | ---: | ---: | ---: | --- |
| target0 | 926 | 9 | 5.206657e-04 | +2.067e-05 | accepted |
| target2 | 928 | 9 | 5.592681e-04 | +5.927e-05 | accepted |
| target1 | 929 | 5 | 5.980473e-04 | +9.805e-05 | accepted |

## Interpretation

Seed10946 is accepted but branch-dependent. All rows preserve the true
geometry, but target0 and target2 fail their first controls and need 9-source
rescues. Target0 remains low-reserve after rescue because late and late_high
diagnostic variants stay below cutoff. Target2 is healthier after rescue, and
target1 is the cleanest row with all six diagnostic margins above cutoff.

## Validation

```text
JSON parse: run_manifest.json and seed10946_branch_summary.json pass
CSV rows: seed10946_branch_summary.csv has 5 data rows
diagnostics CSV rows: seed10946_objective_diagnostics.csv has 30 data rows
base margins figure: 1840x960 RGB, nonwhite_fraction=0.456653
objective heatmap: 1840x960 RGB, nonwhite_fraction=0.348066
target0 rescue trend: 1520x960 RGB, nonwhite_fraction=0.033862
target2 rescue trend: 1520x960 RGB, nonwhite_fraction=0.039888
visual inspection: all four figures are readable and support the summary
figure notes: figures/FIGURE_NOTES.md present and run-specific
resources: no new FDTD scene; GPU was moved to seed17711 target0
```

## Next Decision

Continue full-ringdown replication with seed17711 target0, sources=8, Tx/Rx=60.

---

## Source: `docs/experiments/465_variable_depth_radius_seed17711_target0_sources8_txrx60_ringdown050.md`

# Experiment 465: Seed17711 Target0 Sources=8 Tx/Rx=60 Ringdown050

## Purpose

Run 931 starts the seed17711 Fibonacci replication branch with the standard
8-source target0 full-ringdown production row.

## 931: Coordinate Optimizer Variable-Depth/Radius Seed17711 Target0 Sources=8 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/931_coordinate_optimizer_variable_depth_radius_seed17711_target0_sources8_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 8 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 0 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed17711:1.1,-50.0,1.1,0.10,17711,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed17711 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed17711_target0_sources8_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed17711
target: 0
sources: 8
best: x=150 mm, z=80 mm, r=5.0 mm
next radius: 5.25 mm
absolute radius margin: 4.845798e-04
offset from cutoff: -1.542021e-05
relative radius margin: 3.002982e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.016136620423000157
next radius misfit: 0.016621200209539073
listed competing geometry: x=150 mm, z=81 mm, r=6.0 mm
elapsed: about 619.4 s
```

Diagnostic objective rows all preserve the true target0 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.845798e-04 | below cutoff |
| highband | 6.535950e-04 | above cutoff |
| late | 4.538129e-04 | below cutoff |
| late_high | 5.040231e-04 | above cutoff |
| veryhigh | 6.083831e-04 | above cutoff |
| early_high | 5.461292e-04 | above cutoff |

## Interpretation

Run 931 is exact but weak. The base margin is 1.542e-05 below cutoff, and the
late diagnostic variant is also below cutoff. The true geometry remains rank 1
for all objective variants, so the appropriate response is a source-density
rescue rather than changing the geometry search.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target0 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGB with nonwhite_fraction=0.237961 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one weak row below the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target0 truth_radius_mm is 5.0 mm
resources: GPU utilization held around 90-91%; RAM stayed about 96 GiB available
```

## Next Decision

Run seed17711 target0 with 9 sources before moving to target2.

---

## Source: `docs/experiments/466_variable_depth_radius_seed17711_target0_sources9_txrx60_ringdown050.md`

# Experiment 466: Seed17711 Target0 Sources=9 Tx/Rx=60 Ringdown050

## Purpose

Run 932 tests the targeted 9-source rescue after seed17711 target0 was exact
but weak at the 8-source control.

## 932: Coordinate Optimizer Variable-Depth/Radius Seed17711 Target0 Sources=9 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/932_coordinate_optimizer_variable_depth_radius_seed17711_target0_sources9_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 9 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 0 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed17711:1.1,-50.0,1.1,0.10,17711,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed17711 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed17711_target0_sources9_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed17711
target: 0
sources: 9
best: x=150 mm, z=80 mm, r=5.0 mm
next radius: 5.25 mm
absolute radius margin: 5.524814e-04
offset from cutoff: +5.248144e-05
relative radius margin: 3.469266e-02
confidence label: moderate
fallback warning: none
best misfit: 0.015925024167220303
next radius misfit: 0.01647750561132112
listed competing geometry: x=150 mm, z=81 mm, r=6.0 mm
elapsed: about 715.1 s
```

Diagnostic objective rows all preserve the true target0 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.524814e-04 | above cutoff |
| highband | 7.698372e-04 | above cutoff |
| late | 4.168941e-04 | below cutoff |
| late_high | 5.034981e-04 | above cutoff |
| veryhigh | 8.179990e-04 | above cutoff |
| early_high | 6.819980e-04 | above cutoff |

## Interpretation

Run 932 rescues seed17711 target0. The base margin improves by about
6.79e-05 relative to the 8-source control and clears cutoff. Late-window
reserve remains weak, so this is accepted but not uniformly robust across all
diagnostic windows.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target0 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGB with nonwhite_fraction=0.268613 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target0 truth_radius_mm is 5.0 mm
resources: GPU utilization held around 91-92%; RAM stayed about 96 GiB available
```

## Next Decision

Continue the seed17711 branch with target2, sources=5, Tx/Rx=60, and
ringdown050.

---

## Source: `docs/experiments/467_variable_depth_radius_seed17711_target2_sources5_txrx60_ringdown050.md`

# Experiment 467: Seed17711 Target2 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 933 tests seed17711 target2 at the standard 5-source full-ringdown control
after target0 passed its targeted 9-source rescue.

## 933: Coordinate Optimizer Variable-Depth/Radius Seed17711 Target2 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/933_coordinate_optimizer_variable_depth_radius_seed17711_target2_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed17711:1.1,-50.0,1.1,0.10,17711,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed17711 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed17711_target2_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed17711
target: 2
sources: 5
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 5.362844e-04
offset from cutoff: +3.628436e-05
relative radius margin: 3.132739e-02
confidence label: moderate
fallback warning: none
best misfit: 0.017118703881534532
next radius misfit: 0.017654988242520576
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: about 399.1 s
```

Diagnostic objective rows all preserve the true target2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.362844e-04 | above cutoff |
| highband | 6.565194e-04 | above cutoff |
| late | 7.639589e-04 | above cutoff |
| late_high | 8.045815e-04 | above cutoff |
| veryhigh | 7.058435e-04 | above cutoff |
| early_high | 4.875689e-04 | below cutoff |

## Interpretation

Run 933 is accepted as the seed17711 target2 5-source control. It is less
fragile than target0 because it does not need source-density rescue, although
the early_high diagnostic remains slightly below cutoff.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGB with nonwhite_fraction=0.261390 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: GPU utilization held around 87-88%; RAM stayed about 96 GiB available
```

## Next Decision

Continue the seed17711 branch with target1, sources=5, Tx/Rx=60, and
ringdown050.

---

## Source: `docs/experiments/468_variable_depth_radius_seed17711_target1_sources5_txrx60_ringdown050.md`

# Experiment 468: Seed17711 Target1 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 934 tests seed17711 target1 at the standard 5-source full-ringdown control
after target0 passed its 9-source rescue and target2 passed at 5 sources.

## 934: Coordinate Optimizer Variable-Depth/Radius Seed17711 Target1 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/934_coordinate_optimizer_variable_depth_radius_seed17711_target1_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 1 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed17711:1.1,-50.0,1.1,0.10,17711,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed17711 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed17711_target1_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed17711
target: 1
sources: 5
best: x=250 mm, z=100 mm, r=6.0 mm
next radius: 6.25 mm
absolute radius margin: 5.806372e-04
offset from cutoff: +8.063719e-05
relative radius margin: 3.391829e-02
confidence label: moderate
fallback warning: none
best misfit: 0.017118703881534532
next radius misfit: 0.017699341072235832
listed competing geometry: x=250 mm, z=101 mm, r=6.75 mm
elapsed: about 398.5 s
```

Diagnostic objective rows all preserve the true target1 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.806372e-04 | above cutoff |
| highband | 7.463813e-04 | above cutoff |
| late | 8.824399e-04 | above cutoff |
| late_high | 9.803350e-04 | above cutoff |
| veryhigh | 7.031832e-04 | above cutoff |
| early_high | 5.627863e-04 | above cutoff |

## Interpretation

Run 934 is accepted as the seed17711 target1 5-source control. Target1 is the
cleanest seed17711 row, with all objective variants above cutoff and the exact
geometry ranked first throughout.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target1 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGB with nonwhite_fraction=0.281212 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target1 truth_radius_mm is 6.0 mm
resources: GPU utilization held around 87%; RAM stayed about 96 GiB available
```

## Next Decision

Create a seed17711 target-specific rescue summary across runs 931-934.

---

## Source: `docs/experiments/469_seed17711_ringdown050_target_specific_rescue_summary.md`

# Experiment 469: Seed17711 Ringdown050 Target-Specific Rescue Summary

## Purpose

Run 935 consolidates the seed17711 branch after target0 required 9-source
rescue while target2 and target1 passed at the 5-source control.

## 935: Seed17711 Ringdown050 Target-Specific Rescue Summary

Output:

```text
outputs/experiments/935_seed17711_ringdown050_target_specific_rescue_summary
```

Generation:

```text
CPU-side structured aggregation of completed coordinate-optimizer outputs
931-934. No new FDTD scene was launched for this summary.
```

Source runs:

```text
931 target0 sources=8 ringdown050
932 target0 sources=9 ringdown050
933 target2 sources=5 ringdown050
934 target1 sources=5 ringdown050
```

## Results

Summary counts:

```text
row count: 4
diagnostic objective rows: 24
accepted runs: 932, 933, 934
rejected truth-preserving runs: 931
cutoff: 5.0e-4
```

Best row by target:

| Target | Best run | Sources | Base margin | Offset from cutoff | Status |
| --- | ---: | ---: | ---: | ---: | --- |
| target0 | 932 | 9 | 5.524814e-04 | +5.248e-05 | accepted |
| target2 | 933 | 5 | 5.362844e-04 | +3.628e-05 | accepted |
| target1 | 934 | 5 | 5.806372e-04 | +8.064e-05 | accepted |

Target0 rescue:

| Run | Sources | Base margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | --- |
| 931 | 8 | 4.845798e-04 | -1.542e-05 | rejected |
| 932 | 9 | 5.524814e-04 | +5.248e-05 | accepted |

## Interpretation

Seed17711 is accepted after one source-density rescue. Every row preserves the
true geometry. Target0 is the only row requiring rescue and remains
late-window low-reserve; target2 passes with an early_high caveat; target1 is
clean across all diagnostic objective variants.

## Validation

```text
JSON parse: run_manifest.json and seed17711_branch_summary.json pass
CSV rows: seed17711_branch_summary.csv has 4 data rows
diagnostics CSV rows: seed17711_objective_diagnostics.csv has 24 data rows
base margins figure: 1840x960 RGB, nonwhite_fraction=0.446837
objective heatmap: 1840x960 RGB, nonwhite_fraction=0.647362
target0 rescue trend: 1520x960 RGB, nonwhite_fraction=0.037674
visual inspection: all three figures are readable and support the summary
figure notes: figures/FIGURE_NOTES.md present and run-specific
resources: no new FDTD scene; GPU was moved to seed28657 target0
```

## Next Decision

Continue full-ringdown replication with seed28657 target0, sources=8, Tx/Rx=60.

---

## Source: `docs/experiments/470_variable_depth_radius_seed28657_target0_sources8_txrx60_ringdown050.md`

# Experiment 470: Seed28657 Target0 Sources=8 Tx/Rx=60 Ringdown050

## Purpose

Run 936 starts the seed28657 Fibonacci replication branch with the standard
8-source target0 full-ringdown production row.

## 936: Coordinate Optimizer Variable-Depth/Radius Seed28657 Target0 Sources=8 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/936_coordinate_optimizer_variable_depth_radius_seed28657_target0_sources8_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 8 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 0 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed28657:1.1,-50.0,1.1,0.10,28657,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed28657 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed28657_target0_sources8_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed28657
target: 0
sources: 8
best: x=150 mm, z=80 mm, r=5.0 mm
next radius: 5.25 mm
absolute radius margin: 5.720267e-04
offset from cutoff: +7.202671e-05
relative radius margin: 3.611612e-02
confidence label: moderate
fallback warning: none
best misfit: 0.015838540905748773
next radius misfit: 0.016410567613523313
listed competing geometry: x=150 mm, z=81 mm, r=6.0 mm
elapsed: about 629.7 s
```

Diagnostic objective rows all preserve the true target0 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.720267e-04 | above cutoff |
| highband | 7.533931e-04 | above cutoff |
| late | 4.819107e-04 | below cutoff |
| late_high | 5.892376e-04 | above cutoff |
| veryhigh | 7.154124e-04 | above cutoff |
| early_high | 6.182228e-04 | above cutoff |

## Interpretation

Run 936 is accepted as the seed28657 target0 8-source control. It does not
need a targeted 9-source rescue, although the late objective remains slightly
below cutoff and should be tracked in the branch summary.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target0 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGB with nonwhite_fraction=0.276810 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target0 truth_radius_mm is 5.0 mm
resources: GPU utilization held around 90-91%; RAM stayed about 96 GiB available
```

## Next Decision

Continue the seed28657 branch with target2, sources=5, Tx/Rx=60, and
ringdown050.

---

## Source: `docs/experiments/471_variable_depth_radius_seed28657_target2_sources5_txrx60_ringdown050.md`

# Experiment 471: Seed28657 Target2 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 937 tests seed28657 target2 at the standard 5-source full-ringdown control
after target0 passed at 8 sources.

## 937: Coordinate Optimizer Variable-Depth/Radius Seed28657 Target2 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/937_coordinate_optimizer_variable_depth_radius_seed28657_target2_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed28657:1.1,-50.0,1.1,0.10,28657,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed28657 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed28657_target2_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed28657
target: 2
sources: 5
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 5.256723e-04
offset from cutoff: +2.567229e-05
relative radius margin: 3.077430e-02
confidence label: moderate
fallback warning: none
best misfit: 0.017081534815411168
next radius misfit: 0.01760720710702045
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: about 395.5 s
```

Diagnostic objective rows all preserve the true target2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.256723e-04 | above cutoff |
| highband | 6.509095e-04 | above cutoff |
| late | 7.940133e-04 | above cutoff |
| late_high | 8.557283e-04 | above cutoff |
| veryhigh | 7.020560e-04 | above cutoff |
| early_high | 4.808689e-04 | below cutoff |

## Interpretation

Run 937 is accepted as the seed28657 target2 5-source control. It has a
moderate base row but only modest reserve, and early_high remains slightly
below cutoff. Continue to target1 at 5 sources.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGB with nonwhite_fraction=0.256064 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: GPU utilization held around 87-88%; RAM stayed about 96 GiB available
```

## Next Decision

Continue the seed28657 branch with target1, sources=5, Tx/Rx=60, and
ringdown050.

---

## Source: `docs/experiments/472_variable_depth_radius_seed28657_target1_sources5_txrx60_ringdown050.md`

# Experiment 472: Seed28657 Target1 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 938 tests seed28657 target1 at the standard 5-source full-ringdown control
after target0 and target2 passed without rescue.

## 938: Coordinate Optimizer Variable-Depth/Radius Seed28657 Target1 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/938_coordinate_optimizer_variable_depth_radius_seed28657_target1_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 1 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed28657:1.1,-50.0,1.1,0.10,28657,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed28657 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed28657_target1_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed28657
target: 1
sources: 5
best: x=250 mm, z=100 mm, r=6.0 mm
next radius: 6.25 mm
absolute radius margin: 5.076047e-04
offset from cutoff: +7.604737e-06
relative radius margin: 2.971658e-02
confidence label: moderate
fallback warning: none
best misfit: 0.017081534815411168
next radius misfit: 0.017589139552483382
listed competing geometry: x=250 mm, z=101 mm, r=6.75 mm
elapsed: about 396.7 s
```

Diagnostic objective rows all preserve the true target1 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.076047e-04 | above cutoff |
| highband | 6.675980e-04 | above cutoff |
| late | 7.645037e-04 | above cutoff |
| late_high | 8.557358e-04 | above cutoff |
| veryhigh | 6.011288e-04 | above cutoff |
| early_high | 4.975212e-04 | below cutoff |

## Interpretation

Run 938 is accepted as the seed28657 target1 5-source control, but it is a
low-reserve acceptance. The base margin clears cutoff by only 7.605e-06 and
early_high remains slightly below cutoff.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target1 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGB with nonwhite_fraction=0.247939 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one moderate row barely above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target1 truth_radius_mm is 6.0 mm
resources: GPU utilization held around 87-88%; RAM stayed about 96 GiB available
```

## Next Decision

Create a seed28657 target-specific 8/5/5 summary across runs 936-938.

---

## Source: `docs/experiments/473_seed28657_ringdown050_target_specific_8_5_5_summary.md`

# Experiment 473: Seed28657 Ringdown050 Target-Specific 8/5/5 Summary

## Purpose

Run 939 consolidates the seed28657 branch after target0, target2, and target1
all passed without rescue.

## 939: Seed28657 Ringdown050 Target-Specific 8/5/5 Summary

Output:

```text
outputs/experiments/939_seed28657_ringdown050_target_specific_8_5_5_summary
```

Generation:

```text
CPU-side structured aggregation of completed coordinate-optimizer outputs
936-938. No new FDTD scene was launched for this summary.
```

Source runs:

```text
936 target0 sources=8 ringdown050
937 target2 sources=5 ringdown050
938 target1 sources=5 ringdown050
```

## Results

Summary counts:

```text
row count: 3
diagnostic objective rows: 18
accepted runs: 936, 937, 938
rejected runs: none
cutoff: 5.0e-4
```

Best row by target:

| Target | Run | Sources | Base margin | Offset from cutoff | Status |
| --- | ---: | ---: | ---: | ---: | --- |
| target0 | 936 | 8 | 5.720267e-04 | +7.203e-05 | accepted |
| target2 | 937 | 5 | 5.256723e-04 | +2.567e-05 | accepted |
| target1 | 938 | 5 | 5.076047e-04 | +7.605e-06 | accepted |

## Interpretation

Seed28657 passes the target-specific 8/5/5 full-ringdown policy without
source-density rescue. It is not a high-reserve branch: target1 barely clears
the base cutoff, target0 has a late-window diagnostic caveat, and target2 plus
target1 both have early_high caveats.

## Validation

```text
JSON parse: run_manifest.json and seed28657_branch_summary.json pass
CSV rows: seed28657_branch_summary.csv has 3 data rows
diagnostics CSV rows: seed28657_objective_diagnostics.csv has 18 data rows
base margins figure: 1600x960 RGB, nonwhite_fraction=0.450572
objective heatmap: 1600x920 RGB, nonwhite_fraction=0.637889
offset figure: 1600x960 RGB, nonwhite_fraction=0.129497
visual inspection: all three figures are readable and support the summary
figure notes: figures/FIGURE_NOTES.md present and run-specific
resources: no new FDTD scene; GPU was moved to seed46368 target0
```

## Next Decision

Continue full-ringdown replication with seed46368 target0, sources=8, Tx/Rx=60.

---

## Source: `docs/experiments/474_variable_depth_radius_seed46368_target0_sources8_txrx60_ringdown050.md`

# Experiment 474: Seed46368 Target0 Sources=8 Tx/Rx=60 Ringdown050

## Purpose

Run 940 starts the seed46368 Fibonacci replication branch with the standard
8-source target0 full-ringdown production row.

## 940: Coordinate Optimizer Variable-Depth/Radius Seed46368 Target0 Sources=8 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/940_coordinate_optimizer_variable_depth_radius_seed46368_target0_sources8_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 8 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 0 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed46368:1.1,-50.0,1.1,0.10,46368,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed46368 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed46368_target0_sources8_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed46368
target: 0
sources: 8
best: x=150 mm, z=80 mm, r=5.0 mm
next radius: 5.25 mm
absolute radius margin: 5.065156e-04
offset from cutoff: +6.515585e-06
relative radius margin: 3.139286e-02
confidence label: moderate
fallback warning: none
best misfit: 0.01613473879227533
next radius misfit: 0.01664125437730868
listed competing geometry: x=150 mm, z=81 mm, r=6.0 mm
elapsed: about 628.8 s
```

Diagnostic objective rows all preserve the true target0 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.065156e-04 | above cutoff |
| highband | 6.535266e-04 | above cutoff |
| late | 4.284018e-04 | below cutoff |
| late_high | 4.831265e-04 | below cutoff |
| veryhigh | 6.128766e-04 | above cutoff |
| early_high | 5.527079e-04 | above cutoff |

## Interpretation

Run 940 is accepted as the seed46368 target0 8-source control, but with very
little reserve. The base margin is only 6.516e-06 above cutoff, and both late
diagnostic variants remain below cutoff. Continue to target2, but treat the
seed46368 branch as low-reserve until the branch summary confirms otherwise.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target0 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGB with nonwhite_fraction=0.247093 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one moderate row barely above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target0 truth_radius_mm is 5.0 mm
resources: GPU utilization held around 90-91%; RAM stayed about 96 GiB available
```

## Next Decision

Continue the seed46368 branch with target2, sources=5, Tx/Rx=60, and
ringdown050.

---

## Source: `docs/experiments/475_variable_depth_radius_seed46368_target2_sources5_txrx60_ringdown050.md`

# Experiment 475: Seed46368 Target2 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 941 tests seed46368 target2 at the standard 5-source full-ringdown control
after target0 passed at 8 sources with low reserve.

## 941: Coordinate Optimizer Variable-Depth/Radius Seed46368 Target2 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/941_coordinate_optimizer_variable_depth_radius_seed46368_target2_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed46368:1.1,-50.0,1.1,0.10,46368,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed46368 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed46368_target2_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed46368
target: 2
sources: 5
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 5.164762e-04
offset from cutoff: +1.647617e-05
relative radius margin: 3.001390e-02
confidence label: moderate
fallback warning: none
best misfit: 0.017207898644624273
next radius misfit: 0.017724374811082903
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: about 395.7 s
```

Diagnostic objective rows all preserve the true target2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.164762e-04 | above cutoff |
| highband | 6.104064e-04 | above cutoff |
| late | 7.616623e-04 | above cutoff |
| late_high | 7.374686e-04 | above cutoff |
| veryhigh | 6.350233e-04 | above cutoff |
| early_high | 4.680172e-04 | below cutoff |

## Interpretation

Run 941 is accepted as the seed46368 target2 5-source control, but the branch
is still low-reserve. Base clears cutoff by only 1.648e-05 and early_high is
below cutoff. Continue to target1 at 5 sources.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGB with nonwhite_fraction=0.251590 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: GPU utilization held around 87-88%; RAM stayed about 96 GiB available
```

## Next Decision

Continue the seed46368 branch with target1, sources=5, Tx/Rx=60, and
ringdown050.

---

## Source: `docs/experiments/476_variable_depth_radius_seed46368_target1_sources5_txrx60_ringdown050.md`

# Experiment 476: Seed46368 Target1 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 942 completes the seed46368 target-specific branch with the standard
5-source target1 full-ringdown control after target0 and target2 passed with
low reserve.

## 942: Coordinate Optimizer Variable-Depth/Radius Seed46368 Target1 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/942_coordinate_optimizer_variable_depth_radius_seed46368_target1_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 1 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed46368:1.1,-50.0,1.1,0.10,46368,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed46368 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed46368_target1_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed46368
target: 1
sources: 5
best: x=250 mm, z=100 mm, r=6.0 mm
next radius: 6.25 mm
absolute radius margin: 5.072307e-04
offset from cutoff: +7.230675e-06
relative radius margin: 2.947662e-02
confidence label: moderate
fallback warning: none
best misfit: 0.017207898644624273
next radius misfit: 0.01771512931937187
listed competing geometry: x=250 mm, z=101 mm, r=6.75 mm
elapsed: about 396.7 s
```

Diagnostic objective rows all preserve the true target1 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.072307e-04 | above cutoff |
| highband | 6.566670e-04 | above cutoff |
| late | 8.108111e-04 | above cutoff |
| late_high | 8.866723e-04 | above cutoff |
| veryhigh | 5.510289e-04 | above cutoff |
| early_high | 5.107880e-04 | above cutoff |

## Interpretation

Run 942 is accepted as the seed46368 target1 5-source control. It is the
cleanest target-specific row in this seed because all diagnostic objectives
clear the cutoff, but the base margin is still only 7.231e-06 above cutoff.
Summarize the branch before advancing, because target0 and target1 are both
razor-margin accepted rows.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target1 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGB with nonwhite_fraction=0.247973 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one moderate row slightly above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target1 truth_radius_mm is 6.0 mm
resources: GPU utilization held around 87-88%; RAM stayed about 96 GiB available
```

## Next Decision

Create the seed46368 target-specific 8/5/5 summary before continuing to
seed75025 target0, sources=8, Tx/Rx=60, and ringdown050.

---

## Source: `docs/experiments/477_seed46368_ringdown050_target_specific_8_5_5_summary.md`

# Experiment 477: Seed46368 Ringdown050 Target-Specific 8/5/5 Summary

## Purpose

Run 943 consolidates the seed46368 branch after target0, target2, and target1
all passed without rescue.

## 943: Seed46368 Ringdown050 Target-Specific 8/5/5 Summary

Output:

```text
outputs/experiments/943_seed46368_ringdown050_target_specific_8_5_5_summary
```

Generation:

```text
CPU-side structured aggregation of completed coordinate-optimizer outputs
940-942. No new FDTD scene was launched for this summary.
```

Source runs:

```text
940 target0 sources=8 ringdown050
941 target2 sources=5 ringdown050
942 target1 sources=5 ringdown050
```

## Results

Summary counts:

```text
row count: 3
diagnostic objective rows: 18
accepted runs: 940, 941, 942
rejected runs: none
cutoff: 5.0e-4
```

Best row by target:

| Target | Run | Sources | Base margin | Offset from cutoff | Status |
| --- | ---: | ---: | ---: | ---: | --- |
| target0 | 940 | 8 | 5.065156e-04 | +6.516e-06 | accepted |
| target2 | 941 | 5 | 5.164762e-04 | +1.648e-05 | accepted |
| target1 | 942 | 5 | 5.072307e-04 | +7.231e-06 | accepted |

Diagnostic caveats:

| Target | Caveat |
| --- | --- |
| target0 | late and late_high margins remain below cutoff |
| target2 | early_high margin remains below cutoff |
| target1 | all diagnostic objective margins clear cutoff |

## Interpretation

Seed46368 passes the target-specific 8/5/5 full-ringdown policy without
source-density rescue, but it is one of the lowest-reserve accepted branches in
this replication series. Target0 and target1 clear the base cutoff by less
than 1.0e-05, target2 has only 1.648e-05 reserve, and the branch still carries
late-window and early_high diagnostic caveats. Treat the seed as accepted but
fragile; continue the Fibonacci replication sequence to see whether this
low-reserve pattern persists.

## Validation

```text
JSON parse: run_manifest.json and seed46368_branch_summary.json pass
CSV rows: seed46368_branch_summary.csv has 3 data rows
diagnostics CSV rows: seed46368_objective_diagnostics.csv has 18 data rows
base margins figure: 1600x960 RGB, nonwhite_fraction=0.465796
objective heatmap: 1600x920 RGB, nonwhite_fraction=0.635810
offset figure: 1600x960 RGB, nonwhite_fraction=0.140478
visual inspection: all three figures are readable and support the summary
figure notes: figures/FIGURE_NOTES.md present and run-specific
resources: no new FDTD scene; GPU was moved to seed75025 target0
```

## Next Decision

Continue full-ringdown replication with seed75025 target0, sources=8,
Tx/Rx=60.

---

## Source: `docs/experiments/478_variable_depth_radius_seed75025_target0_sources8_txrx60_ringdown050.md`

# Experiment 478: Seed75025 Target0 Sources=8 Tx/Rx=60 Ringdown050

## Purpose

Run 944 starts the seed75025 Fibonacci replication branch with the standard
8-source target0 full-ringdown production row after seed46368 passed the
target-specific 8/5/5 policy with very low reserve.

## 944: Coordinate Optimizer Variable-Depth/Radius Seed75025 Target0 Sources=8 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/944_coordinate_optimizer_variable_depth_radius_seed75025_target0_sources8_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 8 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 0 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed75025:1.1,-50.0,1.1,0.10,75025,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed75025 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed75025_target0_sources8_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed75025
target: 0
sources: 8
best: x=150 mm, z=80 mm, r=5.0 mm
next radius: 5.25 mm
absolute radius margin: 5.720010e-04
offset from cutoff: +7.200098e-05
relative radius margin: 3.607575e-02
confidence label: moderate
fallback warning: none
best misfit: 0.015855554845454374
next radius misfit: 0.01642755582908833
listed competing geometry: x=150 mm, z=81 mm, r=6.0 mm
elapsed: about 617.1 s
```

Diagnostic objective rows all preserve the true target0 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.720010e-04 | above cutoff |
| highband | 7.522785e-04 | above cutoff |
| late | 4.392433e-04 | below cutoff |
| late_high | 5.251481e-04 | above cutoff |
| veryhigh | 7.188398e-04 | above cutoff |
| early_high | 6.304109e-04 | above cutoff |

## Interpretation

Run 944 is accepted as the seed75025 target0 8-source control. This is a
healthier target0 row than seed46368 target0: the base margin clears cutoff by
7.200e-05 instead of 6.516e-06. The late-window diagnostic remains below
cutoff, so the row is accepted with a late-objective caveat, but no 9-source
rescue is justified for target0.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target0 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.277699 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target0 truth_radius_mm is 5.0 mm
resources: GPU utilization held around 90%; RAM stayed about 96 GiB available
```

## Next Decision

Continue the seed75025 branch with target2, sources=5, Tx/Rx=60, and
ringdown050. That run is underway as experiment 945.

---

## Source: `docs/experiments/479_variable_depth_radius_seed75025_target2_sources5_txrx60_ringdown050.md`

# Experiment 479: Seed75025 Target2 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 945 tests seed75025 target2 at the standard 5-source full-ringdown control
after target0 passed at 8 sources.

## 945: Coordinate Optimizer Variable-Depth/Radius Seed75025 Target2 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/945_coordinate_optimizer_variable_depth_radius_seed75025_target2_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed75025:1.1,-50.0,1.1,0.10,75025,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed75025 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed75025_target2_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact, but the base confidence row is
below the acceptance cutoff:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed75025
target: 2
sources: 5
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 4.472641e-04
offset from cutoff: -5.273592e-05
relative radius margin: 2.693656e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.016604350956199384
next radius misfit: 0.01705161503992771
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: about 397.1 s
```

Diagnostic objective rows all preserve the true target2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.472641e-04 | below cutoff |
| highband | 5.322722e-04 | above cutoff |
| late | 6.077135e-04 | above cutoff |
| late_high | 6.341462e-04 | above cutoff |
| veryhigh | 5.432995e-04 | above cutoff |
| early_high | 3.735408e-04 | below cutoff |

## Interpretation

Run 945 is exact but weak. It is not accepted as the seed75025 target2 control
because the base margin is 5.274e-05 below cutoff, and early_high is also
below cutoff. Since the true geometry is still rank 1 across all diagnostic
objectives, the next action is a source-density rescue rather than a change in
coordinate window.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.221794 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one weak row below the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and flags target2 as weak
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: GPU utilization held around 87-88%; RAM stayed about 96 GiB available
```

## Next Decision

Run the seed75025 target2 9-source rescue. That run is underway as experiment
946.

---

## Source: `docs/experiments/480_variable_depth_radius_seed75025_target2_sources9_txrx60_ringdown050.md`

# Experiment 480: Seed75025 Target2 Sources=9 Tx/Rx=60 Ringdown050 Rescue

## Purpose

Run 946 tests the seed75025 target2 9-source rescue after run 945 recovered the
true target2 geometry at 5 sources but failed the base confidence cutoff.

## 946: Coordinate Optimizer Variable-Depth/Radius Seed75025 Target2 Sources=9 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/946_coordinate_optimizer_variable_depth_radius_seed75025_target2_sources9_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 9 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed75025:1.1,-50.0,1.1,0.10,75025,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed75025 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed75025_target2_sources9_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact and accepted:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed75025
target: 2
sources: 9
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 5.008405e-04
offset from cutoff: +8.404820e-07
relative radius margin: 3.175266e-02
confidence label: moderate
fallback warning: none
best misfit: 0.01577318040652501
next radius misfit: 0.01627402088848135
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: about 717.8 s
```

Diagnostic objective rows all preserve the true target2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.008405e-04 | above cutoff |
| highband | 6.541685e-04 | above cutoff |
| late | 7.489354e-04 | above cutoff |
| late_high | 8.614983e-04 | above cutoff |
| veryhigh | 6.886560e-04 | above cutoff |
| early_high | 4.605865e-04 | below cutoff |

## Interpretation

Run 946 accepts the seed75025 target2 rescue, but only barely. The 9-source
row moves base from weak to moderate, improving the 5-source margin by
5.358e-05 and clearing cutoff by 8.40e-07. Because early_high still sits below
cutoff, target2 should be recorded as accepted with very low reserve.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.245233 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one moderate row barely above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: GPU utilization held around 90-92%; RAM stayed about 96 GiB available
```

## Next Decision

Continue the seed75025 branch with target1, sources=5, Tx/Rx=60, and
ringdown050. That run is underway as experiment 947.

---

## Source: `docs/experiments/481_variable_depth_radius_seed75025_target1_sources5_txrx60_ringdown050.md`

# Experiment 481: Seed75025 Target1 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 947 completes the seed75025 target-specific branch with the standard
5-source target1 full-ringdown control after target2 required a 9-source
rescue.

## 947: Coordinate Optimizer Variable-Depth/Radius Seed75025 Target1 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/947_coordinate_optimizer_variable_depth_radius_seed75025_target1_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 1 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed75025:1.1,-50.0,1.1,0.10,75025,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed75025 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed75025_target1_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed75025
target: 1
sources: 5
best: x=250 mm, z=100 mm, r=6.0 mm
next radius: 6.25 mm
absolute radius margin: 5.109638e-04
offset from cutoff: +1.096378e-05
relative radius margin: 3.077288e-02
confidence label: moderate
fallback warning: none
best misfit: 0.016604350956199384
next radius misfit: 0.01711531473148556
listed competing geometry: x=250 mm, z=101 mm, r=6.75 mm
elapsed: about 391.9 s
```

Diagnostic objective rows all preserve the true target1 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.109638e-04 | above cutoff |
| highband | 6.627702e-04 | above cutoff |
| late | 7.786846e-04 | above cutoff |
| late_high | 8.634345e-04 | above cutoff |
| veryhigh | 5.933329e-04 | above cutoff |
| early_high | 4.979399e-04 | below cutoff |

## Interpretation

Run 947 is accepted as the seed75025 target1 5-source control, but it is
low-reserve. Base clears cutoff by only 1.096e-05 and early_high is just below
cutoff. This completes the branch and warrants a summary that records target2
as a rescued row and target1 as accepted with an early_high caveat.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target1 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.249729 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one moderate row just above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target1 truth_radius_mm is 6.0 mm
resources: GPU utilization held around 87-88%; RAM stayed about 96 GiB available
```

## Next Decision

Create the seed75025 target-specific rescue summary.

---

## Source: `docs/experiments/482_seed75025_ringdown050_target_specific_rescue_summary.md`

# Experiment 482: Seed75025 Ringdown050 Target-Specific Rescue Summary

## Purpose

Run 948 consolidates the seed75025 branch after target2 required a 9-source
rescue while target0 and target1 passed their standard controls.

## 948: Seed75025 Ringdown050 Target-Specific Rescue Summary

Output:

```text
outputs/experiments/948_seed75025_ringdown050_target_specific_rescue_summary
```

Generation:

```text
CPU-side structured aggregation of completed coordinate-optimizer outputs
944-947. No new FDTD scene was launched for this summary.
```

Source runs:

```text
944 target0 sources=8 ringdown050
945 target2 sources=5 ringdown050 weak control
946 target2 sources=9 ringdown050 rescue
947 target1 sources=5 ringdown050
```

## Results

Summary counts:

```text
row count: 4
diagnostic objective rows: 24
accepted runs: 944, 946, 947
rejected truth-preserving runs: 945
cutoff: 5.0e-4
```

Best row by target:

| Target | Best run | Sources | Base margin | Offset from cutoff | Status |
| --- | ---: | ---: | ---: | ---: | --- |
| target0 | 944 | 8 | 5.720010e-04 | +7.200e-05 | accepted |
| target2 | 946 | 9 | 5.008405e-04 | +8.405e-07 | accepted rescue |
| target1 | 947 | 5 | 5.109638e-04 | +1.096e-05 | accepted |

Target2 rescue:

| Run | Sources | Base margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | --- |
| 945 | 5 | 4.472641e-04 | -5.274e-05 | rejected |
| 946 | 9 | 5.008405e-04 | +8.405e-07 | accepted |

## Interpretation

Seed75025 is accepted after one source-density rescue. Every row preserves the
true geometry, but this is a fragile branch: target2 barely clears cutoff after
rescue, target1 clears base with low reserve and an early_high caveat, and
target0 has a late-window caveat. Continue replication to test whether the
target2 rescue fragility persists.

## Validation

```text
JSON parse: run_manifest.json and seed75025_branch_summary.json pass
CSV rows: seed75025_branch_summary.csv has 4 data rows
diagnostics CSV rows: seed75025_objective_diagnostics.csv has 24 data rows
base margins figure: 1839x960 RGBA, nonwhite_fraction=0.446530
objective heatmap: 1839x960 RGBA, nonwhite_fraction=0.683392
target2 rescue trend: 1520x960 RGBA, nonwhite_fraction=0.021952
visual inspection: all three figures are readable and support the summary
figure notes: figures/FIGURE_NOTES.md present and run-specific
resources: no new FDTD scene; GPU was moved to seed121393 target0
```

## Next Decision

Continue full-ringdown replication with seed121393 target0, sources=8,
Tx/Rx=60.

---

## Source: `docs/experiments/483_variable_depth_radius_seed121393_target0_sources8_txrx60_ringdown050.md`

# Experiment 483: Seed121393 Target0 Sources=8 Tx/Rx=60 Ringdown050

## Purpose

Run 949 starts the seed121393 Fibonacci replication branch with the standard
8-source target0 full-ringdown production row after seed75025 was accepted only
after a fragile target2 rescue.

## 949: Coordinate Optimizer Variable-Depth/Radius Seed121393 Target0 Sources=8 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/949_coordinate_optimizer_variable_depth_radius_seed121393_target0_sources8_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 8 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 0 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed121393:1.1,-50.0,1.1,0.10,121393,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed121393 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed121393_target0_sources8_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact, but base confidence is below
cutoff:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed121393
target: 0
sources: 8
best: x=150 mm, z=80 mm, r=5.0 mm
next radius: 5.25 mm
absolute radius margin: 4.651123e-04
offset from cutoff: -3.488769e-05
relative radius margin: 2.978815e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.015614002821777136
next radius misfit: 0.01607911513589835
listed competing geometry: x=150 mm, z=81 mm, r=6.0 mm
elapsed: about 630.5 s
```

Diagnostic objective rows all preserve the true target0 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.651123e-04 | below cutoff |
| highband | 6.435403e-04 | above cutoff |
| late | 3.973800e-04 | below cutoff |
| late_high | 4.775535e-04 | below cutoff |
| veryhigh | 6.147780e-04 | above cutoff |
| early_high | 5.145201e-04 | above cutoff |

## Interpretation

Run 949 is exact but weak. It is rejected as the seed121393 target0 8-source
control because the base margin is 3.489e-05 below cutoff. Since every
objective variant still ranks the true geometry first, the correct next action
is a source-density rescue rather than a change in coordinate window.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target0 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.226339 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one weak row below the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and flags target0 as weak
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target0 truth_radius_mm is 5.0 mm
resources: GPU utilization held around 90-91%; RAM stayed about 96 GiB available
```

## Next Decision

Run the seed121393 target0 9-source rescue. That run is underway as experiment
950.

---

## Source: `docs/experiments/484_variable_depth_radius_seed121393_target0_sources9_txrx60_ringdown050.md`

# Experiment 484: Seed121393 Target0 Sources=9 Tx/Rx=60 Ringdown050 Rescue

## Purpose

Run 950 tests the seed121393 target0 9-source rescue after run 949 recovered
the true target0 geometry at 8 sources but failed the base confidence cutoff.

## 950: Coordinate Optimizer Variable-Depth/Radius Seed121393 Target0 Sources=9 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/950_coordinate_optimizer_variable_depth_radius_seed121393_target0_sources9_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 9 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 0 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed121393:1.1,-50.0,1.1,0.10,121393,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed121393 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed121393_target0_sources9_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact, but base confidence remains
below cutoff:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed121393
target: 0
sources: 9
best: x=150 mm, z=80 mm, r=5.0 mm
next radius: 5.25 mm
absolute radius margin: 4.911751e-04
offset from cutoff: -8.824876e-06
relative radius margin: 3.166350e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.015512343520900886
next radius misfit: 0.016003518644606318
listed competing geometry: x=150 mm, z=81 mm, r=6.0 mm
elapsed: about 716.5 s
```

Diagnostic objective rows all preserve the true target0 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.911751e-04 | below cutoff |
| highband | 7.133895e-04 | above cutoff |
| late | 3.475432e-04 | below cutoff |
| late_high | 4.343539e-04 | below cutoff |
| veryhigh | 7.384943e-04 | above cutoff |
| early_high | 6.218149e-04 | above cutoff |

## Interpretation

Run 950 is exact but still weak. It improves the target0 base margin from
4.651e-04 at 8 sources to 4.912e-04 at 9 sources, but remains 8.825e-06 below
cutoff. Since the failure is confidence-only and close to the threshold, run
one 11-source escalation before deciding whether seed121393 target0 is
unresolved.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target0 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.238059 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one weak row below the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and flags target0 as weak
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target0 truth_radius_mm is 5.0 mm
resources: GPU utilization held around 91-92%; RAM stayed about 96 GiB available
```

## Next Decision

Run the seed121393 target0 11-source rescue. That run is underway as
experiment 951.

---

## Source: `docs/experiments/485_variable_depth_radius_seed121393_target0_sources11_txrx60_ringdown050.md`

# Experiment 485: Seed121393 Target0 Sources=11 Tx/Rx=60 Ringdown050 Escalation

## Purpose

Run 951 tests one final seed121393 target0 source-density escalation after the
9-source rescue improved but did not accept the row.

## 951: Coordinate Optimizer Variable-Depth/Radius Seed121393 Target0 Sources=11 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/951_coordinate_optimizer_variable_depth_radius_seed121393_target0_sources11_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 11 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 0 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed121393:1.1,-50.0,1.1,0.10,121393,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed121393 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed121393_target0_sources11_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact, but base confidence is still
below cutoff:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed121393
target: 0
sources: 11
best: x=150 mm, z=80 mm, r=5.0 mm
next radius: 5.25 mm
absolute radius margin: 4.384714e-04
offset from cutoff: -6.152855e-05
relative radius margin: 3.023674e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.014501279945277617
next radius misfit: 0.01493975139048427
listed competing geometry: x=150 mm, z=81 mm, r=6.0 mm
elapsed: about 927.9 s
```

Diagnostic objective rows all preserve the true target0 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.384714e-04 | below cutoff |
| highband | 5.739507e-04 | above cutoff |
| late | 3.902221e-04 | below cutoff |
| late_high | 4.415917e-04 | below cutoff |
| veryhigh | 5.603996e-04 | above cutoff |
| early_high | 4.936756e-04 | below cutoff |

## Interpretation

Run 951 is exact but weak and worsens the target0 base margin compared with
the 9-source attempt. This closes the source-density rescue path for
seed121393 target0: 8, 9, and 11 sources all preserve truth but fail the base
cutoff.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target0 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.214619 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one weak row below the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and flags target0 as weak
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target0 truth_radius_mm is 5.0 mm
resources: GPU utilization held around 91-92%; RAM stayed about 96 GiB available
```

## Next Decision

Create the seed121393 target0 unresolved source-density summary.

---

## Source: `docs/experiments/486_seed121393_ringdown050_target0_unresolved_source_density_summary.md`

# Experiment 486: Seed121393 Ringdown050 Target0 Unresolved Source-Density Summary

## Purpose

Run 952 consolidates the seed121393 target0 source-density rescue sequence
after 8, 9, and 11 sources all failed the base confidence cutoff.

## 952: Seed121393 Ringdown050 Target0 Unresolved Source-Density Summary

Output:

```text
outputs/experiments/952_seed121393_ringdown050_target0_unresolved_source_density_summary
```

Generation:

```text
CPU-side structured aggregation of completed coordinate-optimizer outputs
949-951. No new FDTD scene was launched for this summary.
```

Source runs:

```text
949 target0 sources=8 ringdown050 weak control
950 target0 sources=9 ringdown050 weak rescue
951 target0 sources=11 ringdown050 weak escalation
```

## Results

Summary counts:

```text
row count: 3
diagnostic objective rows: 18
accepted runs: none
rejected truth-preserving runs: 949, 950, 951
cutoff: 5.0e-4
```

Source-density trend:

| Run | Sources | Base margin | Offset from cutoff | Status |
| ---: | ---: | ---: | ---: | --- |
| 949 | 8 | 4.651123e-04 | -3.489e-05 | rejected |
| 950 | 9 | 4.911751e-04 | -8.825e-06 | rejected |
| 951 | 11 | 4.384714e-04 | -6.153e-05 | rejected |

## Interpretation

Seed121393 target0 is unresolved under source-density rescue. The row is
truth-preserving for all three source counts, but every base margin stays below
cutoff. The 9-source row is the best attempt and still misses by 8.825e-06;
the 11-source escalation worsens the base margin. Stop this branch and
continue replication with the next seed.

## Validation

```text
JSON parse: run_manifest.json and seed121393_target0_unresolved_summary.json pass
CSV rows: seed121393_target0_source_density_summary.csv has 3 data rows
diagnostics CSV rows: seed121393_target0_objective_diagnostics.csv has 18 data rows
source-density trend figure: 1520x960 RGBA, nonwhite_fraction=0.022791
objective heatmap: 1839x960 RGBA, nonwhite_fraction=0.701479
visual inspection: both figures are readable and support the unresolved decision
figure notes: figures/FIGURE_NOTES.md present and run-specific
resources: no new FDTD scene; GPU was moved to seed196418 target0
```

## Next Decision

Continue full-ringdown replication with seed196418 target0, sources=8,
Tx/Rx=60.

---

## Source: `docs/experiments/487_variable_depth_radius_seed196418_target0_sources8_txrx60_ringdown050.md`

# Experiment 487: Seed196418 Target0 Sources=8 Tx/Rx=60 Ringdown050

## Purpose

Run 953 starts the seed196418 Fibonacci replication branch with the standard
8-source target0 full-ringdown production row after seed121393 target0 failed
source-density rescue.

## 953: Coordinate Optimizer Variable-Depth/Radius Seed196418 Target0 Sources=8 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/953_coordinate_optimizer_variable_depth_radius_seed196418_target0_sources8_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 8 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 0 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed196418:1.1,-50.0,1.1,0.10,196418,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed196418 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed196418_target0_sources8_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed196418
target: 0
sources: 8
best: x=150 mm, z=80 mm, r=5.0 mm
next radius: 5.25 mm
absolute radius margin: 6.119611e-04
offset from cutoff: +1.119611e-04
relative radius margin: 3.917625e-02
confidence label: moderate
fallback warning: none
best misfit: 0.015620716591450977
next radius misfit: 0.01623267771538999
listed competing geometry: x=150 mm, z=81 mm, r=6.0 mm
elapsed: about 610.2 s
```

Diagnostic objective rows all preserve the true target0 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 6.119611e-04 | above cutoff |
| highband | 8.141765e-04 | above cutoff |
| late | 5.317649e-04 | above cutoff |
| late_high | 6.091862e-04 | above cutoff |
| veryhigh | 7.548079e-04 | above cutoff |
| early_high | 6.620326e-04 | above cutoff |

## Interpretation

Run 953 is accepted as the seed196418 target0 8-source control. It is a strong
reset after seed121393 target0 was unresolved: every diagnostic objective
clears cutoff and the base margin has more than 1.1e-04 reserve. Continue to
target2 at 5 sources.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target0 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.289483 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one moderate row well above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target0 truth_radius_mm is 5.0 mm
resources: GPU utilization held around 90-91%; RAM stayed about 96 GiB available
```

## Next Decision

Continue the seed196418 branch with target2, sources=5, Tx/Rx=60, and
ringdown050. That run is underway as experiment 954.

---

## Source: `docs/experiments/488_variable_depth_radius_seed196418_target2_sources5_txrx60_ringdown050.md`

# Experiment 488: Seed196418 Target2 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 954 continues the seed196418 branch after the accepted target0 8-source
control. It tests the standard 5-source target2 row under the full-ringdown
source mismatch policy before moving to target1.

## 954: Coordinate Optimizer Variable-Depth/Radius Seed196418 Target2 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/954_coordinate_optimizer_variable_depth_radius_seed196418_target2_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed196418:1.1,-50.0,1.1,0.10,196418,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed196418 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed196418_target2_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed196418
target: 2
sources: 5
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 6.784989e-04
offset from cutoff: +1.784989e-04
relative radius margin: 3.990733e-02
confidence label: moderate
fallback warning: none
best misfit: 0.017001859673317972
next radius misfit: 0.01768035854110113
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: about 402.0 s
```

Diagnostic objective rows all preserve the true target2 geometry:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 6.784989e-04 | above cutoff |
| highband | 8.011855e-04 | above cutoff |
| late | 1.007666e-03 | above cutoff |
| late_high | 1.039656e-03 | above cutoff |
| veryhigh | 8.564834e-04 | above cutoff |
| early_high | 5.566333e-04 | above cutoff |

## Interpretation

Run 954 is accepted as the seed196418 target2 5-source control. The row is
not just exact by the base objective: all six diagnostic objective variants
rank the true target2 geometry first and all margins clear the 5.0e-4 cutoff.
Continue the branch with target1 at 5 sources. If target1 also accepts, create
the seed196418 8/5/5 summary.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.318968 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one moderate row well above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: 5-source GPU checks were about 87-88% utilization with about 96 GiB RAM available
```

## Next Decision

Continue the seed196418 branch with target1, sources=5, Tx/Rx=60, and
ringdown050. That run is underway as experiment 955.

---

## Source: `docs/experiments/489_variable_depth_radius_seed196418_target1_sources5_txrx60_ringdown050.md`

# Experiment 489: Seed196418 Target1 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 955 tests the seed196418 target1 standard 5-source control after target0
and target2 both accepted. The goal is to determine whether seed196418 can
close with an 8/5/5 policy summary or requires a target1 rescue.

## 955: Coordinate Optimizer Variable-Depth/Radius Seed196418 Target1 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/955_coordinate_optimizer_variable_depth_radius_seed196418_target1_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 1 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed196418:1.1,-50.0,1.1,0.10,196418,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed196418 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed196418_target1_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed196418
target: 1
sources: 5
best: x=250 mm, z=100 mm, r=6.0 mm
next radius: 6.25 mm
absolute radius margin: 4.809369e-04
offset from cutoff: -1.906314e-05
relative radius margin: 2.828731e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.017001859673317972
next radius misfit: 0.017482796530220314
listed competing geometry: x=250 mm, z=101 mm, r=6.75 mm
elapsed: about 398.8 s
```

Diagnostic objective rows all preserve the true target1 geometry, but base and
early_high miss the margin cutoff:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.809369e-04 | below cutoff |
| highband | 6.090437e-04 | above cutoff |
| late | 6.809514e-04 | above cutoff |
| late_high | 7.616478e-04 | above cutoff |
| veryhigh | 5.267989e-04 | above cutoff |
| early_high | 4.511748e-04 | below cutoff |

## Interpretation

Run 955 is rejected at the standard confidence threshold despite an exact
final coordinate state. The top-k table shows that this is not a geometry
misidentification; all diagnostic objective variants rank the true target1
candidate first. Because the base miss is only 1.91e-05 below cutoff, the
right next step is a 9-source target1 rescue, not a seed summary or a broader
policy change.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target1 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.233312 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one weak row below the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and flags the weak target1 row
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target1 truth_radius_mm is 6.0 mm
resources: 5-source GPU checks were about 87-88% utilization with about 96 GiB RAM available
```

## Next Decision

Run the target1 9-source rescue. That run is underway as experiment 956.

---

## Source: `docs/experiments/490_variable_depth_radius_seed196418_target1_sources9_txrx60_ringdown050.md`

# Experiment 490: Seed196418 Target1 Sources=9 Tx/Rx=60 Ringdown050

## Purpose

Run 956 rescues seed196418 target1 after the 5-source control was exact but
weak. The question is whether 9 sources move the target1 radius-confidence
margin above the standard 5.0e-4 cutoff.

## 956: Coordinate Optimizer Variable-Depth/Radius Seed196418 Target1 Sources=9 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/956_coordinate_optimizer_variable_depth_radius_seed196418_target1_sources9_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 9 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 1 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed196418:1.1,-50.0,1.1,0.10,196418,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed196418 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed196418_target1_sources9_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed196418
target: 1
sources: 9
best: x=250 mm, z=100 mm, r=6.0 mm
next radius: 6.25 mm
absolute radius margin: 5.895553e-04
offset from cutoff: +8.955533e-05
relative radius margin: 3.814377e-02
confidence label: moderate
fallback warning: none
best misfit: 0.015456137123426649
next radius misfit: 0.016045692449993463
listed competing geometry: x=250 mm, z=101 mm, r=6.75 mm
elapsed: about 715.0 s
```

Diagnostic objective rows all preserve the true target1 geometry and all clear
the cutoff:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.895553e-04 | above cutoff |
| highband | 8.220418e-04 | above cutoff |
| late | 8.834641e-04 | above cutoff |
| late_high | 1.117499e-03 | above cutoff |
| veryhigh | 7.441846e-04 | above cutoff |
| early_high | 6.104081e-04 | above cutoff |

## Interpretation

Run 956 is accepted as the seed196418 target1 rescue. It converts the exact
but weak run 955 into an accepted branch result: the base margin increases by
about 1.086e-04 and the early_high objective no longer carries a caveat.
Close seed196418 with a target-specific rescue summary.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target1 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.281092 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target1 truth_radius_mm is 6.0 mm
resources: 9-source GPU checks were about 91% utilization with about 96 GiB RAM available
```

## Next Decision

Create the seed196418 target-specific rescue summary before starting
seed317811.

---

## Source: `docs/experiments/491_seed196418_ringdown050_target_specific_rescue_summary.md`

# Experiment 491: Seed196418 Ringdown050 Target-Specific Rescue Summary

## Purpose

Run 957 closes the seed196418 branch after target1 required a 9-source rescue.
It aggregates completed coordinate-optimizer outputs into branch-level
CSV/JSON tables and decision figures; it does not launch a new FDTD scene.

## 957: Seed196418 Ringdown050 Target-Specific Rescue Summary

Output:

```text
outputs/experiments/957_seed196418_ringdown050_target_specific_rescue_summary
```

Source runs:

```text
953: target0, sources=8, ringdown050
954: target2, sources=5, ringdown050
955: target1, sources=5, ringdown050, weak control
956: target1, sources=9, ringdown050, rescue
```

## Results

Seed196418 is accepted after a target1 9-source rescue:

| Target | Best run | Sources | Base margin | Offset from cutoff | Status |
| --- | ---: | ---: | ---: | ---: | --- |
| target0 | 953 | 8 | 6.119611e-04 | +1.120e-04 | accepted |
| target2 | 954 | 5 | 6.784989e-04 | +1.785e-04 | accepted |
| target1 | 956 | 9 | 5.895553e-04 | +8.956e-05 | accepted rescue |

Rejected rows: 955.

The target1 rescue trend is the key branch result: target1 increases from
4.809369e-04 at 5 sources to 5.895553e-04 at 9 sources, and all six objective
variants are above cutoff in the rescue row.

## Artifacts

```text
data/seed196418_branch_summary.csv
data/seed196418_branch_summary.json
data/seed196418_objective_diagnostics.csv
figures/seed196418_base_margins_by_run.png
figures/seed196418_objective_margin_heatmap.png
figures/seed196418_target1_rescue_trend.png
figures/FIGURE_NOTES.md
run_manifest.json
```

## Validation

```text
summary JSON: parsed; row_count=4; diagnostic_row_count=24
accepted runs: 953, 954, 956
rejected truth-preserving runs: 955
branch summary CSV rows: 4
objective diagnostics CSV rows: 24
base margins figure: 1824x960 RGBA, nonwhite_fraction=0.533219
objective heatmap: 1824x936 RGBA, nonwhite_fraction=0.647597
target1 rescue trend: 1536x960 RGBA, nonwhite_fraction=0.029429
visual inspection: all three figures are readable and support the summary
figure notes: figures/FIGURE_NOTES.md present
resources: no new FDTD scene; GPU moved to seed317811 target0
```

## Next Decision

Continue full-ringdown target-specific replication with seed317811 target0 at
8 sources. That run is underway as experiment 958.

---

## Source: `docs/experiments/492_variable_depth_radius_seed317811_target0_sources8_txrx60_ringdown050.md`

# Experiment 492: Seed317811 Target0 Sources=8 Tx/Rx=60 Ringdown050

## Purpose

Run 958 starts the seed317811 Fibonacci replication branch with the standard
8-source target0 full-ringdown production row.

## 958: Coordinate Optimizer Variable-Depth/Radius Seed317811 Target0 Sources=8 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/958_coordinate_optimizer_variable_depth_radius_seed317811_target0_sources8_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 8 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 0 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed317811:1.1,-50.0,1.1,0.10,317811,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed317811 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed317811_target0_sources8_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed317811
target: 0
sources: 8
best: x=150 mm, z=80 mm, r=5.0 mm
next radius: 5.25 mm
absolute radius margin: 5.875666e-04
offset from cutoff: +8.756657e-05
relative radius margin: 3.745169e-02
confidence label: moderate
fallback warning: none
best misfit: 0.01568865401625481
next radius misfit: 0.01627622058310597
listed competing geometry: x=150 mm, z=81 mm, r=6.0 mm
elapsed: about 621.0 s
```

Diagnostic objective rows all preserve the true target0 geometry, but late and
late_high are below cutoff:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.875666e-04 | above cutoff |
| highband | 7.340599e-04 | above cutoff |
| late | 4.353008e-04 | below cutoff |
| late_high | 4.663240e-04 | below cutoff |
| veryhigh | 7.014880e-04 | above cutoff |
| early_high | 6.141457e-04 | above cutoff |

## Interpretation

Run 958 is accepted as the seed317811 target0 8-source control, but it should
carry a late-window caveat into the branch summary. The selected geometry is
stable across all objective variants; the caveat is a confidence-margin
reserve issue for late and late_high, not a target0 misidentification.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target0 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.280151 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target0 truth_radius_mm is 5.0 mm
resources: 8-source GPU checks were about 90% utilization with about 96-97 GiB RAM available
```

## Next Decision

Continue the seed317811 branch with target2, sources=5, Tx/Rx=60, and
ringdown050. That run is underway as experiment 959.

---

## Source: `docs/experiments/493_variable_depth_radius_seed317811_target2_sources5_txrx60_ringdown050.md`

# Experiment 493: Seed317811 Target2 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 959 tests the seed317811 target2 standard 5-source control after target0
accepted at 8 sources.

## 959: Coordinate Optimizer Variable-Depth/Radius Seed317811 Target2 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/959_coordinate_optimizer_variable_depth_radius_seed317811_target2_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed317811:1.1,-50.0,1.1,0.10,317811,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed317811 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed317811_target2_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed317811
target: 2
sources: 5
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 4.958067e-04
offset from cutoff: -4.193344e-06
relative radius margin: 2.973991e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.016671422191882383
next radius misfit: 0.01716722884834735
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: about 398.6 s
```

Diagnostic objective rows all preserve the true target2 geometry, but base and
early_high are below cutoff:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.958067e-04 | below cutoff |
| highband | 6.014405e-04 | above cutoff |
| late | 7.283523e-04 | above cutoff |
| late_high | 7.820381e-04 | above cutoff |
| veryhigh | 5.925397e-04 | above cutoff |
| early_high | 4.163696e-04 | below cutoff |

## Interpretation

Run 959 is rejected at the standard confidence threshold despite an exact
final coordinate state. The base miss is small, and the top-k table shows the
truth geometry at rank 1 for every objective variant. Escalate target2 to 9
sources before moving to target1.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.239574 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one weak row just below the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and flags the weak target2 row
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: 5-source GPU checks were about 87-88% utilization with about 96-97 GiB RAM available
```

## Next Decision

Run the target2 9-source rescue. That run is underway as experiment 960.

---

## Source: `docs/experiments/494_variable_depth_radius_seed317811_target2_sources9_txrx60_ringdown050.md`

# Experiment 494: Seed317811 Target2 Sources=9 Tx/Rx=60 Ringdown050

## Purpose

Run 960 rescues seed317811 target2 after the 5-source control was exact but
weak. It tests whether 9 sources clear the base confidence cutoff before
moving to target1.

## 960: Coordinate Optimizer Variable-Depth/Radius Seed317811 Target2 Sources=9 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/960_coordinate_optimizer_variable_depth_radius_seed317811_target2_sources9_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 9 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed317811:1.1,-50.0,1.1,0.10,317811,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed317811 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed317811_target2_sources9_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed317811
target: 2
sources: 9
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 5.095658e-04
offset from cutoff: +9.565767e-06
relative radius margin: 3.263781e-02
confidence label: moderate
fallback warning: none
best misfit: 0.015612745988511288
next radius misfit: 0.01612231175589339
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: about 712.5 s
```

Diagnostic objective rows all preserve the true target2 geometry, but
early_high remains below cutoff:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.095658e-04 | above cutoff |
| highband | 6.487764e-04 | above cutoff |
| late | 7.479119e-04 | above cutoff |
| late_high | 8.204328e-04 | above cutoff |
| veryhigh | 6.551184e-04 | above cutoff |
| early_high | 4.579498e-04 | below cutoff |

## Interpretation

Run 960 is accepted as the seed317811 target2 rescue, but it is a low-reserve
acceptance. The base margin clears cutoff by only 9.57e-06 and early_high
does not clear cutoff. Continue to target1, then carry this caveat into the
seed317811 branch summary.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.245884 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one moderate row just above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: 9-source GPU checks were about 90-91% utilization with about 96-97 GiB RAM available
```

## Next Decision

Continue the seed317811 branch with target1, sources=5, Tx/Rx=60, and
ringdown050. That run is underway as experiment 961.

---

## Source: `docs/experiments/495_variable_depth_radius_seed317811_target1_sources5_txrx60_ringdown050.md`

# Experiment 495: Seed317811 Target1 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 961 tests seed317811 target1 at the standard 5-source control after target2
accepted only by a 9-source rescue.

## 961: Coordinate Optimizer Variable-Depth/Radius Seed317811 Target1 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/961_coordinate_optimizer_variable_depth_radius_seed317811_target1_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 1 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed317811:1.1,-50.0,1.1,0.10,317811,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed317811 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed317811_target1_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed317811
target: 1
sources: 5
best: x=250 mm, z=100 mm, r=6.0 mm
next radius: 6.25 mm
absolute radius margin: 6.017320e-04
offset from cutoff: +1.017320e-04
relative radius margin: 3.609362e-02
confidence label: moderate
fallback warning: none
best misfit: 0.016671422191882383
next radius misfit: 0.01727315419159255
listed competing geometry: x=250 mm, z=101 mm, r=6.75 mm
elapsed: about 400.2 s
```

Diagnostic objective rows all preserve the true target1 geometry and all clear
the cutoff:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 6.017320e-04 | above cutoff |
| highband | 7.778784e-04 | above cutoff |
| late | 8.534321e-04 | above cutoff |
| late_high | 9.351331e-04 | above cutoff |
| veryhigh | 7.038860e-04 | above cutoff |
| early_high | 5.662074e-04 | above cutoff |

## Interpretation

Run 961 is accepted as the seed317811 target1 5-source control. It is cleaner
than the branch's target0/target2 rows because all diagnostic variants clear
the confidence cutoff. Close the seed317811 branch with a rescue summary.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target1 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.284638 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target1 truth_radius_mm is 6.0 mm
resources: 5-source GPU checks were about 87-88% utilization with about 96-97 GiB RAM available
```

## Next Decision

Create the seed317811 target-specific rescue summary before starting
seed514229.

---

## Source: `docs/experiments/496_seed317811_ringdown050_target_specific_rescue_summary.md`

# Experiment 496: Seed317811 Ringdown050 Target-Specific Rescue Summary

## Purpose

Run 962 closes the seed317811 branch after target2 required a 9-source rescue.
It aggregates completed coordinate-optimizer outputs into branch-level
CSV/JSON tables and decision figures; it does not launch a new FDTD scene.

## 962: Seed317811 Ringdown050 Target-Specific Rescue Summary

Output:

```text
outputs/experiments/962_seed317811_ringdown050_target_specific_rescue_summary
```

Source runs:

```text
958: target0, sources=8, ringdown050
959: target2, sources=5, ringdown050, weak control
960: target2, sources=9, ringdown050, rescue
961: target1, sources=5, ringdown050
```

## Results

Seed317811 is accepted after a target2 9-source rescue:

| Target | Best run | Sources | Base margin | Offset from cutoff | Status |
| --- | ---: | ---: | ---: | ---: | --- |
| target0 | 958 | 8 | 5.875666e-04 | +8.757e-05 | accepted with late caveat |
| target2 | 960 | 9 | 5.095658e-04 | +9.566e-06 | accepted rescue |
| target1 | 961 | 5 | 6.017320e-04 | +1.017e-04 | accepted |

Rejected rows: 959.

The branch is truth-preserving on all rows, but it is not high-reserve:
target0 carries late/late_high caveats and target2 rescue clears base by only
9.57e-06 with early_high still below cutoff.

## Artifacts

```text
data/seed317811_branch_summary.csv
data/seed317811_branch_summary.json
data/seed317811_objective_diagnostics.csv
figures/seed317811_base_margins_by_run.png
figures/seed317811_objective_margin_heatmap.png
figures/seed317811_target2_rescue_trend.png
figures/FIGURE_NOTES.md
run_manifest.json
```

## Validation

```text
summary JSON: parsed; row_count=4; diagnostic_row_count=24
accepted runs: 958, 960, 961
rejected truth-preserving runs: 959
branch summary CSV rows: 4
objective diagnostics CSV rows: 24
base margins figure: 1824x960 RGBA, nonwhite_fraction=0.557173
objective heatmap: 1824x936 RGBA, nonwhite_fraction=0.647197
target2 rescue trend: 1536x960 RGBA, nonwhite_fraction=0.032054
visual inspection: all three figures are readable and support the summary
figure notes: figures/FIGURE_NOTES.md present
resources: no new FDTD scene; GPU moved to seed514229 target0
```

## Next Decision

Continue full-ringdown target-specific replication with seed514229 target0 at
8 sources. That run is underway as experiment 963.

---

## Source: `docs/experiments/497_variable_depth_radius_seed514229_target0_sources8_txrx60_ringdown050.md`

# Experiment 497: Seed514229 Target0 Sources=8 Tx/Rx=60 Ringdown050

## Purpose

Run 963 starts the seed514229 Fibonacci replication branch with the standard
8-source target0 full-ringdown production row.

## 963: Coordinate Optimizer Variable-Depth/Radius Seed514229 Target0 Sources=8 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/963_coordinate_optimizer_variable_depth_radius_seed514229_target0_sources8_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 8 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 0 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed514229:1.1,-50.0,1.1,0.10,514229,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed514229 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed514229_target0_sources8_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed514229
target: 0
sources: 8
best: x=150 mm, z=80 mm, r=5.0 mm
next radius: 5.25 mm
absolute radius margin: 5.335991e-04
offset from cutoff: +3.335991e-05
relative radius margin: 3.391275e-02
confidence label: moderate
fallback warning: none
best misfit: 0.015734471669332936
next radius misfit: 0.016268070811906767
listed competing geometry: x=150 mm, z=81 mm, r=6.0 mm
elapsed: about 615.0 s
```

Diagnostic objective rows all preserve the true target0 geometry, but late is
below cutoff:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.335991e-04 | above cutoff |
| highband | 7.094167e-04 | above cutoff |
| late | 4.741325e-04 | below cutoff |
| late_high | 5.566854e-04 | above cutoff |
| veryhigh | 6.925717e-04 | above cutoff |
| early_high | 5.920828e-04 | above cutoff |

## Interpretation

Run 963 is accepted as the seed514229 target0 8-source control with a
late-window caveat. The result is truth-preserving across all diagnostic
objective variants, so continue the branch with target2 at 5 sources.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target0 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.256748 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target0 truth_radius_mm is 5.0 mm
resources: 8-source GPU checks were about 89-91% utilization with about 96-97 GiB RAM available
```

## Next Decision

Continue the seed514229 branch with target2, sources=5, Tx/Rx=60, and
ringdown050. That run is underway as experiment 964.

---

## Source: `docs/experiments/498_variable_depth_radius_seed514229_target2_sources5_txrx60_ringdown050.md`

# Experiment 498: Seed514229 Target2 Sources=5 Tx/Rx=60 Ringdown050

## Purpose

Run 964 tests the seed514229 target2 standard 5-source control after target0
accepted at 8 sources.

## 964: Coordinate Optimizer Variable-Depth/Radius Seed514229 Target2 Sources=5 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/964_coordinate_optimizer_variable_depth_radius_seed514229_target2_sources5_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 5 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed514229:1.1,-50.0,1.1,0.10,514229,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed514229 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed514229_target2_sources5_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed514229
target: 2
sources: 5
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 4.482192e-04
offset from cutoff: -5.177808e-05
relative radius margin: 2.662044e-02
confidence label: weak
fallback warning: radius_weak_confidence
best misfit: 0.016837404661480287
next radius misfit: 0.017285623833411024
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: about 397.5 s
```

Diagnostic objective rows all preserve the true target2 geometry, but base and
early_high are below cutoff:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 4.482192e-04 | below cutoff |
| highband | 5.303128e-04 | above cutoff |
| late | 7.068493e-04 | above cutoff |
| late_high | 7.122514e-04 | above cutoff |
| veryhigh | 5.601667e-04 | above cutoff |
| early_high | 3.752407e-04 | below cutoff |

## Interpretation

Run 964 is rejected at the standard confidence threshold despite an exact
final coordinate state. The top-k table shows truth at rank 1 for every
objective variant, so escalate target2 to 9 sources before moving to target1.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.218874 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one weak row below the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and flags the weak target2 row
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: 5-source GPU checks were about 88-89% utilization with about 96-97 GiB RAM available
```

## Next Decision

Run the target2 9-source rescue. That run is underway as experiment 965.

---

## Source: `docs/experiments/499_variable_depth_radius_seed514229_target2_sources9_txrx60_ringdown050.md`

# Experiment 499: Seed514229 Target2 Sources=9 Tx/Rx=60 Ringdown050

## Purpose

Run 965 rescues seed514229 target2 after the 5-source control was exact but
weak.

## 965: Coordinate Optimizer Variable-Depth/Radius Seed514229 Target2 Sources=9 Tx/Rx=60 Ringdown050

Output:

```text
outputs/experiments/965_coordinate_optimizer_variable_depth_radius_seed514229_target2_sources9_txrx60_ringdown050_objectives
```

Command:

```text
/home/lam001/miniforge3/envs/FNO/bin/python -u run_multi_rebar_coordinate_optimizer.py \
  --backend gpu-cpml \
  --grid-step-mm 1.0 \
  --sources 9 \
  --tx-rx-offset-mm 60 \
  --frequency-ghz 1.5 \
  --true-x-values-mm 150,250,350 \
  --true-z-values-mm 80,100,120 \
  --truth-radius-values-mm 5,6,8 \
  --initial-x-values-mm 150,250,350 \
  --initial-z-values-mm 80,100,120 \
  --initial-radius-values-mm 5,6,8 \
  --target-indices 2 \
  --passes 1 \
  --x-offsets-mm=0 \
  --z-offsets-mm=0:1:1 \
  --radius-offsets-mm=0:1.25:0.25 \
  --replication-cases source_mismatch_ringdown050_noise10_seed514229:1.1,-50.0,1.1,0.10,514229,0.5,180.0,0.8 \
  --update-case-label source_mismatch_ringdown050_noise10_seed514229 \
  --source-frequency-scales 0.9,1.0,1.1 \
  --fit-ringdown-coefficient \
  --source-ringdown-delay-ps 180.0 \
  --source-ringdown-frequency-scale 0.8 \
  --source-time-shift-ps-values=-50,0,50 \
  --diagnostic-objective-variants 'base:1.0,7.0,0.3,none,none,0.0|highband:1.0,7.0,0.3,1.1,3.4,0.15|late:1.5,5.5,0.2,none,none,0.0|late_high:1.5,5.5,0.2,1.1,3.4,0.15|veryhigh:1.0,7.0,0.3,1.8,4.2,0.15|early_high:0.8,3.5,0.2,1.1,3.4,0.15' \
  --top-k 12 \
  --progress-every 2 \
  --run-name coordinate_optimizer_variable_depth_radius_seed514229_target2_sources9_txrx60_ringdown050_objectives
```

## Results

The final recovered coordinate state is exact:

```text
x = [150, 250, 350] mm
z = [80, 100, 120] mm
r = [5, 6, 8] mm
```

Base confidence row:

```text
case: source_mismatch_ringdown050_noise10_seed514229
target: 2
sources: 9
best: x=350 mm, z=120 mm, r=8.0 mm
next radius: 8.75 mm
absolute radius margin: 5.346563e-04
offset from cutoff: +3.465626e-05
relative radius margin: 3.412615e-02
confidence label: moderate
fallback warning: none
best misfit: 0.015667055158168856
next radius misfit: 0.016201711413251923
listed competing geometry: x=350 mm, z=121 mm, r=8.75 mm
elapsed: about 712.6 s
```

Diagnostic objective rows all preserve the true target2 geometry, but
early_high remains just below cutoff:

| Objective | Margin | Status |
| --- | ---: | --- |
| base | 5.346563e-04 | above cutoff |
| highband | 6.939286e-04 | above cutoff |
| late | 8.168860e-04 | above cutoff |
| late_high | 8.841093e-04 | above cutoff |
| veryhigh | 7.287743e-04 | above cutoff |
| early_high | 4.968836e-04 | below cutoff |

## Interpretation

Run 965 is accepted as the seed514229 target2 rescue. It remains a caveated
acceptance because early_high is 3.12e-06 below cutoff, but the base objective
clears cutoff and truth is rank 1 across all objective variants. Continue to
target1.

## Validation

```text
JSON parse: run_manifest.json and multi_rebar_coordinate_optimizer_summary.json pass
CSV rows: confidence=1, objective diagnostics=6, objective top candidates=72, state history=2, candidates=12
top candidates: exact target2 geometry is rank 1 for all six objective variants
figure validation: coordinate_confidence_margins.png is 1549x903 RGBA with nonwhite_fraction=0.257640 and nonzero dynamic range
visual inspection: confidence figure is readable and shows one moderate row above the 5.0e-4 cutoff
figure notes: figures/FIGURE_NOTES.md present and run-specific
metadata validation: summary truth_radius_values_mm is target-specific at [5.0, 6.0, 8.0]; target2 truth_radius_mm is 8.0 mm
resources: 9-source GPU checks were about 90-92% utilization with about 96-97 GiB RAM available
```

## Next Decision

Continue the seed514229 branch with target1, sources=5, Tx/Rx=60, and
ringdown050. That run is underway as experiment 966.
