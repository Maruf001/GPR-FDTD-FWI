# Experiment 71: Current Evidence Synthesis

## Purpose

Create a concise current-evidence synthesis from the packaged marathon results.
This is a report seed, not a new experiment.

## 538: Current Evidence Synthesis

Output:

```text
outputs/experiments/538_current_evidence_synthesis
```

Artifacts:

```text
README.md
data/current_evidence_synthesis.json
run_manifest.json
```

Strongest claims:

```text
variable-depth/radius staged coordinate pipeline: replicated exact final truth
Tx/Rx=50 mm: leading tested interval-narrowing acquisition geometry
veryhigh: branch-level reporting diagnostic for Tx/Rx=50 variable-depth/radius
fitted ringdown: three-target/three-seed exact under tested stress
source-shape center: report 6.0-6.2 mm interval
shallow r=4: report nominal point plus nuisance-aware interval
close50 metadata: repaired with explicit filled-default labels
```

Non-claims:

```text
no universal high-precision radius claim
no global veryhigh promotion
no free material parameters in the default optimizer
no broad all-parameter or dense coupled source-shape sweeps under current evidence
```

## Next Decision

Use this synthesis as the seed for a concise paper/report section. Continue
CPU/reporting work unless the handoff matrix exposes a new concrete physics
gap.
