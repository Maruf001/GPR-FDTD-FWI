# Experiment 72: Results Section Draft

## Purpose

Convert the run 538 evidence synthesis into a concise report/paper results
section. This is a writing artifact, not a new experiment.

## 539: Results Section Draft

Output:

```text
outputs/experiments/539_results_section_draft
```

Artifact:

```text
results_section_draft.md
```

The draft covers:

```text
variable-depth/radius staged recovery
Tx/Rx=50 mm acquisition interval narrowing
veryhigh as branch-level reporting diagnostic
fitted-ringdown guardrail
source-shape center interval non-transfer
shallow r=4 material/source-aware interval
negative claims that should not be made
```

## Next Decision

Use this draft as a paper/report seed. If more writing is needed, add a compact
methods paragraph and a table of evidence runs before scheduling any new GPU
work.
