# Experiment Category Index

## Purpose

Keep the numbered experiment archive stable while making the research branches
easier to scan.

Canonical synthetic simulation output paths remain:

```text
outputs/experiments/NNN_run_name
```

Field, lab, public, and benchmark-data outputs use dataset-local run streams:

```text
outputs/field_experiments/<dataset_id>/NNN_run_name
```

The synthetic category view is additional:

```text
outputs/experiments/_by_category_symlinks/single_rebar/
outputs/experiments/_by_category_symlinks/multi_rebar/
outputs/experiments/_by_category_symlinks/infrastructure_smoke/
```

Those category folders contain symlinks to the canonical synthetic numbered
folders. They are for browsing and inspection only, not for writing new
outputs.

## Current Categories

| Category | Canonical range | Count | Category view |
| --- | --- | ---: | --- |
| Single rebar | `001`-`062`, `107`-`109`, `111`-`113`, `116`-`121`, `123`-`124`, `126`, `128`, `131`-`132`, `135`-`140`, `142`-`143`, `145`-`148`, `150`-`151`, `153`, `155`-`157`, `159`, `162`, `164`, `166`-`167`, `169`, `172`-`173`, `175`-`176`, `178`, `181`-`182`, `184`, `186`-`187`, `190`-`192`, `194`-`196`, `198`, `200`, `421`-`424` | 126 | `outputs/experiments/_by_category_symlinks/single_rebar/` |
| Multi rebar | `063`-`106`, `110`, `114`-`115`, `203`, `206`, `208`, `210`, `216`, `219`, `221`-`223`, `225`-`227`, `230`, `233`-`235`, `239`, `242`-`244`, `247`, `250`, `252`, `254`-`255`, `258`-`259`, `261`, `265`, `267`, `269`-`270`, `274`, `276`, `278`-`279`, `281`-`283`, `285`-`288`, `290`-`292`, `294`-`296`, `298`-`300`, `302`-`304`, `306`-`309`, `311`-`313`, `315`-`318`, `320`-`322`, `324`-`326`, `328`-`330`, `332`-`334`, `336`-`340`, `342`-`348`, `350`-`355`, `357`-`359`, `361`-`363`, `365`-`367`, `369`-`371`, `373`-`375`, `377`-`380`, `382`-`384`, `386`-`389`, `391`-`393`, `395`-`398`, `400`-`402`, `404`-`407`, `409`-`411`, `413`-`417`, `425`-`434`, `436`-`438`, `440`-`441`, `443`, `445`, `447`, `450`-`451`, `455`-`460`, `462`, `464`-`469`, `472`, `474`-`479`, `482`-`483`, `485`-`486`, `488`-`489`, `491`-`492`, `495`-`496`, `499`, `501`, `504`, `506`, `508`, `510`, `512`, `515`, `518`, `521`, `523`, `526`, `528`, `740`-`900`, `902`-`903`, `905`-`908`, `910`-`913`, `915`-`917`, `919`-`923`, `925`-`929`, `931`-`934`, `936`-`938`, `940`-`942`, `944`-`947`, `949`-`951`, `953`-`956`, `958`-`961`, `963`-`966`, `968`-`971`, `973`-`978`, `980`-`984`, `986`-`993`, `995`-`1004`, `1006`-`1118`, `1120`-`1221`, `1223`-`1226`, `1234`-`1236`, `1240`, `1244`-`1246`, `1250`-`1252`, `1257`, `1259`, `1265`, `1267`, `1269`, `1271`-`1272`, `1274`, `1294`-`1295`, `1302`, `1316`, `1326`-`1337`, `1340`-`1355`, `1357`-`1358` | 771 | `outputs/experiments/_by_category_symlinks/multi_rebar/` |
| Infrastructure/reporting smoke | `122`, `125`, `127`, `129`-`130`, `133`-`134`, `141`, `144`, `149`, `152`, `154`, `158`, `160`-`161`, `163`, `165`, `168`, `170`-`171`, `174`, `177`, `179`-`180`, `183`, `185`, `188`-`189`, `193`, `197`, `199`, `201`-`202`, `204`-`205`, `207`, `209`, `211`-`215`, `217`-`218`, `220`, `224`, `228`-`229`, `231`-`232`, `236`-`238`, `240`-`241`, `245`-`246`, `248`-`249`, `251`, `253`, `256`-`257`, `260`, `262`-`264`, `266`, `268`, `271`-`273`, `275`, `277`, `280`, `284`, `289`, `293`, `297`, `301`, `305`, `310`, `314`, `319`, `323`, `327`, `331`, `335`, `341`, `349`, `356`, `360`, `364`, `368`, `372`, `376`, `381`, `385`, `390`, `394`, `399`, `403`, `408`, `412`, `418`-`420`, `435`, `439`, `442`, `444`, `446`, `448`-`449`, `452`-`454`, `461`, `463`, `470`-`471`, `473`, `480`-`481`, `484`, `487`, `490`, `493`-`494`, `497`-`498`, `500`, `502`-`503`, `505`, `507`, `509`, `511`, `513`-`514`, `516`-`517`, `519`-`520`, `522`, `524`-`525`, `527`, `529`-`739`, `901`, `904`, `909`, `914`, `918`, `924`, `930`, `935`, `939`, `943`, `948`, `952`, `957`, `962`, `967`, `972`, `979`, `985`, `994`, `1005`, `1222`, `1227`-`1233`, `1237`-`1239`, `1241`-`1243`, `1247`-`1249`, `1253`-`1256`, `1258`, `1260`-`1264`, `1266`, `1268`, `1270`, `1273`, `1275`-`1293`, `1296`-`1301`, `1303`-`1315`, `1317`-`1325`, `1338`, `1356` | 459 | `outputs/experiments/_by_category_symlinks/infrastructure_smoke/` |
| Field/lab data | historical synthetic-archive run `1119`; dataset-local run `local_gssi_51600s_2026_06_09/001` | 1 historical + 1 field-archive run | `outputs/field_experiments/` |

## Recent Local 2D State-Consistency Branch

Runs `1374`-`1381` are a local 2D multi-rebar state-consistency branch. They are
canonical synthetic 2D experiment-track runs under `outputs/experiments/`, not
field runs, BEM runs, or weekly summary-table folders.

| Run | Role | Current decision |
| ---: | --- | --- |
| 1374 | State-consistency guard enforcement | Fixed-neighbor geometry must be accepted, measured, jointly optimized, or explicitly uncertain before broad continuation. |
| 1375 | Corrected-state objective-window ladder | The corrected state selects the truth geometry across all six tested objective windows. |
| 1376 | Small neighbor perturbation audit | Small fixed-neighbor perturbations pass under the narrow tested setup. |
| 1377 | Wide radius ladder | Broad radius tolerance fails; a practical boundary exists. |
| 1378 | Radius-boundary refinement | Narrow-window tolerance passes through near +2.0 mm and far -1.5 mm, then fails. |
| 1379 | Expanded x-window audit | Corrected state remains truth, but selected radius perturbations fail once x=187 mm is available. |
| 1380 | Claim-boundary synthesis | Promote only the corrected-state local mechanism; do not promote broad radius tolerance, broad batches, GPU work, field transfer, or field FWI. |
| 1381 | Margin-guard audit | For finite-margin rows, a zero-threshold wrong-minus-truth guard catches all observed failures and flags no corrected-state rows; non-positive or missing margins should be marked uncertain. |
| 1382 | Acceptance-status synthesis | Promote only 12 corrected-state positive-margin rows; hold six missing-margin rows, keep 66 truth-selected perturbation rows as observations only, and reject 32 wrong-geometry rows. |
| 1383 | Objective-support map | Use `base` and `highband` as the core regression objectives; `early_high`, `late`, `late_high`, and `veryhigh` are expanded-window observations only. |
| 1384 | Core regression pack | Convert the accepted boundary into 88 core regression rows: 8 positive acceptances, 20 rejections, 2 hold cases, and 58 observation-only rows. Broad radius tolerance, GPU work, field transfer, and field FWI remain blocked. |
| 1385 | Core regression pack validator | Validate the run `1384` pack from a consumer perspective: 9 of 9 checks pass, zero blocking failures, and broad radius tolerance, GPU work, field transfer, field FWI, and 3D/HPC remain blocked. |
| 1386 | Core regression sentinel suite | Select 11 sentinel rows from the 88-row core pack while covering all 32 observed run/objective/role/status/truth-margin/perturbation tokens. Use as a fast smoke layer only; the full pack remains authoritative and broad radius tolerance, GPU work, field transfer, field FWI, and 3D/HPC remain blocked. |
| 1387 | Core regression sentinel validator | Validate the run `1386` sentinel suite from a consumer perspective: 9 of 9 checks pass, the 11 sentinel rows are a subset of the 88-row core pack, all 32 coverage tokens are covered, and the full pack remains authoritative. Broad radius tolerance, GPU work, field transfer, field FWI, and 3D/HPC remain blocked. |
| 1388 | Sentinel margin-risk audit | Audit whether the 11-row sentinel covers the closest finite margins in the 88-row core pack. It remains valid as category smoke, but it misses the closest negative-rejection and observation-only margins, so a two-row risk-augmented sentinel add-on is recommended before relying on sentinel-only smoke for boundary-sensitive changes. |
| 1389 | Risk-augmented sentinel suite | Add the two closest-margin rows from run `1388` to the 11 category sentinels, producing a 13-row optional fast smoke suite with zero duplicate keys. The full 88-row core pack remains authoritative, and broad radius tolerance, GPU work, field transfer, field FWI, and 3D/HPC remain blocked. |
| 1390 | Risk-augmented sentinel validator | Validate the run `1389` suite from a consumer perspective: 9 of 9 checks pass, all 13 rows are in the full core pack, the 11 category sentinels are preserved, and the two margin add-ons match run `1388`. The 13-row suite is optional fast smoke only; the 88-row full pack remains authoritative. |
| 1391 | Sentinel leave-one-out audit | Audit whether the 13-row risk-augmented sentinel suite is minimal. It covers all 32 required coverage tokens, but only nine rows are coverage-mandatory; the two margin add-ons remain mandatory for margin-risk coverage, and two category rows are individually redundant under the current token definition. Keep the 13-row suite as optional fast smoke, not as a minimal or authoritative replacement for the 88-row pack. |
| 1392 | Reduced sentinel candidate | Remove the two individually redundant category rows from run `1391`, producing an 11-row reduced optional fast-smoke candidate. The reduced candidate still covers all 32 required coverage tokens and preserves both margin-risk add-ons, but it remains pending consumer validation and does not replace the full 88-row core pack. |
| 1393 | Reduced sentinel validator | Validate the run `1392` reduced candidate from a consumer perspective: 9 of 9 checks pass, all 11 rows are in the full core pack, all 32 required tokens remain covered, both margin-risk add-ons are preserved, and only redundant indices 2 and 5 were removed. Use the 11-row reduced sentinel as the preferred optional fast-smoke suite; the full 88-row pack remains authoritative. |
| 1394 | Reduced sentinel sensitivity | Stress-test the run `1393` validator with five controlled table mutations: the exact reduced sentinel passes, while missing a margin add-on, missing a coverage-mandatory row, duplicating a key row, or reintroducing a removed row fails as expected. Use this as the guard for the optional 11-row fast-smoke suite; the full 88-row pack remains authoritative. |
| 1395 | Reduced sentinel adoption contract | Define the safe-use boundary for the reduced 11-row sentinel: six of six contract items pass, the suite is adoptable as optional CPU fast smoke for consumer wiring/schema/parsing/plotting changes, but it does not replace the 88-row core pack and cannot support boundary, objective, margin, token-definition, GPU, field-transfer, field-FWI, or 3D/HPC decisions. |
| 1396 | Reduced sentinel provenance lock | Lock the accepted 11-row reduced sentinel to the 88-row full core pack with stable row-key digests. Eight of eight checks pass, the reduced rows remain an exact subset, the two removed redundant rows remain absent, all 32 required tokens remain covered, and the full pack remains authoritative. No GPU, field-transfer, field-FWI, or 3D/HPC promotion follows from this lock. |
| 1397 | Reduced sentinel provenance lock sensitivity | Stress-test the run `1396` lock with six table scenarios. The exact and row-reordered sentinels pass, while duplicate-key, key-field mutation, removed-row reintroduction, and row-deletion cases fail with zero unexpected outcomes. The lock is stable to harmless row order and sensitive to real drift; the full 88-row pack remains authoritative and GPU, field-transfer, field-FWI, and 3D/HPC remain blocked. |
| 1398 | Regression tier router contract | Convert the reduced-sentinel adoption, provenance lock, and lock-sensitivity results into a routing contract. The 11-row sentinel is allowed for consumer smoke and harmless row-order checks only; row-key drift, boundary/objective/margin/token changes, physical claims, GPU, field transfer, field FWI, and 3D/HPC require the 88-row full pack or remain blocked. |
| 1399 | Regression tier router validator | Validate the run `1398` router from a consumer perspective: eight of eight checks pass, the router has two fast-smoke routes and four full-pack-required routes, the consumer route can use the reduced sentinel, boundary changes require the full pack, GPU/field/3D remain blocked, and the reduced sentinel does not replace the full pack. |
| 1400 | Regression tier router sensitivity | Stress-test the run `1399` validator with six scenarios. The exact router passes, while missing the consumer fast-smoke route, allowing boundary changes to use the sentinel, marking GPU ready, making the sentinel replace the full pack, and route-count drift all fail as expected with zero unexpected outcomes. |
| 1401 | Regression execution packet | Join the validated router to the concrete regression tables. Two fast-smoke routes use the 11-row reduced sentinel, while four boundary-sensitive or blocked routes use the 88-row full core pack or remain blocked. The inventory matches router counts, the execution packet is ready, and the full pack remains authoritative for boundary, objective, margin, token, physical-claim, GPU, field-transfer, field-FWI, and 3D/HPC decisions. |
| 1402 | Regression execution packet validator | Validate the run `1401` packet from a consumer perspective. Nine of nine checks pass: route count, table inventory count, fast/full route counts, reduced-sentinel fast-smoke-only role, full-pack authoritative role, packet-ready status, sentinel-does-not-replace-full-pack rule, and blocked GPU/field/3D states. |
| 1403 | Regression execution packet sensitivity | Stress-test the run `1402` validator with 10 scenarios. The exact packet passes, while nine damaged packets fail as expected: missing route, boundary route using the sentinel, sentinel marked boundary-authoritative, full pack not authoritative, wrong sentinel row count, packet not ready, sentinel replacing the full pack, GPU marked ready, and field transfer marked ready. Zero unexpected outcomes. |
| 1404 | Regression execution dry run | Resolve the run `1401` packet against actual CSV tables. The two 11-row reduced-sentinel routes pass, but all four full-pack routes fail because the packet expects 88 rows while the selected full-pack CSV contains 116 rows. This exposes a table-materialization repair requirement; physical-claim promotion, GPU work, field transfer, field FWI, and 3D/HPC remain blocked. |
| 1405 | Regression packet core-table repair | Materialize the intended 88-row core regression table from the broader 116-row case table and rewrite the four full-pack routes to use it. The two reduced-sentinel routes remain unchanged, the repaired packet is ready for a repeat dry run, and physical-claim promotion, GPU work, field transfer, field FWI, and 3D/HPC remain blocked. |
| 1406 | Repaired regression execution dry run | Repeat the execution dry run against the repaired packet. All six routes pass: two reduced-sentinel routes resolve to 11 rows, four full-pack routes resolve to the materialized 88-row core table, and schema checks pass. Use runs `1405`-`1406` as the repaired local 2D regression execution-packet guard; physical claims, GPU work, field transfer, field FWI, and 3D/HPC remain blocked. |
| 1407 | Repaired regression execution validator | Validate the run `1405` repair and run `1406` repaired dry run from a consumer perspective. Ten of ten checks pass with zero blocking failures, confirming the 88-row materialized core table, route counts, clean dry-run status, full-pack authority, sentinel fast-smoke-only status, and blocked physical/GPU/field/3D states. |
| 1408 | Repaired regression execution sensitivity | Stress-test the run `1407` validator with 13 scenarios. The exact repaired packet passes, while 12 damaged cases fail as expected for row-count drift, excluded-row drift, route-count drift, repair not ready, dry-run failure, row mismatch, schema failure, route-mode drift, sentinel replacing the full pack, GPU readiness, and field-transfer readiness. Zero unexpected outcomes. |
| 1409 | Repaired execution claim-boundary refresh | Convert runs `1405`-`1408` into a compact claim boundary. One claim is guarded-ready: the repaired 88-row regression execution packet with the 11-row fast-smoke sentinel. Four claims remain blocked: treating the original packet as resolved, letting the sentinel replace the full pack, promoting physical/broad-radius claims, and launching GPU/field/FWI/3D work. |
| 1410 | Repaired execution claim-boundary validator | Validate the run `1409` claim boundary from a consumer perspective. Eight of eight checks pass with zero blocking failures, confirming the single recommended repaired guard, 88-row core table, four original row-count mismatches, sentinel fast-smoke-only role, and blocked physical/GPU/field/FWI/3D states. |
| 1411 | Repaired execution claim-boundary sensitivity | Stress-test the run `1410` validator with 18 scenarios. The exact boundary passes, while 17 damaged cases fail as expected for count drift, missing/duplicate recommended guard, repaired row drift, original mismatch drift, sentinel/full-pack authority drift, physical-claim readiness, and GPU/field/FWI/3D readiness. Zero unexpected outcomes. |
| 1412 | Repaired execution CI adoption checklist | Convert the repaired dry run and guarded claim boundary into six adoption routes: two 11-row reduced-sentinel fast-smoke routes, two repaired 88-row full-core routes, one new-design-required physical/broad-radius route, and one blocked GPU/field/FWI/3D route. The full pack remains authoritative and the sentinel remains fast-smoke-only. |
| 1413 | Repaired execution CI adoption validator | Validate the run `1412` checklist from a consumer perspective. Six of six checks pass, confirming source readiness, route counts, dry-run table resolution, fast/full authority split, blocked route behavior, and blocked physical/GPU/field/FWI/3D states. Sensitivity remains required before treating the checklist as fully guarded. |
| 1414 | Repaired execution CI adoption sensitivity | Stress-test the run `1413` validator with 21 scenarios. The exact checklist passes, while 20 damaged cases fail as expected for source-readiness drift, route-count drift, dry-run failure, row-count mismatch, fast/full authority drift, blocked-route drift, sentinel/full-pack authority drift, and physical/GPU/field/FWI/3D readiness. Zero unexpected outcomes. |
| 1415 | Repaired execution CI route execution manifest | Convert the guarded adoption checklist into six CI route classes: two runnable reduced-sentinel fast-smoke routes, two runnable repaired 88-row full-core gates, one new-design-required route, and one blocked-current-evidence route. The sentinel remains fast-smoke-only, the full pack remains authoritative, and physical/GPU/field/FWI/3D claims remain blocked. |
| 1416 | Repaired execution CI route execution validator | Validate the run `1415` route execution manifest from a consumer perspective. Five of five checks pass, confirming six routes, four runnable jobs, two blocked/design-review routes, 11-row sentinel fast-smoke authority, 88-row full-core authority for boundary-sensitive routes, and blocked physical/GPU/field/FWI/3D claims. Sensitivity remains required before treating the route execution manifest as fully guarded. |
| 1417 | Repaired execution CI route execution sensitivity | Stress-test the run `1416` validator with 22 scenarios. The exact manifest passes, while 21 damaged cases fail as expected for source-readiness drift, route-count drift, job-class drift, fast/full authority drift, blocked-route drift, row-count mismatch, sentinel/full-pack authority drift, and physical/GPU/field/FWI/3D readiness. Zero unexpected outcomes. |
| 1418 | Repaired execution CI route command plan | Convert the guarded route manifest into a non-executed command plan. Four CPU pytest commands are executable: two reduced-sentinel fast-smoke commands and two repaired full-core gate commands. Two routes remain non-executable: one requires a new design and one is blocked by current 2D evidence. Commands are not executed; the full pack remains authoritative and physical/GPU/field/FWI/3D claims remain blocked. |
| 1419 | Repaired execution CI route command plan validator | Validate the run `1418` command plan from a consumer perspective. Five of five checks pass, confirming source readiness, six command rows, four runnable pytest command templates, two blocked non-executable routes with reasons, no command execution, fast/full table authority, and blocked physical/GPU/field/FWI/3D claims. Sensitivity remains required before treating the command plan as fully guarded. |
| 1420 | Repaired execution CI route command plan sensitivity | Stress-test the run `1419` command-plan validator with 33 scenarios. The exact command plan passes, while 32 damaged cases fail as expected for source-readiness drift, command-count drift, malformed executable commands, blocked routes with commands, missing blocked reasons, command-execution drift, fast/full authority drift, and false physical/GPU/field/FWI/3D promotion. Zero unexpected outcomes. Use runs `1418-1420` as the guarded non-executed CI command-plan package. |
| 1421 | Repaired execution CI route command execution smoke | Execute the four runnable CPU pytest command rows from run `1418`: two reduced-sentinel fast-smoke routes and two repaired full-core gate routes. All four commands pass, the two blocked routes remain unexecuted, the full 88-row pack remains authoritative for boundary-sensitive routes, and physical/GPU/field/FWI/3D claims remain blocked. |
| 1422 | Repaired execution CI route command execution validator | Validate the run `1421` execution smoke from a consumer perspective. Five of five checks pass, confirming six execution rows, four executed runnable commands, zero failed commands, two blocked routes not executed, fast/full command group counts, full-pack authority, and blocked physical/GPU/field/FWI/3D states. Sensitivity remains required before treating the execution smoke as fully guarded. |
| 1423 | Repaired execution CI route command execution sensitivity | Stress-test the run `1422` validator with 26 scenarios. The exact execution smoke passes, while 25 damaged cases fail as expected for count drift, missing rows, command-group drift, failed or unexecuted runnable commands, nonzero return codes, blocked-route execution, missing blocked reasons, full-pack authority drift, sentinel/full-pack drift, and false physical/GPU/field/FWI/3D readiness. Zero unexpected outcomes. Use runs `1421-1423` as the guarded executed CPU smoke package. |
| 1424 | Repaired execution CI adoption boundary | Convert runs `1421-1423` into the current CI adoption boundary. Four items are ready: reduced-sentinel fast smoke, repaired full-core gate, execution sensitivity guard, and full-pack authority. Three items remain blocked: sentinel replacing the full pack, physical/broad-radius claim promotion, and GPU/field/FWI/3D routes. Adopt the four passing CPU command routes only as local 2D CI guards. |
| 1425 | Repaired execution CI adoption boundary validator | Validate the run `1424` boundary from saved artifacts. Five of five checks pass, confirming boundary counts, scoped CPU CI adoption, passed and guarded execution evidence, full-pack authority, and blocked physical/GPU/field/FWI/3D claims. Sensitivity remains required before treating the adoption boundary as fully guarded. |
| 1426 | Repaired execution CI adoption boundary sensitivity | Stress-test the run `1425` validator with 23 scenarios. The exact boundary passes, while 22 damaged cases fail as expected for count drift, CI-route adoption drift, execution-evidence drift, full-pack authority drift, sentinel replacement, and false physical/GPU/field/FWI/3D readiness. Zero unexpected outcomes. Use runs `1424-1426` as the guarded local 2D CI adoption boundary. |
| 1427 | Physical claim design-gap audit | Convert the blocked physical and broad-radius claims into a next-design matrix. The corrected-state mechanism remains promoted inside the tested local setup, but broad radius and physical claims remain blocked by expanded-window failures and negative wrong-minus-truth margins. Five CPU design blocks are ready; broad GPU, field-transfer, field-FWI, and 3D/HPC escalation remain blocked. |
| 1428 | Expanded-window radius bracket | Execute the first CPU follow-up from the run `1427` design matrix. Near-neighbor radius increase passes through +1.75 mm across all six objectives, while far-neighbor radius decrease first fails at -1.00 mm under the `veryhigh` objective. Broad radius, physical, GPU, field-transfer, field-FWI, and 3D/HPC claims remain blocked. |
| 1429 | Expanded-window objective failure diagnosis | Diagnose the run `1428` failure mode from saved result and candidate tables. The failures are isolated to the `veryhigh` objective for far-neighbor radius decreases at -1.00 mm and -1.25 mm; the top wrong candidates at x=187-189 mm beat the truth x=190 mm by 0.0035579159181040702. Broad radius, physical, GPU, field-transfer, field-FWI, and 3D/HPC claims remain blocked. |
| 1430 | Far-radius veryhigh boundary refinement | Refine the far-neighbor `veryhigh` failure boundary under the same expanded window and six-objective audit. Far-neighbor radius decrease passes all objectives at -0.75 mm and first fails at -0.80 mm under `veryhigh`; the same x=187 mm wrong selection persists through -1.00 mm. Broad radius, physical, GPU, field-transfer, field-FWI, and 3D/HPC claims remain blocked. |
| 1431 | Post far-radius refinement claim boundary | Refresh the current local 2D claim boundary after runs `1427-1430`. Promoted claims are limited to the corrected-state local mechanism and objective-failure isolation to `veryhigh`; near-radius +1.75 mm and far-radius -0.75 to -0.80 mm are bounded observations only. Broad radius, physical, GPU, field-transfer, field-FWI, and 3D/HPC claims remain blocked. |
| 1432 | Far-radius veryhigh plateau diagnosis | Diagnose the wrong-x plateau behind the run `1430` far-radius `veryhigh` failures. For every failed perturbation, x=187, 188, and 189 mm tie at the best `veryhigh` misfit while truth x=190 mm ranks fourth by 0.0035579159181040702. Broad radius, physical, GPU, field-transfer, field-FWI, and 3D/HPC claims remain blocked. |
| 1433 | Far-radius veryhigh tie-policy audit | Test whether the run `1432` wrong-x plateau can be repaired by selection policy alone. Exact tie policies never select truth; truth appears only in a top-4 or tolerance-band accepted set spanning x=187-190 mm, so tie-policy-only and unique tolerance repair are blocked. Future 2D work should revise or downweight `veryhigh`; broad radius, physical, GPU, field-transfer, field-FWI, and 3D/HPC claims remain blocked. |
| 1434 | Far-radius objective revision policy audit | Audit candidate objective-revision policies for the run `1430` far-radius `veryhigh` failures. The five failures are isolated to `veryhigh`; dropping `veryhigh` or using a majority vote recovers truth on the saved failed cases, but only as a candidate repair requiring independent validation. Broad radius, physical, GPU, field-transfer, field-FWI, and 3D/HPC claims remain blocked. |
| 1435 | Objective revision holdout audit | Check the run `1434` candidate policies against the saved run `1428` expanded-window radius bracket. Across ten holdout perturbations, including the deeper -1.25 mm far-radius case, drop-`veryhigh` and majority vote recover truth while failures remain isolated to `veryhigh`. This supports a prospective validation sweep but does not promote revised objective, broad-radius, physical, GPU, field-transfer, field-FWI, or 3D/HPC claims. |
| 1436 | Objective revision prospective sweep | Execute a five-perturbation CPU sweep for the drop-`veryhigh` and majority-vote candidates. `veryhigh` fails at far -1.10 mm, far -1.40 mm, and near +2.00 mm, while all non-`veryhigh` objectives select truth across all five cases. Drop-`veryhigh` and majority-vote policies recover truth across the sweep; broad-radius, physical, GPU, field-transfer, field-FWI, and 3D/HPC claims remain blocked pending a claim-boundary refresh. |
| 1437 | Post objective-revision claim boundary | Refresh the local 2D claim boundary after the run `1436` prospective sweep. The revised local objective policy is validated for the current local setup, and `veryhigh` instability is a bounded observation across far -1.10 mm, far -1.40 mm, and near +2.00 mm. Broad-radius, physical, GPU, field-transfer, field-FWI, and 3D/HPC claims remain blocked. |
| 1438 | Post objective-revision claim-boundary validator | Validate the saved run `1437` claim boundary from artifacts. Six of six checks pass, confirming the one local-ready claim, one bounded `veryhigh` observation, three blocked downstream claims, matching veryhigh failure labels, and false broad/physical/GPU/field/FWI/3D readiness. Sensitivity remains required before treating this validator as guarded. |
| 1439 | Post objective-revision claim-boundary sensitivity | Stress-test the run `1438` validator. The exact run `1437` boundary passes, while 30 damaged variants fail as expected for row drift, status drift, promotion drift, repair-policy support drift, missing `veryhigh` labels, summary-count drift, local-validation drift, and false broad/physical/GPU/field/FWI/3D readiness. Zero unexpected outcomes. Use runs `1437-1439` as the guarded local 2D objective-revision claim boundary. |
| 1440 | Objective-revision adoption checklist | Convert the guarded run `1437-1439` claim boundary into six local-use routes: drop-`veryhigh` as the preferred local objective policy, majority vote as a local cross-check, `veryhigh` as diagnostic-only, one new-design-required broad-radius route, and two blocked physical/GPU/field/FWI/3D routes. Do not promote broad-radius, physical-transfer, GPU, field-FWI, or 3D/HPC claims from this branch. |
| 1441 | Objective-revision adoption checklist validator | Validate the saved run `1440` checklist from artifacts. Seven of seven checks pass, confirming the two local-use routes, one bounded `veryhigh` diagnostic route, one new-design-required route, two blocked routes, and false broad/physical/GPU/field/FWI/3D readiness. Sensitivity remains required before treating this validator as guarded. |
| 1442 | Objective-revision adoption checklist sensitivity | Stress-test the run `1441` validator. The exact run `1440` checklist passes, while 36 damaged variants fail as expected for route-count drift, route status drift, local-use flag drift, missing `veryhigh` labels, blocked-route flag drift, guard-readiness drift, and false broad/physical/GPU/field/FWI/3D readiness. Zero unexpected outcomes. Use runs `1440-1442` as the guarded local 2D objective-revision adoption checklist. |
| 1443 | Objective-revision execution manifest | Convert the guarded adoption checklist into a local execution manifest. Primary selection uses five non-`veryhigh` objectives, majority vote remains a local cross-check across all six objectives, and `veryhigh` is diagnostic only. Broad-radius, physical, GPU, field-transfer, field-FWI, and 3D/HPC routes remain blocked. |
| 1444 | Objective-revision execution manifest validator | Validate the saved run `1443` execution manifest from artifacts. Nine of nine checks pass, confirming that primary selection excludes `veryhigh`, majority vote retains all six objective labels as a cross-check, `veryhigh` is diagnostic-only, blocked routes are not executable, and broad/physical/GPU/field/FWI/3D readiness remains false. Sensitivity remains required before treating this validator as guarded. |
| 1445 | Objective-revision execution manifest sensitivity | Stress-test the run `1444` validator. The exact run `1443` manifest passes, while 36 damaged variants fail as expected for `veryhigh` primary-policy promotion, cross-check objective drift, diagnostic flag drift, blocked-route executability drift, objective-role drift, summary drift, downstream promotion, figure-validation drift, and script-snapshot drift. Zero unexpected outcomes. Use runs `1443-1445` as the guarded local 2D objective-revision execution manifest. |
| 1446 | Objective-revision execution materialization | Materialize the guarded run `1443-1445` execution manifest against the saved run `1436` prospective sweep evidence. The drop-`veryhigh` primary route and majority-vote cross-check each recover truth on all five saved cases; the `veryhigh` diagnostic route recovers two of five and fails three. Broad-radius, physical, GPU, field-transfer, field-FWI, and 3D/HPC routes remain blocked. |
| 1447 | Objective-revision execution materialization validator | Validate the saved run `1446` materialization from artifacts. Nine of nine checks pass, confirming the route, case, diagnostic, figure, and script-snapshot files. The drop-`veryhigh` primary route and majority-vote cross-check remain materialized, `veryhigh` remains diagnostic-only with three failures, and broad/physical/GPU/field/FWI/3D routes remain blocked. |
| 1448 | Objective-revision execution materialization sensitivity | Stress-test the run `1447` validator. The exact run `1446` materialization passes, while 47 damaged variants fail as expected for primary-route drift, majority-vote drift, `veryhigh` diagnostic drift, blocked-route promotion, case-table drift, summary drift, downstream promotion, figure-validation drift, and script-snapshot drift. Zero unexpected outcomes. Use runs `1446-1448` as the guarded local 2D objective-revision execution materialization. |
| 1449 | Objective-revision coverage-gap audit | Audit the guarded run `1446-1448` materialization boundary. Drop-`veryhigh` and majority vote each recover truth on all five saved prospective cases, but the evidence covers only one local geometry family, one noise seed, nominal source frequency/amplitude, two time-shift states, and a small near/far radius perturbation set. Four CPU next-design blocks are ready; broad-radius, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked. |
| 1450 | Objective-revision boundary-extension stress | Execute a seven-perturbation CPU stress extension under the same local synthetic geometry. Drop-`veryhigh` and majority vote recover 5 of 7 cases, but both fail at far radius -1.80 mm and the combined near +1.90 mm plus far -1.60 mm perturbation because all six objectives select the wrong x=187 mm candidate. Preserve the revised-policy result only inside its narrow validated scope; broad-radius, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked. |
| 1451 | Objective-revision boundary-extension stress validator | Validate the saved run `1450` stress result. Seven of seven checks pass, confirming three all-objectives-truth cases, two `veryhigh`-only failures, two all-objectives failures, zero mixed failures, and only 5 of 7 stress cases recovered by drop-`veryhigh` or majority vote. The failed stress result is guarded and non-promoted; broad-radius, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked. |
| 1452 | Objective-revision boundary-extension stress sensitivity | Stress-test the run `1451` validator. The exact run `1450` stress result passes, while 14 damaged variants fail as expected for objective-row drift, failure-taxonomy drift, false candidate-policy promotion, downstream gate promotion, blank figure validation, missing script snapshots, and policy-row drift. Zero unexpected outcomes. Use runs `1450-1452` as the guarded failed boundary-extension stress block. |
| 1453 | Objective-revision boundary-extension failure anatomy | Audit the saved run `1450` stress result to explain the failure boundary. Three cases have all objectives selecting truth, two cases are isolated `veryhigh` failures recoverable by drop-`veryhigh` and majority vote, and two harder cases are all-objective wrong-x locks where all six objectives select x=187 mm. Objective filtering cannot repair those wrong-lock cases, so broad-radius, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked pending a new disambiguating design. |
| 1454 | Wrong-X lock candidate-rank audit | Audit the candidate ranks behind the two all-objective wrong-x locks from run `1453`. All 12 wrong-lock objective rows share the same x=187-188-189 top plateau, while the truth x=190 mm sits outside the plateau at rank 4 or 5. The reported x=187 result is the lowest-x representative of that plateau, not a unique isolated minimum. The next 2D design needs a disambiguator that separates x=187-189 from x=190; objective filtering alone is not enough. |
| 1455 | Wrong-X lock ambiguity policy audit | Test whether the x=187-189 plateau can be repaired by selection policy alone. Lowest-plateau, highest-plateau, and midpoint point selections recover truth in 0 of 12 wrong-lock objective rows. Truth is included in all rows only by nonunique ambiguity sets: top-5 candidates or a 0.025 misfit-tolerance band. Use these policies only as ambiguity flags, not exact repairs; broad-radius, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked. |
| 1456 | Wrong-X lock dense-scan probe | Run a bounded CPU probe with extra scan positions at 178, 190, and 202 mm around the x=187-190 ambiguity. The correct-state control still passes all objectives. The far-radius -1.80 mm case improves from an all-objective wrong lock to a `veryhigh`-only failure, but the combined near +1.90 mm plus far -1.60 mm case remains an all-objective wrong-x failure. Dense scanning partially helps but does not resolve the branch; broad-radius, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked. |
| 1457 | Combined lock neighbor-radius disambiguation probe | Run a bounded CPU probe on the hard combined near/far perturbation with four neighbor-radius model assumptions. The combined perturbed-neighbor model and far-corrected-only model both keep all six objectives locked on wrong x=187 mm. Correcting the near-neighbor radius restores all six objectives to truth x=190 mm, whether or not the far neighbor remains perturbed. This identifies near-neighbor radius error as the local mechanism to address next; broad-radius, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked. |
| 1458 | Near-neighbor radius-error threshold probe | Sweep the near-neighbor radius error for the hard combined wrong-lock case while keeping the far neighbor at -1.6 mm. With near-neighbor error at 0.0 mm, all six objectives select truth x=190 mm. At +0.5 and +1.0 mm, four objectives fail while base/highband still select truth. At +1.5 and +1.9 mm, all six objectives lock onto x=187 mm. This makes the local mechanism threshold-like and near-neighbor dominated; broad-radius, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked. |
| 1459 | Near-neighbor radius-error threshold validator | Validate the saved run `1458` threshold result from artifacts. Seven of seven checks pass, confirming the source counts, +0.0 mm last all-objectives-truth threshold, +0.5 mm first any-failure threshold, +1.5 mm first all-objective-failure threshold, failure counts by near-radius delta, blocked downstream states, figure output, and script snapshots. Use this as the guarded local near-neighbor threshold result. |
| 1460 | Near-neighbor radius-error threshold sensitivity | Stress-test the run `1459` validator with 16 scenarios. The exact run `1458` threshold result passes, while 15 damaged variants fail as expected for source-count drift, threshold-value drift, failure-count drift, taxonomy drift, false downstream promotion, figure drift, and missing script snapshots. Use runs `1458-1460` as the guarded local near-neighbor threshold block; broad-radius, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked. |
| 1461 | Near/far radius-error interaction grid probe | Extend the guarded near-neighbor threshold into a 5 by 3 near/far radius-error grid under the same dense scan. With no far-radius error, partial failure starts at near +1.5 mm. With far-radius errors of -0.8 mm or -1.6 mm, partial failure starts earlier at near +0.5 mm. Across all three far settings, all-objective wrong-lock failure starts at near +1.5 mm. This strengthens the local mechanism story but keeps broad-radius, physical-transfer, GPU, field-FWI, and 3D/HPC claims blocked. |
| 1462 | Near/far radius-error interaction grid validator | Validate the saved run `1461` interaction grid from artifacts. Seven of seven checks pass, confirming the 15-model grid, exact failure-count heatmap, partial-failure threshold shift by far-radius condition, stable all-objective failure threshold at near +1.5 mm, blocked downstream states, figure output, and script snapshots. Use run `1462` as the validator for the local interaction-grid result. |
| 1463 | Near/far radius-error interaction grid sensitivity | Stress-test the run `1462` validator with 15 scenarios. The exact run `1461` grid passes, while 14 damaged variants fail as expected for row-count drift, summary-count drift, failure-grid drift, threshold drift, taxonomy drift, downstream promotion, figure drift, and script-snapshot drift. Use runs `1461-1463` as the guarded local near/far interaction block; broad-radius, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked. |
| 1464 | Post near/far interaction claim boundary | Refresh the local 2D claim boundary after the guarded near/far interaction block. Three local mechanism claims are guarded: near-neighbor threshold behavior, far-neighbor shifting of partial failures, and near-dominated all-objective wrong-lock onset in the tested grid. Three broader claims remain blocked: broad radius tolerance, physical/field transfer, and GPU/field-FWI/3D-HPC escalation. |
| 1465 | Post near/far interaction claim-boundary validator | Validate the saved run `1464` claim boundary from artifacts. Seven of seven checks pass, confirming claim counts, three ready guarded-local claims, three blocked broader claims with reasons, source guard readiness, blocked downstream states, figure output, and script snapshots. Use run `1465` as the validator for the post near/far interaction claim boundary. |
| 1466 | Near/far interaction generalization design | Define the next independent CPU checks required before promoting the local near/far mechanism beyond its current scope. The design packet has eight cases across five axes: target position, target depth, neighbor spacing, source model, and acquisition layout. No case executes now; broad-radius, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked. |
| 1467 | Near/far interaction generalization design validator | Validate the saved run `1466` design packet. Seven of seven checks pass, confirming eight future CPU sweep cases, five independent design axes, passed acceptance checks, zero immediate execution, blocked downstream states, figure output, and script snapshots. Use run `1467` as the guarded next-design packet before executing any generalization sweep. |
| 1468 | Near/far position generalization probe | Execute the first CPU generalization sweep from the guarded near/far design block. The target family is translated by -20 mm, 0 mm, and +20 mm while keeping the shifted truth inside the local candidate grid. The zero-shift slice reproduces the run `1461` severe all-objective failure threshold at near +1.5 mm, but the translated slices produce only partial failures and no all-objective failure models. This shows the severe boundary is local to the original aperture position rather than a broad position-invariant rule; broad-radius, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked. |
| 1469 | Near/far position generalization validator | Validate the saved run `1468` position generalization probe from artifacts. Seven of seven checks pass, confirming source counts, the exact position-by-near-by-far failure cube, threshold summaries, translated-position softening of all-objective failures, blocked downstream states, figure output, and script snapshots. Use runs `1468-1469` as the first guarded target-position generalization block; the severe all-objective boundary is aperture-position dependent in the tested configuration. |
| 1470 | Near/far depth generalization probe | Execute the target-depth generalization sweep from the guarded near/far design block. The target family is moved 10 mm shallower, kept at baseline depth, and moved 10 mm deeper while the shifted truth remains in the candidate grid. The shallower slice has no failures in the tested grid, the baseline slice reproduces run `1461`, and the deeper slice keeps severe all-objective failures only when far-neighbor radius error is present. This shows the near/far mechanism severity is also depth-dependent; broad-radius, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked. |
| 1471 | Near/far depth generalization validator | Validate the saved run `1470` depth generalization probe from artifacts. Seven of seven checks pass, confirming source counts, the exact depth-by-near-by-far failure cube, threshold summaries, the shallower no-failure slice, the deeper far-error all-objective failure pattern, blocked downstream states, figure output, and script snapshots. Use runs `1470-1471` as the guarded target-depth generalization block; the severe boundary is both position-dependent and depth-dependent in the tested configurations. |
| 1472 | Post near/far generalization claim boundary | Refresh the local 2D claim boundary after the guarded position and depth generalization blocks. Four claims are guarded local/design evidence and three remain blocked. The near/far mechanism is real in the tested local synthetic setup, but severe all-objective failure is not position-invariant or depth-invariant. Broad-radius tolerance, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked until additional spacing, source, acquisition, or measured-field evidence closes those gaps. |
| 1473 | Post near/far generalization claim-boundary validator | Validate the saved run `1472` claim-boundary artifact. Six of six checks pass, confirming claim counts, guarded versus blocked claim states, position- and depth-dependent boundary flags, blocked downstream states, figure output, and script snapshots. Use run `1473` as the validator for the current 2D near/far claim boundary; broad-radius, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked. |
| 1474 | Near/far spacing generalization probe | Execute the neighbor-spacing generalization sweep from the guarded near/far design block. Across 45 spacing/near/far models, 23 recover truth across all objectives, 22 have at least one objective failure, and 12 have all-objective failures. Narrower and baseline spacing show far-radius errors moving partial failure onset from near +1.5 mm to near +0.5 mm, while wider spacing suppresses far-error-driven failures across the tested near-radius range. The severe boundary is therefore spacing dependent; broad-radius, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked. |
| 1475 | Near/far spacing generalization validator | Validate the saved run `1474` spacing-generalization result from artifacts. Eight of eight checks pass, confirming the 45-model source grid, 270 objective rows, 1080 candidate rows, exact 23/22/12 truth/failure taxonomy, spacing-dependent first-failure threshold maps, blocked downstream states, figure output, and script snapshots. Use runs `1474-1475` as the guarded neighbor-spacing generalization block. |
| 1476 | Near/far spacing generalization sensitivity | Stress-test the run `1475` validator. The exact run `1474` result passes, while nine damaged variants fail as expected for source-policy drift, row-count drift, failure-taxonomy drift, threshold drift, false downstream promotion, figure-validation drift, and script-snapshot drift. Zero unexpected outcomes. Use runs `1474-1476` as the guarded neighbor-spacing generalization block. |
| 1477 | Post spacing generalization claim boundary | Refresh the local 2D claim boundary after position, depth, and spacing generalization. Six claims are guarded local/design evidence and three remain blocked. The near/far mechanism is real in the tested local synthetic setup, but severe all-objective failure is not position-invariant, depth-invariant, or spacing-invariant. Broad-radius tolerance, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked until source, acquisition, or measured-field evidence closes those gaps. |
| 1478 | Post spacing generalization claim-boundary validator | Validate the saved run `1477` claim boundary from saved artifacts. Seven of seven checks pass, confirming claim counts, spacing result counts, position/depth/spacing boundary flags, blocked downstream states, figure output, and script snapshots. Use run `1478` as the validator for the current 2D near/far generalization claim boundary. |
| 1479 | Post spacing generalization claim-boundary sensitivity | Stress-test the run `1478` validator. The exact run `1477` boundary passes, while ten damaged variants fail as expected for claim-count drift, spacing-count drift, boundary-flag drift, blocked-claim promotion, downstream promotion, figure-validation drift, and script-snapshot drift. Zero unexpected outcomes. Use runs `1477-1479` as the guarded current 2D near/far generalization claim-boundary block. |
| 1480 | Near/far source-model generalization probe | Execute the source-model axis from the guarded run `1466` design. The CPU probe repeats the near/far radius-error grid for matched `-50 ps` and `+50 ps` source time-shift variants: 30 grid models, 180 objective rows, and 720 candidate rows. The first any-objective failure threshold is unchanged between source variants, and far-error cases still reach all-objective failure at near +1.5 mm. Positive time shift only softens the far-error-free all-objective failure at the largest near-radius errors. Broad-radius, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked pending validation and the remaining acquisition-layout axis. |
| 1481 | Near/far source-model generalization validator | Validate the saved run `1480` source-model generalization result from artifacts. Eight of eight checks pass, confirming source/grid row counts, 10 all-objectives-truth models, 20 any-failure models, 10 all-objective-failure models, stable source/far threshold maps, blocked downstream states, figure output, and script snapshots. Sensitivity hardening remains required before treating the source-model validator as guarded. |
| 1482 | Near/far source-model generalization sensitivity | Stress-test the run `1481` validator. The exact run `1480` source-model result passes, while ten damaged variants fail as expected for source/count drift, result-row drift, failure-taxonomy drift, threshold-map drift, downstream promotion, figure-validation drift, and script-snapshot drift. Zero unexpected outcomes. Use runs `1480-1482` as the guarded 2D source-model generalization block. |
| 1483 | Near/far acquisition-layout generalization probe | Execute the acquisition-layout axis from the guarded run `1466` design. The CPU probe repeats the near/far radius-error grid for matched 20 mm and 45 mm Tx/Rx offsets: 30 grid models, 180 objective rows, and 720 candidate rows. The 20 mm layout reproduces far-error-driven failures, while the 45 mm layout suppresses far-error-driven failures in this grid and leaves failures only for far=0 at near +1.5/+1.9 mm. Broad-radius, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked pending validation and integration with the position, depth, spacing, and source-model blocks. |
| 1484 | Near/far acquisition-layout generalization validator | Validate the saved run `1483` acquisition-layout result from artifacts. Eight of eight checks pass, confirming Tx/Rx offset/grid row counts, 18 all-objectives-truth models, 12 any-failure models, 8 all-objective-failure models, stable acquisition/far threshold maps, blocked downstream states, figure output, and script snapshots. Sensitivity hardening remains required before integrating the acquisition-layout result into the full near/far claim boundary. |
| 1485 | Near/far acquisition-layout generalization sensitivity | Stress-test the run `1484` validator. The exact run `1483` acquisition-layout result passes, while ten damaged variants fail as expected for offset/count drift, row-count drift, failure-taxonomy drift, threshold-map drift, downstream promotion, figure-validation drift, and script-snapshot drift. Zero unexpected outcomes. Use runs `1483-1485` as the guarded 2D acquisition-layout generalization block. |
| 1486 | Post full generalization claim boundary | Refresh the local 2D near/far claim boundary after all five planned axes: position, depth, spacing, source timing, and acquisition layout. Twelve claims are recorded: nine guarded local/design observations and three blocked downstream claims. All five axes are ready, but broad invariant radius-tolerance, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked because the severe failure boundary changes with geometry and acquisition layout. |
| 1487 | Post full generalization claim-boundary validator | Validate the saved run `1486` full-generalization claim boundary from saved artifacts. Nine of nine checks pass, confirming claim counts, five-axis readiness, spacing/source/acquisition result counts, source threshold behavior, acquisition far-error suppression, blocked downstream states, figure output, and script snapshots. Use run `1487` as the validator for the full local 2D near/far generalization boundary. |
| 1488 | Post full generalization claim-boundary sensitivity | Stress-test the run `1487` validator. The exact run `1486` boundary passes, while twelve damaged variants fail as expected for policy-label drift, claim-count drift, axis-readiness drift, spacing/source/acquisition result drift, source-threshold drift, acquisition-suppression drift, blocked-claim promotion, downstream promotion, figure-validation drift, and script-snapshot drift. Zero unexpected outcomes. Use runs `1486-1488` as the guarded full 2D near/far generalization claim-boundary block. |
| 1489 | Near/far acquisition-layout offset-transition probe | Expand the guarded acquisition-layout check from two Tx/Rx offsets to five offsets: 20, 30, 35, 40, and 45 mm. The CPU run evaluates 75 grid models, 450 objective rows, and 1800 candidate rows. Any-objective far-error failures persist at 30, 35, and 40 mm and are first fully suppressed at 45 mm; all-objective far-error failures are present at 20 and 30 mm and suppressed from 35 mm onward. Broad-radius, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked pending validation. |
| 1490 | Near/far acquisition-layout offset-transition validator | Validate the saved run `1489` five-offset transition result from artifacts. Eight of eight checks pass, confirming run identity, 75 grid models, 450 objective rows, 1800 candidate rows, 45 all-objectives-truth models, 30 any-failure models, 18 all-objective failure models, the first-suppressed far-error offset at 45 mm, blocked downstream states, figure validation, and script snapshots. |
| 1491 | Near/far acquisition-layout offset-transition sensitivity | Stress-test the run `1490` validator. The exact run `1489` artifact set passes, while twelve damaged variants fail for offset-list drift, offset/count drift, row-count drift, taxonomy drift, threshold drift, suppression-threshold drift, downstream promotion, figure-validation drift, and script-snapshot drift. Use runs `1489-1491` as the guarded acquisition-layout offset-transition block; broad-radius, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked pending claim-boundary integration. |
| 1492 | Post offset-transition claim boundary | Refresh the local 2D near/far claim boundary after the five-offset acquisition-layout transition block. Thirteen claims are recorded: ten guarded local/design observations and three blocked downstream claims. The refined acquisition result shows two thresholds in the tested grid: all-objective far-error failures clear by 35 mm, while any-objective far-error failures persist through 40 mm and clear at 45 mm. Broad invariant radius-tolerance, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked. |
| 1493 | Post offset-transition claim-boundary validator | Validate the saved run `1492` refined claim boundary from artifacts. Nine of nine checks pass, confirming claim counts, five-axis readiness, spacing/source/acquisition counts, the two-threshold acquisition-layout result, blocked claims, downstream guardrails, figure validation, and script snapshots. |
| 1494 | Post offset-transition claim-boundary sensitivity | Stress-test the run `1493` validator. The exact run `1492` boundary passes, while fifteen damaged variants fail for policy-label drift, claim-count drift, axis-readiness drift, spacing-count drift, source-threshold drift, acquisition suppression and transition-threshold drift, blocked-claim promotion, downstream promotion, figure-validation drift, and script-snapshot drift. Use runs `1492-1494` as the guarded refined local 2D near/far offset-transition claim-boundary block. |
| 1495 | Near/far acquisition-layout offset-transition fine probe | Refine the 40-45 mm transition from run `1489` with six Tx/Rx offsets: 40, 41, 42, 43, 44, and 45 mm. Across 90 grid models, 540 objective rows, and 2160 candidate rows, far-error any-objective failures persist through 44 mm and first disappear at 45 mm for both far-error settings. All-objective far-error failures are absent throughout the 40-45 mm fine sweep. Broad-radius, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked pending validation. |
| 1496 | Near/far acquisition-layout offset-transition fine validator | Validate the saved run `1495` fine offset-transition probe from artifacts. Eight of eight checks pass, confirming source counts, failure taxonomy counts, first any-failure and all-objective failure threshold maps, the 45 mm first-suppressed far-error offset, blocked downstream states, figure validation, and script snapshots. Sensitivity hardening remains required before claim-boundary integration. |
| 1497 | Near/far acquisition-layout offset-transition fine sensitivity | Stress-test the run `1496` validator. The exact run `1495` fine transition result passes, while twelve damaged variants fail as expected for offset-list drift, offset/count drift, row-count drift, taxonomy drift, threshold drift, suppression-threshold drift, downstream promotion, figure-validation drift, and script-snapshot drift. Use runs `1495-1497` as the guarded fine 2D acquisition-layout offset-transition block. |
| 1498 | Post fine offset-transition claim boundary | Integrate the guarded fine transition into the local 2D near/far claim boundary. The boundary now has 14 claims: 11 guarded and three blocked. The new guarded acquisition claim is that far-error any-objective failures persist through 44 mm and first clear at 45 mm in the tested 40-45 mm sweep, while all-objective far-error failures remain absent throughout that sweep. Broad-radius, physical-transfer, GPU, field-FWI, and 3D/HPC claims remain blocked. |
| 1499 | Post fine offset-transition claim-boundary validator | Validate the saved run `1498` fine-refined claim boundary from artifacts. Eight of eight checks pass, confirming 14 total claims, 11 guarded claims, three blocked downstream claims, 90 fine grid models, 540 objective rows, 2160 candidate rows, far-error any-objective failures persisting through 44 mm and clearing at 45 mm, blocked broad/physical/GPU/field/3D states, figure validation, and script snapshots. |
| 1500 | Post fine offset-transition claim-boundary sensitivity | Stress-test the run `1499` validator. The exact run `1498` claim boundary passes, while twenty damaged variants fail for policy-label drift, claim-count drift, base-boundary drift, design-axis drift, fine-validation drift, fine offset-list drift, fine row-count drift, fine taxonomy drift, fine threshold drift, fine claim-row support drift, blocked-claim promotion, downstream promotion, figure-validation drift, and script-snapshot drift. Use runs `1498-1500` as the guarded fine-refined local 2D near/far offset-transition claim-boundary block. |
| 1501 | Fine offset-transition margin audit | Audit saved per-objective misfit margins around the guarded 40-45 mm acquisition-layout transition. The 24 far-error stress models have negative minimum margins before 45 mm and positive minimum margins at 45 mm: maximum pre-45 minimum margin is `-3.74885e-4`, while the 45 mm minimum margin is `2.29050e-4`. This confirms that the 45 mm clearing is visible in the saved misfit margins, not only in binary pass/fail labels. Broad physical, GPU, field-transfer, field-FWI, and 3D/HPC claims remain blocked. |
| 1502 | Fine offset-transition margin-audit validator | Validate the saved run `1501` margin audit from artifacts. Eight of eight checks pass, confirming the guarded source claim boundary, 90 model-margin rows, 24 transition-stress rows, 58 all-objectives-truth models, 32 any-failure models, 12 all-objective failure models, negative pre-45 minimum margin, positive 45 mm minimum margin, blocked downstream states, figure validation, and script snapshots. |
| 1503 | Fine offset-transition margin-audit sensitivity | Stress-test the run `1502` validator. The exact run `1501` margin audit passes, while thirteen damaged variants fail for source identity drift, guarded-claim drift, row-count drift, taxonomy drift, transition-count drift, margin-sign drift, downstream promotion, figure-validation drift, and script-snapshot drift. Use runs `1501-1503` as the guarded fine-transition margin-audit block. |
| 1504 | Post fine-margin claim boundary | Integrate the guarded fine-transition margin audit into the local 2D near/far claim boundary. The boundary now has 15 claims: 12 guarded and three blocked. The added guarded claim states that the tested far-error stress cases have negative minimum margins before 45 mm and positive minimum margins at 45 mm. Broad physical, GPU, field-transfer, field-FWI, and 3D/HPC claims remain blocked. |
| 1505 | Post fine-margin claim-boundary validator | Validate the saved run `1504` post-margin claim boundary from artifacts. Seven of seven checks pass, confirming 15 claims, 12 guarded claims, three blocked downstream claims, the positive-margin claim row, negative pre-45 minimum margin, positive 45 mm minimum margin, blocked downstream states, figure validation, and script snapshots. |
| 1506 | Post fine-margin claim-boundary sensitivity | Stress-test the run `1505` validator. The exact run `1504` boundary passes, while thirteen damaged variants fail for claim-count drift, base-count drift, margin-claim drift, margin metric drift, blocked-claim promotion, downstream promotion, figure-validation drift, and script-snapshot drift. Use runs `1504-1506` as the guarded post-margin local 2D claim-boundary block. |
| 1507 | Fine transition crossing estimate audit | Estimate the zero-margin crossing inside the guarded 44-45 mm acquisition-layout transition from saved margins. The crossing is at about 44.621 mm, leaving only 0.379 mm clearance at the 45 mm layout. The four stress curves are identical in the saved artifact. Treat 45 mm as a narrow local clearance in the tested synthetic setup, not as a broad acquisition rule; physical, GPU, field-transfer, field-FWI, and 3D/HPC claims remain blocked. |
| 1508 | Fine transition crossing estimate validator | Validate the saved run `1507` crossing estimate from artifacts. Eight of eight checks pass, confirming the 44.621 mm crossing estimate, zero crossing-range across the four stress curves, 0.379 mm clearance at 45 mm, identical margin and failure curves, narrow-clearance status, blocked downstream states, figure output, and script snapshots. |
| 1509 | Fine transition crossing estimate sensitivity | Stress-test the run `1508` validator. The exact run `1507` crossing estimate passes, while 19 damaged variants fail as expected for identity drift, count drift, crossing-value drift, curve-identity drift, guarded-state drift, downstream promotion, figure-validation drift, and script-snapshot drift. Use runs `1507-1509` as the guarded fine-transition crossing-estimate block. |
| 1510 | Post crossing-estimate claim boundary | Integrate the guarded crossing estimate into the local 2D claim boundary. The boundary now has 16 claims: 13 guarded and three blocked. The added claim states that the zero-margin crossing is about 44.621 mm and the 45 mm layout has only about 0.379 mm local clearance. Broad physical, GPU, field-transfer, field-FWI, and 3D/HPC claims remain blocked. |
| 1511 | Post crossing-estimate claim-boundary validator | Validate the saved run `1510` claim boundary from artifacts. Seven of seven checks pass, confirming claim counts, the new crossing-estimate claim row, crossing metrics, boundary-ready state, blocked claim rows, downstream blocked states, figure output, and script snapshots. |
| 1512 | Post crossing-estimate claim-boundary sensitivity | Stress-test the run `1511` validator. The exact run `1510` boundary passes, while 17 damaged variants fail as expected for claim-count drift, crossing-claim row drift, crossing-metric drift, boundary-ready drift, blocked-claim drift, downstream promotion, figure-validation drift, and script-snapshot drift. Use runs `1510-1512` as the guarded post-crossing local 2D claim-boundary block. |
| 1513 | Near/far acquisition-layout midpoint probe | Directly probe fractional Tx/Rx offsets around the 44.621 mm crossing estimate. The full CPU selection pipeline still shows failures at 44.625 mm and 44.75 mm, and the first suppression of the far-error cases remains at 45.0 mm. Treat the 44.621 mm value as a margin-only estimate, not as a promoted operating threshold; broad physical, GPU, field-transfer, field-FWI, and 3D/HPC claims remain blocked. |
| 1514 | Near/far acquisition-layout midpoint validator | Validate the saved run `1513` midpoint probe. Eight of eight checks pass, confirming counts, unique fractional Tx/Rx labels, failure-threshold maps, below-45 mm failure persistence, first far-error suppression at 45.0 mm, blocked downstream states, figure validation, and script snapshots. Use runs `1513-1514` as the guarded midpoint-probe correction block. |
| 1515 | Near/far acquisition-layout midpoint sensitivity | Stress-test the run `1514` midpoint-probe validator. The exact run `1513` probe passes, while 13 damaged variants fail as expected for policy drift, offset drift, fractional-label collision, row-count drift, taxonomy drift, below-45 failure removal, 45 mm suppression removal, suppression-offset drift, downstream promotion, figure drift, and script-snapshot drift. Use runs `1513-1515` as the guarded midpoint-probe correction block. |
| 1516 | Post-midpoint claim boundary | Integrate the guarded midpoint-probe correction into the local 2D near/far claim boundary. The boundary now has 17 claims: 14 guarded and three blocked. The 44.621 mm crossing is retained as a margin-only estimate, while the directly tested far-error suppression transition remains 45.0 mm. Broad physical, GPU, field-transfer, field-FWI, and 3D/HPC claims remain blocked. |
| 1517 | Post-midpoint claim-boundary validator | Validate the saved run `1516` corrected claim boundary. Eight of eight checks pass, confirming claim counts, the margin-only crossing-estimate row, the direct midpoint 45.0 mm transition row, midpoint metrics, blocked downstream rows, figure validation, and script snapshots. Sensitivity hardening remains required before closing the block. |
| 1518 | Post-midpoint claim-boundary sensitivity | Stress-test the run `1517` corrected claim-boundary validator. The exact run `1516` boundary passes, while 14 damaged variants fail as expected for claim-count drift, margin-only wording loss, midpoint-claim drift, suppression-offset drift, linear-threshold promotion, blocked-row drift, downstream promotion, figure drift, and script-snapshot drift. Use runs `1516-1518` as the guarded post-midpoint local 2D claim-boundary block. |
| 1519 | Acquisition-layout high-side stability probe | Directly test whether the sampled 45.0 mm Tx/Rx far-error suppression remains stable above 45.0 mm. The CPU run evaluates 75 grid models, 450 objective rows, and 1800 candidate rows at 45.0, 45.125, 45.25, 45.5, and 46.0 mm. The 45.0 mm point still suppresses the negative far-radius cases, but failures reappear at 45.125 mm and remain present at higher offsets for near +1.5/+1.9 mm. This makes the acquisition-layout effect non-monotonic in the tested local synthetic pipeline; broad physical, GPU, field-transfer, field-FWI, and 3D/HPC claims remain blocked. |
| 1520 | Acquisition-layout high-side stability validator | Validate the saved run `1519` high-side result from artifacts. Nine of nine checks pass, confirming source counts, threshold maps, the sampled 45.0 mm suppression point, the 45.125 mm failure reappearance point, blocked downstream states, figure validation, and script snapshots. Use runs `1519-1520` as the guarded high-side correction block; sensitivity hardening remains required before claim-boundary integration. |
| 1521 | Acquisition-layout high-side stability sensitivity | Stress-test the run `1520` high-side validator. The exact run `1519` artifact passes, while 14 damaged variants fail as expected for policy drift, offset drift, label collision, row-count drift, taxonomy drift, removed 45.0 mm suppression, removed or shifted 45.125 mm reappearance, downstream promotion, figure drift, and script-snapshot drift. Use runs `1519-1521` as the guarded high-side correction block; the monotonic larger-offset safety claim remains blocked. |
| 1522 | Post high-side stability claim boundary | Integrate the guarded high-side correction into the local 2D claim boundary. The boundary now has 18 claims: 15 guarded and three blocked. The added claim states that 45.0 mm remains a sampled far-error suppression point, but failures reappear at 45.125 mm and persist at larger tested offsets, so a monotonic larger-offset safety claim is blocked. Broad physical, GPU, field-transfer, field-FWI, and 3D/HPC claims remain blocked. |
| 1523 | Post high-side stability claim-boundary validator | Validate the saved run `1522` claim boundary from artifacts. Eight of eight checks pass, confirming claim counts, the high-side correction claim row, the updated midpoint claim row, the 45.0 mm suppression and 45.125 mm reappearance metrics, blocked downstream states, figure validation, and script snapshots. Sensitivity hardening remains required before closing the block. |
| 1524 | Post high-side stability claim-boundary sensitivity | Stress-test the run `1523` validator. The exact run `1522` boundary passes, while 15 damaged variants fail as expected for claim-count drift, high-side row drift, missing 45.125 mm evidence, false stability, monotonic safety promotion, blocked-row drift, downstream promotion, figure drift, and script-snapshot drift. Use runs `1522-1524` as the guarded post-high-side local 2D claim-boundary block. |
| 1525 | High-side reappearance edge probe | Refine the 45.0-45.125 mm high-side transition with six Tx/Rx offsets. The sampled 45.0 mm point still suppresses far -0.8 and -1.6 mm failures, but both far-error failures reappear at the first tested offset above 45.0 mm: 45.015625 mm. Treat 45.0 mm as a narrow sampled local point, not a monotonic larger-offset safety threshold; broad physical, GPU, field-transfer, field-FWI, and 3D/HPC claims remain blocked pending validation. |
| 1526 | High-side reappearance edge validator | Validate the saved run `1525` edge probe from artifacts. Nine of nine checks pass, confirming source identity, 90 grid models, 540 objective rows, 2160 candidate rows, exact threshold maps, 45.0 mm suppression, 45.015625 mm reappearance, blocked downstream states, figure validation, and script snapshots. Use run `1526` as the validator for the narrow high-side edge result. |
| 1527 | High-side reappearance edge sensitivity | Stress-test the run `1526` validator. The exact run `1525` artifact set passes, while 14 damaged variants fail as expected for policy drift, offset drift, label collision, row-count drift, taxonomy drift, removed 45.0 mm suppression, removed or shifted 45.015625 mm reappearance, downstream promotion, figure drift, and script-snapshot drift. Use runs `1525-1527` as the guarded high-side reappearance-edge block; no larger-offset acquisition safety rule is promoted. |
| 1528 | Post high-side reappearance-edge claim boundary | Integrate the guarded run `1525-1527` edge block into the local 2D claim boundary. The boundary now has 19 claims: 16 guarded and three blocked. The new guarded claim states that 45.0 mm is a narrow sampled suppression point and negative far-radius failures reappear at 45.015625 mm, so no monotonic larger-offset acquisition safety rule is supported. Broad physical, GPU, field-transfer, field-FWI, and 3D/HPC claims remain blocked. |
| 1529 | Post high-side reappearance-edge claim-boundary validator | Validate the saved run `1528` claim boundary from artifacts. Eight of eight checks pass, confirming claim counts, high-side and edge claim rows, 45.0 mm suppression, 45.015625 mm reappearance, blocked downstream states, figure validation, and script snapshots. Sensitivity hardening remains required before closing the block. |
| 1530 | Post high-side reappearance-edge claim-boundary sensitivity | Stress-test the run `1529` validator. The exact run `1528` boundary passes, while 18 damaged variants fail as expected for claim-count drift, high-side row drift, edge row drift, 45.015625 mm metric drift, false stability, monotonic safety promotion, blocked-row drift, downstream promotion, figure drift, and script-snapshot drift. Use runs `1528-1530` as the guarded post-high-side reappearance-edge claim-boundary block. |
| 1531 | Low-side persistence edge probe | Refine the transition immediately below 45.0 mm with six Tx/Rx offsets from 44.875 to 45.0 mm. Negative far-radius failures persist at the closest tested point below 45.0 mm, labeled 44.992188 mm, while 45.0 mm suppresses both negative far-radius cases. This complements the high-side reappearance result and treats 45.0 mm as a narrow sampled suppression point, not a broad acquisition rule; physical, GPU, field-transfer, field-FWI, and 3D/HPC claims remain blocked pending validation. |
| 1532 | Low-side persistence edge validator | Validate the saved run `1531` low-side edge probe from artifacts. Nine of nine checks pass, confirming source identity, 90 grid models, 540 objective rows, 2160 candidate rows, exact threshold maps, failure persistence at 44.992188 mm below 45.0 mm, suppression at 45.0 mm, blocked downstream states, figure validation, and script snapshots. Sensitivity hardening remains required before claim-boundary integration. |
| 1533 | Low-side persistence edge sensitivity | Stress-test the run `1532` validator. The exact run `1531` artifacts pass, while 14 damaged variants fail as expected for offset drift, label collision, row-count drift, taxonomy drift, low-side failure removal, 45.0 mm suppression removal, downstream promotion, figure drift, and script-snapshot drift. Use runs `1531-1533` as the guarded low-side persistence-edge block. |
| 1534 | Post two-sided edge claim boundary | Integrate the guarded low-side block into the current 2D claim boundary. The boundary now has 20 claims: 17 guarded and three blocked. The new guarded claim records that negative far-radius failures persist at 44.992188 mm below 45.0 mm and suppress at 45.0 mm; together with high-side reappearance at 45.015625 mm, 45.0 mm is a narrow sampled suppression point, not a broad acquisition rule. Broad physical, GPU, field-transfer, field-FWI, and 3D/HPC claims remain blocked. |
| 1535 | Post two-sided edge claim-boundary validator | Validate the saved run `1534` claim boundary from artifacts. Eight of eight checks pass, confirming 20 claims, 17 guarded claims, three blocked claims, stable high-side and low-side edge rows, failure persistence at 44.992188 mm below 45.0 mm, suppression at 45.0 mm, reappearance at 45.015625 mm above 45.0 mm, blocked downstream states, figure validation, and script snapshots. Sensitivity hardening remains required before closing the block. |
| 1536 | Post two-sided edge claim-boundary sensitivity | Stress-test the run `1535` validator. The exact run `1534` artifacts pass, while 21 damaged variants fail as expected for identity drift, claim-count drift, high-side row drift, low-side row drift, low-side failure drift, suppression drift, reappearance drift, false stability, broad-safety promotion, blocked-row drift, downstream promotion, figure drift, and script-snapshot drift. Use runs `1534-1536` as the guarded post-two-sided-edge local 2D claim-boundary block. |
| 1537 | Two-sided edge suppression-window width audit | Quantify the sampled bracket around the guarded 45.0 mm suppression point. The nearest sampled failure below 45.0 mm is 44.992188 mm, the sampled suppression point is 45.0 mm, and the first sampled reappearance above 45.0 mm is 45.015625 mm. The failure-to-failure bracket span is only 0.023437 mm, with one sampled suppression point inside it. This supports a narrow sampled suppression-window statement only; wide-window, broad monotonic acquisition, physical, GPU, field, and 3D claims remain blocked. |
| 1538 | Two-sided edge suppression-window width-audit validator | Validate the saved run `1537` width audit from artifacts. Six of six checks pass, confirming source identity, three window rows, the 44.992188/45.0/45.015625 mm edge points, 0.007812 and 0.015625 mm gaps, 0.023437 mm failure-to-failure span, blocked downstream states, figure validation, and script snapshots. Sensitivity hardening remains required before closing the block. |
| 1539 | Two-sided edge suppression-window width-audit sensitivity | Stress-test the run `1538` validator. The exact run `1537` width audit passes, while 13 damaged variants fail as expected for source identity drift, row drift, gap drift, false wide-window promotion, downstream promotion, figure drift, and script-snapshot drift. Use runs `1537-1539` as the guarded width-audit block; wide-window, monotonic acquisition, physical, GPU, field, and 3D claims remain blocked. |
| 1540 | Post width-audit claim boundary | Integrate the guarded width audit into the current 2D claim boundary. The boundary now has 21 claims: 18 guarded and three blocked. The new guarded claim records that the sampled 45.0 mm suppression point is bounded by failures at 44.992188 mm and 45.015625 mm, leaving a 0.023437 mm failure-to-failure bracket with one sampled suppression point. Wide-window, monotonic acquisition, physical, GPU, field, and 3D claims remain blocked. |
| 1541 | Post width-audit claim-boundary validator | Validate the saved run `1540` claim boundary from artifacts. Seven of seven checks pass, confirming claim counts, the width-audit claim row, 0.007812 and 0.015625 mm gaps, 0.023437 mm bracket span, blocked claim rows, downstream blocked states, figure validation, and script snapshots. Sensitivity hardening remains required before closing the block. |
| 1542 | Post width-audit claim-boundary sensitivity | Stress-test the run `1541` validator. The exact run `1540` claim boundary passes, while 15 damaged variants fail as expected for claim drift, width-metric drift, false wide-window promotion, downstream promotion, figure drift, and script-snapshot drift. Use runs `1540-1542` as the guarded post-width-audit local 2D claim-boundary block; wide-window, monotonic acquisition, physical, GPU, field, and 3D claims remain blocked. |
| 1543 | Two-sided edge follow-up offset plan | Define a small artifact-only follow-up plan for shrinking the sampled 45.0 mm bracket if future FDTD compute is justified. The plan proposes five offsets: 44.996094, 45.007812, 44.998047, 45.003906, and 45.011719 mm. With far-radius deltas -0.8 and -1.6 mm and near-radius deltas +1.5 and +1.9 mm, this would be 20 planned cases. No FDTD was executed and no physical, GPU, field, or 3D claim is promoted. |
| 1544 | Two-sided edge follow-up offset-plan validator | Validate the saved run `1543` follow-up offset plan from artifacts. Six of six checks pass, confirming the five planned offsets, 20 planned cases, non-executed FDTD state, blocked physical/GPU/field/3D states, figure validation, and script snapshots. Sensitivity hardening remains required before closing the plan block. |
| 1545 | Two-sided edge follow-up offset-plan sensitivity | Stress-test the run `1544` validator. The exact run `1543` offset plan passes, while 12 damaged variants fail as expected for offset drift, case-matrix drift, false execution, downstream promotion, figure drift, and script-snapshot drift. Use runs `1543-1545` as the guarded non-executed follow-up offset-plan block. |
| 1546 | Two-sided edge follow-up offset probe | Execute the guarded run `1543` follow-up plan as a bounded 20-case CPU probe. All 20 planned cases have at least one objective failure and zero cases have all-objective failure. Every planned offset, both below and above 45.0 mm, still shows first any-objective failure at near +1.5 mm for both far-radius error settings. This does not support a broader suppression window or physical acquisition rule; physical, GPU, field-transfer, field-FWI, and 3D/HPC claims remain blocked pending validation. |
| 1547 | Two-sided edge follow-up offset-probe validator | Validate the saved run `1546` executed follow-up offset probe from artifacts. Seven of seven checks pass, confirming source identity, 20 planned cases, 120 objective rows, 480 candidate rows, 20 any-failure models, zero all-objective-failure models, stable edge rows, blocked downstream states, figure validation, and script snapshots. Sensitivity hardening remains required before closing the block. |
| 1548 | Two-sided edge follow-up offset-probe validation sensitivity | Stress-test the run `1547` validator. The exact run `1546` artifacts pass, while 12 damaged variants fail as expected for source identity drift, row-count drift, failure-taxonomy drift, offset-matrix drift, edge-row drift, false downstream promotion, figure drift, and script-snapshot drift. Use runs `1546-1548` as the guarded executed follow-up offset-probe block. The wider suppression-window, physical acquisition, GPU, field, and 3D/HPC claims remain blocked. |
| 1549 | Post follow-up offset-probe claim boundary | Fold the executed follow-up offset probe into the current local 2D claim boundary. The boundary now has 22 claims: 19 guarded and three blocked. The new guarded claim records that all 20 planned offset cases executed, all 20 had at least one objective failure, and zero had all-objective failure. Wide-window, monotonic acquisition, physical, GPU, field, and 3D/HPC claims remain blocked. |
| 1550 | Post follow-up offset-probe claim-boundary validator | Validate the saved run `1549` claim boundary from artifacts. Seven of seven checks pass, confirming claim counts, the follow-up claim row, 20 planned cases, 20 any-failure cases, zero all-objective-failure cases, blocked downstream states, figure validation, and script snapshots. Sensitivity hardening remains required before closing the block. |
| 1551 | Post follow-up offset-probe claim-boundary sensitivity | Stress-test the run `1550` validator. The exact run `1549` boundary passes, while 13 damaged variants fail as expected for claim drift, follow-up row drift, follow-up count drift, false wide-window promotion, downstream promotion, figure drift, and script-snapshot drift. Use runs `1549-1551` as the guarded 2D post-follow-up offset-probe claim-boundary block. |
| 1552 | Follow-up offset objective-failure taxonomy audit | Audit the executed run `1546` follow-up offset probe by objective family. All 20 follow-up models share the same failure pattern: `base` and `early_high` fail, while `highband`, `late`, `late_high`, and `veryhigh` pass. This explains the failure mechanism but does not support a broad suppression-window, physical acquisition, GPU, field, or 3D/HPC claim. |
| 1553 | Follow-up offset objective-failure taxonomy validator | Validate the saved run `1552` taxonomy audit from artifacts. Six of six checks pass, confirming source identity, counts, the `base;early_high` dominant failure pattern, blocked downstream states, figure validation, and script snapshots. Sensitivity hardening remains required before closing the taxonomy block. |
| 1554 | Follow-up offset objective-failure taxonomy sensitivity | Stress-test the run `1553` validator. The exact run `1552` artifacts pass, while nine damaged variants fail as expected for label drift, count drift, failure-pattern drift, row drift, downstream promotion, figure drift, and script-snapshot drift. Use runs `1552-1554` as the guarded objective-failure taxonomy block. |
| 1555 | Post objective-taxonomy claim boundary | Fold the guarded objective-failure taxonomy into the local 2D claim boundary. The boundary now has 23 claims: 20 guarded and three blocked. The new guarded claim states that all 20 follow-up models share the same objective-failure pattern: `base` and `early_high` fail, while `highband`, `late`, `late_high`, and `veryhigh` pass. Wide-window, monotonic, physical, GPU, field, and 3D/HPC claims remain blocked. |
| 1556 | Post objective-taxonomy claim-boundary validator | Validate the saved run `1555` boundary from artifacts. Seven of seven checks pass, confirming claim counts, the taxonomy claim row, taxonomy metrics, blocked claim rows, blocked downstream states, figure validation, and script snapshots. Sensitivity hardening remains required before closing the block. |
| 1557 | Post objective-taxonomy claim-boundary sensitivity | Stress-test the run `1556` validator. The exact run `1555` artifacts pass, while 12 damaged variants fail as expected for label drift, claim-count drift, taxonomy-support drift, taxonomy-evidence drift, objective-count drift, universal-failure count drift, dominant-pattern drift, source-readiness demotion, blocked-support drift, downstream promotion, figure drift, and script-snapshot drift. Use runs `1555-1557` as the current guarded 2D post-taxonomy claim-boundary block. |
| 1558 | Follow-up objective-policy recommendation | Convert the guarded objective-failure taxonomy into a narrow objective-policy recommendation for the saved follow-up offset probe. Retain `highband`, `late`, `late_high`, and `veryhigh` as candidate objectives because they have 80 passes and zero failures; exclude `base` and `early_high` because they have 40 failures and zero passes. GPU, field, and 3D/HPC claims remain blocked. |
| 1559 | Follow-up objective-policy recommendation validator | Validate the saved run `1558` policy from artifacts. Seven of seven checks pass, confirming candidate/excluded objective sets, policy-group counts, narrow scope, stable `base;early_high` failure pattern, blocked downstream states, figure validation, and script snapshots. |
| 1560 | Follow-up objective-policy recommendation sensitivity | Stress-test the run `1559` validator. The exact run `1558` policy passes, while 12 damaged variants fail as expected for count drift, candidate/excluded-list drift, policy-row drift, group-count drift, scope drift, downstream promotion, figure drift, and script-snapshot drift. Use runs `1558-1560` as the guarded objective-policy recommendation block. |
| 1561 | Post objective-policy claim boundary | Fold the guarded objective-policy recommendation into the local 2D claim boundary. The boundary now has 24 claims: 21 guarded and three blocked. The new guarded claim retains `highband`, `late`, `late_high`, and `veryhigh` as candidate objectives and excludes `base` and `early_high` for the saved follow-up offset probe only. GPU, field, and 3D/HPC claims remain blocked. |
| 1562 | Post objective-policy claim-boundary validator | Validate the saved run `1561` boundary from artifacts. Eight of eight checks pass, confirming source identity, claim counts, the base-boundary relationship, the objective-policy row, policy metrics, blocked claim rows, blocked downstream states, figure validation, and script snapshots. |
| 1563 | Post objective-policy claim-boundary sensitivity | Stress-test the run `1562` validator. The exact run `1561` artifacts pass, while 15 damaged variants fail as expected for label drift, count drift, policy-row drift, policy-metric drift, scope drift, readiness demotion, blocked-support drift, downstream promotion, figure drift, and script-snapshot drift. Use runs `1561-1563` as the current guarded 2D post-objective-policy claim-boundary block. |
| 1564 | Follow-up objective-policy command plan | Convert the guarded objective-policy recommendation into a non-executed command and selection plan. The plan has seven rows: one optional aggregate CPU replay command, four retained objective-selection rows (`highband`, `late`, `late_high`, `veryhigh`), and two blocked objective rows (`base`, `early_high`). The underlying probe has no per-objective CLI, so no per-objective execution command is invented. |
| 1565 | Follow-up objective-policy command-plan validator | Validate the saved run `1564` command plan. Six of six checks pass, confirming source readiness, row counts, objective sets, replay-command scope, no hidden per-objective CLI, and blocked physical/GPU/field/3D claims. |
| 1566 | Follow-up objective-policy command-plan sensitivity | Stress-test the run `1565` validator. The exact run `1564` plan passes, while 24 damaged variants fail as expected for readiness, row-count, objective-label, command-shape, command-execution, per-objective CLI, and downstream-promotion damage. Use runs `1564-1566` as the guarded non-executed command-plan package for the saved follow-up objective policy. |
| 1567 | Follow-up objective-policy command replay audit | Execute the aggregate CPU replay authorized by run `1564` in a new experiment folder. The replay reproduces the saved 20-case probe counts and failure taxonomy: 20 grid models, 120 objective rows, 480 candidate rows, 20 any-failure models, and zero all-objective-failure models. No per-objective CLI, physical claim, GPU work, field transfer, field FWI, or 3D/HPC claim is promoted. |
| 1568 | Follow-up objective-policy command replay-audit validator | Validate the saved run `1567` replay audit from artifacts. Six of six checks pass, confirming replay readiness, aggregate execution state, count and failure-taxonomy agreement, replay output scope, blocked downstream claims, figure validation, and script snapshots. |
| 1569 | Follow-up objective-policy command replay-audit sensitivity | Stress-test the run `1568` validator. The exact run `1567` replay audit passes, while 18 damaged variants fail as expected for replay-readiness drift, execution-state drift, count drift, failure-taxonomy drift, output-scope drift, downstream promotion, figure drift, and script-snapshot drift. Use runs `1567-1569` as the guarded executed aggregate CPU replay block for the follow-up objective-policy command plan. |
| 1570 | Post command-replay claim boundary | Fold the executed aggregate CPU replay into the current 2D claim boundary. The boundary now has 25 claims: 22 guarded and three blocked. The new guarded claim records the executed aggregate replay while retaining blocked physical, GPU, field-transfer, field-FWI, and 3D/HPC states. |
| 1571 | Post command-replay claim-boundary validator | Validate run `1570` from artifacts. Six of six checks pass, confirming claim counts, replay-claim insertion, replay metrics, objective-policy scope, blocked downstream states, figure validation, and script snapshots. |
| 1572 | Post command-replay claim-boundary sensitivity | Stress-test the run `1571` validator. The exact run `1570` boundary passes, while 28 damaged variants fail as expected for count drift, replay-state drift, objective-scope drift, inserted-claim placement damage, downstream promotion, figure drift, and script-snapshot drift. Use runs `1570-1572` as the guarded post-command-replay 2D claim-boundary block. |
| 1573 | Follow-up objective-policy command-replay runtime audit | Measure the executed aggregate CPU replay cost from run `1567`. The replay remains ready and reproduces 20 grid models, 120 objective rows, 480 candidate rows, 20 any-failure models, and zero all-objective-failure models. Runtime is 782.566 seconds: 39.1283 seconds per grid model, 6.52138 seconds per objective row, and 1.63035 seconds per candidate row. No per-objective CLI, physical claim, GPU work, field transfer, field FWI, or 3D/HPC readiness is promoted. |
| 1574 | Follow-up objective-policy command-replay runtime-audit validator | Validate run `1573` from artifacts. Five of five checks pass, confirming runtime readiness, replay counts, timing rates, failure taxonomy, blocked downstream states, figure validation, and script snapshots. |
| 1575 | Follow-up objective-policy command-replay runtime-audit sensitivity | Stress-test the run `1574` validator. The exact run `1573` runtime audit passes, while 18 damaged variants fail as expected for readiness, count, timing-rate, downstream-state, figure-validation, and script-snapshot damage. Use runs `1573-1575` as the guarded command-replay runtime-audit block; the measured cost does not change the physical, GPU, field-transfer, field-FWI, or 3D/HPC guardrails. |
| 1576 | Post command-replay runtime-audit claim boundary | Fold the guarded runtime audit into the current 2D claim boundary. The boundary now has 26 claims: 23 guarded and three blocked. The new guarded claim records the aggregate replay cost: 782.566 seconds for 20 grid models, 120 objective rows, and 480 candidate rows. Physical, GPU, field-transfer, field-FWI, and 3D/HPC claims remain blocked. |
| 1577 | Post command-replay runtime-audit claim-boundary validator | Validate run `1576` from artifacts. Five of five checks pass, confirming claim counts, runtime-claim support, runtime metrics, blocked downstream states, figure validation, and script snapshots. |
| 1578 | Post command-replay runtime-audit claim-boundary sensitivity | Stress-test the run `1577` validator. The exact run `1576` boundary passes, while 26 damaged variants fail as expected for claim-count drift, runtime-claim support drift, runtime-metric drift, blocked-row drift, downstream promotion, figure drift, and script-snapshot drift. Use runs `1576-1578` as the guarded 2D post-runtime-audit claim-boundary block. |
| 1579 | Runtime budget scaling audit | Estimate bounded CPU runtime budgets from the measured aggregate replay cost in run `1573`. The measured rate is 39.1283 seconds per grid model. Five planning rows are written, from the observed 20-grid replay through a 200-grid bounded CPU screen estimate of 130.4277 minutes. No new FDTD is executed, and physical, GPU, field-transfer, field-FWI, and 3D/HPC claims remain blocked. |
| 1580 | Runtime budget scaling-audit validator | Validate run `1579` from artifacts. Five of five checks pass, confirming forecast shape, measured-rate provenance, exact replay matching, the 200-grid longest forecast, blocked downstream states, figure validation, and script snapshots. |
| 1581 | Runtime budget scaling-audit sensitivity | Stress-test the run `1580` validator. The exact run `1579` audit passes, while 22 damaged variants fail as expected for readiness drift, scenario-count drift, forecast-shape drift, measured-rate drift, false FDTD execution, downstream promotion, figure drift, and script-snapshot drift. Use runs `1579-1581` as the guarded 2D runtime-budget scaling audit block. |
| 1582 | Post runtime-budget scaling-audit claim boundary | Fold the guarded runtime-budget scaling audit into the current 2D claim boundary. The boundary now has 27 claims: 24 guarded and three blocked. The new guarded claim records five forecast scenarios, a 200-grid largest bounded planning case, and a 130.4277-minute CPU estimate from the measured replay rate. No new FDTD, physical, GPU, field-transfer, field-FWI, or 3D/HPC claim is promoted. |
| 1583 | Post runtime-budget scaling-audit claim-boundary validator | Validate run `1582` from artifacts. Five of five checks pass, confirming claim counts, runtime-budget claim support, budget metrics, blocked rows, downstream blocked states, figure validation, and script snapshots. |
| 1584 | Post runtime-budget scaling-audit claim-boundary sensitivity | Stress-test the run `1583` validator. The exact run `1582` boundary passes, while 22 damaged variants fail as expected for claim-count drift, runtime-budget claim drift, budget metric drift, false new FDTD execution, downstream promotion, figure drift, and script-snapshot drift. Use runs `1582-1584` as the current guarded 2D post-runtime-budget claim-boundary block. |
| 1585 | Runtime budget triage policy | Convert the guarded runtime forecast into CPU-screen budget tiers. The default 60-minute budget supports a 90-grid screen, the two-hour budget still selects 90 grids, and the 200-grid screen first fits at the 150-minute tier. No new FDTD, physical, GPU, field-transfer, field-FWI, or 3D/HPC claim is promoted. |
| 1586 | Runtime budget triage-policy validator | Validate run `1585` from artifacts. Five of five checks pass, confirming the five budget tiers, 15/30/60/120/150-minute selections, the one-hour and two-hour decisions, no new FDTD execution, blocked downstream states, figure validation, and script snapshots. |
| 1587 | Runtime budget triage-policy validation sensitivity | Stress-test the run `1586` validator. The exact run `1585` policy passes, while 26 damaged variants fail as expected for source-readiness drift, budget-tier drift, default and two-hour decision drift, false FDTD execution, downstream promotion, figure drift, and script-snapshot drift. Use runs `1585-1587` as the guarded 2D runtime-budget triage-policy block. |
| 1588 | Post runtime-budget triage-policy claim boundary | Fold the guarded runtime-budget triage policy into the current 2D claim boundary. The boundary now has 28 claims: 25 guarded and three blocked. The new guarded claim records the 90-grid one-hour default, the 90-grid two-hour ceiling, and the 150-minute first budget tier for the 200-grid screen. No new FDTD, physical, GPU, field-transfer, field-FWI, or 3D/HPC claim is promoted. |
| 1589 | Post runtime-budget triage-policy claim-boundary validator | Validate run `1588` from artifacts. Five of five checks pass, confirming claim counts, triage-claim support, budget metrics, blocked rows, downstream blocked states, figure validation, and script snapshots. |
| 1590 | Post runtime-budget triage-policy claim-boundary sensitivity | Stress-test the run `1589` validator. The exact run `1588` boundary passes, while 25 damaged variants fail as expected for claim-count drift, triage-claim drift, budget metric drift, false FDTD execution, downstream promotion, figure drift, and script-snapshot drift. Use runs `1588-1590` as the current guarded 2D post-runtime-budget-triage claim-boundary block. |
| 1591 | Runtime budget command plan | Convert the runtime-budget triage policy into a non-executed planning run sheet. The 60-minute default remains a 90-grid CPU screen, the 200-grid screen first appears at the 150-minute tier, and no command templates or new FDTD execution are emitted. |
| 1592 | Runtime budget command-plan validator | Validate run `1591` from artifacts. Six of six checks pass, confirming budget rows, default and large-screen decisions, absence of hidden command execution, blocked downstream states, figure validation, and script snapshots. |
| 1593 | Runtime budget command-plan validation sensitivity | Stress-test the run `1592` validator. The exact run `1591` plan passes, while 27 damaged variants fail as expected for source-readiness drift, budget drift, hidden execution, command-template drift, downstream promotion, figure drift, and script-snapshot drift. Use runs `1591-1593` as the guarded 2D runtime-budget command-plan block. |
| 1594 | Post runtime-budget command-plan claim boundary | Fold the guarded command-plan block into the current 2D claim boundary. The boundary now has 29 claims: 26 guarded and three blocked. The new guarded claim records a five-row non-executed plan, a 90-grid one-hour default, a 200-grid screen at the 150-minute tier, zero emitted commands, and no downstream promotion. |
| 1595 | Post runtime-budget command-plan claim-boundary validator | Validate run `1594` from artifacts. Five of five checks pass, confirming claim counts, command-plan support, zero executable commands, blocked downstream states, figure validation, and script snapshots. |
| 1596 | Post runtime-budget command-plan claim-boundary sensitivity | Stress-test the run `1595` validator. The exact run `1594` boundary passes, while 29 damaged variants fail as expected for claim-count drift, command-plan drift, budget drift, hidden command execution, downstream promotion, figure drift, and script-snapshot drift. Use runs `1594-1596` as the current guarded 2D post-runtime-budget-command-plan claim-boundary block. |
| 1597 | 90-grid dry-run payload manifest | Convert the guarded runtime-budget command plan into a concrete 90-row dry-run payload manifest. The manifest has five objective profiles, 18 transition bins, a 58.69245-minute estimated runtime under the 60-minute budget, zero executable commands, and no new FDTD execution. |
| 1598 | 90-grid dry-run payload-manifest validator | Validate run `1597` from artifacts. Five of five checks pass, confirming the 90-row shape, five objective profiles, 18 transition bins, the one-hour runtime envelope, blocked execution, blocked downstream states, figure validation, and script snapshots. |
| 1599 | 90-grid dry-run payload-manifest sensitivity | Stress-test the run `1598` validator. The exact run `1597` manifest passes, while 22 damaged variants fail as expected for count drift, budget drift, hidden execution, downstream promotion, figure drift, and script-snapshot damage. Use runs `1597-1599` as the guarded dry-run payload-manifest block before any executable 2D CPU-screen script is created. |
| 1600 | Post 90-grid dry-run payload-manifest claim boundary | Fold the guarded dry-run payload manifest into the current 2D claim boundary. The boundary now has 30 claims: 27 guarded and three blocked. The new guarded claim records 90 payload rows, five objective profiles, 18 transition bins, a 58.69245-minute estimated runtime under the one-hour budget, zero executable commands, and no downstream promotion. |
| 1601 | Post 90-grid dry-run payload-manifest claim-boundary validator | Validate run `1600` from artifacts. Five of five checks pass, confirming claim counts, the manifest claim row, manifest metrics, blocked downstream states, figure validation, and script snapshots. |
| 1602 | Post 90-grid dry-run payload-manifest claim-boundary sensitivity | Stress-test the run `1601` validator. The exact run `1600` boundary passes, while 29 damaged variants fail as expected for claim-count drift, manifest-claim drift, manifest-metric drift, hidden execution, downstream promotion, figure drift, and script-snapshot damage. Use runs `1600-1602` as the current guarded 2D post-90-grid-manifest claim-boundary block. |
| 1603 | 90-grid execution-readiness gate | Convert the guarded dry-run payload manifest into an execution gate. The gate evaluates 10 requirements: four pass and six block execution. The blockers are insufficient budget headroom, missing run-specific execution script, missing executable command inventory, missing output contract, missing resume policy, and missing resource guard. No FDTD is executed. |
| 1604 | 90-grid execution-readiness gate validator | Validate run `1603` from artifacts. Six of six checks pass, confirming the 10-requirement gate, four passed requirements, six preserved execution blockers, 1.30755 minutes of budget headroom versus a 5.0-minute requirement, blocked execution, figure validation, and script snapshots. |
| 1605 | 90-grid execution-readiness gate sensitivity | Stress-test the run `1604` validator. The exact run `1603` gate passes, while 22 damaged variants fail as expected for gate-count drift, source-metric drift, execution promotion, budget-headroom promotion, execution-contract promotion, hidden execution, downstream promotion, figure damage, and script-snapshot damage. Use runs `1603-1605` as the guarded execution-readiness block before any 90-grid 2D CPU screen is run. |
| 1606 | 84-grid budget-feasible subset design | Repair the budget-headroom blocker from the 90-grid execution gate without executing FDTD. Dropping six interior midpoint rows yields 84 selected rows, preserves all five objective profiles and all transition endpoints, reduces estimated runtime to 54.77962 minutes, and leaves 5.22038 minutes of headroom against the 5.0-minute requirement. Script, command inventory, output contract, resume policy, and resource guard remain blocked. |
| 1607 | 84-grid budget-feasible subset-design validator | Validate run `1606` from artifacts. Five of five checks pass, confirming the 84/6 selected/removed split, midpoint-only removals, endpoint preservation, budget-headroom repair, five remaining execution-contract blockers, blocked execution, figure validation, and script snapshots. |
| 1608 | 84-grid execution-contract skeleton | Materialize the non-budget execution contracts for the validated 84-grid subset without executing FDTD. The run writes 84 output-contract rows, 84 planned command rows, four resume-policy rows, and five resource-guard rows. Output, resume, resource, and planned command inventory are now present, but the row executor script is absent and zero commands are executable, so execution remains blocked. |
| 1609 | 84-grid execution-contract skeleton validator | Validate run `1608` from artifacts. Five of five checks pass, confirming 84 output rows, 84 planned command rows, four resume-policy rows, five resource-guard rows, zero executable commands, two remaining execution-contract blockers, blocked execution, figure validation, and script snapshots. |
| 1610 | 84-grid executor-interface guard audit | Add and audit the guarded row-executor interface for the 84-grid CPU screen without executing FDTD. Three of three interface checks pass: a valid payload row is accepted in contract-check mode, an unknown row is rejected, and real execution is refused. The missing-interface blocker is closed, but executable real command count remains zero, so execution stays blocked pending command inventory refresh. |
| 1611 | 84-grid executor-interface guard audit validator | Validate run `1610` from artifacts. Five of five checks pass, confirming the three saved interface cases, explicit real-execution refusal, zero executable real commands, one remaining command-refresh blocker, blocked downstream states, figure validation, and script snapshots. |
| 1612 | 84-grid contract-check command inventory refresh | Refresh the 84-grid planned command inventory against the guarded executor interface without executing FDTD. All 84 selected payload rows receive `--contract-check-only` commands and all 84 in-process interface probes pass. The contract-check inventory is ready, but real execution command count remains zero and real FDTD execution stays disabled. |
| 1613 | 84-grid contract-check command inventory refresh validator | Validate run `1612` from artifacts. Five of five checks pass, confirming 84 contract-check-only command rows, 84 passing probes, zero real execution commands, blocked real FDTD execution, blocked downstream states, figure validation, and script snapshots. |
| 1614 | 84-grid contract-check command execution smoke | Execute the refreshed 84-row contract-check command inventory without running FDTD. All 84 contract-check JSON outputs are written and all 84 pass, while real execution command count remains zero, real FDTD execution stays disabled, and physical/GPU/field/3D downstream states remain blocked. |
| 1615 | 84-grid contract-check command execution smoke validator | Validate run `1614` from artifacts. Four of four checks pass, confirming 84 execution rows, 84 contract-check JSON outputs, 84 passing hashes, zero real FDTD execution, blocked downstream states, figure validation, and script snapshots. |
| 1616 | 84-grid contract-check command execution smoke sensitivity | Stress-test the run `1615` validator. The exact run `1614` smoke passes, while nine damaged variants fail as expected for command-count drift, missing execution rows, output-count drift, output-hash damage, hidden FDTD execution, real-command promotion, downstream physical-claim promotion, figure damage, and script-snapshot damage. Use runs `1614-1616` as the guarded 84-grid contract-check execution-smoke block. |
| 1617 | Post 84-grid contract-check execution-smoke claim boundary | Fold the guarded 84-grid contract-check execution-smoke block into the local 2D claim boundary. The boundary now has 31 claims: 28 guarded and three blocked. The new guarded claim records 84 contract-check commands, 84 JSON outputs, 84 passes, zero real execution commands, zero executable real commands, and no FDTD execution or downstream promotion. |
| 1618 | Post 84-grid contract-check execution-smoke claim-boundary validator | Validate run `1617` from artifacts. Five of five checks pass, confirming 31 claims, 28 guarded claims, three blocked claims, the 84-grid contract-check smoke claim row, 84 commands, 84 outputs, 84 passes, zero real execution commands, blocked downstream states, figure validation, and script snapshots. |
| 1619 | Post 84-grid contract-check execution-smoke claim-boundary sensitivity | Stress-test the run `1618` validator. The exact run `1617` boundary passes, while nine damaged variants fail as expected for claim-count drift, smoke-claim support drift, output-count drift, real-command promotion, hidden FDTD promotion, downstream GPU promotion, blocked-support drift, figure damage, and script-snapshot damage. Use runs `1617-1619` as the guarded post-contract-check-smoke local 2D claim-boundary block. |
| 1620 | 84-grid pilot execution design | Select a five-row pilot from the guarded 84-row subset before any full 2D screen. The pilot covers all five objective profiles, both transition endpoints, and the midpoint band, with a 3.26069-minute estimate inside a 10-minute pilot budget. Real execution remains blocked pending a duplicated pilot executor, pilot-only real command inventory, and pilot output validator. |
| 1621 | 84-grid pilot execution-design validator | Validate run `1620` from artifacts. Five of five checks pass, confirming guarded source readiness, five-row pilot coverage, bounded pilot runtime, preserved execution blockers, figure validation, and script snapshots. |
| 1622 | 84-grid pilot execution-design validation sensitivity | Stress-test the run `1621` validator. The exact run `1620` pilot design passes, while 11 damaged variants fail as expected for source guard damage, pilot structure damage, budget damage, premature execution promotion, action damage, figure damage, and script-snapshot damage. Use runs `1620-1622` as the guarded five-row pilot-design block before building a real pilot executor. |
| 1623 | 84-grid pilot executor-interface guard audit | Add a pilot-only executor interface for the five selected rows from run `1620`. All five pilot rows pass contract-check mode, a non-pilot row is rejected, and real FDTD execution is refused. Zero real commands are executable and downstream states remain blocked. |
| 1624 | 84-grid pilot executor-interface guard validator | Validate run `1623` from artifacts. Four of four checks pass, confirming source pilot readiness, executor script availability, five valid pilot contract checks, non-pilot rejection, real-execution refusal, blocked downstream states, figure validation, and script snapshots. |
| 1625 | 84-grid pilot executor-interface guard sensitivity | Stress-test the run `1624` validator. The exact run `1623` executor audit passes, while 10 damaged variants fail as expected for source-readiness damage, audit-count drift, pilot-scope drift, real-execution promotion, downstream promotion, figure damage, and script-snapshot damage. Use runs `1623-1625` as the guarded pilot executor-interface block. |
| 1626 | 84-grid pilot contract-check command inventory refresh | Convert the guarded five-row pilot executor interface into five concrete contract-check commands. All five in-process probes pass, while real execution command count and executable real command count remain zero. The full 84-row screen remains blocked. |
| 1627 | 84-grid pilot contract-check command-inventory validator | Validate run `1626` from artifacts. Four of four checks pass, confirming source readiness, five contract-check command rows, five passing probes, zero executable real commands, blocked downstream states, figure validation, and script snapshots. |
| 1628 | 84-grid pilot contract-check command-inventory sensitivity | Stress-test the run `1627` validator. The exact run `1626` command inventory passes, while nine damaged variants fail as expected for source-readiness damage, command-count drift, probe failure, real-command promotion, command execution promotion, FDTD execution promotion, downstream promotion, figure damage, and script-snapshot damage. Use runs `1626-1628` as the guarded pilot command-inventory block. |
| 1629 | 84-grid pilot contract-check command execution smoke | Execute the five pilot contract-check commands from run `1626`. All five write JSON outputs and pass contract checks, while real FDTD execution remains disabled and downstream states remain blocked. Use this as the command-smoke checkpoint before any real pilot execution implementation. |
| 1630 | 84-grid pilot contract-check command execution-smoke validator | Validate run `1629` from artifacts. Four of four checks pass, confirming the source chain, five hashed JSON outputs, blocked real execution, nonblank figure, and script snapshots. |
| 1631 | 84-grid pilot contract-check command execution-smoke validation sensitivity | Stress-test the run `1630` validator. The exact run `1629` smoke passes, while 10 damaged variants fail as expected for source-chain damage, output count drift, missing rows, hash drift, payload/row FDTD promotion, real-command promotion, downstream promotion, figure damage, and script-snapshot damage. |
| 1632 | 84-grid pilot real-execution preflight gap audit | Probe the current pilot executor in real mode for the five selected rows. All five probes are refused with exit code 2, real FDTD execution remains disabled, and four implementation blockers remain: a separate real pilot executor, output schema/validator, bounded pilot execution, and output validation before any full 84-row screen. |
| 1633 | 84-grid pilot real-execution preflight gap-audit validator | Validate run `1632` from artifacts. Five of five checks pass, confirming source readiness, five real-mode refusals, four implementation blockers, blocked execution/downstream states, figure validation, and script snapshots. |
| 1634 | 84-grid pilot real-execution preflight gap-audit sensitivity | Stress-test the run `1633` validator. The exact run `1632` gap audit passes, while 10 damaged variants fail as expected for source-chain damage, probe-count/refusal drift, accidental execution, execution-permission promotion, action drift, downstream promotion, figure damage, and script-snapshot damage. |
| 1635 | 84-grid pilot real-output schema contract | Define the acceptance schema for future five-row real pilot results. The contract specifies five result JSON files and 50 required fields, with zero real outputs currently present or accepted. Execution, GPU work, field transfer, and 3D/HPC remain blocked until real outputs satisfy this schema. |
| 1636 | 84-grid pilot real-output schema-contract validator | Validate run `1635` from artifacts. Five of five checks pass, confirming source readiness, five future result files, 50 required fields, contract-only zero-output state, three blocked actions, blocked downstream states, figure validation, and script snapshots. |
| 1637 | 84-grid pilot real-output schema-contract validation sensitivity | Stress-test the run `1636` validator. The exact run `1635` contract passes, while 12 damaged variants fail as expected for source-chain damage, count/field drift, real-output or schema-acceptance promotion, FDTD promotion, template substitution, action/downstream promotion, figure damage, and script-snapshot damage. |
| 1638 | 84-grid pilot real-execution implementation-gap audit | Connect the run `1635-1637` schema block to the current pilot executor. All five real-mode executor probes are refused, no new FDTD runs execute, no real output files are present or accepted, and four implementation blockers remain before any five-row pilot can become evidence. |
| 1639 | 84-grid pilot real-execution implementation-gap audit validator | Validate run `1638` from artifacts. Five of five checks pass, confirming source readiness, five executor refusals, absent/unaccepted real outputs, four preserved blockers, blocked downstream states, figure validation, and script snapshots. |
| 1640 | 84-grid pilot real-execution implementation-gap audit validation sensitivity | Stress-test the run `1639` validator. The exact run `1638` artifacts pass, while 15 damaged variants fail as expected for executor drift, premature execution, output presence or acceptance, template substitution, blocker damage, downstream promotion, figure damage, and script-snapshot damage. |
| 1641 | 84-grid pilot real-result file acceptance gate | Define the acceptance gate for future five-row real pilot result files. The gate requires five result JSON files and 50 field checks, while accepting zero current files, fields, templates, or FDTD executions. |
| 1642 | 84-grid pilot real-result file acceptance-gate validator | Validate run `1641` from artifacts. Five of five checks pass, confirming source readiness, five-file shape, 50 field gates, zero accepted outputs, blocked actions/downstream states, figure validation, and script snapshots. |
| 1643 | 84-grid pilot real-result file acceptance-gate validation sensitivity | Stress-test the run `1642` validator. The exact run `1641` artifacts pass, while 15 damaged variants fail as expected for file, field, action, downstream, figure, and script-snapshot damage. |
| 1644 | 84-grid pilot real-result file staging scaffold | Create the filesystem target for the future five-row real pilot returns. One required staging directory is present, five required result JSON files are missing, no unexpected files are present, and all downstream claims remain blocked. |
| 1645 | 84-grid pilot real-result file staging-scaffold validator | Validate run `1644` from artifacts. Five of five checks pass, confirming the empty staging directory, five missing required JSON files, three preserved staging actions, blocked downstream states, figure validation, and script snapshots. |
| 1646 | 84-grid pilot real-result file staging-scaffold validation sensitivity | Stress-test the run `1645` validator. The exact run `1644` artifacts pass, while 15 damaged variants fail as expected for directory, file, action, downstream, figure, and script-snapshot damage. |
| 1647 | 84-grid pilot real-result file identity contract | Lock the five future pilot JSON result identities. The contract records five unique file identities, one shared ten-field sequence, 50 required fields, one present staging directory, zero real files, zero parser readiness, and zero FDTD execution. |
| 1648 | 84-grid pilot real-result file identity-contract validator | Validate run `1647` from artifacts. Five of five checks pass, confirming unique file identities, ten fields per JSON file, the staging directory, blocked file/parser/FDTD states, figure validation, and script snapshots. |
| 1649 | 84-grid pilot real-result file identity-contract validation sensitivity | Stress-test the run `1648` validator. The exact run `1647` artifacts pass, while 11 damaged variants fail as expected for source, identity, directory, file, parser, FDTD, downstream, figure, and script-snapshot damage. |
| 1650 | 84-grid pilot real-result field value-domain contract | Define value-domain checks for the 50 required fields in the five future pilot JSON outputs. The contract covers five payloads, ten required field names, integer/hash/runtime/object/boolean domains, zero present fields, and zero accepted values. |
| 1651 | 84-grid pilot real-result field value-domain validator | Validate run `1650` from artifacts. Four of four checks pass, confirming the 50-field shape, expected domain counts, zero accepted field values, blocked actions/downstream states, figure validation, and script snapshots. |
| 1652 | 84-grid pilot real-result field value-domain validation sensitivity | Stress-test the run `1651` validator. The exact run `1650` artifacts pass, while nine damaged variants fail as expected for source, field-row, domain, field-promotion, FDTD-promotion, figure, and script-snapshot damage. |
| 1653 | 84-grid pilot real-result producer checklist | Convert the guarded file-identity and field-domain requirements into a five-file producer checklist. The checklist requires five pending real pilot JSON outputs with 50 fields total and keeps FDTD evidence, field transfer, GPU work, and 3D/HPC blocked. |
| 1654 | 84-grid pilot real-result command plan | Convert the five-file producer checklist into five non-executed JSON parse/checksum commands. No command is run, no result file is accepted, and full 84-row execution remains blocked until the five-row pilot passes acceptance. |
| 1655 | 84-grid pilot real-result fillable template pack | Create five parseable JSON templates for the future real pilot outputs. The templates contain 50 total fields, prefill only 15 stable design identity fields, leave 35 real solver/runtime/hash/result fields blank or null, and keep FDTD execution, pilot evidence, GPU work, field transfer, and 3D/HPC readiness blocked. |
| 1656 | 84-grid pilot real-result fillable template-pack validator | Validate the run `1655` template pack with five passing checks: source readiness, exact five-file/50-field shape, blank real-result fields, blocked downstream states, figure output, and frozen script snapshots. |
| 1657 | 84-grid pilot real-executor implementation-target audit | Confirm that the five-row pilot has validated result templates but still lacks a real executor. The guarded pilot executor script exists and refuses real mode, the proposed real executor script is absent, all five implementation dependencies are ready, and all five implementation items remain blocking. |
| 1658 | 84-grid pilot real-executor translation-gap audit | Show that the five-row pilot has a reusable CPU FDTD route and four parseable standard objective profiles, but still lacks a transition-bin-to-model mapper, a concrete `retained_blend` objective definition, and the separate real pilot executor. Direct pilot execution remains blocked. |
| 1659 | 84-grid pilot transition-bin mapper candidate design | Map the 18-bin pilot axis to a candidate 40-45 mm Tx/Rx-offset axis using saved fine-transition margins. All five pilot rows map, but three are interpolated, only one is at or above the saved crossing, `retained_blend` remains undefined, and real execution remains blocked. |
| 1660 | 84-grid pilot transition-bin mapper candidate validator | Validate the run `1659` mapper with five passing checks: source readiness, 18-bin axis shape, five-row pilot mapping shape, interpolation/crossing counts, and blocked execution states. The mapper is internally consistent but still not an accepted execution contract. |
| 1661 | 84-grid pilot retained-blend resolution audit | Compare five ways to resolve the undefined `retained_blend` pilot row. The conservative candidate replaces row `86` with parser-supported row `68`, `veryhigh` at the same transition bin, preserving the five-row pilot and crossing-pair evidence while keeping execution blocked. |
| 1662 | 84-grid pilot retained-blend resolution validator | Validate the run `1661` revised-pilot candidate with six passing checks. The candidate has five rows, bins 0/4/9/13/17, zero `retained_blend` rows, two `veryhigh` rows, parser-supported objective definitions, and no accepted execution or downstream promotion. |
| 1663 | 84-grid pilot revised artifact compatibility audit | Audit the old producer checklist, command plan, and fillable template pack against the run `1662` revised pilot. All three artifacts are stale: they share four payloads, still retain old payload `86`, miss revised payload `68`, and must be refreshed before any real executor or FDTD run. |
| 1664 | 84-grid pilot revised real-result producer checklist | Refresh the producer checklist around revised payloads `1;23;46;68;72`. The checklist now includes payload `68`, excludes stale payload `86` and `retained_blend`, and keeps command-plan refresh, template refresh, real execution, GPU work, field transfer, and 3D/HPC blocked. |
| 1665 | 84-grid pilot revised real-result command plan | Convert the revised checklist into five non-executed JSON parse/checksum commands for payloads `1;23;46;68;72`. The plan includes payload `68`, excludes stale payload `86` and `retained_blend`, keeps all commands unexecuted, and leaves template refresh, real execution, GPU work, field transfer, and 3D/HPC blocked. |
| 1666 | 84-grid pilot revised real-result fillable template pack | Refresh the JSON template pack around payloads `1;23;46;68;72`. The templates include payload `68`, exclude stale payload `86` and `retained_blend`, prefill four design identity fields per file, leave measured fields blank or null, and keep real execution, GPU work, field transfer, and 3D/HPC blocked. |
| 1667 | 84-grid pilot revised real-result fillable template-pack validator | Validate the run `1666` revised template pack with six passing checks: source readiness, exact file/field shape, revised payload identity, blank measured-result fields, blocked downstream states, figure output, and frozen script snapshots. No FDTD, GPU, field, or 3D/HPC promotion is made. |
| 1668 | 84-grid pilot revised real-result fillable template-pack validation sensitivity | Stress-test the run `1667` validator. The exact run `1666` source passes, while eight damaged states fail as expected for summary readiness damage, missing manifest row, stale payload `86` summary, stale `retained_blend` template identity, filled solver status, downstream promotion, figure damage, and missing script snapshots. |
| 1669 | 84-grid pilot revised real-executor preimplementation closure audit | Revisit the real-executor gap after the revised pilot removes `retained_blend`. The semantic closure items are all ready: payloads `1;23;46;68;72`, payload `68` included, stale payload `86` excluded, zero `retained_blend` rows, five parser-supported objective profiles, validated mapper candidate, and validated revised templates. Execution remains blocked by the absent real executor script and absent real FDTD result files. |
| 1670 | 84-grid pilot revised real-executor design contract | Define the concrete real-executor implementation contract for payloads `1;23;46;68;72`. All five payloads bind to candidate Tx/Rx offsets, result templates, and validation commands; the contract defines six CLI arguments and eight implementation steps. No real executor script or FDTD result file exists yet. |
| 1671 | 84-grid pilot real-executor creation impact audit | Audit the test and script impact of creating the real executor script. The scan finds 19 impacted files: 14 scripts, five tests, five direct refactor-required tests, eight dynamic filesystem dependencies, and five no-executor absence assertions. Real executor creation is not ready until those historical absence checks are refactored or versioned. |
| 1672 | 84-grid pilot real-executor absence refactor contract | Convert run `1671` into four refactor groups: historical dynamic scripts, historical absence tests, future executor tests, and suite validation. The contract identifies 15 required refactor items and keeps real executor creation blocked until historical no-executor checks are versioned. |
| 1673 | 84-grid pilot real-executor absence refactor applied audit | Verify the historical no-executor refactor. Historical audits now use frozen run-time no-executor status rather than live `REAL_EXECUTOR_SCRIPT.exists()` checks; six scanner string references remain, zero executable live filesystem dependencies remain, and executor creation remains blocked until future-specific tests are added. |
| 1674 | 84-grid pilot real-executor shell contract audit | Add and validate the real-executor shell. The shell reads the run `1670` design contract, passes contract checks for payloads `1;23;46;68;72`, accepts payload `68`, rejects stale payload `86`, refuses real mode, and executes no FDTD. |
| 1675 | 84-grid pilot real-executor solver-binding gap audit | Inspect the low-level CPU probe interface after adding the executor shell. The shell is present and `run_candidate_family` is callable, but eight bindings remain blocking: observed data, scan positions, time values, mute mask, input-contract writer, solver-log writer, result JSON writer, and post-write command runner. No FDTD is executed. |
| 1676 | 84-grid pilot real-executor input-contract writer audit | Add and audit the revised pilot input-contract writer. Five input contracts are written for payloads `1;23;46;68;72`, payload `68` is included, stale payload `86` is absent, the input-contract writer binding is closed, and seven solver/output bindings remain blocking. No FDTD is executed. |
| 1677 | 84-grid pilot real-executor input-contract writer validator | Validate run `1676` from artifacts. Eight of eight checks pass, confirming five input contracts, revised payload identity, preserved unresolved solver bindings, zero FDTD execution, one closed binding, seven remaining blockers, blocked downstream states, figure output, and script snapshots. |
| 1678 | 84-grid pilot real-executor input-contract writer validation sensitivity | Stress-test the run `1677` validator. The exact run `1676` source passes, while eight damaged states fail as expected for source readiness damage, missing input contracts, payload identity duplication, FDTD promotion, binding-count drift, downstream GPU promotion, figure damage, and missing script snapshots. |
| 1679 | 84-grid pilot real-executor solver-array binding design contract | Map the four remaining solver-array bindings to concrete producers: `base.generate_time_array`, `base._build_mute_window`, `base.build_scan_positions`, and `base.build_observed_cases`. Producer functions are available and the five revised payload contracts are present, but no arrays are materialized and real FDTD remains blocked. |
| 1680 | 84-grid pilot real-executor solver-array binding design-contract validator | Validate run `1679` from artifacts. Eight of eight checks pass, confirming the exact producer map, zero materialized arrays, revised payload identity, `run_candidate_family` interface coverage, no execution promotion, blocked downstream states, figure output, and script snapshots. |
| 1681 | 84-grid pilot real-executor solver-array binding design-contract validation sensitivity | Stress-test the run `1680` validator. The exact run `1679` source passes, while ten damaged states fail as expected for source readiness damage, producer-map damage, producer availability loss, array-materialization promotion, payload identity damage, interface damage, FDTD promotion, downstream GPU promotion, figure damage, and missing script snapshots. |
| 1682 | 84-grid pilot real-executor safe solver-array materialization audit | Materialize only the safe non-observed solver arrays for the revised five-row pilot. `time_values`, `mute`, and five per-payload `scan_positions` are materialized, while `observed_by_case` remains unbuilt to avoid solver execution. No FDTD is executed and downstream states remain blocked. |
| 1683 | 84-grid pilot real-executor safe solver-array materialization validator | Validate run `1682` from artifacts. Eight of eight checks pass, confirming stable `time_values`, `mute`, and scan-position materialization for payloads `1;23;46;68;72`, preserving the blocked `observed_by_case` binding, and preventing FDTD, GPU, field-transfer, field-FWI, or 3D/HPC promotion. |
| 1684 | 84-grid pilot real-executor safe solver-array materialization validation sensitivity | Stress-test the run `1683` validator. The exact run `1682` materialization state passes, while ten damaged states fail as expected for source readiness removal, safe-array identity/length drift, scan-payload removal, observed-data promotion, binding-count drift, FDTD promotion, downstream promotion, figure damage, and missing script snapshots. |
| 1685 | 84-grid pilot real-executor observed-by-case preflight gap audit | Audit the remaining `observed_by_case` binding. The producer `base.build_observed_cases` is callable and has the expected six-parameter signature, but it calls `simulate_bscan`, so observed-data materialization is the FDTD execution boundary. Five blockers remain: true-model binding, case-set binding, solver execution budget, observed-array cache/output contract, and real-executor result writer. |
| 1686 | 84-grid pilot real-executor observed-by-case preflight gap audit validator | Validate run `1685` from artifacts. Six of six checks pass, confirming the producer signature and execution boundary, five preserved blockers, no observed-data materialization, no FDTD execution, blocked downstream states, and valid figure/script artifacts. |
| 1687 | 84-grid pilot real-executor observed-by-case preflight gap audit validation sensitivity | Stress-test the run `1686` validator. The exact run `1685` audit passes, while ten damaged states fail as expected for source readiness removal, producer signature damage, unsafe materialization promotion, blocker removal/readiness promotion, observed-data promotion, FDTD promotion, downstream GPU promotion, figure damage, and missing script snapshots. |
| 1688 | 84-grid pilot real-executor observed-by-case bounded input design contract | Define the bounded input contract for the remaining `observed_by_case` step without running FDTD. The contract binds three true targets, two perturbation cases, revised payloads `1;23;46;68;72`, forty scan positions, ten future `simulate_bscan` calls, and eighty future trace solves. Execution remains blocked by solver approval, observed-array cache/output, and result-writer contracts. |
| 1689 | 84-grid pilot real-executor observed-by-case bounded input design-contract validator | Validate run `1688` from artifacts. Eight of eight checks pass, confirming true-model target identity, two-case perturbation identity, revised payload budget, three preserved blockers, no observed-data materialization, no FDTD execution, blocked downstream states, and valid figure/script artifacts. |
| 1690 | 84-grid pilot real-executor observed-by-case bounded input design-contract validation sensitivity | Stress-test the run `1689` validator. The exact run `1688` contract passes, while twelve damaged states fail as expected for source readiness removal, target/case/payload identity damage, solve-budget damage, blocker removal/readiness promotion, observed-data promotion, FDTD promotion, downstream GPU promotion, figure damage, and missing script snapshots. |
| 1691 | 84-grid pilot real-executor observed-by-case execution contract | Define the non-executing execution contract for future `observed_by_case` materialization. The contract contains ten payload/case jobs, ten planned cache arrays, ten planned result JSON files, eighty future trace solves, sixteen result-schema fields, and three closure items. No commands are executed, no observed arrays are materialized, no FDTD is run, and downstream states remain blocked. |
| 1692 | 84-grid pilot real-executor observed-by-case execution-contract validator | Validate run `1691` from artifacts. Six of six checks pass, confirming the ten-job plan, eighty future trace solves, sixteen-field result schema, three non-ready closure items, zero command execution, zero observed-array materialization, blocked downstream states, and valid figure/script artifacts. |
| 1693 | 84-grid pilot real-executor observed-by-case execution-contract validation sensitivity | Stress-test the run `1692` validator. The exact run `1691` contract passes, while fifteen damaged states fail as expected for source readiness removal, job/payload/case/trace-budget damage, command execution, observed-array/result promotion, schema damage, closure-readiness promotion, FDTD promotion, downstream GPU promotion, figure damage, and missing script snapshots. |
| 1694 | 84-grid pilot real-executor observed-by-case materialization approval gate | Define the filesystem approval boundary for the future `observed_by_case` materialization step. The gate requires one explicit approval token and twenty per-job artifacts: ten cache arrays and ten result JSON files for the run `1691` jobs. No token or artifacts are present, no commands are executed, no observed arrays are materialized, no FDTD is run, and downstream states remain blocked. |
| 1695 | 84-grid pilot real-executor observed-by-case materialization approval-gate validator | Validate run `1694` from artifacts. Five of five checks pass, confirming the absent approval token, twenty absent planned artifacts, zero materialization, zero command execution, zero FDTD execution, blocked downstream states, and valid figure/script artifacts. |
| 1696 | 84-grid pilot real-executor observed-by-case materialization approval-gate validation sensitivity | Stress-test the run `1695` validator. The exact run `1694` approval gate passes, while fourteen damaged states fail as expected for approval-token promotion, artifact promotion, materialization promotion, command/FDTD execution promotion, downstream GPU promotion, figure damage, and missing script snapshots. |
| 1697 | 84-grid pilot real-executor observed-by-case materialization approval-token schema contract | Define the 12-field approval-token schema and output-local template required before future `observed_by_case` materialization can be accepted. Four real approval fields remain blank, the locked external approval token is still absent, downstream permission is explicitly false, and materialization/FDTD/GPU/field/3D states remain blocked. |
| 1698 | 84-grid pilot real-executor observed-by-case materialization approval-token schema-contract validator | Validate run `1697` from artifacts. Five of five checks pass, confirming the 12-field schema, four placeholder fields, local incomplete template, absent external token, blocked materialization/FDTD/downstream states, and valid figure/script artifacts. |
| 1699 | 84-grid pilot real-executor observed-by-case materialization approval-token schema-contract validation sensitivity | Stress-test the run `1698` validator. The exact run `1697` schema contract passes, while fourteen damaged states fail as expected for schema drift, scope/permission damage, external-token promotion, action/materialization/FDTD/downstream promotion, figure damage, and missing script snapshots. |
| 1700 | 84-grid pilot real-executor observed-by-case materialization approval-token external staging guard | Audit the external approval-token boundary after runs `1697-1699`. The local template remains incomplete and output-local, the locked external approval token is absent, zero of 20 planned materialization artifacts exist, and materialization, FDTD execution, GPU work, field transfer, field FWI, and 3D/HPC remain blocked. |
| 1701 | 84-grid pilot real-executor observed-by-case materialization approval-token external staging guard validator | Validate run `1700` from artifacts. Five of five checks pass, confirming the output-local incomplete template, absent external approval token, absent materialization artifacts, blocked execution/downstream states, and valid figure/script artifacts. |
| 1702 | 84-grid pilot real-executor observed-by-case materialization approval-token external staging guard validation sensitivity | Stress-test the run `1701` validator. The exact run `1700` guard passes, while sixteen damaged states fail as expected for template promotion, external-token promotion, materialization-artifact promotion, FDTD/downstream promotion, figure damage, and missing script snapshots. |
| 1703 | 84-grid pilot real-executor observed-by-case materialization approval-token completion route spec | Convert the approval-token blocker into a four-field completion route: `approval_id`, `approval_created_at_utc`, `approved_by`, and `approval_reason`. No fields are complete, no external token is present, and materialization/FDTD/GPU/field/3D states remain blocked. |
| 1704 | 84-grid pilot real-executor observed-by-case materialization approval-token completion route-spec validator | Validate run `1703` from artifacts. Five of five checks pass, confirming the four real approval fields, four-phase route shape, absent external approval token, blocked materialization/FDTD/downstream states, and valid figure/script artifacts. |
| 1705 | 84-grid pilot real-executor observed-by-case materialization approval-token completion route-spec validation sensitivity | Stress-test the run `1704` validator. The exact run `1703` route passes, while fourteen damaged states fail as expected for field completion, external-token promotion, materialization/FDTD/downstream promotion, figure damage, and missing script snapshots. |
| 1706 | 84-grid pilot real-executor observed-by-case materialization approval-token synthetic completion smoke | Fill the four approval-token completion fields in an output-local synthetic token and validate all 12 schema rules. The synthetic token is not the locked external approval token, the real external token remains absent, and materialization/FDTD/GPU/field/3D states remain blocked. |
| 1707 | 84-grid pilot real-executor observed-by-case materialization approval-token synthetic completion smoke validator | Validate run `1706` from artifacts. Five of five checks pass, confirming 12 schema rules, four synthetic completion fields, output-local non-approval token placement, absent external approval, blocked materialization/FDTD/downstream states, and valid figure/script artifacts. |
| 1708 | 84-grid pilot real-executor observed-by-case materialization approval-token synthetic completion smoke validation sensitivity | Stress-test the run `1707` validator. The exact run `1706` smoke passes, while sixteen damaged states fail as expected for schema drift, synthetic-token promotion, external-token promotion, materialization/FDTD/downstream promotion, figure damage, and missing script snapshots. |
| 1709 | 84-grid pilot real-executor observed-by-case materialization approval-token post-synthetic completion external guard | Audit the external approval boundary after the synthetic completion smoke. The synthetic token remains output-local, the locked external approval token is absent, zero of 20 planned materialization artifacts exist, and materialization, FDTD execution, GPU work, field transfer, field FWI, and 3D/HPC remain blocked. |
| 1710 | 84-grid pilot real-executor observed-by-case materialization approval-token post-synthetic completion external guard validator | Validate run `1709` from artifacts. Five of five checks pass, confirming source readiness, upstream chain readiness, synthetic-token placement outside the approval path, absent materialization artifacts, blocked execution/downstream states, and valid figure/script artifacts. |
| 1711 | 84-grid pilot real-executor observed-by-case materialization approval-token post-synthetic completion external guard validation sensitivity | Stress-test the run `1710` validator. The exact run `1709` guard passes, while twenty damaged states fail as expected for synthetic-token promotion, external-token promotion, materialization-artifact promotion, FDTD/downstream promotion, figure damage, and missing script snapshots. |
| 1712 | 84-grid pilot real-executor observed-by-case materialization artifact frontier | Enumerate the materialization artifact frontier for the ten planned observed-by-case jobs. The run covers 1024 job-subset scenarios and finds that zero partial scenarios satisfy the conservative artifact gate: all ten jobs and all 20 cache/result artifacts are required before materialization or FDTD execution can proceed. |
| 1713 | 84-grid pilot real-executor observed-by-case materialization artifact frontier validator | Validate run `1712` from artifacts. Five of five checks pass, confirming the 10-job/20-artifact plan, the 1024-scenario frontier shape, all-job-only completion, blocked materialization/FDTD/downstream state, and valid figure/script artifacts. |
| 1714 | 84-grid pilot real-executor observed-by-case materialization artifact frontier validation sensitivity | Stress-test the run `1713` validator. The exact run `1712` frontier passes, while sixteen damaged states fail as expected for job-table drift, artifact promotion, partial-completion promotion, materialization/FDTD promotion, downstream promotion, figure damage, and missing script snapshots. |
| 1715 | 84-grid pilot real-executor observed-by-case materialization work-split policy | Convert the `1712-1714` frontier into a work-split policy: one external approval token, ten cache-array artifacts, ten result-JSON artifacts, and one final all-artifact acceptance gate. No partial delivery authorizes materialization or FDTD execution. |
| 1716 | 84-grid pilot real-executor observed-by-case materialization work-split policy validator | Validate run `1715` from artifacts. Five of five checks pass, confirming the approval/cache/result/acceptance route, 21 required items, 20 materialization artifacts, blocked materialization/FDTD/downstream state, and valid figure/script artifacts. |
| 1717 | 84-grid pilot real-executor observed-by-case materialization work-split policy validation sensitivity | Stress-test the run `1716` validator. The exact run `1715` policy passes, while eighteen damaged states fail as expected for approval-count damage, cache/result count damage, dependency damage, materialization/FDTD/downstream promotion, figure damage, and missing script snapshots. |
| 1718 | 84-grid pilot real-executor observed-by-case materialization post-work-split external guard | Audit the locked external approval-token path and 20 planned materialization artifact paths after the work-split policy block. The guard checks 21 external items and confirms zero parent directories, zero present items, zero accepted items, zero FDTD execution, and blocked downstream states. |
| 1719 | 84-grid pilot real-executor observed-by-case materialization post-work-split external guard validator | Validate run `1718` from artifacts. Five of five checks pass, confirming exact 21-row external shape, absent approval/materialization files, blocked materialization/FDTD/downstream state, and valid figure/script artifacts. |
| 1720 | 84-grid pilot real-executor observed-by-case materialization post-work-split external guard validation sensitivity | Stress-test the run `1719` validator. The exact run `1718` guard passes, while ten damaged states fail as expected for row removal, false external token or artifact presence, false acceptance, materialization/FDTD/downstream promotion, figure damage, and missing script snapshots. |
| 1721 | 84-grid pilot real-executor observed-by-case materialization post-work-split approval-token template pack | Create one output-local fillable approval-token template tied to the latest schema and external guard. The template has 12 fields, with eight prefilled values and four real approval placeholders, but it is not the locked external token. Zero external approval/materialization files are present, and materialization/FDTD/downstream states remain blocked. |
| 1722 | 84-grid pilot real-executor observed-by-case materialization post-work-split approval-token template-pack validator | Validate run `1721` from artifacts. Five of five checks pass, confirming the output-local template placement, 12-field schema payload, four real approval placeholders, blocked external approval/materialization state, blocked FDTD/downstream states, figure validation, and script snapshots. |
| 1723 | 84-grid pilot real-executor observed-by-case materialization post-work-split approval-token template-pack validation sensitivity | Stress-test the run `1722` validator. The exact run `1721` template pack passes, while nineteen damaged states fail as expected for template/schema/path damage, false external approval, false materialization, FDTD/downstream promotion, figure damage, and missing script snapshots. |
| 1724 | 84-grid pilot real-executor observed-by-case materialization result JSON template pack | Create ten output-local result JSON templates for the future observed-by-case materialization jobs. The templates cover five payloads and two cases, prefill stable identity fields, leave 100 future solver/hash/runtime placeholders, and keep all external result files, materialization, FDTD execution, GPU work, field transfer, and 3D/HPC blocked. |
| 1725 | 84-grid pilot real-executor observed-by-case materialization result JSON template-pack validator | Validate run `1724` from artifacts. Five of five checks pass, confirming ten templates, five-payload/two-case coverage, 160 schema fields, blank future solver/hash/runtime values, absent external results, blocked FDTD/downstream states, figure validation, and script snapshots. |
| 1726 | 84-grid pilot real-executor observed-by-case materialization result JSON template-pack validation sensitivity | Stress-test the run `1725` validator. The exact run `1724` template pack passes, while twenty-one damaged states fail as expected for source readiness, row/schema/payload damage, false external result promotion, false materialization/FDTD/downstream promotion, figure damage, and missing script snapshots. |
| 1727 | 84-grid pilot real-executor observed-by-case materialization result JSON template-pack external-result path guard | Audit the locked external result paths after the run `1724-1726` result-template block. Ten output-local templates exist, zero external result files exist, zero templates overlap or sit under the external-result root, and materialization/FDTD/downstream states remain blocked. |
| 1728 | 84-grid pilot real-executor observed-by-case materialization result JSON template-pack external-result path-guard validator | Validate run `1727` from artifacts. Five of five checks pass, confirming ten-row/five-payload/two-case shape, empty and separated external result paths, blocked materialization/FDTD/downstream states, figure validation, and script snapshots. |
| 1729 | 84-grid pilot real-executor observed-by-case materialization result JSON template-pack external-result path-guard validation sensitivity | Stress-test the run `1728` validator. The exact run `1727` guard passes, while twenty-four damaged states fail as expected for row shape, external-result promotion, path overlap, template-under-external-root promotion, FDTD/downstream promotion, figure damage, and missing script snapshots. |
| 1730 | 84-grid pilot real-executor observed-by-case materialization return-packet intake contract | Join the external approval token, ten planned cache arrays, and ten result JSON files into one 21-item non-executed intake contract. The approval token and result JSON items have output-local templates, cache arrays do not, zero external items are present or accepted, and materialization/FDTD/GPU/field/3D states remain blocked. |
| 1731 | 84-grid pilot real-executor observed-by-case materialization return-packet intake-contract validator | Validate run `1730` from artifacts. Five of five checks pass, confirming the 21-item/3-role shape, approval/result-template linkage, cache arrays without templates, zero present or accepted external items, blocked materialization/FDTD/downstream states, figure validation, and script snapshots. |
| 1732 | 84-grid pilot real-executor observed-by-case materialization return-packet intake-contract validation sensitivity | Stress-test the run `1731` validator. The exact run `1730` contract passes, while twenty-five damaged states fail as expected for source/shape damage, template-boundary damage, external-item promotion, materialization/FDTD/downstream promotion, figure damage, and missing script snapshots. |
| 1733 | 84-grid pilot real-executor observed-by-case materialization return-packet sandbox completion smoke | Exercise the positive intake path for the run `1730` 21-item contract in an output-local sandbox. One synthetic approval JSON, ten synthetic cache NPZ files, and ten synthetic result JSON files pass file/parse/load mechanics; zero external items are present, zero observed data are materialized, and FDTD/GPU/field/3D states remain blocked. |
| 1734 | 84-grid pilot real-executor observed-by-case materialization return-packet sandbox completion-smoke validator | Validate run `1733` from artifacts. Five of five checks pass, confirming the 21-item/3-role sandbox shape, file parse/load mechanics, empty external paths, zero materialization, zero FDTD execution, blocked downstream states, figure validation, and script snapshots. |
| 1735 | 84-grid pilot real-executor observed-by-case materialization return-packet sandbox completion-smoke validation sensitivity | Stress-test the run `1734` validator. The exact run `1733` smoke passes, while twenty-eight damaged states fail as expected for source readiness, packet shape, file-mechanics damage, external/materialization/FDTD/GPU/field/3D promotion, figure damage, and missing script snapshots. |
| 1736 | 84-grid pilot real-executor observed-by-case materialization return-packet post-sandbox external-path guard | Audit the locked external return paths after the run `1733-1735` sandbox completion block. All 21 expected external paths remain empty, all 21 sandbox items remain output-local, zero sandbox items overlap or sit under the external return root, zero observed data are materialized, and FDTD/GPU/field/3D states remain blocked. |
| 1737 | 84-grid pilot real-executor observed-by-case materialization return-packet post-sandbox external-path guard validator | Validate run `1736` from artifacts. Five of five checks pass, confirming exact 21-row/3-role guard shape, empty and separated external paths, blocked materialization/FDTD/downstream states, figure validation, and script snapshots. |
| 1738 | 84-grid pilot real-executor observed-by-case materialization return-packet post-sandbox external-path guard validation sensitivity | Stress-test the run `1737` validator. The exact run `1736` guard passes, while twenty-eight damaged states fail as expected for row-shape damage, external-item promotion, path overlap, sandbox-under-external-root promotion, materialization/FDTD/downstream promotion, figure damage, and missing script snapshots. |
| 1739 | 84-grid pilot real-executor observed-by-case materialization return-packet live delta monitor | Convert the post-sandbox external-path guard into a live readiness monitor. Zero of 21 expected external items are present, all 21 are missing, all three roles are incomplete, and materialization/FDTD/GPU/field/3D states remain blocked. |
| 1740 | 84-grid pilot real-executor observed-by-case materialization return-packet live delta monitor validator | Validate run `1739` from artifacts. Five of five checks pass, confirming exact 21-item/3-role shape, empty external return packet, blocked materialization/FDTD/downstream states, figure validation, and script snapshots. |
| 1741 | 84-grid pilot real-executor observed-by-case materialization return-packet live delta monitor validation sensitivity | Stress-test the run `1740` validator. The exact run `1739` monitor passes, while twenty damaged states fail as expected for item/role/count damage, external-item promotion, materialization or FDTD promotion, downstream promotion, figure damage, and missing script snapshots. |
| 1742 | 84-grid pilot real-executor observed-by-case materialization return-packet live delta closure sequence | Convert the live delta into a closure sequence: one external approval token, ten cache arrays, ten result JSON files, and a final materialization gate. Zero of 21 external items are present, so materialization, FDTD execution, GPU work, field transfer, and 3D/HPC remain blocked. |
| 1743 | 84-grid pilot real-executor observed-by-case materialization return-packet live delta closure sequence validator | Validate run `1742` from artifacts. Six of six checks pass, confirming exact 21-item/4-action shape, 1/10/10 approval-cache-result accounting, empty external packet, blocked materialization/FDTD/downstream states, figure validation, and script snapshots. |
| 1744 | 84-grid pilot real-executor observed-by-case materialization return-packet live delta closure sequence validation sensitivity | Stress-test the run `1743` validator. The exact closure sequence passes, while seventeen damaged states fail as expected for shape/count drift, fake external returns, materialization or execution promotion, downstream promotion, figure damage, and missing script snapshots. |
| 1745 | 84-grid pilot real-executor observed-by-case materialization critical-path audit | Convert the 21-item return-packet closure into a dependency audit: one root approval-token blocker, twenty parallel cache/result artifact blockers, and a final all-items materialization gate. Zero external items are present, so materialization, FDTD execution, GPU work, field transfer, and 3D/HPC remain blocked. |
| 1746 | 84-grid pilot real-executor observed-by-case materialization critical-path audit validator | Validate run `1745` from artifacts. Six of six checks pass, confirming 21-item/4-action/3-level dependency shape, one root token, twenty parallel artifacts, empty external packet state, blocked materialization/FDTD/downstream states, figure validation, and script snapshots. |
| 1747 | 84-grid pilot real-executor observed-by-case materialization critical-path audit validation sensitivity | Stress-test the run `1746` validator. The exact critical-path audit passes, while sixteen damaged states fail as expected for source/shape/dependency damage, false external-item promotion, false materialization/FDTD/GPU/field/3D promotion, figure damage, and missing script snapshots. |
| 1748 | 84-grid pilot real-executor observed-by-case materialization approval-token fillability audit | Expand the root approval-token blocker into 12 fields: eight prefilled context fields and four real approval fields still blank (`approval_id`, `approval_created_at_utc`, `approved_by`, and `approval_reason`). The external approval token is absent, so materialization and FDTD execution remain blocked. |
| 1749 | 84-grid pilot real-executor observed-by-case materialization approval-token fillability audit validator | Validate run `1748` from artifacts. Six of six checks pass, confirming 12-field/3-action shape, four missing approval fields, absent external approval token, blocked materialization/FDTD/downstream states, figure validation, and script snapshots. |
| 1750 | 84-grid pilot real-executor observed-by-case materialization approval-token fillability audit validation sensitivity | Stress-test the run `1749` validator. The exact approval-token fillability audit passes, while sixteen damaged states fail as expected for source/shape/field-count damage, false approval/external-token promotion, false materialization/FDTD/GPU/field/3D promotion, figure damage, and missing script snapshots. |
| 1751 | 84-grid pilot real-executor observed-by-case materialization approval-token live-path rescan | Rescan the locked external approval-token path after run `1748`. The blocker is now split into three concrete items: the external parent directory is missing, the external approval token file is missing, and the four real approval fields are still blank. Materialization and FDTD execution remain blocked. |
| 1752 | 84-grid pilot real-executor observed-by-case materialization approval-token live-path rescan validator | Validate run `1751` from artifacts. Six of six checks pass, confirming 12-field/5-action shape, four blank approval fields, absent external approval directory and token file, blocked materialization/FDTD/downstream states, figure validation, and script snapshots. |
| 1753 | 84-grid pilot real-executor observed-by-case materialization approval-token live-path rescan validation sensitivity | Stress-test the run `1752` validator. The exact approval-token live-path rescan passes, while seventeen damaged states fail as expected for source/shape/count damage, false external directory or token promotion, false materialization/FDTD/GPU/field/3D promotion, figure damage, and missing script snapshots. |
| 1754 | 84-grid pilot real-executor observed-by-case materialization approval-token directory scaffold | Create or verify the external approval-token drop directory. The directory now exists and is empty; the external token file is still absent and four real approval fields remain blank. Materialization and FDTD execution remain blocked. |
| 1755 | 84-grid pilot real-executor observed-by-case materialization approval-token directory scaffold validator | Validate run `1754` from artifacts. Six of six checks pass, confirming one scaffold path, parent directory present, external token absent, four real approval fields still missing, one completed action, and blocked materialization/FDTD/GPU/field/3D states. |
| 1756 | 84-grid pilot real-executor observed-by-case materialization approval-token directory scaffold validation sensitivity | Stress-test the run `1755` validator. The exact directory-scaffold state passes, while fifteen damaged states fail for source, table-shape, directory, token, approval-count, downstream-promotion, figure, and script-snapshot damage. |
| 1757 | 84-grid pilot real-executor observed-by-case materialization approval-token post-scaffold live-state refresh | Refresh the live state after the directory scaffold. The parent directory is now present, but the external approval token is still absent and four real approval fields remain blank. Materialization and FDTD execution remain blocked. |
| 1758 | 84-grid pilot real-executor observed-by-case materialization approval-token post-scaffold live-state refresh validator | Validate run `1757` from artifacts. Seven of seven checks pass, confirming directory closure, absent token, four missing approval fields, two completed actions, and blocked materialization/FDTD/GPU/field/3D states. |
| 1759 | 84-grid pilot real-executor observed-by-case materialization approval-token post-scaffold live-state refresh validation sensitivity | Stress-test the run `1758` validator. The exact post-scaffold state passes, while fifteen damaged states fail for source, table-shape, directory, token, approval, downstream-promotion, figure, and script-snapshot damage. |
| 1760 | 84-grid pilot real-executor observed-by-case materialization approval-token handoff template pack | Create a non-live handoff template for the remaining approval token. The template preserves eight context fields, leaves four real approval fields blank, does not create a live external token, and keeps materialization/FDTD/GPU/field/3D work blocked. |
| 1761 | 84-grid pilot real-executor observed-by-case materialization approval-token handoff template pack validator | Validate run `1760` from artifacts. Eight of eight checks pass, confirming template-only status, four blank approval fields, absent external token, one completed context action, and blocked downstream execution. |
| 1762 | 84-grid pilot real-executor observed-by-case materialization approval-token handoff template pack validation sensitivity | Stress-test the run `1761` validator. The exact non-live handoff-template state passes, while sixteen damaged or falsely promoted states fail as expected. |
| 1763 | Post-1762 2D decision frontier audit | Consolidate the current 2D objective-revision branch from saved artifacts. The audit records eight decision blocks and four frontier items: the revised objective is guarded local evidence, the 45 mm acquisition result is a narrow sampled edge, the 84-row subset and five-row pilot are specified, and observed_by_case materialization remains blocked by four blank approval fields and an absent external approval token. |
| 1764 | Post-1762 2D decision frontier audit validator | Validate run `1763` from artifacts. Nine of nine checks pass, confirming the decision/frontier shape, local objective success, mechanism and acquisition-edge metrics, runtime and pilot counts, preserved approval blocker, blocked downstream gates, figure output, and script snapshots. |
| 1765 | Post-1762 2D decision frontier audit validation sensitivity | Stress-test the run `1764` validator. The exact run `1763` artifact passes, while sixteen damaged or falsely promoted states fail with zero unexpected outcomes. Use runs `1763-1765` as the guarded current 2D decision frontier. |
| 1766 | 84-grid materialization staged return packet | Convert the existing critical path and approval-token handoff into a six-stage external return packet. The path is approval token, first nominal job smoke, first time-shift job smoke, middle payload jobs, late payload jobs, then final materialization gate. All 21 external items remain absent, so materialization, FDTD execution, GPU work, field transfer, field FWI, and 3D/HPC remain blocked. |
| 1767 | 84-grid stage-1 approval-token smoke | Fill the approval-token schema in an output-local synthetic token. All four approval fields and eight context fields are present, but the real external approval token and all twenty materialization artifacts remain absent, so materialization and FDTD execution remain blocked. |
| 1768 | 84-grid stage-1 live approval contract | Define the exact external approval-token file that must replace the synthetic approval smoke. The expected file is `APPROVED_1691_OBSERVED_BY_CASE_EXECUTION.json` with four real approval fields. The parent directory exists, but the live token is absent, so materialization and FDTD execution remain blocked. |
| 1769 | 84-grid first nominal job live artifact contract | Define the exact cache array and result JSON files for the first nominal observed-by-case job after approval. The `payload_001` parent directory and both artifacts are absent, so materialization and FDTD execution remain blocked. |
| 1770 | 84-grid first time-shift job live artifact contract | Define the exact cache array and result JSON files for the paired first time-shift observed-by-case job after approval. Together with run `1769`, the first-payload smoke return now requires four external artifacts. The `payload_001` parent directory and both time-shift artifacts are absent, so materialization and FDTD execution remain blocked. |
| 1771 | 84-grid middle payload live artifact contract | Define the exact cache array and result JSON files for payloads `023` and `046`, including nominal and time-shift cases. The middle stage requires eight artifacts across four jobs, bringing the cumulative artifact contract through this stage to twelve artifacts. The payload parent directories and artifacts are absent, so materialization and FDTD execution remain blocked. |
| 1772 | 84-grid late payload live artifact contract | Define the exact cache array and result JSON files for payloads `068` and `072`, including nominal and time-shift cases. The late stage adds eight artifacts across four jobs, bringing the full artifact contract to twenty files after approval. The payload parent directories and artifacts are absent, so materialization and FDTD execution remain blocked. |
| 1773 | 84-grid external return contract ledger | Close the six-stage external-return checklist into one ledger: one approval token, twenty artifact files, ten cache arrays, ten result JSON files, ten artifact jobs, and five payloads. Only the approval-token parent directory exists; all twenty-one external items are absent, so materialization and FDTD execution remain blocked. |
| 1774 | 84-grid external return contract ledger validator | Validate the saved run `1773` ledger from artifacts. Seven of seven checks pass, confirming source readiness, twenty-one external items, one approval token, twenty artifacts, preserved stage shape, absent external state, blocked materialization, blocked downstream states, and figure/script snapshot presence. |
| 1775 | 84-grid external return contract ledger validation sensitivity | Sensitivity-harden the run `1774` validator. The exact run `1773` ledger passes, while eight damaged states fail as expected: source readiness damage, external-item count drift, file-status damage, stage-shape damage, false materialization, downstream promotion, figure damage, and script-snapshot damage. Use runs `1773-1775` as the guarded 2D external-return ledger block. |
| 1776 | 84-grid external return live intake gate | Convert the external-return ledger into a live intake gate for real files. The gate checks the approval token, ten cache arrays, ten result JSON files, and ten paired artifact jobs. The current live area has zero accepted items: one approval token, ten cache arrays, and ten result JSON files are missing, so materialization and FDTD execution remain blocked. |
| 1777 | 84-grid external return live intake gate validator | Validate the saved run `1776` live intake artifacts. Seven of seven checks pass, confirming the 21-item return contract, ten paired artifact jobs, missing current live files, zero accepted items, blocked materialization, blocked downstream states, and figure/script snapshot presence. |
| 1778 | 84-grid external return live intake gate validation sensitivity | Sensitivity-harden the run `1777` validator. The exact run `1776` live intake state passes, while ten damaged states fail as expected: source readiness damage, item-count drift, file-status damage, intake-status damage, false item acceptance, false paired-job acceptance, false materialization, downstream promotion, figure damage, and script-snapshot damage. Use runs `1776-1778` as the guarded 2D external-return live intake block. |
| 1779 | 84-grid external return package template pack | Write one output-local approval JSON template and ten output-local result JSON templates for the 21-item external return package. No fake cache-array templates are written, no external evidence is accepted, and materialization/FDTD remain blocked until the real approval token, ten real cache arrays, and ten real result JSON files pass live intake. |
| 1780 | 84-grid external return package template pack validator | Validate the saved run `1779` package template artifacts. Seven of seven checks pass, confirming the 21-item package, eleven JSON templates, zero cache-array templates, preserved stage shape, ten represented artifact jobs, blocked materialization, and figure/script snapshot presence. |
| 1781 | 84-grid external return package template pack validation sensitivity | Sensitivity-harden the run `1780` validator. The exact run `1779` template pack passes, while eleven damaged states fail as expected: source readiness damage, item-count drift, template-count drift, fake cache-template creation, stage-shape damage, artifact-job damage, false acceptance, false materialization, false FDTD promotion, figure damage, and script-snapshot damage. Use runs `1779-1781` as the guarded 2D external-return package-template block. |
| 1782 | 84-grid external return package live intake reconciliation | Reconcile the run `1779` package templates against the run `1776` live intake paths. The current state has one output-local approval template, ten output-local result JSON templates, zero cache-array templates, zero live files, zero accepted external items, and zero artifact jobs ready for guarded intake. Materialization and FDTD remain blocked until the real approval token, ten real cache arrays, and ten real result JSON files pass live intake. |
| 1783 | 84-grid external return package live intake reconciliation validator | Validate the saved run `1782` reconciliation artifacts. Seven of seven checks pass, confirming the 21 external items, five-stage shape, one approval template, ten result JSON templates, zero cache-array templates, zero live files, preserved status split, blocked artifact jobs, blocked materialization, and figure/script snapshot presence. |
| 1784 | 84-grid external return package live intake reconciliation validation sensitivity | Sensitivity-harden the run `1783` validator. The exact run `1782` reconciliation state passes, while fifteen damaged states fail as expected: source flag damage, item-count damage, stage-count damage, template-count damage, cache-template damage, live-file promotion, ready-for-intake promotion, false acceptance, status-split damage, artifact-job count damage, artifact-job promotion, materialization promotion, FDTD promotion, figure damage, and script-snapshot damage. Use runs `1782-1784` as the guarded 84-grid external-return live-intake reconciliation block. |
| 1785 | 84-grid external return staging plan | Create the non-executed staging plan for the 21 real external-return items: one approval token, ten cache arrays, and ten result JSON files. The run defines exact live-path copy commands and six guarded action groups, but executes zero commands, stages zero templates, materializes no observed-by-case data, and executes no FDTD. |
| 1786 | 84-grid external return staging plan validator | Validate the saved run `1785` staging plan. Seven of seven checks pass, confirming the 21 external items, one approval-token requirement, ten cache-array requirements, ten result-JSON requirements, ten artifact jobs, non-stageable templates, absent real files, 21 non-executed commands, blocked materialization/FDTD states, and figure/script snapshot presence. |
| 1787 | 84-grid external return staging plan validation sensitivity | Sensitivity-harden the run `1786` validator. The exact run `1785` non-executed staging plan passes, while twenty-two damaged states fail as expected: source flag damage, item-count damage, stage-count damage, approval/cache/result/job count damage, template-copy permission damage, real-file and live-file promotions, ready-to-stage promotion, executed-command promotion, copy-command damage, action-count damage, ready-action promotion, materialization promotion, FDTD promotion, downstream promotion, figure damage, and script-snapshot damage. Use runs `1785-1787` as the guarded 84-grid external-return staging-plan block. |
| 1788 | 84-grid external return preflight gate | Define the real-file preflight gate for the 21 external-return items. The gate separately checks one approval JSON, ten cache-array NPZ files, ten result JSON files, and ten paired artifact jobs before staging. The current state remains pre-return: zero producer files are present, zero files pass core checks, zero paired jobs are ready, zero items are stageable, and materialization/FDTD remain blocked. |
| 1789 | 84-grid external return preflight gate validator | Validate the saved run `1788` preflight gate. Seven of seven checks pass, confirming the 21-item/5-stage shape, approval/cache/result/job accounting, absent producer files, zero core preflight passes, zero stageable or executed items, blocked materialization/FDTD/downstream states, and figure/script snapshot presence. |
| 1790 | 84-grid external return preflight gate validation sensitivity | Sensitivity-harden the run `1789` validator. The exact run `1788` state passes, while twenty-five damaged states fail as expected for source/shape/count damage, file-presence promotion, approval/cache/result core-check promotion, paired-job promotion, stage/execute promotion, materialization/FDTD/downstream promotion, figure damage, and script-snapshot damage. Use runs `1788-1790` as the guarded 84-grid external-return preflight block. |
| 1791 | 84-grid external return preflight claim boundary | Record the claim boundary after the preflight block. Two claims are guarded: external-return requirements and the fail-closed preflight gate. Three claims remain blocked: observed-by-case materialization, new FDTD execution or physical claims, and field/3D transfer. Zero candidate files are present, zero items pass preflight, and no downstream claim is promoted. |
| 1792 | 84-grid external return preflight claim boundary validator | Validate the saved run `1791` claim boundary. Seven of seven checks pass, confirming claim counts, source preflight metrics, guarded rows, blocked rows, downstream blocks, figure validation, and script snapshots. |
| 1793 | 84-grid external return preflight claim boundary validation sensitivity | Sensitivity-harden the run `1792` validator. The exact run `1791` boundary passes, while fourteen damaged states fail as expected for policy-label damage, claim-count damage, guarded-count damage, blocked-count damage, guarded-ready damage, blocked-ready promotion, candidate-file promotion, preflight-pass promotion, materialization promotion, FDTD promotion, downstream promotion, sensitivity damage, figure damage, and script-snapshot damage. Use runs `1791-1793` as the guarded post-preflight claim-boundary block. |
| 1794 | 84-grid external return dependency audit | Split the guarded external-return blocker into one approval dependency and ten paired artifact jobs. The audit confirms 21 required return items, 10 cache-array NPZ files, 10 result JSON files, 10 paired jobs, zero producer files present, zero paired jobs ready, zero preflight-passed items, and no materialization/FDTD/field/3D promotion. |
| 1795 | 84-grid external return dependency audit validator | Validate the saved run `1794` dependency audit. Eight of eight checks pass, confirming the source identity, stage counts, job-pair requirements, blocked preflight state, action rows, downstream blocked states, figure validation, and script snapshots. |
| 1796 | 84-grid external return dependency audit validation sensitivity | Sensitivity-harden the run `1795` validator. The exact run `1794` dependency audit passes, while nineteen damaged states fail as expected, including count damage, fake producer-file promotion, fake ready-pair promotion, action-readiness promotion, materialization promotion, FDTD promotion, downstream promotion, figure damage, and script-snapshot damage. |
| 1797 | 84-grid external return file-slot manifest | Convert the guarded dependency audit into a file-level checklist: one approval JSON, ten cache-array NPZ files, and ten result JSON files across ten paired artifact jobs. Zero producer files are present, zero slots pass preflight, zero paired jobs are ready, and observed-by-case materialization, new FDTD execution, field transfer, and 3D/HPC remain blocked. |
| 1798 | 84-grid external return file-slot manifest validator | Validate the saved run `1797` file-slot manifest. Eight of eight checks pass, confirming the 21-slot shape, 1/10/10 approval-cache-result split, ten paired artifact jobs, zero producer files, zero ready slots, blocked materialization/FDTD states, figure validation, and script snapshots. |
| 1799 | 84-grid external return file-slot manifest validation sensitivity | Sensitivity-harden the run `1798` validator. The exact run `1797` manifest passes, while fifteen damaged states fail as expected for slot/stage/kind/pair damage, fake producer files, fake preflight or ready slots, materialization/FDTD/downstream promotion, figure damage, and script-snapshot damage. |
| 1800 | 84-grid external return file-slot manifest claim boundary | Record the claim boundary after the 21-slot external-return manifest. Two claims are guarded: the file-slot manifest and the paired artifact job contract. Three claims remain blocked: real external-return files, observed-by-case materialization, and new FDTD execution or downstream transfer. |
| 1801 | 84-grid external return file-slot manifest claim boundary validator | Validate the saved run `1800` boundary. Seven of seven checks pass, confirming the five-claim shape, two guarded claims, three blocked claims, one approval JSON slot, ten cache NPZ slots, ten result JSON slots, ten paired artifact jobs, zero producer files, blocked materialization/FDTD/downstream states, figure validation, and script snapshots. |
| 1802 | 84-grid external return file-slot manifest claim boundary validation sensitivity | Sensitivity-harden the run `1801` validator. The exact run `1800` boundary passes, while twenty-two damaged states fail as expected for claim-count drift, missing guarded claims, file-slot/stage/count damage, paired-job damage, false producer-file/preflight/materialization/FDTD/field/3D promotion, figure damage, and script-snapshot damage. |
| 1803 | 84-grid external return approval template pack | Create an output-local approval JSON template for the guarded 84-grid external-return package. The ten expected artifact jobs are prefilled, but the approval token remains false, approval provenance is blank, no template is written under the external return root, and artifact intake, materialization, new FDTD execution, field transfer, and 3D/HPC remain blocked. |
| 1804 | 84-grid external return approval template pack validator | Validate the saved run `1803` approval template pack. Seven of seven checks pass, confirming the one-template shape, ten expected artifact jobs, false approval token, blank approval provenance, output-local placement, zero live approvals, blocked artifact intake/materialization/FDTD/downstream states, figure validation, and script snapshots. |
| 1805 | 84-grid external return approval template pack validation sensitivity | Sensitivity-harden the run `1804` validator. The exact run `1803` draft approval template passes, while twenty-one damaged states fail as expected for source/template/payload damage, approval-token and provenance promotion, external-root damage, live-approval promotion, materialization/FDTD/field/3D promotion, figure damage, and script-snapshot damage. |
| 1806 | BEM stage-1 complex FDTD producer request boundary audit | Audit whether the current 2D FDTD approval-template branch can authorize the new BEM stage-1 complex-field producer request. The BEM request is one row, receiver `15` at `1.0 GHz`; the current 2D approval template is a ten-payload draft template with no live approval token, zero live external files, and no authority to generate or accept the BEM return. FDTD execution, real BEM/FDTD comparison, field transfer, and 3D/HPC remain blocked. |
| 1807 | BEM stage-1 complex FDTD producer request boundary audit validator | Validate the saved run `1806` boundary audit. Eight of eight checks pass, confirming the five-row boundary, one-row BEM request, receiver `15` at `1.0 GHz`, separate ten-payload 2D draft approval template, zero live 2D external files, blocked FDTD authorization/execution, blocked real BEM/FDTD comparison, blocked field/3D transfer, figure validation, and script snapshots. |
| 1808 | BEM stage-1 complex FDTD producer request boundary audit validation sensitivity | Sensitivity-harden the run `1807` validator. The exact run `1806` non-authorizing boundary passes, while twenty damaged states fail as expected for source readiness, boundary shape, BEM receiver/frequency identity, false 2D approval, false live files, false BEM partial/full files, false FDTD authorization/execution, comparison/field/3D promotion, figure damage, and script-snapshot damage. Use runs `1806-1808` as the guarded 2D boundary for the BEM stage-1 FDTD producer request. |
| 1809 | BEM stage-1 complex FDTD live approval contract | Define the 2D-side live approval contract for the first BEM stage-1 complex-field FDTD return. The contract has two rows: one live approval JSON with nine required approval fields and one BEM partial-return CSV with the 12-column complex adapter schema. The target is receiver `15` at `1.0 GHz`; no live approval JSON or partial-return CSV exists, so FDTD execution and BEM/FDTD comparison remain blocked. |
| 1810 | BEM stage-1 complex FDTD live approval contract validator | Validate the saved run `1809` live approval contract. Eight of eight checks pass, confirming the two-row contract, nine approval fields, 12 partial-return CSV columns, receiver `15` at `1.0 GHz`, absent live approval and BEM partial files, zero accepted approvals, blocked FDTD authorization/execution, blocked BEM/FDTD comparison, figure validation, and script snapshots. |
| 1811 | BEM stage-1 complex FDTD live approval contract validation sensitivity | Sensitivity-harden the run `1810` validator. The exact run `1809` non-authorizing contract passes, while eighteen damaged states fail as expected for source readiness, contract shape, approval fields, partial-return schema, receiver/frequency identity, false live approval or BEM partial files, false approval acceptance, FDTD authorization/execution, comparison/field/3D promotion, figure damage, and script-snapshot damage. Use runs `1809-1811` as the guarded 2D live-approval contract for the BEM stage-1 FDTD return. |
| 1812 | BEM stage-1 complex FDTD live approval directory scaffold | Create the empty live-approval drop directory for the BEM stage-1 FDTD return. The directory exists under the 2D external-return area, but the live approval JSON is absent, no approval is accepted, no FDTD producer is authorized, no FDTD is executed, and BEM/FDTD comparison remains blocked. |
| 1813 | BEM stage-1 complex FDTD live approval directory scaffold validator | Validate the saved run `1812` directory scaffold. Six of six checks pass, confirming the scaffold row, present approval directory, absent live approval JSON, zero accepted approvals, blocked FDTD authorization/execution, blocked BEM/FDTD comparison, figure validation, and script snapshots. |
| 1814 | BEM stage-1 complex FDTD live approval directory scaffold validation sensitivity | Sensitivity-harden the run `1813` validator. The exact run `1812` empty-directory state passes, while fourteen damaged states fail as expected for source readiness, row shape, directory absence, false approval-file presence, false approval acceptance, FDTD authorization/execution, comparison/field/3D promotion, figure damage, and script-snapshot damage. Use runs `1812-1814` as the guarded empty approval-directory scaffold block. |
| 1815 | BEM stage-1 complex FDTD live approval post-scaffold live-state refresh | Refresh the live state after the BEM-specific approval directory scaffold. The directory now exists, but the live approval JSON is still absent, all nine approval fields are missing, the stage-1 partial complex-field return CSV is absent, only one of five actions is complete, and FDTD execution plus BEM/FDTD comparison remain blocked. |
| 1816 | BEM stage-1 complex FDTD live approval post-scaffold live-state refresh validator | Validate the saved run `1815` state. Seven of seven checks pass, confirming source readiness, two state rows, five action rows, directory presence, approval-file absence, nine missing approval fields, partial-return absence, one completed action, blocked FDTD execution/comparison/downstream states, figure validation, and script snapshots. |
| 1817 | BEM stage-1 complex FDTD live approval post-scaffold live-state refresh validation sensitivity | Sensitivity-harden the run `1816` validator. The exact run `1815` post-scaffold state passes, while seventeen damaged states fail as expected for source, row-shape, approval-directory, approval-file, approval-field, approval-acceptance, partial-return, action-completion, FDTD execution, comparison, field/3D, figure, and script-snapshot damage. Use runs `1815-1817` as the guarded BEM stage-1 post-scaffold live-state refresh block. |
| 1818 | BEM stage-1 complex FDTD live approval template pack | Create an output-local approval JSON template for the first BEM stage-1 FDTD return. The template pre-fills five immutable target fields, including the partial-return CSV schema, and leaves four approval-provenance fields blank. It is not written under the live approval root, no live approval is accepted, FDTD remains unauthorized, and BEM/FDTD comparison remains blocked. |
| 1819 | BEM stage-1 complex FDTD live approval template pack validator | Validate the saved run `1818` template pack. Six of six checks pass, confirming source readiness, one output-local template, nine required approval fields, five target fields prefilled, four approval-provenance fields blank, draft status, blocked live approval, blocked FDTD execution, blocked comparison/downstream states, figure validation, and script snapshots. |
| 1820 | BEM stage-1 complex FDTD live approval template pack validation sensitivity | Sensitivity-harden the run `1819` validator. The exact output-local draft template state passes, while eighteen damaged states fail as expected for source readiness, row/field damage, target-prefill damage, approval-provenance promotion, draft-status promotion, live-root promotion, live-approval/acceptance promotion, FDTD authorization/execution, comparison, field/3D, figure, and script-snapshot damage. Use runs `1818-1820` as the guarded BEM stage-1 approval-template block. |
| 1821 | BEM stage-1 complex FDTD live approval parser positive control | Create an output-local positive control for the approval parser. The payload has all five parser checks passing and matches the requested one-row BEM stage-1 FDTD return shape, but it is not written under the live approval root, no live approval is accepted, FDTD remains unexecuted, and comparison/field/3D promotion remains blocked. |
| 1822 | BEM stage-1 complex FDTD live approval parser positive control validator | Validate the saved run `1821` positive-control state. Five of five checks pass, confirming source readiness, parser payload shape, output-local non-live placement, zero accepted live approvals, blocked FDTD execution/comparison/downstream states, figure validation, and script snapshots. |
| 1823 | BEM stage-1 complex FDTD live approval parser positive control validation sensitivity | Sensitivity-harden the run `1822` validator. The exact non-live parser positive-control state passes, while seventeen damaged states fail as expected for source/template readiness, control-file damage, parser-row/check damage, target-field damage, live-root/live-approval/acceptance promotion, FDTD authorization/execution, comparison, field/3D, figure, and script-snapshot damage. Use runs `1821-1823` as the guarded parser positive-control block. |
| 1824 | BEM stage-1 complex FDTD live approval acceptance gate | Evaluate the real live approval path after the parser positive-control block. The output-local positive control is not accepted as authorization: the live approval file is absent, all six approval gates fail closed, zero live approvals are accepted, FDTD remains unauthorized and unexecuted, and BEM/FDTD comparison remains blocked. |
| 1825 | BEM stage-1 complex FDTD live approval acceptance gate validator | Validate the saved run `1824` fail-closed gate. Five of five checks pass, confirming source readiness, six-gate shape, zero passed gates, zero accepted live approvals, absent live approval, blocked FDTD execution/comparison/downstream states, figure validation, and script snapshots. |
| 1826 | BEM stage-1 complex FDTD live approval acceptance gate validation sensitivity | Sensitivity-harden the run `1825` validator. The exact absent-live-approval fail-closed state passes, while twelve damaged states fail as expected for source readiness, gate-count damage, gate-pass promotion, live-file promotion, accepted-approval promotion, FDTD authorization/execution, comparison, field/3D, figure, and script-snapshot damage. Use runs `1824-1826` as the guarded live approval acceptance gate block. |
| 1830 | BEM stage-1 complex FDTD external return hygiene audit | Audit the two external paths needed for the first BEM stage-1 complex-field FDTD return: the live approval JSON and the stage-1 partial-return CSV. Both target files are absent, the approval directory is clean and empty, the partial-return parent has no direct files, zero live approvals are accepted, and FDTD execution plus BEM/FDTD comparison remain blocked. |
| 1831 | BEM stage-1 complex FDTD external return hygiene audit validator | Validate the saved run `1830` hygiene audit. Six of six checks pass, confirming the two-row contract shape, clean absent approval path, absent partial-return target, blocked execution/comparison/3D states, figure validation, and script snapshots. |
| 1832 | BEM stage-1 complex FDTD external return hygiene audit validation sensitivity | Sensitivity-harden the run `1831` validator. The exact clean non-authorizing state passes, while thirteen damaged states fail as expected for false approval, dirty approval parent, false partial return, accepted approval promotion, FDTD authorization/execution, comparison, 3D, figure damage, and script-snapshot damage. Use runs `1830-1832` as the guarded no-data hygiene block for the BEM stage-1 2D external-return paths. |
| 1827 | BEM stage-1 complex FDTD live approval action rollup | Roll up the exact 2D-side actions required before the first BEM stage-1 FDTD producer can be authorized. The output-local template is ready, but the live approval JSON, four approval-provenance fields, and the 12-column BEM partial-return CSV remain absent; all six live approval gates still fail closed, only one of six actions is complete, and FDTD execution plus BEM/FDTD comparison remain blocked. |
| 1828 | BEM stage-1 complex FDTD live approval action rollup validator | Validate the saved run `1827` action rollup. Six of six checks pass, confirming source readiness, two artifact rows, nine approval-field rows, six action rows, zero present live files, four blank approval-provenance fields, six failed gates, one completed action, blocked execution/downstream states, figure validation, and script snapshots. |
| 1829 | BEM stage-1 complex FDTD live approval action rollup validation sensitivity | Sensitivity-harden the run `1828` validator. The exact run `1827` missing-live-artifact state passes, while seventeen damaged states fail as expected for source readiness, row-shape damage, false live-artifact promotion, approval-field damage, gate-pass promotion, accepted-approval promotion, FDTD authorization/execution, comparison, field/3D, figure, and script-snapshot damage. Use runs `1827-1829` as the guarded BEM stage-1 live approval action rollup block. |
| 1833 | BEM stage-1 complex FDTD external artifact acceptance watchlist | List the two live external artifacts required before the BEM stage-1 complex FDTD producer can be authorized: the live approval JSON and the BEM stage-1 partial-return CSV. Both parent directories are present, both live files are absent, zero artifacts are accepted, and FDTD execution plus BEM/FDTD comparison remain blocked. |
| 1834 | BEM stage-1 complex FDTD external artifact acceptance watchlist validator | Validate the saved run `1833` watchlist. Six of six checks pass, confirming source readiness, two-artifact shape, absent live files, parent/template state, blocked FDTD execution/comparison/downstream states, figure validation, and script snapshots. |
| 1835 | BEM stage-1 complex FDTD external artifact acceptance watchlist validation sensitivity | Sensitivity-harden the run `1834` validator. The exact absent-artifact state passes, while eighteen damaged states fail as expected for false live-file promotion, false artifact acceptance, damaged parent/template counts, false live approval, false partial return, FDTD authorization/execution, comparison, field/3D promotion, figure damage, and script-snapshot damage. Use runs `1833-1835` as the guarded external artifact acceptance block. |
| 1836 | BEM stage-1 complex FDTD external artifact acceptance claim boundary | Record the post-watchlist claim boundary. Three claims are guarded: the watchlist, validator, and sensitivity result. Five claims remain blocked: live artifact acceptance, FDTD authorization, FDTD execution, real BEM/FDTD comparison, and field/3D transfer. The path still has two required live files, zero present live files, and zero accepted artifacts. |
| 1837 | BEM stage-1 complex FDTD external artifact acceptance claim-boundary validator | Validate run `1836` from artifacts. Six of six checks pass, confirming claim-boundary readiness, stable claim shape, blocked live-artifact counts, blocked execution/downstream states, figure validation, and script snapshots. |
| 1838 | BEM stage-1 complex FDTD external artifact acceptance claim-boundary validation sensitivity | Sensitivity-harden the run `1837` validator. The exact absent-artifact claim boundary passes, while eighteen damaged states fail as expected for claim-count damage, false artifact acceptance, FDTD authorization/execution promotion, comparison/field/3D promotion, figure damage, and script-snapshot damage. |
| 1839 | BEM stage-1 complex FDTD external artifact receipt checklist | Create a two-row receipt checklist for the live approval JSON and BEM stage-1 partial-return CSV. Both rows are pending, zero live files are present, zero artifacts are accepted, and FDTD authorization, FDTD execution, BEM/FDTD comparison, field transfer, and 3D/HPC remain blocked. |
| 1840 | BEM stage-1 complex FDTD external artifact receipt-checklist validator | Validate run `1839` from artifacts. Seven of seven checks pass, confirming two pending receipt rows, absent and unaccepted live artifacts, blank receipt fields, artifact-specific schema checks, blocked FDTD/comparison/downstream states, figure validation, and script snapshots. |
| 1841 | BEM stage-1 complex FDTD external artifact receipt-checklist validation sensitivity | Sensitivity-harden the run `1840` validator. The exact absent-live-artifact receipt checklist passes, while eighteen damaged states fail as expected for checklist/row/artifact damage, false live-file or artifact acceptance, filled receipt fields, FDTD authorization/execution promotion, comparison/field/3D promotion, figure damage, and script-snapshot damage. |
| 1842 | BEM stage-1 complex FDTD external artifact receipt live-state refresh | Rescan the two required external artifacts after the guarded receipt checklist. Both live artifacts remain absent, so no observed hashes, file sizes, parse checks, accepted artifacts, FDTD authorization, BEM/FDTD comparison, field transfer, or 3D/HPC promotion is present. |
| 1843 | BEM stage-1 complex FDTD external artifact receipt live-state refresh validator | Validate run `1842` from artifacts. Six of six checks pass, confirming source readiness, two-artifact shape, absent live files, blank observation and parse fields, blocked FDTD/comparison/downstream states, figure validation, and script snapshots. |
| 1844 | BEM stage-1 complex FDTD external artifact receipt live-state refresh validation sensitivity | Sensitivity-harden the run `1843` validator. The exact no-live-artifact refresh passes, while twenty damaged states fail as expected for false file presence, filled observation or parse fields, accepted-artifact promotion, FDTD authorization/execution promotion, BEM/FDTD comparison promotion, field/3D promotion, GPU promotion, figure damage, and script-snapshot damage. |
| 1845 | BEM stage-1 complex FDTD external artifact authorization decision gate | Convert the no-live-artifact refresh into an explicit authorization decision. Both required live artifacts are absent, receipt observations are blank, two prerequisite checks block authorization, and FDTD producer authorization, execution, BEM/FDTD comparison, field transfer, and 3D/HPC remain blocked. |
| 1846 | BEM stage-1 complex FDTD external artifact authorization decision gate validator | Validate run `1845` from artifacts. Six of six checks pass, confirming decision identity, row shape, two blockers, no-live-artifact authorization deferral, closed action state, blocked downstream states, figure validation, and script snapshots. |
| 1847 | BEM stage-1 complex FDTD external artifact authorization decision gate validation sensitivity | Sensitivity-harden the run `1846` validator. The exact no-live-artifact/no-authorization decision passes, while twenty-one damaged states fail as expected for false live-file, hash, ready-recheck, authorization, command, accepted-artifact, FDTD execution, comparison, field/3D/GPU, figure, and script-snapshot promotion. Use runs `1845-1847` as the guarded no-authorization decision block for the BEM stage-1 external artifacts. |
| 1848 | BEM stage-1 complex FDTD external artifact operator placement packet | Package the two required live artifact paths and receipt fields for operator/advisor placement. Both parent directories are ready, one source template is present, zero live files and zero receipt observations are present, two decisions still block authorization, and FDTD execution, BEM/FDTD comparison, field transfer, GPU, and 3D/HPC remain blocked. |
| 1848 | BEM stage-1 complex FDTD source/receiver geometry lock audit | Convert the guarded BEM acquisition-geometry result into six required geometry locks for any future FDTD partial return: Tx/Rx offset, antenna z, source/receiver coordinates, phase-center reference, BEM geometry case identity, and geometry sidecar or columns. Both live artifacts remain absent, zero locks are satisfied, and FDTD authorization, FDTD execution, BEM/FDTD comparison, field transfer, and 3D/HPC remain blocked. |
| 1849 | BEM stage-1 complex FDTD source/receiver geometry lock audit validator | Validate run `1848` from artifacts. Six of six checks pass, confirming the six-lock shape, required geometry fields, no-live-artifact state, BEM geometry metric basis, blocked downstream scope, figure validation, and script snapshots. Use runs `1848-1849` as the guarded 2D-side geometry-lock boundary for the BEM stage-1 FDTD return. |
| 1850 | BEM stage-1 complex FDTD source/receiver geometry lock audit validation sensitivity | Sensitivity-harden the run `1849` validator. The exact geometry-lock state passes, while twenty-one damaged or prematurely promoted states fail as expected for lock damage, false lock satisfaction, block demotion, live-file promotion, FDTD authorization/execution promotion, BEM/FDTD comparison promotion, BEM metric damage, field/3D/GPU promotion, figure damage, and script-snapshot damage. Use runs `1848-1850` as the guarded 2D-side geometry-lock boundary for the BEM stage-1 FDTD return. |
| 1851 | BEM stage-1 complex FDTD external artifact receipt recheck sentinel | Rerun the current live-path receipt check for the two required BEM stage-1 external artifacts and rebuild the authorization decision without executing acceptance, FDTD, comparison, field, GPU, or 3D/HPC work. Both live artifacts remain absent, zero receipt observations are present, the same two decisions block authorization, and downstream states remain blocked. |
| 1852 | Experiment archive duplicate ID collision audit | Scan numbered experiment output folders and experiment documentation files for duplicate numeric IDs without renumbering, moving, or deleting artifacts. The current output-archive collision is ID `1848`; doc collisions also exist at IDs `13`, `656`, `665`, `672`, and `1848`. Use the next safe numeric output ID for new experiment outputs and plan any collision cleanup separately. |
| 1853 | Experiment archive next ID allocation guard | Use the duplicate-ID audit's recommended next safe output ID and verify that run `1853` is uniquely allocated in outputs and docs. No renumbering is performed; `1854` becomes the next safe output numeric ID for subsequent experiment outputs. |
| 1854 | BEM stage-1 complex FDTD external artifact intake validator | Validate the current intake state for the two required live artifacts without executing acceptance, FDTD, comparison, field, GPU, or 3D/HPC work. Both live artifacts remain missing, two intake blockers remain active, and the next required action is to place the live approval JSON and BEM stage-1 partial-return CSV. |
| 1855 | Experiment archive post-intake next ID guard | Verify that `1854` consumed the previously advertised next safe ID, allocate the guard itself at `1855`, and advance the next safe output ID to `1856` without renumbering or repairing existing duplicate numeric IDs. |
| 1856 | BEM stage-1 external artifact template placeholder validator | Validate that the two BEM stage-1 fill-in templates remain parseable placeholder-only artifacts, with ten placeholder values, zero expected live paths present, zero template/live path collisions, and two intake blockers still active. Acceptance, FDTD producer authorization, BEM/FDTD comparison, field transfer, GPU, and 3D/HPC remain blocked. |
| 1857 | Experiment archive post-template-placeholder next ID guard | Verify that `1856` consumed the previously advertised next safe ID, allocate the guard itself at `1857`, and advance the next safe output ID to `1858` without renumbering or repairing existing duplicate numeric IDs. |
| 1858 | Synthetic 2D next question matrix post current status v3 refresh | Refresh the synthetic 2D next-question matrix from existing archive evidence only. Ten candidate questions are ranked, the top question is the current synthetic publication bundle, target1 acquisition and exception-map evidence are included, and immediate/conditional GPU candidate counts remain zero. |
| 1859 | Experiment archive post-synthetic-next-question next ID guard | Verify that `1858` consumed the previously advertised next safe ID, allocate the guard itself at `1859`, and advance the next safe output ID to `1860` without renumbering or repairing existing duplicate numeric IDs. |
| 1860 | BEM stage-1 complex FDTD external artifact receipt recheck post synthetic next ID | Rerun the current live-path receipt check for the two BEM stage-1 external artifacts after the synthetic next-ID guard. Both live artifacts remain missing, zero receipt observations or parse checks are present, and guarded acceptance, FDTD authorization, BEM/FDTD comparison, field transfer, GPU, and 3D/HPC stay blocked. |
| 1861 | Experiment archive post-BEM-receipt-recheck next ID guard | Verify that `1860` consumed the previously advertised next safe ID, allocate the guard itself at `1861`, and advance the next safe output ID to `1862` without renumbering or repairing existing duplicate numeric IDs. |
| 1862 | Synthetic 2D next question matrix post field live recheck status v4 refresh | Refresh the synthetic 2D next-question matrix after the field live-receipt recheck and status packet v4. Ten candidate questions are ranked, the top question remains the current synthetic publication bundle, target1 acquisition and exception-map evidence are included, and immediate/conditional GPU candidate counts remain zero. |
| 1863 | Experiment archive post-synthetic-field-recheck next ID guard | Verify that `1862` consumed the previously advertised next safe ID, allocate the guard itself at `1863`, and advance the next safe output ID to `1864` without renumbering or repairing existing duplicate numeric IDs. |
| 1864 | BEM stage-1 complex FDTD external artifact receipt recheck post status v5 | Rerun the current live-path receipt check for the two BEM stage-1 external artifacts after status packet v5 advertised `1864` as the next safe ID. Both live artifacts remain missing, zero receipt observations or parse checks are present, and guarded acceptance, FDTD authorization, BEM/FDTD comparison, field transfer, GPU, and 3D/HPC stay blocked. |
| 1865 | Experiment archive post-status-v5 BEM recheck next ID guard | Verify that `1864` consumed the previously advertised next safe ID, allocate the guard itself at `1865`, and advance the next safe output ID to `1866` without renumbering or repairing existing duplicate numeric IDs. |
| 1866 | Synthetic 2D next question matrix post field/BEM archive guard refresh | Refresh the synthetic 2D next-question matrix after the post-BEM/archive field live-receipt recheck. Ten candidate questions are ranked, the top question remains the current synthetic publication bundle, target1 acquisition and exception-map evidence are included, and immediate/conditional GPU candidate counts remain zero. |
| 1867 | Experiment archive post-synthetic-BEM-field-recheck next ID guard | Verify that `1866` consumed the previously advertised next safe ID, allocate the guard itself at `1867`, and advance the next safe output ID to `1868` without renumbering or repairing existing duplicate numeric IDs. |
| 1868 | BEM stage-1 complex FDTD external artifact receipt recheck post status v6 | Rerun the current live-path receipt check for the two BEM stage-1 external artifacts after status packet v6 advertised `1868` as the next safe ID. Both live artifacts remain missing, zero receipt observations or parse checks are present, and guarded acceptance, FDTD authorization, BEM/FDTD comparison, field transfer, GPU, and 3D/HPC stay blocked. |
| 1869 | Experiment archive post-status-v6 BEM recheck next ID guard | Verify that `1868` consumed the previously advertised next safe ID, allocate the guard itself at `1869`, and advance the next safe output ID to `1870` without renumbering or repairing existing duplicate numeric IDs. |
| 1870 | Synthetic 2D next question matrix post status-v6 field recheck refresh | Refresh the synthetic 2D next-question matrix after the post-status-v6 field live-receipt recheck. Ten candidate questions are ranked, the top question remains the current synthetic publication bundle, target1 acquisition and exception-map evidence are included, and immediate/conditional GPU candidate counts remain zero. |
| 1871 | Experiment archive post-synthetic-status-v6-field-recheck next ID guard | Verify that `1870` consumed the previously advertised next safe ID, allocate the guard itself at `1871`, and advance the next safe output ID to `1872` without renumbering or repairing existing duplicate numeric IDs. |
| 1872 | BEM stage-1 complex FDTD external artifact receipt recheck post synthetic status-v6 field guard | Rerun the current live-path receipt check for the two BEM stage-1 external artifacts after the synthetic status-v6 field guard advertised `1872` as the next safe ID. Both live artifacts remain missing, zero receipt observations or parse checks are present, and guarded acceptance, FDTD authorization, BEM/FDTD comparison, field transfer, GPU, and 3D/HPC stay blocked. |
| 1873 | Experiment archive post-synthetic-status-v6-field BEM recheck next ID guard | Verify that `1872` consumed the previously advertised next safe ID, allocate the guard itself at `1873`, and advance the next safe output ID to `1874` without renumbering or repairing existing duplicate numeric IDs. |
| 1874 | Synthetic 2D next question matrix post synthetic-status-v6-field BEM guard refresh | Refresh the synthetic 2D next-question matrix after the post-synthetic-status-v6-field BEM recheck guard. Ten candidate questions are ranked, the top question remains the current synthetic publication bundle, target1 acquisition and exception-map evidence are included, and immediate/conditional GPU candidate counts remain zero. |
| 1875 | Experiment archive post-synthetic-status-v6-field-BEM-guard synthetic next ID guard | Verify that `1874` consumed the previously advertised next safe ID, allocate the guard itself at `1875`, and advance the next safe output ID to `1876` without renumbering or repairing existing duplicate numeric IDs. |

## Canonical Category Definitions

Single rebar:

```text
one circular steel rebar target; includes single-rebar radius/depth/location,
source-wavelet mismatch, bandwidth, W2/OT diagnostics, material tradeoff, and
single-target source-profiled polish experiments.
```

Multi rebar:

```text
multiple circular steel rebars in one model; includes common/per-target radius
profiles, per-target x/z/r coupling, confidence reports, coordinate optimizer
runs, guarded revisit runs, and objective-diagnostic matrices.
```

Infrastructure/reporting smoke:

```text
plotting, aggregation, CLI, and reporting runs that summarize physical
experiments but are not themselves a new physical inversion scene.
```

Field/lab data:

```text
measured or lab-calibrated data intake, import/QC, velocity calibration, and
field-to-synthetic bridge runs. These are kept separate from synthetic 2D FWI
evidence until geometry and calibration are sufficient for inversion.
Use `outputs/field_experiments/<dataset_id>/NNN_run_name` for future field,
lab, public, or benchmark-data work. The earlier `1119` run remains in
`outputs/experiments/` as a historical pre-split QC baseline.
```

## Future Categories

Reserve new category names when the physics changes enough that direct
comparison to the current two groups would be confusing:

| Future category | Use when |
| --- | --- |
| `multi_rebar_variable_depth` | two or more rebars at different depths |
| `multi_rebar_variable_radius` | multiple rebars with intentionally different sizes |
| `shape_material_variants` | non-circular bars, corrosion, coatings, or material-property sweeps beyond the current single-rebar material diagnostic |

## Symlink Policy

The symlink view is safe as long as these rules are followed:

- Keep `outputs/experiments/NNN_run_name` as the canonical path in manifests,
  docs, scripts, and generated summaries for synthetic and historical
  numbered runs.
- Keep field/lab/public data manifests under
  `outputs/field_experiments/<dataset_id>/NNN_run_name`.
- Do not move existing numbered folders into category folders.
- Do not write new experiments through `_by_category_symlinks` paths.
- Avoid recursive symlink-following commands such as `find -L`, `cp -L`, or
  `rsync -L` unless duplicate traversal is intended.

If a future tool needs category awareness, it should read an explicit category
field or this index, not infer category from a symlink path.

## Current Layout Policy

Keep the flat `outputs/experiments/` archive for synthetic FDTD/FWI simulation
runs and infrastructure smoke tests. Use `outputs/field_experiments/` for
measured/lab/public data streams, with one dataset/source-family folder per
data source and run numbering local to that dataset family.
