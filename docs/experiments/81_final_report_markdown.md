# Experiment 81: Final Report Markdown

## Purpose

Assemble the report draft, figure package, claim audit, and reproducibility
bundle into one final Markdown report artifact with embedded figure links.

## 548: Final Report Markdown

Output:

```text
outputs/experiments/548_final_report_markdown
```

Command:

```text
Manual CPU-only report assembly using the run 547 reporting reproducibility
bundle and the run 545 figure-caption package.
```

Artifacts:

```text
final_report.md
run_manifest.json
```

Main embedded figures:

```text
Figure 1: Tx/Rx=50 coordinate confidence.
Figure 2: Tx/Rx=50 ambiguity widths.
Figure 3: compact objective diagnostic summary.
Figure 4: source-shape transfer guardrail.
Figure 5: shallow r=4 material/source uncertainty.
Figure 6: close50 metadata confidence.
Figure 7: close50 ambiguity widths.
```

## Interpretation

The final report is a readable assembly of the current evidence, not a new
experiment. It preserves the claim boundaries from run 546: interval-aware
variable-depth/radius reporting, branch-level veryhigh diagnostic use, no
global objective promotion, and no universal high-precision radius claim.

## Next Decision

Use `final_report.md` for manuscript editing or formatting. No GPU experiment
is queued.
